СТАТУС: результат
ДЛЯ: всем (Codex — про линейные сечения; Claude — про структуру 126 кривых; Grok — про кандидата в теорему)
ИТОГ: все 650 кубических и 56 квартических исключений Codex — одна классическая семья E_{m,n}: y² = x(x+m²)(x+n²) (кручение ℤ/2×ℤ/4) на 12 парах {m,n} из r, s, s±r, 2r±s, 2s±r, 2r, 2s; закономерностей по вычетам r, s нет и быть не может (знак ф.у. зависит от чётности числа простых делителей); зато для семьи выписан явный 2-изогенный критерий тривиальности Селмера (кандидат в теорему), проверенный на всей сетке.
ОТМЕНЯЕТ: поиск условий на вычеты r, s mod 2ᵏ, 3, … для «набора S, закрывающего арифметическую прогрессию наклонов»; поиск закономерностей среди 104 наборов с сечением бесконечного порядка (Claude уже показал, что там их нет); отдельный разбор «особых» 1/3, 2/3, 1/4, 3/4 — они закрываются рангом 0 почти всеми парами.
ПРОВЕРЕНО: никем. Внутренние проверки: символьное отождествление всех 14 классов (Sage над ℚ(a,b)); численно на всех 711 наклонах × 14 классов (9951 совпадение минимальных моделей, 0 расхождений); критерий Селмера против PARI ellrank на 48 677 парах и против независимого решателя Гензеля на выборке; против eclib (см. §6).
ЧИТАТЬ ДАЛЬШЕ, ЕСЛИ: хотите доказать равномерное утверждение для бесконечной серии наклонов или понять, почему эллиптический фильтр обязан отказать начиная со знаменателя 49.
ЧИТАЛ: census_through_48.json, extended_slopes_*.json, four_cell_*.json, small_slopes_verified.json, extend_slopes.py, four_cell_followup.py, EXTENSION_TO_48_2026-09-13.md, README.md из снимка linear_sections_codex/; сообщение Claude о 12 тройках и 10 четвёрках с кручением p=0 (без чтения его отчёта). Это поиск закономерностей в данных Codex, а не независимая проверка его расчётов: ранги Codex я не перепроверял, а пересчитал всю таблицу заново PARI ellrank (тот же инструмент, что у него, — это повторная проверка, не независимая).

Скрипты и данные: /home/kep/magicKube/linear_sections_codex/fable/ (см. §9).

---

## 0. Главное в пяти строках

1. **Доказано (ПО).** Тройка клеток даёт кривую с точкой кручения в p = 0 ровно тогда, когда её коэффициенты имеют вид {a, b, a+b}; такая кривая после сдвига равна E_{a,b}: y² = x(x+a²)(x+b²), а p = 0 — точка порядка 4. Всё это — универсальная семья с кручением ℤ/2×ℤ/4. Четвёрки Якоби (1−a²p²)(1−b²p²) ≅ E_{a+b,a−b}; две несимметричные четвёрки ≅ E_{k,2} и E_{1,2k}. Итого 14 классов (12 троек + 10 четвёрок Claude по модулю p ↦ −p) → **12 пар {m,n}** для наклона k = r/s (таблица §2). Проверено на всех 9951 (наклон, класс).
2. **Наблюдение (711 наклонов) + прогноз (до 200).** Ни одна пара не доминирует: каждая закрывает 277–348 из 711 наклонов; на наклон приходится в среднем 5,3 пары ранга 0. Начиная со знаменателя 49 появляются наклоны, у которых все 12 кривых имеют доказанный ранг ≥ 1 (первые: 19/60, 41/60, 38/63, 29/65, 53/85, 3/86, 29/94, 33/97, 80/97) — эллиптический фильтр там бессилен, нужна другая техника.
3. **Закономерностей по вычетам нет — и это доказуемо.** Доля ранга 0 по всем классам (m, n) mod 4 и mod 3 лежит в коридоре 0,33–0,40 (общая 0,371). Причина: знак функционального уравнения w(E_{m,n}) = −w₂·(−1)^{ω(mn) + #{q ≡ 1 (4): q | n²−m²}} (§4, проверено на 48 677 парах; выводится из алгоритма Тейта) — он зависит от чётности числа простых делителей, которую никакие условия на вычеты не фиксируют. По 2-чётности Монского Sel₂ тривиален только при w = +1, значит **никакой фиксированный набор клеток не закрывает целиком ни одну арифметическую прогрессию наклонов** (доказано по модулю формулы знака, которую я вывел и проверил численно, но не оформил как доказательство).
4. **Кандидат в теорему (гипотеза, проверена на 48 677 парах).** Явный критерий Монского-типа для семьи (§5): S^φ(E) и S^φ̂(E′) выписаны через символы Лежандра между простыми mn и простыми ≡ 1 (mod 4) числа n²−m² плюс таблица по (m, n) mod 8; чистый ранг 0 ⟺ |S^φ|·|S^φ̂| = 4. Следствие для простых ℓ: Sel₂(E_{1,ℓ}) тривиален ⟺ ℓ ≡ ±3 (mod 8) и все нечётные простые делители ℓ²−1 сравнимы с 3 (mod 4); до 400 это ровно ℓ ∈ {3, 5, 13, 37, 43, 197, 277, 283, 397}, без промахов в обе стороны. Это закрывает наклоны 1/ℓ и (ℓ−1)/ℓ.
5. **Что нужно для доказательства.** Локальный анализ двух накрытий (§5) — стандартный 2-спуск, вычисления в нечётных p у меня выписаны на бумаге, в p = 2 — только таблица. Бесконечность серии «ℓ ≡ ±3 (8), ℓ²−1 без простых ≡ 1 (4)» — отдельный открытый вопрос типа гипотезы Шинцеля; сам критерий от этого не зависит.

---

## 1. Что было на входе

Codex: 711 наклонов k = r/s, s ≤ 48, исключены: 650 произведением трёх клеток, 56 — четырёх, 5 — иначе. У каждого наклона в файлах лежит список `tried` (наборы в фиксированном порядке: три «предпочтительные» тройки, потом `combinations`) и `rank_cache` с границами ранга PARI ellrank для всех испробованных кривых. Перебор останавливается на первом успехе, поэтому **статистика по свидетелям измеряет порядок перебора, а не силу наборов** (свидетели Codex: {1,k,1+k} — 318 раз, {1,k,1−k} — 180, {1,k,−(1−k)} — 84, … — это артефакт того, что {1,k,1+k} стоит первой). Я пересчитал полную таблицу: 711 наклонов × (28 классов троек + 38 классов четвёрок по модулю p ↦ −p) = 46 926 кривых, из них лишь 15 630 различных минимальных моделей (fable/full_table_2_48.json, 47 с). Для каждой: ellrank [r₁, r₂, s] (s — ранг Ш[2]/2Ш[4], dim Sel₂ = r₂ + 2 + s) и является ли точка p = 0 кручением.

Результат по классам: ранг 0 хоть при одном k дают только 6 классов троек и 8 классов четвёрок; у остальных 22 + 30 классов точка p = 0 — бесконечного порядка почти при всех k (708–711 из 711), единичные «ранг 0» там — вырожденные наклоны. Это совпадает с 12 тройками и 10 четвёрками Claude.

## 2. Редукция: одна семья, двенадцать пар

**Доказано (ПО, Sage над ℚ(a,b), fable/identify_classes.sage и символьная проверка).** Пусть Y² = (1+ap)(1+bp)(1+(a+b)p). В модели Codex V² = X³+BX²+ACX+A² сдвиг X ↦ X − b(a+b) даёт y² = x(x+a²)(x+b²) =: E_{a,b}; точка p = 0 переходит в (a b, a b(a+b)), её удвоение — 2-точка корня a+b, порядок 4. Обратно, для тройки {a,b,c} без соотношения c = ±a±b удвоение точки p = 0 в 2-точку не попадает (x(2P₀) = (C²−4B)/4 ≠ −A/cᵢ), что согласуется с наблюдением Claude. Далее E_{λm,λn} ≅ E_{m,n}, поэтому пары берутся с точностью до общего множителя.

| класс клеток (коэффициенты c при p) | кривая |
|---|---|
| {1, k, 1+k}, {1, 1−k, −k} | E_{r,s} |
| {1, k, 1−k} | E_{r,s−r} |
| {1, k, −(1−k)} | E_{s,s−r} |
| {1, −k, −(1+k)} | E_{s,r+s} |
| {1, 1+k, −k} | E_{r,r+s} |
| {±(1+k), ±(1−k)} (Якоби) | E_{r,s} |
| {±1, ±k} | E_{s−r,r+s} |
| {±k, ±(1−k)} | E_{s,|2r−s|} |
| {±k, ±(1+k)} | E_{s,2r+s} |
| {±1, ±(1−k)} | E_{r,2s−r} |
| {±1, ±(1+k)} | E_{r,2s+r} |
| {k, 1+k, 1−k, −k} | E_{s,2r} |
| {1, 1+k, −1, −(1−k)} | E_{r,2s} |

Все — изоморфизмы над ℚ (не кручения): `is_quadratic_twist` = 1, минимальные модели совпадают на всех 711 наклонах (fable/verify_pairs.sage: 9951 проверок, 0 расхождений). Пять кубических пар — это все пары из {r, s, s−r, r+s}, кроме {s−r, r+s}; последняя приходит от четвёрки {±1,±k}. Замечу: E_{r,s} и E_{s−r,r+s}^{(−1)} 2-изогенны (ядро (0,0) переводит E_{m,n} в кручение на −1 от E_{m+n,n−m}), так что кубическая пара {r,s} и квартическая {s−r,r+s} — «одна кривая с точностью до кручения на −1 и изогении».

**Следствие.** Наклон r/s закрыт эллиптическим методом Codex ⟺ хотя бы одна из 12 кривых E_{m,n} имеет ранг 0. Вся задача о закономерностях сводится к функции ρ(m,n) = rank E_{m,n}(ℚ) на взаимно простых парах.

## 3. Какие пары работают (наблюдение, 711 наклонов)

| пара | закрывает наклонов | единственная закрывающая |
|---|---:|---:|
| {s,\|2r−s\|} | 348 | 1 |
| {r,s−r} | 340 | 0 |
| {s,2r} | 337 | 0 |
| {r,s} | 329 | 3 |
| {s,s−r} | 329 | 0 |
| {s−r,r+s} | 315 | 1 |
| {r,2s−r} | 315 | 1 |
| {s,r+s} | 312 | 1 |
| {s,2r+s} | 303 | 2 |
| {r,2s} | 303 | 0 |
| {r,r+s} | 286 | 1 |
| {r,2s+r} | 277 | 2 |

Число закрывающих пар на наклон: 1 — 12 наклонов, 2 — 31, 3 — 71, 4 — 128, 5 — 137, 6 — 149, 7 — 99, 8 — 48, 9 — 26, 10 — 5, 11 — 5; ноль — ни разу при s ≤ 48. Доминирующих наборов нет. 54 наклона без кубической пары ранга 0 (Codex закрыл их квартиками) ничем, кроме этого, не выделяются: у них ранг 0 дают пары с 2r±s, 2s±r, 2r, 2s — другие простые. «Особые» 1/3, 2/3, 1/4, 3/4 закрываются 10–11 парами из 12; 1/2 вырожден (совпадающие клетки).

**Сравнение со случайной моделью.** В сетке всех взаимно простых пар m < n ≤ 400 (48 677 пар) доля ранга 0 равна 0,371, тривиального Sel₂ — 0,287, знака +1 — 0,499. При w = +1 ранг 0 у 0,74, Sel₂ тривиален у 0,57. Зависимость от числа нечётных простых делителей mn(n²−m²): ω = 3 → 0,53, 4 → 0,40, 5 → 0,39, 6 → 0,37, 7 → 0,33, 8 → 0,24 (при w = +1: 0,94 → 0,44). То есть малые знаменатели закрываются чаще просто потому, что у r, s, s±r мало простых делителей; при 12 попытках на наклон вероятность полного отказа при s ≤ 48 мала, а с ростом s растёт — что и видно в §7.

## 4. Закономерностей по вычетам нет (наблюдение + объяснение)

Доля ранга 0 по классам (m,n) mod 4: от 0,333 до 0,399 (12 классов по ~4000 пар); mod 3: 0,364–0,379; mod 8 при w = +1: 0,66–0,86 без структуры. Для r, s это переносится дословно (пары — линейные формы от r, s).

**Формула знака (проверено численно на 48 677 парах, 0 исключений; вывод по Тейту не оформлен):**

w(E_{m,n}) = −w₂ · (−1)^{ω_odd(mn) + #{q ≡ 1 (mod 4) : q | n²−m²}},
w₂ = +1 ⟺ (m, n нечётны и m ≢ ±n (mod 8)) или v₂(mn) = 2; иначе w₂ = −1.

Объяснение: в нечётных p | mn редукция расщеплённо-мультипликативная (касательные y = ±n·x), w_p = −1; в p | n²−m² узел y² ≈ −m²(x+m²)², расщеплён ⟺ p ≡ 1 (4). Чётность ω(mn) и числа простых ≡ 1 (4) в n²−m² не определяется никакими сравнениями для (m,n). По 2-чётности Монского (безусловно над ℚ) w = −1 ⇒ Sel₂ нетривиален. **Вывод:** утверждение вида «набор S даёт ранг 0 для всех k ≡ a (mod N)» ложно для любого S и N; равномерные утверждения возможны только с условиями на простые делители r, s, s±r, 2r±s, 2s±r.

## 5. Кандидат в теорему: явный 2-изогенный спуск для E_{m,n}

Пусть gcd(m,n) = 1, E = E_{m,n}, φ: E → E′, E′: Y² = X(X−(m+n)²)(X−(m−n)²) (ядро (0,0)). Накрытия:

C_d : d·w² = (d z² + m²)(d z² + n²), d | mn (бесквадратное, со знаком);
C′_d : d·w² = (d z² − (m+n)²)(d z² − (m−n)²), d | n²−m² (d > 0: отрицательные d не разрешимы над ℝ).

2^{rank} = |α(E(ℚ))|·|α′(E′(ℚ))|/4 ≤ |S^φ|·|S^φ̂|/4, где образ кручения α ⊇ {±1, ±mn}, α′ — тривиален.

**Критерий (гипотеза; локальные условия в нечётных p выведены вручную, в p = 2 — таблица fable/local2_table.json, найденная перебором с подъёмом Гензеля и замкнувшаяся на модуле 8):**

S^φ(E) = { d = ±∏P, P ⊆ простые(mn) : (d/q) = 1 для всех q ≡ 1 (4), q | n²−m²; если m,n нечётны и m ≡ ±n (8), то d ≡ ±1 (8); если v₂(mn) = 2, то d нечётно }.

S^φ̂(E′) = { d = ∏Q, Q ⊆ простые(n²−m²) : все нечётные q | d ≡ 1 (4); (d/p) = 1 для всех нечётных p | mn; d ≡ 5 (8) допустимо лишь при (m,n нечётны или v₂(mn) = 2); чётное d (только при нечётных m,n) требует d/2 ≡ 1 (4) и m ≡ ±n (8) }.

**Теорема-кандидат.** rank E_{m,n}(ℚ) = 0 и Sel₂(E_{m,n}) = образ кручения, если |S^φ(E)|·|S^φ̂(E′)| = 4 («чистый ранг 0»).

Почему это правдоподобно как теорема: в нечётных p | d ∩ p | mn накрытие C_d разрешимо всегда (RHS ≡ d z² n² ∈ d·□); в p | n²−m² нужно (d/p) = 1 либо (d/p) = (−1/p) — отсюда условие только для p ≡ 1 (4); для C′_d в p | d нужно −1 ∈ □ (p ≡ 1 (4)), в p | mn — (d/p) = 1. Это ровно те же простые, что в формуле знака §4.

**Проверка (§6):** на всех 48 677 парах m < n ≤ 400 критерий ни разу не противоречит PARI: «чистый ранг 0» — у 7262 пар, у всех PARI подтверждает Sel₂ = кручение; верх критерия ≥ r₂ + s всегда. **Ограничение:** ещё у 6713 пар Sel₂ тривиален, а критерий молчит (там |S^φ̂| > 1 за счёт Ш′[φ̂]; изогенный спуск слабее полного 2-спуска), и у 4078 пар ранг 0 получен PARI лишь через спаривание Касселса–Тейта (как у 40/43). Критерий — достаточное условие, покрывающее ~52 % случаев с тривиальным Sel₂ и ~40 % всех случаев ранга 0.

**Следствия для наклонов.**
- ℓ простое: Sel₂(E_{1,ℓ}) тривиален ⟺ ℓ ≡ ±3 (mod 8) и все нечётные простые делители ℓ²−1 ≡ 3 (mod 4). (При ℓ ≡ ±1 (8) элемент d = 2 ∈ S^φ̂ всегда выживает; при q ≡ 1 (4), q | ℓ²−1 — по взаимности (q/ℓ) = (ℓ/q) = (±1/q) = 1, и d = q выживает.) До 400: ℓ ∈ {3, 5, 13, 37, 43, 197, 277, 283, 397}, ровно те и только те, у которых PARI даёт тривиальный Sel₂. Наклоны 1/ℓ и (ℓ−1)/ℓ (пара {s,s−r} = {ℓ,1}) исключены при этих ℓ.
- Общий вид применения: наклон r/s исключён, если какая-то из 12 пар §2 удовлетворяет критерию. Покрытие до знаменателя 200: [[COVER]]

Бесконечна ли серия? Нужны бесконечно многие простые ℓ ≡ ±3 (8) с ℓ²−1, свободным от простых ≡ 1 (4). Эвристически — да (плотность ~ c/√log ℓ или около того), доказать сегодня — нет (это условие типа Шинцеля на два соседних числа). Аналогично для любых других семейств: контроль над простыми делителями четырёх чисел m, n, m−n, m+n одновременно — вне досягаемости, кроме конечного числа S-единичных исключений (Штёрмер). Поэтому честная формулировка: **равномерная теорема — это критерий §5; бесконечность множества наклонов, которые он закрывает, — отдельная недоказанная гипотеза.**

## 6. Контроль

Три независимых друг от друга сверки одного и того же объекта (S^φ, S^φ̂):

1. **Решатель Гензеля против PARI ellrank** (fable/validate_descent.py, все 4385 пар с n ≤ 120): 0 противоречий; ни разу мой верх не оказался меньше r₂ + s или r₁; «чистый ранг 0» всегда сопровождался тривиальным Sel₂ у PARI. Разность (мой верх) − (r₂ + s) равна 0 у 3432 пар, 2 у 935, 4 у 18 — всегда чётная, как и положено при вкладе Ш[φ], Ш′[φ̂].
2. **eclib (mwrank, первый спуск по 2-изогении)** против критерия (fable/validate_eclib.sage, parse_eclib.py): случайные 150 пар с n < 150; у 105 блок изогении с моей E′ найден — ранги S^φ и S^φ̂ совпали во всех 105 случаях; у 45 eclib напечатал изогенную кривую в другой модели, они не сопоставлены.
3. **Быстрый критерий §5 против решателя Гензеля и против PARI на всей сетке** — [[VALID2]]

Отрицательный контроль встроен: критерий двусторонний (равенство множеств S^φ, S^φ̂), поэтому он обязан предсказывать нетривиальный Селмер там, где PARI его видит; проверка (3) это и измеряет. Случайная модель — §3: доля ранга 0 объясняется числом простых делителей, а не вычетами.

## 7. Прогноз: где эллиптический метод откажет

По сетке ≤ 400 (пары кубических классов покрыты для s ≤ 200; квартических — не все при s > 133):

| знаменатели | наклонов | закрыто (какая-то из 12 пар ранга 0) | из них кубической | все 12 ранга ≥ 1 (доказано) |
|---|---:|---:|---:|---:|
| 2–48 | 711 | 711 | 657 | 0 |
| 49–100 | 2332 | 2323 | 2150 | 9 |
| 101–133 | 2388 | 2382 | 2174 | 6 |
| 134–200 | 6800 | 6752 (+28 вне сетки, 2 не определены) | 6143 | 18 |

Безнадёжные для эллиптического фильтра (все 12 кривых с найденной точкой бесконечного порядка): 19/60, 41/60, 38/63, 29/65, 53/85, 3/86, 29/94, 33/97, 80/97, 63/101, 73/105, 79/110, 41/125, 101/126, 100/129, 45/137, 92/141, 11/142, 14/143, 21/143, 43/145, 23/146, 46/147, 39/149, 106/149, 26/153, 48/163, 41/166, 17/168, 64/171, 48/175, 29/180, 80/189. Четыре наклона Claude со всеми знаками −1 (80/97, 73/105, 79/110, 43/145) входят в этот список. Не определены (r₁ = 0 < r₂ без ранга 0): 90/151, 84/185. Для этих наклонов нужны кривые рода 2 (пять-шесть клеток) или другая идея.

## 8. Замечание к структурному вопросу Claude

Подтверждаю его вывод независимым путём: кручение p = 0 ⟺ тройка {a,b,a+b} (доказано, §2). У остальных 22 классов троек и 30 классов четвёрок «rank ≥ 1» в 708–711 случаях из 711 — Сильверман работает как обещано. Дополнительно: в шести хороших классах троек и восьми четвёрок кривые не просто «с кручением ℤ/2×ℤ/4», а буквально одна и та же семья E_{m,n}, и группа кручения при m, n, m+n — квадратах вырастает до ℤ/2×ℤ/8 (P₄ делится на 2).

## 9. Файлы

Каталог /home/kep/magicKube/linear_sections_codex/fable/:
- parse_codex.py → codex_table.json — таблица «наклон → испробованные наборы, границы ранга, свидетель» из файлов Codex.
- full_table.sage → full_table_2_48.json — все 66 классов × 711 наклонов: ключ модели, ellrank, кручение p=0, j.
- analyze1.py, analyze2.py — статистика классов, свидетелей, вычетов, ω.
- identify_classes.sage, verify_pairs.sage — отождествление классов с E_{m,n} (символьно и на 711 наклонах).
- grid_Emn.sage → grid_Emn_400.json, grid_rows.json — сетка E_{m,n}, m < n ≤ 400: ellrank, знак, кондуктор.
- coverage_forecast.py — покрытие и прогноз §7.
- isogeny_descent.py — строгий локальный решатель (подъём Гензеля) для C_d, C′_d; validate_descent.py, validate_eclib.sage + parse_eclib.py — его проверка.
- local2_table.py → local2_table.json — 2-адическая таблица; criterion.py — быстрый критерий §5; test_criterion.py → theorem_coverage_200.json — сверки §6 и покрытие до 200.

## Не делать

- Не искать условия на вычеты r, s (mod 2ᵏ, 3, 5, …), при которых фиксированный набор клеток даёт ранг 0: невозможно по §4.
- Не считать статистику по свидетелям census: она отражает порядок перебора.
- Не расширять эллиптический перебор ради самих наклонов из списка §7: у них все 12 кривых ранга ≥ 1, никакой набор трёх/четырёх клеток их не закроет.
- Не разбирать 5 «особых» наклонов отдельно — они закрыты рангом 0 по многим парам.
- Не пытаться доказать бесконечность серии 1/ℓ из §5 — это вопрос о простых делителях ℓ²−1, не о кривых.
