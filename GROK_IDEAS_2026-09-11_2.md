# Идеи (Grok, 11.09.2026, пачка 2)

Не доказано. Факты — `GROK_INFO_2026-09-11_2.md`.
Скрипты Sage не менял. `h0_quotient.sage` сейчас крутится — ниже, почему его редукция неверна.

---

## 1. Самый дешёвый следующий Magma-запрос: J₂ над ℚ

Rank E₁(ℚ(√15)) = rank Jac(C)(ℚ), C : y² = 65 t(t²−1)(109t−229)(229t−109).

Это якобиан рода 2 **над ℚ**, нечётная степень, все точки Вейерштрасса рациональны. `RankBounds(J)` для такого — штатная функция Magma над ℚ, обычно секунды. Онлайн-калькулятор это переварит лучше, чем кривую над ℚ(√15).

Готовый payload (отдельный файл `GROK_REQUEST_TO_CODEX_MAGMA_J2.md`):

```
P<x> := PolynomialRing(Rationals());
f := 65*x*(x^2-1)*(109*x-229)*(229*x-109);
C := HyperellipticCurve(f);
print "genus:", Genus(C);
J := Jacobian(C);
print "2-torsion order:", #TwoTorsionSubgroup(J); // ожидается 16
print "RankBounds:", RankBounds(J);
```

Контроль: genus 2; #J[2]=16. Если верхняя граница 1 — **сечение s=1/5 закрыто** (нижняя 1 уже есть от G₀ / точки t=9/4). Если [1,3] — то же, что сейчас. Если 1 или 3, но не 1: дальше Fisher–Yan CT (github TomAFisher/Cassels-Tate-pairing--Genus-2), не FourDescent на E₁/K.

Второй прогон с 39 вместо 65 (класс 6) должен дать тот же ранг (изогенны над ℚ(√15)).

Не путать с H_A, H_B рода 3 из Codex (`RankBounds` [0,5] и [0,4]) — это другие кривые.

---

## 2. Остановить/не доверять текущему h0_quotient.sage

Редукция H₀ = k⁴u⁴ G(s,w), s=k+1/k, w=u+1/u **невозможна**: H₀ не инвариантен относительно (k,u)↦(k,1/u). Проверено на `h0.txt`. В носителе H₀/(k⁴u⁴) есть 18 мономов с ab<0.

Скрипт либо упадёт на assert, либо выдаст ложный G. Не ждать M1/M2 с этого файла как род фактора.

Правильный фактор одной инволюции (k,u)↦(1/k,1/u):

- инварианты s=k+1/k, w=u+1/u, p=(k−1/k)(u−1/u);
- p²=(s²−4)(w²−4);
- H₀/(k⁴u⁴)=A(s,w)+p B(s,w);
- образ в плоскости (s,w): Q := A² − (s²−4)(w²−4)B² = 0.

Это кривая в (s,w), не «(4,4)». Род Q — вот что нужно вместо G. Считать M1/M2 от Q, не от несуществующего G.

Я начал извлекать A,B через Чебышёва; первая версия не прошла численный контроль (знак ψ при отрицательных показателях). Не подкладывать те многочлены. Переписать редукцию в Laurent: орбита {k^a u^b + k^{-a} u^{-b}} = (θ_a θ_b + ψ_a ψ_b p)/2 с **знаковым** ψ_{-n}=−ψ_n. Контроль: A+pB в случайных (k,u) против H₀/(k⁴u⁴).

Пока `genus_h0.sage nom1` ест 9 ГБ — не запускать ещё один Singular genus параллельно.

---

## 3. Не ждать MordellWeilShaInformation на E₁/K

Codex уже отменил Effort:=2: онлайн-калькулятор не вернул вывод. Три изогенные модели могут снова дать [1,3] (ранг изогенно инвариантен; Ш — нет, но RankBounds часто одинаков). Имеет смысл **один** короткий J₂-запрос над ℚ вместо трёх квадратичных.

---

## 4. Если RankBounds(J₂)=[1,3] снова

Тогда не крутить E₁. Следующее:

1. `HasSquareSha(J)` в том же Magma-запуске (дешёво).
2. Fisher–Yan CT на 2-Selmer J₂ — код на github, attach в локальной Magma, если появится. Онлайн-калькулятор attach, скорее всего, не примет.
3. Richelot: 15 рациональных (2,2)-изогений (всё 2-кручение рационально). RankBounds на одном эллиптическом факторе **над ℚ** после Richelot, если фактор определён над ℚ. Сейчас факторы E₁ живут над ℚ(√15), так что Richelot над ℚ даёт абелеву поверхность / другую модель J, не E/ℚ.

---

## 5. Цепочка s=1/5

Звенья 0–3 независимо закрыты, 4 закрыто при rank=1, насыщение ⟨G₀⟩ закрыто оценкой Тамагавы. Не перепроверять. Единственный открытый шаг — верхняя граница ранга. J₂ над ℚ — прямой удар туда.

Конец.
