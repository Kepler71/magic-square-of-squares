# Независимая реализация эллиптического Шаботи для сечения (71,49,61)

Дата: 2026-09-12. Исполнитель: отдельная сессия (Claude Opus 5), **без загрузки и импорта**
`family/chabauty.sage`, `family/batch_chabauty.sage`, `family/mw_sieve.sage`.
Прочитан глазами только `family/pipeline.sage` (постановка семейства) и `FAMILY_SECTIONS.md`.
Весь код написан с нуля: `family/independent_chabauty/` (`setup.sage`, `explore.sage`, `gen.sage`,
`sat_ext.sage`, `orders.sage`, `chab.sage`, `verify.sage`, `diag.sage`, `final.sage`; логи `*.log` там же).

**Краткий итог: полный учёт точек для (71,49,61) ПОДТВЕРЖДАЕТСЯ, при p = 7**
(и независимо воспроизводится при p = 113). Однако подсчёт нулей у соседней реализации
избыточен: «призрачные нули при m = −1/2» — это не отдельные нули, а **дубли** тех же
p-адических «линий»; при корректном (без дублей) перечислении классов граница равна **8**,
и 8 = ровно сумма кратностей пяти известных точек. Необъяснённых нулей **нет**.

> **Продолжение:** та же реализация прогнана по всем 11 сечениям семейства —
> `INDEPENDENT_CHABAUTY_ALL_2026-09-12.md` (везде граница 8 = 8, полный учёт при одном простом;
> для (41,1,29) решето Морделла–Вейля не нужно: p = 41 даёт полный учёт).

---

## 1. Что построено заново

### 1.1 Вывод кривой (независимый, символически проверен)

Для сечения b² + h² = 2n², A = (h²+n²)/2, C = (b²+n²)/2, β′ = (C−A)/(C+A), e = 1 + β′:

* z = (t−1)/(t+1) переводит `D·W² = t(t²−1)(At−C)(Ct−A)` в
  `D·W² = cz·z(z²−1)(z²−β′²)`, **cz = −4(A+C)²** (проверено символически, `explore.sage`, `control cz: True`).
* u = z + β′/z, sb = √β′ ∈ k = ℚ(√dk), dk = sqfree((C−A)(C+A)):
  z(z²−1)(z²−β′²) = z³(u²−e²), (z+sb)² = z(u+2sb) ⇒ z³ = (z+sb)⁶/(u+2sb)³, откуда
  **D·V² = cz·(u²−e²)(u+2sb)**, V = W(u+2sb)²/(z+sb)³.
* Вейерштрасс: x = c₀u, c₀ = cz/D (с точностью до квадрата),
  **E: Y² = (x − c₀e)(x + c₀e)(x + 2·sb·c₀)**.

Для (71,49,61), D = 671: A = 3061, C = 4381, β′ = 660/3721, dk = **165**, sb = 2√165/61,
после сокращения на квадраты корни 2-кручения — **±2939651 и 163724√165**, т.е.

    E₁ : y² = (x − 163724√165)(x² − 2939651²),   x = −2496791·u

что **совпадает с моделью в постановке** (`pipeline.sage`, «(3) E1 над k»). Кручение E₁(k) = (ℤ/2)².

### 1.2 Точки с рациональным u

Если u ∈ ℚ и rhs — квадрат в k, то N(rhs) = c₀⁶(u²−e²)²(u²−4β′) — квадрат в ℚ,
значит u² − 4β′ ∈ ℚ², т.е. **u = z + β′/z с z ∈ ℚ** (обратно тривиально). Поэтому поиск ведётся по z.
Перебор z = p/q, |p|, q ≤ 200 (`explore.sage`) даёт для D = 671 ровно

| u | t = (1+z)/(1−z) | X = √t |
|---|---|---|
| ∞ (точка O) | ±1 | ±1 |
| 4381/3721 = e | C/A, ∞ | — |
| −4381/3721 = −e | 0, A/C | 0 |
| **71/61 = b/n** | **121, 36/25** | **11, 6/5** |

То есть известные точки E₁(k) с u ∈ ℙ¹(ℚ): **O, T₁ = (−2939651,0), T₂ = (2939651,0), ±G**,
G = (−2906101, 54028920√165 + 297159060), u(G) = 71/61. Третья точка 2-кручения
T₃ = (163724√165, 0) имеет иррациональное u. Итого **5 точек**.

### 1.3 Генератор и насыщенность (`gen.sage`, `sat_ext.sage`)

* ĥ(G) = **8.20254500578807** — совпадает со значением 8.2025 из `INDEPENDENT_CHECK_N61_2026-09-12.md`.
* Точный тест `(G − T).division_points(ℓ)` пуст для всех T ∈ tors и ℓ ∈ {2,3,5,7,11,13} — G не делится.
* Локальный тест насыщенности: для простого 𝔭 с ℓ | #E(F_𝔭) проверяется, что образ G − T не лежит
  в ℓ·E(F_𝔭) ни для одного T. Перебор 𝔭 с N(𝔭) < 3000² дал доказательство для **всех ℓ ≤ 71**
  и ещё для 89, 97, 101, 137, 139, 151, 157, 173, 223, 229, 233, 263, 269, 353, 397, 521, 541, 773, 1097, 1301, 1489, 1889.

⇒ E₁(k) = ⟨G⟩ ⊕ (ℤ/2)² при условии rank = 1 (доказано ранее и перепроверено — CTP).
Остаточное допущение: индекс не делится на простое ℓ ≥ 73; иначе нашлась бы точка с
ĥ ≤ 8.2025/73² ≈ 0.0015, что заведомо невозможно по любой разумной нижней оценке высоты
(строгой безусловной нижней оценки ĥ над k я не вычислял — это **единственное оставшееся допущение**).

---

## 2. Реализация Шаботи (`chab.sage`)

p расщепляется в k, хорошая редукция; σ₁, σ₂ : k → ℚ_p (w ↦ ±√dk).

* Формальная группа E₁ вычисляется **точно над k** (x(t), y(t), log, exp = log.reverse()).
* R = N·G, N = lcm порядков редукций Ḡ в E(F_𝔭₁), E(F_𝔭₂); t_R,i = −σ_i(x_R)/σ_i(y_R) ∈ p·ℤ_p.
* z_i(s) = exp_F(s·log_F(t_R,i)) — ряд по s.
* x(Q + [t]) считается **точно над k** по формуле хорды, затем φ(X) = 1/(X−γ),
  γ ∈ ℤ выбирается так, чтобы x − γ была единицей в обоих дисках; тогда φ(x(Q+[t])) ∈ ℤ_p[[t]].
* θ(s) = σ₁(Φ)(z₁(s)) − σ₂(Φ)(z₂(s)). θ(s) = 0 ⟺ σ₁x = σ₂x ⟺ x ∈ ℚ ∪ {∞}
  (x = a + bw, σ₁x − σ₂x = 2b√dk).

**Строгая оценка хвоста.** d_m ∈ ℤ_p (φ∘x регулярна и ограничена единицей на диске Q̄;
сдвиг на Q — автоморфизм гладкой ℤ_p-модели); exp_F(W) = Σ b_j W^j/j!, b_j ∈ ℤ_p (Silverman IV.6.4),
v_p(log_F(t_R)) = v_p(t_R) = v ≥ 1; мультиномиальные коэффициенты целы ⇒

    v_p(c_n) ≥ n·v − v_p(n!) ≥ n(p−2)/(p−1).

Для p = 7, M = 24: при n ≥ 24 имеем v₇(c_n) ≥ 5n/6 ≥ 20, а min v₇(c_n) ≤ 3 — хвост заведомо не мешает.
В прогоне явно проверено (а) целость всех коэффициентов Φ в обоих вложениях,
(б) выполнение оценки v_p(c_n) ≥ n·v − v_p(n!) на всех вычисленных коэффициентах.

**Сквозная проверка (`verify.sage`).** Для всех 24 классов и s ∈ {0, ±1, ±2, 3} значение ряда θ(s)
сравнивалось с прямым вычислением σ₁x(P) − σ₂x(P) по глобальной точке P = (m₀+Ns)G + T.
Совпадение во всех случаях с точностью до ошибки обрезания (единственное «расхождение» —
7¹⁸ против точного нуля при M = 20, т.е. ровно уровень обрезания).

---

## 3. Ключевая поправка: «призраки» = дубли классов

Для p нечётного и p ∤ #E(F_𝔭) расщепление E(k_𝔭) ≅ Ê × E(F_𝔭) **каноническое**
(Ext¹(конечная группа порядка, взаимно простого с p; ℤ_p) = 0). Формальная группа
без кручения ⇒ для T ∈ E(k)[2] формальная часть **равна нулю**: T = τ(T̄). Поэтому

    P(m) = m·G + T  ↦  τ(m₀Ḡ + T̄) + m·Ĝ,   m ∈ ℤ_p,   m ≡ m₀ (mod N),

и множество («линия»), пробегаемое при m ∈ ℤ_p, зависит **только от пары редукций**
a = (m₀Ḡ_i + T̄_i)_{i=1,2}, т.е. от образа точки в E(F_𝔭₁) × E(F_𝔭₂).
Число различных линий = |образ E(k) → E(F_𝔭₁) × E(F_𝔭₂)|, а не 4N.

Для (71,49,61), p = 7: |E(F_𝔭)| = 12 и 4, ord(Ḡ) = 6 и 2, N = 6, но T̄₃ = 3Ḡ **в обоих вложениях**,
поэтому 24 пары (T, m₀) склеиваются по две: **12 различных линий**.
(Совпадение вычисленных границ Штрассмана внутри каждой склейки — независимая проверка: True во всех 12.)

Симметрия: −P(m) = τ(−a) + (−m)Ĝ и x(−P) = x(P) ⇒ если −a = a (обе координаты 2-кручение),
то θ_L(m) чётна, и нуль при m = 0 имеет **чётный** порядок (≥ 2).

Отсюда «призрак при m = −1/2» в наивном перечислении: класс (T, m₀ = N/2) — это та же линия,
что и класс (T + T′, 0), где T′ — точка 2-кручения с T̄′ = (N/2)Ḡ; «нуль в s = −1/2» есть
**реальная точка** T + T′ при m = 0, а не призрак.

---

## 4. Результат для (71,49,61), p = 7 (`final.sage`)

12 линий, границы Штрассмана:

| линия (редукции 𝔭₁, 𝔭₂) | склейка (T, m₀) | Штрассман | известные точки |
|---|---|---:|---|
| (O, O) | (T₃,3), (O,0) | **2** | O при m = 0, кратность 2 |
| ((6:0:1), (6:0:1)) | (T₁,0), (T₂,3) | **2** | T₁ при m = 0, кратность 2 |
| ((1:0:1), (1:0:1)) | (T₁,3), (T₂,0) | **2** | T₂ при m = 0, кратность 2 |
| ((5:4:1), (5:0:1)) | (T₃,4), (O,1) | **1** | G при m = 1 |
| ((5:3:1), (5:0:1)) | (T₃,2), (O,5) | **1** | −G при m = −1 |
| остальные 7 линий | — | **0** | — |

**ИТОГО: нулей ≤ 8; известные точки дают ≥ 8 ⇒ равенство ⇒ других точек нет.**

Технические проверки (хвост, целость коэффициентов, оценка v_p(c_n), согласованность склеек): все True.

### Второй descent-класс

ELS-классы сечения: d₃ ∈ {±671, ±915}. 915/671 = (61√165/671)² — квадрат в k, поэтому
E_{+,915} ≅ E_{+,671} над k с **тем же** отображением u; аналогично −915 ~ −671.
Далее E_{+,−D} = (E_{+,D})^σ с заменой u ↦ −u (σ: √165 ↦ −√165), значит множество
рациональных u для D = −671 — это отражение множества для D = +671.
Прямой прогон `final.sage 71 49 61 -671 -71/61 7` независимо даёт **то же самое: 8 = 8, полный учёт**.

### Другие простые для (71,49,61)

| p | \|E(F_𝔭₁)\|, \|E(F_𝔭₂)\| | ord(Ḡ) | N | линий | нулей ≤ | известных | итог |
|---|---|---|---:|---:|---:|---:|---|
| **7** | 12, 4 | 6, 2 | 6 | 12 | **8** | 8 | **полный учёт** |
| 13 | — | — | — | — | — | — | плохая редукция (13⁴ \| disc) |
| 23 | 20, 28 | 10, 14 | 70 | 280 | — | — | не считал (слишком долго) |
| 29 | 32, 32 | 8, 8 | 8 | 32 | 10 | 8 | **2 лишних нуля** (линии T₃, m₀ = 3 и 5) |
| **113** | 96, 96 | 6, 12 | 12 | 48 | **8** | 8 | **полный учёт** |

Прогон при p = 29 важен как **отрицательный контроль**: реализация не выдаёт «полный учёт» автоматически —
при неудачном простом остаются лишние нули (как и отмечено в `FAMILY_SECTIONS.md` для других сечений).
Полного учёта при одном простом (p = 7, независимо подтверждено при p = 113) достаточно.

---

## 5. Контроли

| контроль | результат |
|---|---|
| s = 1/5 (17,7,13), A=109, C=229, k=ℚ(√15), **p = 11** | 12 линий, нулей ≤ **8**, известных 8 ⇒ **полный учёт** (совпадает с известным результатом) |
| то же, **p = 7** | нулей ≤ 8 = 8 ⇒ полный учёт |
| то же, **p = 17** | нулей ≤ 8 = 8 ⇒ полный учёт |
| модель E₁ для (71,49,61) | совпала с моделью постановки побитово (корни ±2939651, 163724√165) |
| ĥ(G) | 8.20254500578807 = значение независимой проверки CTP |
| сквозная проверка ряда θ на целых s (24 класса × 6 значений) | совпадение с прямым вычислением |
| согласованность границ внутри склеек линий | True (все линии, оба сечения, все простые) |
| отрицательный контроль | p = 29 для (71,49,61) даёт 10 > 8 — метод не «самоподтверждающийся» |

Контроль s = 1/5 воспроизводит известный результат ⇒ реализация валидна.

---

## 6. Вывод и что осталось непроверенным

**Подтверждается.** При rank E₁(ℚ(√165)) = 1 и насыщенности ⟨G, tors⟩ эллиптический Шаботи при p = 7
(и независимо при p = 113, и для обоих descent-классов d₃ = ±671)
даёт: единственные точки E₁(k) с u ∈ ℙ¹(ℚ) — это O, T₁, T₂, ±G, т.е.
u ∈ {∞, ±4381/3721, ±71/61} (по обоим descent-классам), откуда все рациональные t на C_J₂:

    t ∈ {0, ±1, ∞, A/C, C/A, 36/25, 121},  X² = t  ⇒  X ∈ {0, ±1, ±6/5, ±11}

— ровно вырожденные параметры сечения. Новых точек нет; сечение (71,49,61) закрыто.

**Расхождение в бухгалтерии.** Соседняя реализация даёт «15 нулей» и объясняет часть из них
«призраками m = −1/2». Моя реализация при наивном перечислении 4N = 24 классов даёт 16
(разница с 15, по-видимому, в трактовке диска точки O: у меня там честный **двойной** нуль).
При корректном перечислении по 12 различным линиям граница равна 8 и совпадает с суммой
кратностей пяти известных точек **без остатка**: «призраки» — артефакт двойного счёта
(T̄₃ = 3Ḡ в обоих вложениях). Содержательный вывод соседней реализации при этом верен.

**Осталось непроверенным / допущения:**
1. rank E₁(k) = 1 — берётся извне (спуск + CTP; уже перепроверено другой сессией). Я его не перевычислял.
2. Насыщенность ⟨G, tors⟩: доказана для всех ℓ ≤ 71 (локально) и ℓ ≤ 13 (точно, division_points).
   Для ℓ ≥ 73 — эвристика по высоте (ĥ(G)/ℓ² < 0.0016); строгой безусловной нижней оценки
   канонической высоты над k не вычислял.
3. Сведение исходной задачи (магический квадрат) к C_J₂ и полнота списка 4 ELS-классов —
   берутся из постановки (`pipeline.sage`), заново не проверялись.
4. Целость d_m ∈ ℤ_p обоснована теоретически (гладкая модель) и проверена численно во всех классах,
   но не доказана формально в коде.
