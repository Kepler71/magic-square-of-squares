СТАТУС: результат — теорема о ранге E(a,b) через 3-изогению (доказана вручную, локальные леммы — вручную; вычислительно подтверждена на всех 12 231 парах b ≤ 200) + точные 2-изогенные оценки (ПО) + покрытие наклонов
ДЛЯ: всем; Codex/Claude — проверить доказательство §2 (особенно лемму C(iv), p = 3); Grok — литература по 3-спуску для y² = x³ + (αx+β)²
ИТОГ: для E(a,b) доказан явный критерий: rank ≤ dim₃V + dim₃W − 1, где V и W заданы кубическими вычетами и кубическими характерами по простым делителям a, b, a+b (тип 1) и a−b, a+2b, 2a+b (тип 2); он закрывает 173 из 1101 пар при b ≤ 60 (1262 из 12 231 при b ≤ 200), в том числе пару (50,59), где 2-спуск PARI даёт [0,2]; вместе с точными 2-изогенными оценками семья Z/2×Z/6 закрывает 547 наклонов со знаменателем ≤ 200, НЕ закрытых семьёй Z/2×Z/4 (12 + 78 + 457), и покрытие поднимается до 701/710, 2238/2332, 8398/9188
ОТМЕНЯЕТ: ничего; уточняет оценку §4 THEOREMS_Z2Z6 (Codex): границу через Γ можно заменять или дополнять границей через 3-изогению
ПРОВЕРЕНО: мной, свой код (`fable_z2z6/`): сертифицированный расчёт локальных образов на всех плохих простых 12 231 пар совпал с формулой теоремы (0 расхождений); граница ни разу не ниже нижней оценки PARI (1101 пар b ≤ 60 + 300 случайных при 60 < b ≤ 200); разрыв «оценка − ранг» на 211 парах ранга 0 совпал с 3-частью Ш_an(E)·Ш_an(E′) (211/211); арифметические шаги доказательств проверены на всех 12 231 парах (0 нарушений). Внешней рецензии нет. Проверка НЕ слепая (см. ЧИТАЛ)
ЧИТАЛ: PROTOCOL_NOTES, THEOREMS_Z2Z6 (Codex, §1–5 целиком), RESULT_ODD_CRITERION (Claude), check_codex_z2z6/NOTE_CHECK_Z2Z6, RESULT_29_WALLS §3 (лемма о кручении), selmer_exact.py (Claude; использован как решатель квартик для §5), coverage_exact.json (старое покрытие, как контрольные данные). Код Codex не открывал. Ожидаемых ответов для 3-изогении ни у кого не было
ЧИТАТЬ ДАЛЬШЕ, ЕСЛИ: проверяете доказательство (§2), хотите применить критерий (§1, §3) или сравнить покрытие (§6)

# Теорема о 3-изогении для семьи Z/2×Z/6 и покрытие наклонов

14 сентября 2026. Fable. Код и журналы: `/home/kep/magicKube/fable_z2z6/` (список в §9).

## 0. Обозначения

a, b — взаимно простые натуральные, a ≠ b, c = a+b. Кривая Codex E(a,b): y² = x(x+M)(x+N), M = b³(2a+b), N = a³(a+2b).
Сдвиг x → x + a²b² даёт (тождество проверено символьно, `basics.sage`):

    E:  y² = (x + a²b²)(x + b²c²)(x + a²c²) = x³ + (αx + β)²,   α = a² + ab + b²,   β = a²b²(a+b)².

T = (0, β) — точка перегиба (касательная y = αx + β пересекает E только в T), значит **точка порядка 3** — доказано.
Дискриминанты (символьно, `velu.sage`):

    Δ(E) = 16 a⁶b⁶(a+b)⁶ · (a−b)²(a+2b)²(2a+b)²,        4α³ − 27β = ((a−b)(a+2b)(2a+b))².

φ: E → E′ = E/⟨T⟩ (формулы Велю; E′: y² = x³ + α²x² − 18a²b²c²α·x − a²b²c²(a²+ab+4b²)(4a²+ab+b²)(4a²+7ab+4b²)),
φ̂: E′ → E — двойственная, φ̂∘φ = [3]. Δ(E′) = 16 a²b²(a+b)² (a−b)⁶(a+2b)⁶(2a+b)⁶.

**Типы простых.** Положим F₁ = ab(a+b), F₂ = (a−b)(a+2b)(2a+b).

- P₁ = {p нечётное : p | F₁} ∪ {2, если 4 | F₁};
- P₂ = {p нечётное : p | F₂} ∪ {2, если 2 ∥ F₁}.

(Ровно одно из a, b, a+b чётно, так что 2 всегда попадает ровно в одно из множеств.)
**Лемма A (доказано).** P₁ ∩ P₂ = ∅, и P₁ ∪ P₂ ∪ {3} ⊇ все простые плохой редукции E.
*Доказательство.* Для p ≥ 5: НОД любых двух из шести форм делит 6 (например (a, a+2b) | 2, (a−b, a+2b) | 3, (a+2b, 2a+b) | 3, (a−b, a+b) | 2, остальные пары взаимно просты), поэтому p делит не более одной формы. Для p = 3: 3 | a−b ⇔ 3 | a+2b ⇔ 3 | 2a+b, а по модулю 3 всегда a ≡ ±b или 3 | ab, так что 3 делит ровно одно из F₁, F₂. Для p = 2 — по определению. Плохие простые делят Δ(E). ∎

## 1. Теорема

**Теорема 1 (доказано; §2).** Пусть

- V = { d ∈ ℚ*/ℚ*³ : d = ∏_{p∈P₁} p^{e_p}, e_p ∈ {0,1,2}, причём d — кубический вычет по модулю каждого q ∈ P₂ с q ≡ 1 (mod 3), и d ≡ ±1 (mod 9), если 3 ∈ P₂ };
- W = { χ — характер Дирихле порядка 1 или 3 с кондуктором, делящим 9^{[3∈P₂]} · ∏_{q∈P₂, q≡1(3)} q, такой что χ(p) = 1 для всех p ∈ P₁ }.

V и W — векторные пространства над 𝔽₃. Тогда

    rank E(a,b)(ℚ) ≤ dim₃ V + dim₃ W − 1.

Всегда 4ab(a+b) ∈ V (это образ точки T), так что dim V ≥ 1, если 4ab(a+b) — не куб.

**Следствие (критерий ранга 0).** Если (i) единственный характер в W — тривиальный и (ii) V = ⟨4ab(a+b)⟩ одномерно, то rank E(a,b) = 0, и по лемме о кручении Codex (RESULT_29_WALLS §3, доказана) на обоих наклонах, которым отвечает пара (a,b), нет магического квадрата с шестью клетками 1±az, 1±bz, 1±(a+b)z (см. §6).

Пример (2,3): F₁ = 30, F₂ = −56. P₁ = {3, 5}, P₂ = {2, 7}. V: d = 3^e 5^f, кубический вычет mod 7 — кубы mod 7 суть {±1}; 3 и 5 — не кубы, 15 ≡ 1 — куб: V = ⟨15⟩, dim 1 (и 4·2·3·5 = 120 ≡ 15 ✓). W: характер кондуктора 7 должен иметь χ(3) = χ(5) = 1, но 3 — образующая (ℤ/7)*, значит χ = 1: dim W = 0. Ранг 0. (Codex получил то же через Γ, §4 его записки.)

Пример (50,59) — **новое**: PARI `ellrank` даёт [0, 2] (2-спуск не закрывает), теорема 1 даёт V = ⟨4·50·59·109⟩, W = 0, ранг 0 — доказано.

## 2. Доказательство

### 2.1. Аппарат (стандартный)

Модуль E[φ] = ⟨T⟩ ≅ ℤ/3 с тривиальным действием Галуа, потому по спариванию Вейля E′[φ̂] ≅ μ₃. Точная последовательность
0 → E′(ℚ)[φ̂]/φ(E(ℚ)[3]) → E′(ℚ)/φE(ℚ) → E(ℚ)/3E(ℚ) → E(ℚ)/φ̂E′(ℚ) → 0
с E′(ℚ)[φ̂] = μ₃(ℚ) = 0 и |E(ℚ)/3E(ℚ)| = 3^{rank}·|E(ℚ)[3]| = 3^{rank+1} (E(ℚ)_tors = ℤ/2×ℤ/6) даёт

    3^{rank+1} = |E′(ℚ)/φE(ℚ)| · |E(ℚ)/φ̂E′(ℚ)| ≤ |Sel^φ(E)| · |Sel^φ̂(E′)|.                       (1)

Отображения Куммера. Функция f = y − αx − β имеет дивизор 3(T) − 3(O), и
δ: E(ℚ)/φ̂E′(ℚ) ↪ H¹(ℚ, μ₃) = ℚ*/ℚ*³, δ(P) = y − αx − β (для P ≠ O, T; δ(T) = δ(−T)⁻¹ = (−2β)⁻¹ ≡ 4β² ≡ 4ab(a+b)).
Двойственно, над K = ℚ(√−3): E′[φ̂] порождена T′ = (x′₀, s√−3), x′₀ = −4α²/3 — единственный рациональный корень 3-многочлена деления E′ (символьно, `velu.sage`), и
δ′: E′(ℚ)/φE(ℚ) ↪ H¹(ℚ, ℤ/3) ≅ (K*/K*³)^{−}, δ′(P′) = y′ − ℓ(x′), ℓ — касательная к E′ в T′.
Локальные образы δ_v(E(ℚ_v)) ⊂ H¹(ℚ_v, μ₃) и δ′_v(E′(ℚ_v)) ⊂ H¹(ℚ_v, ℤ/3) — точные аннуляторы друг друга при локальном спаривании Тэйта; при хорошей редукции и v ∤ 3 образ δ_v — неразветвлённые классы (единицы по модулю кубов), образ δ′_v — неразветвлённые характеры. Над ℝ обе группы H¹ тривиальны. (Кассельс; Шефер–Штолль, «How to do a p-descent», §3.)

Таким образом Sel^φ̂(E′) = {d ∈ ℚ*/ℚ*³ : v_p(d) ≡ 0 (3) вне плохих простых и 3; d ∈ im δ_p при всех плохих p и p = 3},
Sel^φ(E) = {χ : кондуктор χ делится только на плохие простые и 3; χ_p ∈ im δ′_p при всех таких p} = {χ : χ_p ⊥ im δ_p}.
Остаётся вычислить im δ_p для p ∈ P₁ ∪ P₂ ∪ {3} — это леммы B, C.

### 2.2. Лемма B (типы редукции и положение T) — доказано

Обозначим E₀(ℚ_p) ⊃ E₁(ℚ_p) — точки с неособой редукцией и ядро редукции (для минимальной модели).

**(i) p ≥ 5.** p делит ровно одну форму (лемма A), с показателем v. В модели y² = x(x+M)(x+N):
- p | a: N ≡ 0 (v_p(N) = 3v), M ≡ b⁴ — единица-квадрат. Узел в (0,0) с касательными y = ±b²x: **расщеплённая мультипликативная**, минимальная (v(c₄) = 0), тип I_{6v}. T = (a²b², a²b²c²) ≡ (0,0) — **T в узле**, T ∉ E₀.
- p | b: симметрично (N ≡ a⁴). T в узле.
- p | a+b: M − N = (b−a)(a+b)³ ≡ 0, узел в x = −M; −M ≡ a⁴ (из b ≡ −a) — расщеплённая, I_{6v}; x(T) + M = b²(a+b)² ≡ 0, y(T) ≡ 0 — **T в узле**.
- p | a−b: узел в x = −M ≡ −N, v_p(M−N) = v, I_{2v}; касательные y² = −M·(x+M)² с −M ≡ −3b⁴: **расщеплённая ⇔ (−3/p) = 1 ⇔ p ≡ 1 (3)**; x(T) + M ≡ 4b⁴ ≠ 0 — **T ∈ E₀**.
- p | a+2b: узел в 0 (N ≡ 0, v_p(N) = v), касательные y² = Mx², M ≡ −3b⁴: расщеплённая ⇔ p ≡ 1 (3); x(T) = a²b² ≢ 0 — T ∈ E₀.
- p | 2a+b: узел в 0 (M ≡ 0), N ≡ −3a⁴: расщеплённая ⇔ p ≡ 1 (3); T ∈ E₀.

**(ii) p = 3, тип 1 (3 | F₁).** Те же вычисления, что в (i) для p | a, p | b, p | a+b (они не используют p ≠ 3): расщеплённая мультипликативная, T в узле.

**(iii) p = 2.** Пусть F — чётная из a, b, a+b, v = v₂(F). Модель Фрея: для y² = x(x−A)(x+B) с B ≡ A+1 (mod 4), 16 | B подстановка x = 4X, y = 8Y + 4X даёт целую модель Y² + XY = X³ + ((B−A−1)/4)X² − (AB/16)X с редукцией Y² + XY = X³ + c̄X² — узел в (0,0), расщеплённый ⇔ c = (B−A−1)/4 чётно.
- 2 | a: A = −M, B = N = a³(a+2b), v₂(B) ≥ 5, M = b³(2a+b) нечётно. Если v ≥ 2: 2a+b ≡ b (mod 8), M ≡ b⁴ ≡ 1 (mod 8), N ≡ 0 (mod 128): условия выполнены, c чётно — расщеплённая; X(T) = a²b²/4 ≡ 0 (mod 4), Y(T) = a²b²((a+b)²−1)/8 ≡ 0 (mod 2) — T в узле. Если v = 1: 2a+b ≡ b+4 (mod 8), M ≡ 5 (mod 8), c нечётно — **нерасщеплённая**; X(T) = (a/2)²b² нечётно — T ∈ E₀.
- 2 | b: симметрично.
- a, b нечётны: сдвиг x → x − M: y² = x(x−M)(x+(N−M)), A = M, B = N − M = (a−b)(a+b)³, v₂(B) = v₂(a−b) + 3v₂(a+b) ≥ 5; N − 2M − 1 ≡ −2(1+ab) (mod 8), так что B − A ≡ 1 (mod 4) всегда, а c ≡ (1+ab)/2 (mod 2): c чётно ⇔ ab ≡ 3 (4) ⇔ 4 | a+b (тип 1, расщеплённая) и c нечётно ⇔ 4 | a−b (тип 2, нерасщеплённая). X(T) = b²(a+b)²/4: при 4 | a+b это ≡ 0 (mod 4) и Y(T) = b²(a+b)²(a²−1)/8 чётно — T в узле; при 2 ∥ a+b это нечётно — T ∈ E₀.
Итог: **при p = 2 тип 1 = расщеплённая с T в узле, тип 2 = нерасщеплённая с T ∈ E₀.** (Модель минимальна, так как редукция узловая.)

**(iv) p = 3, тип 2 (a ≡ b (mod 3)).** Пишем b = a + 3t. Тогда α = (a−b)² + 3ab, v₃(α) = 1; β = a²(a+3t)²(2a+3t)² ≡ 4a⁶ ≡ 4 (mod 9). c₄(E) = 16·α·(a⁶ + 3a⁵b − 5a³b³ + 3ab⁵ + b⁶), и разложение по степеням 3t даёт второй множитель ≡ 3a⁶ (mod 27); значит v₃(c₄) = 2 < 4, модель y² = x³ + (αx+β)² **минимальна при 3**. Редукция: y² = x³ + 1 = (x+1)³ — **куспидальная (аддитивная)**, Ẽ_ns(𝔽₃) ≅ 𝔽₃, |E₀/E₁| = 3. Точки порядка 2: x = −a²b², −b²c², −a²c² ≡ −1 (mod 3) — все три лежат в куспиде, т.е. E(ℚ₃)[2] ∩ E₀ = 0, откуда (ℤ/2)² ↪ E(ℚ₃)/E₀ = Φ(𝔽₃); у аддитивной редукции |Φ| ≤ 4, значит **c₃(E) = 4, и 3 ∤ c₃**. T ∈ E₀ (иначе 3 | |Φ|) и T ∉ E₁ (у формального группового закона над ℤ₃ нет 3-кручения: e = 1 < p − 1).

Все утверждения леммы B численно сверены с `local_data` Sage на 1101 паре b ≤ 60 (`kodaira.log`: типы I_{6v}/I_{2v} при p = 3, I_{6(v−1)} (c = 6(v−1)) / нерасщеплённая I_{2(v′−1)} (c = 2) при p = 2, I*_{2k} с c = 4 при p = 3 тип 2), а сравнения по модулю 8, 9, 27 — на всех 12 231 парах b ≤ 200 (`verify_steps.py`, 0 нарушений).

### 2.3. Лемма C (локальные образы) — доказано

**C1. Мультипликативная расщеплённая редукция, T в узле** (все p ∈ P₁, включая 2 и 3). По теореме Тэйта E(ℚ_p) ≅ ℚ_p*/q^ℤ, причём E₀ ↔ ℤ_p*. Точка T порядка 3 вне E₀ отвечает классу t с t³ ∈ q^ℤ, t ∉ ℤ_p*·q^ℤ; заменяя T на −T и t на t·q^m, добиваемся t³ = q. Гомоморфизм ℚ_p*/q^ℤ → ℚ_p*/t^ℤ, индуцированный тождественным отображением, имеет ядро ⟨T⟩, значит E′ ≅ кривая Тэйта с параметром t, φ индуцирована тождественным, а φ̂ (единственная с φ̂φ = [3]) — возведением в куб. Тогда
E(ℚ_p)/φ̂E′(ℚ_p) = ℚ_p*/⟨q, ℚ_p*³⟩ = ℚ_p*/ℚ_p*³ (q = t³ — куб).
Поскольку δ_p инъективно, **im δ_p = ℚ_p*/ℚ_p*³ (всё)**: dim 2 при p ≡ 1 (3) и p = 3, dim 1 иначе.

**C2. Мультипликативная расщеплённая, T ∈ E₀** (p ∈ P₂, p ≡ 1 (mod 3)). T ↔ единица t с t³ = 1, т.е. t = ζ₃ ∈ ℚ_p. φ: u ↦ u³ (ядро μ₃ = ⟨T⟩), E′ = ℚ_p*/q^{3ℤ}, φ̂ индуцирована тождественным и **сюръективна**: im δ_p = 1.

**C3. Мультипликативная нерасщеплённая, T ∈ E₀** (p ∈ P₂, p ≡ 2 (mod 3), и p = 2 в типе 2). Пусть L/ℚ_p — неразветвлённое квадратичное расширение; E(ℚ_p) ≅ {u ∈ L* : N_{L/ℚ_p}(u) ∈ q^ℤ}/q^ℤ (Тэйт, нерасщеплённый случай). T ↔ ζ₃ ∈ L (N(ζ₃) = ζ₃^{p+1} = 1). φ = возведение в куб, E′(ℚ_p) = {w : N(w) ∈ q^{3ℤ}}/q^{3ℤ}, φ̂ — тождественное. Для u с N(u) = q^k берём j ≡ 2k (mod 3): w = u·q^{−j} имеет N(w) = q^{k−2j} ∈ q^{3ℤ}, и u ≡ φ̂(w) (mod q^ℤ). Значит φ̂ сюръективна, **im δ_p = 1**.

**C4. p = 3, тип 2** (аддитивная). Достаточно показать, что δ₃ тривиально на E₁(ℚ₃) и на T, ибо тогда δ₃ пропускается через E(ℚ₃)/(E₁ + ⟨T⟩), группу порядка |E/E₀|·|E₀/E₁|/3 = 4·3/3 = 4 (лемма B(iv)), а группа порядка 4 отображается в группу экспоненты 3 тривиально.
- T: δ(T) ≡ 4ab(a+b) = 4a(a+3t)(2a+3t) ≡ 8a³ = (2a)³ (mod 9), т.е. 4ab(a+b)/(2a)³ ≡ 1 (mod 9) — куб в ℤ₃.
- P ∈ E₁ с формальным параметром z ∈ 3ℤ₃: x = z⁻²(1 − α²z² + …), y = −z⁻³(1 − α²z² + …), откуда
  y − αx − β = −z⁻³·(1 + αz − α²z² + βz³ + O(z⁴)), и в скобке v₃(αz) ≥ 2, v₃(α²z²) ≥ 4, v₃(βz³) ≥ 3: скобка ≡ 1 (mod 9) — куб; −z⁻³ — куб.
Итак **im δ₃ = 1**. ∎

Сводка: **im δ_p = всё при p ∈ P₁ (включая 3 ∈ P₁ и 2 ∈ P₁), im δ_p = 1 при p ∈ P₂ (включая 3 ∈ P₂ и 2 ∈ P₂).** По двойственности im δ′_p = 1 при p ∈ P₁ и im δ′_p = всё при p ∈ P₂.

### 2.4. Сборка

Sel^φ̂(E′): d должен быть кубом в ℚ_p при p ∈ P₂ и вне плохих простых, без условий при p ∈ P₁. Куб в ℚ_p для единицы d: автоматически при p ≡ 2 (3) и p = 2; кубический вычет при p ≡ 1 (3); d ≡ ±1 (mod 9) при p = 3. Значит носитель d ⊂ P₁, и Sel^φ̂ = V.
Sel^φ(E): χ_p = 1 при p ∈ P₁ (неразветвлён и χ(p) = χ(Frob_p) = 1), χ неразветвлён вне P₂ ∪ {3}, без условий при p ∈ P₂ (в том числе допускается ветвление при p ≡ 1 (3) и при 9, если 3 ∈ P₂). Циклические кубические характеры с кондуктором на {q ∈ P₂ : q ≡ 1 (3)} ∪ {9 при 3 ∈ P₂} — это в точности W без условия χ(p) = 1; с ним Sel^φ = W.
Из (1): rank + 1 ≤ dim V + dim W. ∎

Замечание. Для ручного счёта: dim V = |P₁| − rank₃ H_V, где строки H_V — векторы (ind_q(p))_{p∈P₁} по q ∈ P₂, q ≡ 1 (3) (ind_q — дискретный логарифм кубического символа) и (ind₉(p))_{p∈P₁} при 3 ∈ P₂; dim W = |{q ∈ P₂ : q ≡ 1 (3)}| + [3 ∈ P₂] − rank₃ H_W, строки H_W — (ind_q(p))_q по p ∈ P₁. Реализация: `explicit_formula.py`.

## 3. Контроли

Все — свой код, Sage 10.9 (PARI внутри), каталог `fable_z2z6/`.

1. **Сертифицированный локальный расчёт** (`three_isog.sage`), не использующий лемм B, C: для каждого плохого p ищутся точки E(ℚ_p) и E′(ℚ_p), считаются классы δ, δ′ (в ℚ_p*/ℚ_p*³ и в (K_v*/K_v*³)^−; при p = 3 — по модулю λ⁴, λ = √−3; при p = 2 — в целом базисе {1, ζ₃}); найденные подгруппы — нижние оценки образов, и по двойственности Тэйта равенство dim(найдено) + dim(найдено′) = dim H¹(ℚ_p, μ₃) доказывает точность обоих. Затем V, W собираются перебором. **На всех 12 231 парах 1 ≤ a < b ≤ 200 сертификация прошла, и размерности совпали с формулой теоремы: 0 расхождений** (`explicit_200.log`). Локальные образы совпали с леммой C на всех плохих простых всех 1101 пар b ≤ 60 (`local_rules.py`: 3 тип 1 — 9 классов, тип 2 — 1; p = 2 — 3 или 1 в точности по правилу «4 | F₁»).
2. **Отрицательный контроль (PARI):** граница теоремы ≥ нижней оценки `ellrank` на всех 1101 парах b ≤ 60 и на 300 случайных парах 60 < b ≤ 200 — 0 нарушений (`analyze.py`, `sample_pari.log`).
3. **Положительный контроль:** теорема даёт ранг 0 у 173 из 1101 пар (b ≤ 60; PARI: верхняя оценка 0 у 462) и у 1262 из 12 231 (b ≤ 200). Образ точки T, 4ab(a+b), лежит в V всегда (assert на 12 231 парах). Отображения δ и δ′ проверены на гомоморфность и обнуление на φ(E(ℚ)), φ̂(E′(ℚ)) для образующих Морделла–Вейля 7 пар (`delta_prime_control.sage`, 19/19).
4. **Контроль разрыва через Ш_an** (`sha_control.log`): для всех 211 пар ранга 0 при b ≤ 40 условие «оценка > 0» выполняется тогда и только тогда, когда 3 | Ш_an(E)·Ш_an(E′) — 211 из 211. Примеры: (6,17): оценка 2, Ш_an(E′) = 9; (27,28): оценка 4, Ш_an(E′) = 81. Это объясняет слабость критерия: лишнее сидит в Ш(E′)[φ̂], а не в ошибке.
5. **Чётность:** на всех 1101 парах 3-изогенная граница ≡ 2-изогенной (§5) по модулю 2 — 0 несовпадений.
6. **Общность лемм при p = 2, 3** (`generic_local.sage`, случайные (α, β) вне семьи): правило при p = 2 («v₂(β) ≥ 4 ⇒ всё, v₂(β) = 2 ⇒ ничего») и «3 | β, 3 ∤ α ⇒ всё» держатся для произвольных α, β, а правило «v₃(α) = 1, 3 ∤ β ⇒ ничего» **не общее**: при β ≢ 4, 5 (mod 9) образ одномерен. Для семьи β ≡ 4 (mod 9), и лемма C4 доказана именно с этим — наблюдение, подтверждающее, что использование сравнения β ≡ 4 (mod 9) в доказательстве существенно.

## 4. Что даёт 3-изогения по сравнению с 2-спуском

При b ≤ 60: ранг 0 у 173 пар по теореме 1, у 349 — по лучшей из трёх точных 2-изогенных оценок (§5), у 386 — хотя бы одной; 37 пар закрываются только 3-изогенией: (11,26), (29,38), (24,43), (1,28), (13,34), (27,37), (1,43), (22,31), (10,43), (3,50), (1,33), (19,54), (17,59), (4,25), (2,47), (49,58), (1,45), (13,48), (1,49), (27,53), (6,37), (30,37), (9,41), (55,58), (23,59), (18,31), (18,43), (5,57), (1,26), (23,41), (19,43), (4,19), (20,27), (34,49), (32,53), (50,59), (31,50).
Пара **(50,59)** (наклоны 50/59, 50/109, 59/109): полный 2-спуск PARI даёт [0,2] — здесь Ш[2] ≠ 0 или спуск не хватает; 3-изогения даёт ранг 0 — доказано.
Обратная сторона: у 461 из 1101 пар 3-изогенная оценка выше верхней оценки PARI (разрыв всегда чётный, 2 или 4) — это Ш(E′)[φ̂] (контроль 4). Кривая E′ (ядро μ₃) систематически имеет большое 3-Ш; спуск через φ̂ «не тем концом».

## 5. Точные 2-изогенные оценки (доказано (ПО) для каждой пары)

Для каждой из трёх точек порядка 2 (e₀ = 0, e₁ = −M, e₂ = −N) кривая переносится в y² = x(x² + A_i x + B_i), A_i = h_i + k_i, B_i = h_i k_i (обозначения Codex), E′_i: Y² = X(X² − 2A_iX + (h_i−k_i)²); группы Селмера считаются точным решателем Claude `selmer_exact.locally_solvable` по всем простым, делящим 2B_i(h_i−k_i), и над ℝ (`two_isog.py`); граница rank ≤ log₂(|S^φ_i||S^φ̂_i|) − 2, берётся минимум по i. Контроль: ≥ нижней оценки PARI на тех же 1401 парах, 0 нарушений. Числа (b ≤ 60): ранг 0 по корню e₀ — 127, e₁ — 209, e₂ — 161, по минимуму — 349. Равномерных ручных лемм для 2-изогении здесь не выписываю: механизм тот же (кривая Тэйта; нерасщеплённые случаи дают подгруппы индекса 2, требующие отдельного описания), и «одна строгая теорема» — это §1.

## 6. Покрытие наклонов (цель B)

Наклон k = r/s (0 < r < s, (r,s) = 1, 2r ≠ s) даёт две тройки {a, b, a+b}: (r, s−r) и (r, s); ранг 0 у E(a,b) исключает наклон по лемме о кручении Codex. Считалось по всем 12 231 парам b ≤ 200 (`coverage.py`, `coverage_result.json`). Старое покрытие — семья Z/2×Z/4 с точным Селмером 2-изогении по 12 парам форм (Claude, `coverage_exact.json`).

| знаменатели | наклонов | теорема 1 | 2-изогения (§5) | Z/2×Z/6 всего | старая Z/2×Z/4 | **новых** (закрыто теперь, открыто раньше) | новых, закрытых теоремой 1 (из них только ею) | открыто после обеих семей |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 2–48 | 710 | 271 | 429 | 462 | 689 | **12** (из 21) | 7 (2) | 9 |
| 49–100 | 2332 | 569 | 1002 | 1180 | 2160 | **78** (из 172) | 44 (21) | 94 |
| 101–200 | 9188 | 1810 | 3106 | 4012 | 7941 | **457** (из 1247) | 207 (137) | 790 |

Суммарно семья Z/2×Z/6 закрывает **547 наклонов, не закрытых семьёй Z/2×Z/4** (из них 160 — только теоремой 1); объединённое покрытие 701/710 (98.7 %), 2238/2332 (96.0 %), 8398/9188 (91.4 %). Сама по себе семья Z/2×Z/6 слабее старой (462 против 689 при s ≤ 48), но дополняет её. Список новых — в `coverage_result.json` («newly»); среди них шесть из семи «пересмотренных оснований» Codex (41/60, 38/63, 33/97, 80/97, 41/125, 92/141; седьмое, 21/143, изогенными оценками не закрывается — у Codex оно закрыто полным 2-спуском), а также 9/31, 17/38, 19/41, 17/47, 23/47 и др.
Открытые при s ≤ 48 после обеих семей: 24/29, 29/30, 28/39, 20/41, 40/41, 41/42, 3/43, 17/43, 40/43 — они и есть «трудные» Codex/Claude.
Трудные наклоны: 11/142 — пары (11,131): оценки (3-из., 2-из.) = (1,1), (11,142): (3,3); 48/163 — (48,163): (3,1), (48,115): (1,1); **не закрыты**. 79/110: (31,79): (1,1), (79,110): (1,1) — не закрывается (у Claude закрыт через род 2). 19/60: (19,41): (3,1), (19,60): (2,2) — не закрывается (у Codex — спаривание Касселса–Тэйта).

## 7. Что не доказано и что честно сказать

- Проверка не слепая: я прочёл записки Codex и Claude до расчёта; но ожидаемых значений для 3-изогенных групп Селмера ни у кого не было, и мой расчёт при p ≥ 5, 2, 3 не опирался на их код.
- Новизна: 3-спуск для кривых с рациональной точкой порядка 3 — классика (Сельмер; Кассельс; Коэн–Пазуки 2009 «Elementary 3-descent with a 3-isogeny»; Топ; Фишер). Специализация к E(a,b) с двумя типами простых, по-видимому, не выписывалась; проверить литературу без сети не могу — **не объявлять новым**.
- Теорема 1 — только достаточное условие ранга 0; разрыв объясняется Ш(E′)[φ̂] (контроль 4, при BSD), не ошибкой.
- Ссылки на стандартные факты (Тэйт, Шефер–Штолль, двойственность) даны без страниц; при рецензии их стоит выверить.

## 8. Не делать

- Не запускать полный перебор Γ или матричный критерий Codex для пар, уже закрытых теоремой 1 (список: `grid/shard_200_*.jsonl`, поле `isog3.bound = 0`; 1262 пары).
- Не пытаться усилить 3-изогенную границу через «второй» спуск на E′: разрыв — это Ш(E′)[φ̂] (контроль 4); полезнее спаривание Касселса–Тэйта на Sel^φ̂(E′) или полный 3-спуск на E.
- Не считать PARI `ellrank` для пар b ≤ 60 и выборки 300 пар — сделано (`grid/shard_60_*.jsonl`, `sample_pari.jsonl`).
- Не искать наклоны 11/142, 48/163 в этой семье через 2- или 3-изогенные оценки — обе кривые каждого наклона имеют границу ≥ 1.
- Не повторять контроль Ш_an при b ≤ 40 — сделан (211 пар).

## 9. Файлы (`/home/kep/magicKube/fable_z2z6/`)

`basics.sage` (порядок T, модель), `velu.sage` (E′, дискриминанты, формальная группа φ, 3-многочлен деления E′), `three_isog.sage` (сертифицированные группы Селмера 3-изогении), `two_isog.py` (точные 2-изогенные оценки по трём корням; решатель — `criterion_proof/selmer_exact.py` Claude), `grid.sage` + `grid/shard_60_*.jsonl` (1101 пар с PARI) и `grid/shard_200_*.jsonl` (12 231 пар), `analyze.py`, `local_rules.py`, `explicit_formula.py` (формула теоремы; `explicit_200.log`), `kodaira.sage`/`kodaira.log` (типы редукции), `generic_local.sage`/`.log`, `sha_check.sage`, `sha_control.sage`/`.log`/`.jsonl`, `sample_pari.sage`/`.log`/`.jsonl`, `delta_prime_control.sage`, `verify_steps.py`, `coverage.py`/`coverage.log`/`coverage_result.json`.
Воспроизведение: `sage grid.sage <shard> <nshards> 60` (с PARI) или `... 200 nopari`; затем `python3 analyze.py 60`, `python3 explicit_formula.py 200`, `python3 coverage.py`.
