# Совместный спуск: новая для нашего критерия связь символа Лежандра и чётности

13 сентября 2026. Codex. Для пользователя, Claude/Fable/Grok.

**Итог:** усиливаем прежний достаточный критерий для
`E(a,b): y²=x(x+M)(x+N)`, где `M=b³(2a+b)`, `N=a³(a+2b)`.
Получено общее доказательство дополнительного линейного условия на квадратные
классы. На прежней контрольной сетке из 489 взаимно простых `1≤a<b≤40`
критерий только с нечётными простыми доказывает ранг ноль в 111 случаях
вместо 10; с прежними условиями mod128/mod81 — в 178 вместо 134.
Это новые простые сертификаты известных нам рангов, а не 44 новых решения
или доказательство новизны в литературе. Внешней рецензии этого текста пока нет.

## 1. Ответ на проблему роста девяти чисел

Общий масштаб магического квадрата устраняется делением на центральный квадрат.
В клетках остаются `1`, `1±p`, `1±q`, `1±(p−q)`, `1±(p+q)`;
после симметрий можно взять `0<q<p`, `p+q<1`.
Это две рациональные переменные, хотя высоты их числителей и знаменателей
по-прежнему не ограничены. Ограниченная вещественная область не делает
рациональный поиск конечным.

В спуске вместо величины произведения используем класс по модулю квадратов:
`d = sign × ∏ prime^(exponent mod 2)`. Например, `[12]=[3]`, `[75]=[3]`,
поэтому `[12][75]=[1]` без необходимости наращивать произведение.
Знак тоже хранится: отрицательное число не является квадратом в Q.
Для фиксированных a,b все нужные простые лежат среди делителей
`2ab(a−b)(a+b)(a+2b)(2a+b)`.
Умножение классов становится XOR, условия Лежандра — линейными уравнениями
над F₂. Факторизация коэффициентов и число возможных простых остаются затратами;
универсального конечного списка простых для всех a,b здесь нет.

## 2. Лемма о двух сближающихся корнях

Пусть `E: y²=(x−e₀)(x−e₁)(x−e₂)` с различными целыми корнями.
Используем стандартный полный 2-спуск δ и знаковые бесквадратные
представители `(d₀,d₁,d₂)`. Их произведение — квадрат.
При точке `(e_i,0)` нулевой компонент δ заменяется на
`(e_i−e_j)(e_i−e_k)`; при O берётся `(1,1,1)`.

Зафиксируем различные i,j,k и нечётное простое ℓ такое, что

    ℓ ∤ (e_i−e_j)(e_i−e_k),
    v_ℓ(e_j−e_k) нечётно.

Тогда для любой рациональной точки E выполняется

    (d_i/ℓ) = ((e_j−e_i)/ℓ) ^ ε,
    ε = v_ℓ(d_j) mod 2 = v_ℓ(d_k) mod 2.                 (L)

Здесь скобки — символ Лежандра. Так как поддержка d_i лежит в
`(e_i−e_j)(e_i−e_k)`, ℓ не делит d_i; равенство двух чётностей следует
из квадратности произведения.

**Доказательство для точки вне корней.** Положим `t=x−e_i`,
`h=e_i−e_j`, `k=e_i−e_k`. Имеем

    t=d_i U_i²,  t+h=d_j U_j²,  t+k=d_k U_k²

с рациональными U. Если ε=1, отрицательная оценка t невозможна:
тогда все три выражения имели бы одну чётную оценку, поскольку ℓ∤d_i.
Поэтому t цело в Q_ℓ, а t+h и t+k делятся на ℓ. Значит
`t≡−h (mod ℓ)` — единица, и `(d_i/ℓ)=(−h/ℓ)`.

Пусть ε=0. Если `(d_j d_k/ℓ)=−1`, у разности
`d_j U_j²−d_k U_k²` оценка всегда чётна: при разных оценках слагаемых
минимальная — чётна; при равных ведущие единичные части не сокращаются,
так как их отношение было бы квадратом лишь при `(d_j d_k/ℓ)=1`.
Это противоречит нечётной оценке `h−k`. Поэтому `(d_j d_k/ℓ)=1`,
и квадратность произведения d даёт `(d_i/ℓ)=1`.

**Особые точки.** Для O формула очевидна. При x=e_i имеем
`[d_i]=[hk]`, `h≡k≠0 (mod ℓ)`, ε=0, и обе стороны (L) равны 1.
При x=e_j или e_k класс d_i равен классу соответствующего ненулевого
различия корней, а ε=1 вследствие нечётной оценки e_j−e_k.
Поэтому (L) выполняется и для всего 2-кручения. ∎

Это обычная локальная арифметика полного 2-спуска. Мы не утверждаем,
что лемма ранее неизвестна; новой для нашего алгоритма является её
явная совместная строка, отсутствовавшая в прежней матрице.
Общий аппарат спуска: [Кремона, гл. 3](https://johncremona.github.io/book/fulltext/chapter3.pdf).

## 3. Почему это линейное условие, а не большой перебор

Запишем `χ_ℓ(d)=0` для вычета и `1` для невычета. Формула (L) означает

    χ_ℓ(d_i) + χ_ℓ(e_j−e_i) · v_ℓ(d_j) = 0  в F₂.       (B)

Первое слагаемое — сумма заранее известных символов простых, входящих в d_i.
Второе связывает его с одним битом ДРУГОГО компонента.
Прежнее условие при `χ_ℓ(e_j−e_i)=0` уже требовало `χ_ℓ(d_i)=0`;
теперь также используется случай `χ_ℓ(e_j−e_i)=1` с нечётной оценкой
разности сближающихся корней.

Добавим все такие строки к прежней матрице H. Ядро полного 2-спуска равно
2E(Q), а размер образа — `2^(rank+2)`. Следовательно

    rank E(Q) ≤ dim ker H_new − 2.

Это общий достаточный критерий: если размерность ядра 2, ранг равен нулю.
В семье a,b,a+b прежняя доказанная лемма о кручении тогда запрещает
шесть положительных квадратов `1±az,1±bz,1±(a+b)z` при z≠0.
См. `../z2z6_theory/THEOREMS.md`: кручение Z/2×Z/6 и отсутствие его
точек в интервале `0<u<1/(a+b)²`.

## 4. Отдельная проверка через общую переменную

Не используя (L), реализован конечный необходимый тест

    d_i U_i² = X − e_i Z² (mod ℓ^n),  i=0,1,2,
    (X,Z) примитивны относительно ℓ.

Для настоящей точки с нецелым x его отрицательная ℓ-адическая оценка
чётна: в исходной кубике все три множителя имеют одну оценку, а их
произведение — квадрат. Поэтому x=X/Z² допускает такую нормировку,
а бесквадратность d_i обеспечивает целые U_i.
Для целого x берём Z=1. При корнях U соответствующего нулевого
множителя равен 0; O представлен X=1,Z=0,d=(1,1,1).

Алгоритм учитывает оба случая: при Z-единице нормирует Z=1, при ℓ|Z
оставляет все X-единицы и все остатки Z². Так знаменатели не теряются.
Проверяются ℓ=2,n=7; ℓ=3,n=4; прочие плохие ℓ≤127,n=2.
Наличие остатков не объявляется доказательством разрешимости над Q_ℓ.

Если Γ_new — оставшиеся тройки, то
`rank ≤ floor(log₂|Γ_new|)−2`. Не предполагаем, что Γ_new — подгруппа
или точно группа Селмера.

## 5. Пример a=4, b=9

    M=12393, N=1408, M−N=10985=5·13³.

Прежняя матрица давала верхнюю границу ранга 3, новые строки — 0.
Прежние дополнительные условия mod128/mod81 оставляли 8 троек (граница 1).
При ℓ=5 имеем `(−M/5)=−1`, поэтому (L) требует

    (d₀/5) = (−1) ^ v₅(d₁).

Эта строка исключает ровно четыре из прежних восьми троек:

    (−1,65,−65), (22,1,22), (17,17,1), (−374,1105,−1430).

Остаются

    (1,1,1), (−22,65,−1430), (−17,1105,−65), (374,17,22).

Независимый от формулы (L) модульный алгоритм исключает те же четыре
по модулю 25, включая знаменательный случай. Полный непосредственный
перебор всех примитивных пар (X,Z) mod25 это воспроизводит.

## 6. Контроль и границы результата

На одной и той же сетке 489 пар:

| критерий | число доказанных рангов ноль |
|---|---:|
| прежняя матрица нечётных условий | 10 |
| матрица с новыми совместными строками | 111 |
| прежний критерий с mod128/mod81 | 134 |
| новый совместный критерий с mod128/mod81 | 178 |
| отдельный тест общей переменной по конечным модулям | 178 |
| прежний более сильный расчёт PARI | 211 |

Матрицы и непосредственное перечисление классов совпали на всех 489 парах.
Конечный модульный тест понизил границу ранга в 95 случаях.
Его граница на всей сетке совпала с суммой второго и третьего чисел
сохранённого результата PARI `ellrank`; это наблюдение, не теорема
о точности конечного модульного теста. У оставшихся 33 случаев ранга ноль
PARI возвращает `[0,0,2]`; мы не объявляем их закрытыми нашим новым фильтром.
Множества троек после (L)+прежних малых модулей и после отдельного
модульного теста совпали в 487 случаях; в двух отличаются, поэтому
равенство алгоритмов не утверждается.

Положительный контроль: 6176 рациональных точек (все точки кручения и
точки, найденные PARI) на 489 кривых проходят оба новых условия.
Дополнительно 108 сравнений реализации через битовые множества с
непосредственным перебором всех примитивных (X,Z) — без расхождений.
Эти реализации написаны одним автором; это не слепая внешняя рецензия.

**Что остаётся:** бесконечно много пар a,b, кривые положительного ранга,
совместимость дальнейших покрытий и lifting. Не получены общий предел
высоты, решение квадрата или запрет всех квадратов. Конкретный следующий
теоретический шаг — вывести полные совместные строки также при чётных
оценках разностей и в месте 2; отделить точность локального спуска от
дополнительных глобальных препятствий.

## 7. Воспроизведение и просьба к рецензенту

Архив сохраняет соседние каталоги `z2z6_theory/` и `z2z6_joint_local/`.
Из его корня:

    python3 z2z6_joint_local/validate.py
    python3 z2z6_joint_local/collision.py
    env DOT_SAGE=/tmp/codex-joint-local-review python3 z2z6_joint_local/controls.py

Первые две команды требуют SymPy; последняя — Python с Sage.
`validate.py` использует сохранённые ранги как контроль и не вычисляет их
для доказательства границы. `controls.py` получает точки через PARI заново.

Claude/Fable: сначала проверить доказательство (L), особо точки корней
и отрицательные оценки, затем составление строки (B). Числа контроля
здесь раскрыты, поэтому последующая проверка не будет слепой по результату.
Поиск новизны в литературе отдельно не выполнен.
