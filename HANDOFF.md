СТАТУС: рабочее — точка входа для сессии без контекста
ДЛЯ: всем, в первую очередь новой сессии
ИТОГ: текущее состояние проекта; §0 — необратимые внешние действия, которые нельзя повторять
ОТМЕНЯЕТ: редакцию от 11.09 целиком (род H0 доказан, приоритеты сменились)
ПРОВЕРЕНО: сверено с SUMMARY и записками Codex/Grok на 12.09 ~20:45
ЧИТАТЬ ДАЛЬШЕ, ЕСЛИ: вы не помните контекста проекта или собираетесь что-то отправить вовне

# Записка для продолжения — актуальна на 12 сентября 2026, вечер

Если вы читаете это в новой сессии без контекста — начните отсюда, потом `SUMMARY_2026-09-12.md`.
Предыдущая редакция (11.09) устарела целиком: род H₀ давно доказан, приоритеты сменились дважды.

**Задача:** магический квадрат 3×3 из девяти различных точных квадратов. Открыта с XVIII века.
Не решена. Ничто ниже не утверждает обратного.

**Главное правило проекта:** у каждого утверждения метка статуса — доказано / доказано (ПО) /
проверено численно / наблюдение / гипотеза. На каждый вывод ставится контроль.
За проект поймано **девять** ошибок, все одного устройства: **вывод верный, обоснование неверное**.
Три последние — мои, найдены 12.09 вечером (см. `SUMMARY` §6).

---

## 0. ⚠️ ЧТО УЖЕ СДЕЛАНО ВОВНЕ — не повторять

Это самое важное в записке. Действия необратимы, повтор навредит.

- **Семь писем математикам ОТПРАВЛЕНЫ** 12.09 (Auel+Singer, Fisher, Stoll, Bruin, Скоробогатов,
  Зархин, Boyer). Тексты — в `LETTER_DRAFT_2026-09-12.md`. **Не отправлять повторно.**
  - Письмо Зархину не доставлено (адрес недоступен). Подробности — в локальных файлах, не публикуются.
    Переотправлять некуда; вопрос переадресован Скоробогатову.
  - В письме Штоллю есть ошибка: «ваш доклад на ICM 2026» — статья существует (arXiv:2509.24604),
    докладом ICM не является. Исправить при первом продолжении переписки, отдельного письма не слать.
  - **Готов, но НЕ отправлен** черновик поправки Фишеру (расчёт всё-таки завершился, наш диагноз был
    неполон) — в начале `LETTER_DRAFT_2026-09-12.md`. Ждёт решения пользователя.
- **Репозиторий опубликован:** https://github.com/Kepler71/magic-square-of-squares (аккаунт Kepler71).
  Push делает только пользователь — мне блокирует классификатор прав.
  В публикацию НЕ идут: `CONTACTS_*`, `LETTER_DRAFT_*`, `chrome_bridge/messages.json` (личные данные).
- **Артефакт-страница** (приватная): https://claude.ai/code/artifact/b6b5ec5a-0ec1-4576-a1ab-9078955aaccc
- **Страница Codex** (публичная): https://magic-squares-field-notes.kellerbm71.chatgpt.site/

## 1. Кто работает и как устроен обмен

Три системы параллельно на одной машине: **Claude** (расчёты, спуск, CTP), **GPT/Codex** (Sage, Magma,
аудит; в файлах зовётся Codex или Astra), **Grok** (литература, слежка за сессиями, идеи).
Плюс **Claude in Chrome** — браузерный агент, у него доступ к Gmail и GitHub.

Обмен: файлы в корне проекта (`NOTE_*`, `REPLY_*`, `RESULT_*`, `REVIEW_*`, `GROK_*`) плюс чтение
чужих журналов сессий. **Мост с браузерным агентом:** `chrome_bridge/server.py`, страница на
http://127.0.0.1:8787/ , отправка сообщения — `python3 chrome_bridge/post.py "текст"`.
Magma есть только у Codex. Письма и push — только через пользователя.

## 2. Где мы сейчас — коротко

**Закрыто безусловно:** три отношения рёберных пар исключены при любом масштабе —
(17:7:13), (23:7:17), (71:49:61). Только рёберные пары; угловые не покрыты.

**Семейство G1 (m,n ≤ 20, 127 пар):** исключено **124**, остаются **(13,8), (15,8), (16,5)**.
Метод последних закрытий — необходимый класс Куммера δ = (1,s,s) в образе 2-спуска на E.
У (13,8), (15,8), (16,5) требуемый класс в образе ПРИСУТСТВУЕТ, поэтому этот тест их не берёт.

**Слабое место, требующее внимания:** исключения (11,4) и (19,5) держатся на границе rank E = 1,
полученной ТОЛЬКО из PARI `ellrank`; eclib даёт [1,3], второй спуск неубедителен, а требуемый класс
лежит в группе Селмера. При ранге 3 образ совпал бы с Селмером и заявления были бы ложны.
Прочно стоит только (15,1) (eclib certain=True + аналитический ранг 1 при знаке −1 ⇒ Гросс–Загир и Колывагин).
Лечение — сертификат спаривания Касселса–Тейта, как у (19,16): ненулевое спаривание исключает класс
**независимо от ранга**. Прогон был запущен 12.09 ~20:30.

**Условно:** восемь сечений из одиннадцати — вывод Шаботи при недоказанном ранге 1. Нужен Magma.

## 3. Что считается прямо сейчас (12.09, ~20:45)

- сертификаты CTP для (11,4) и (19,5) — снять зависимость от ранга;
- зацепка Grok: 2-адическое препятствие при чётном n на всём G1 (закрыло бы (13,8) и (15,8));
- встречная проверка трёх исключений Codex (три скептика на каждое);
- фоном: ядра изогении для (79,47,65), `descent/run_quartic_sections.sage`.

## 4. Отрицательные результаты — не переоткрывать

- **Квадратичный Шаботи неприменим** ко всему семейству сечений: End⁰(Res) = ℚ ⇒ ρ = 1, нужно ≥ 2.
- **Готового решателя QC для рода 5 не существует** (QCMod — модулярные кривые, QC_bielliptic — род 2).
  Критерий Балакришнан–Догра даёт лишь конечность, не алгоритм.
- **Поиски пусты:** 808 млн состояний, 13,8 млрд точек, 3,6·10¹³ пар на GPU. Единственная находка везде —
  известный квадрат Бремнера–Саллоуса, он же наш контроль.
- **Чаффин перебрал** все конфигурации Бойера с семью клетками до центра 10²⁸ (квадратный центр).
  Расширять перебор по конфигурациям Бремнера в том же боксе бессмысленно.
- **(89,23,65):** спаривание посчитано, ранг 2, граница ≤ 5 — хуже известной ≤ 3. Гипотеза не подтвердилась.

## 5. Чего не хватает

**Magma** (`TwoCoverDescent`, `Chabauty`) — блокирует восемь условных сечений.
**Минимизация моделей рода 1** по Кремоне–Фишеру–Штоллю — даст ~20× по скорости в CTP через квартики.
**Человеческая экспертиза** по рациональным точкам на поверхностях общего типа.

## 6. Грабли, на которые уже наступали

- Терялся **квадратичный твист**: кривая остаётся похожей, ранг считается без ошибок — и он не тот.
- **Пустой перебор** выдавался за доказательство отсутствия (дважды: прообразы; точка на D04 при одном t).
- `pkill -f` убивал мою собственную оболочку. Останавливать только по конкретным PID.
- `Ep.gens()` не в форме Смита — нужен `Ep.abelian_group().gens()`.
- Точность 53 бита при коэффициентах 10³¹ — гарантированный мусор; брать точность по диапазону.
- Искать людей надо **обоими написаниями**: Zarhin в статьях, Zarkhin в гражданских документах.
- Институциональный каталог (pure.psu.edu) **не доказывает, что человек жив**.
