# Закрытие наклона 126/451: явные высоты и конечное решето

Дата: 25 сентября 2026. Для Claude / Fable и пользователя.

**Результат:** получено доказательство с вычислительной проверкой отсутствия невырожденного квадрата из девяти положительных рациональных квадратов при фиксированном наклоне 126/451. Новое здесь — строгая сертификация ранее численного шага, а не первое наблюдение отсутствия решений на этом наклоне. Проверка выполнена SageMath 10.9; независимая проверка всей цепочки другим участником пока не выполнена.

Это не доказательство отсутствия магических квадратов при всех рациональных наклонах. В частности, прежние 239 флагов `closed` не становятся строгими автоматически.

## 1. Точное утверждение

Не существует ненулевого z ∈ Q, для которого все элементы

```
1−451z   1+577z   1−126z
1+325z      1     1−325z
1+126z   1−577z   1+451z
```

являются положительными рациональными квадратами. При z≠0 эти девять значений попарно различны. Положительность требует |z|<1/577. Здесь 126/451 — фиксированный наклон, а z — свободный параметр масштаба отклонений; не смешивать эти две переменные.

Для множества T обозначим y_T=∏_{c∈T} sqrt(1+cz). В настоящем решении каждый y_T рационален. Используем

```
T0={−577,−451,−325,−126}, T1={−325,−126,126}, T2={−126,126,325}.
E: y²=x³−21739732x+23939545056.
```

Кривые y_T²=∏(1+cz) изоморфны E. Их образы обозначаем Q0,Q1,Q2.
Полностью явное построение: c0=min(T), w=1/(z+1/c0),
X=Lw, Y=Lc0w²y_T, где L — старший коэффициент кубики

```
g(w)=∏_{c∈T\{c0}} ((c0−c)w+cc0)                  (|T|=4),
g(w)=c0w ∏_{c∈T\{c0}} ((c0−c)w+cc0)             (|T|=3).
```

Модель Y²=L²g(X/L) приводится к E. В `models.py` это реализовано без импорта кода Fable; `verify.py` проверяет тождества уравнений кривых и сложения.
В частности, x(Qi)=Mi(z) для дробно-линейных отображений с матрицами

```
M0=[−2730602,   7490; −577, 1],
M1=[−3439800, 100333; −2925,9],
M2=[ 647388,    7490; −126, 1].
```

Матрица [a,b;c,d] означает (az+b)/(cz+d). На проективных моделях отображения продолжаются в полюсах. Для положительных клеток z=−1/c0 невозможен, но конечная проверка всё равно учитывает точки O и кручение.

## 2. Нормировка и первая строгая оценка

Во всём доказательстве

```
hhat(P)=lim 4^(−n) h(x(2^nP)), h(x(O))=0,
<P,Q>=(hhat(P+Q)−hhat(P−Q))/4.
```

Это положительно определённая квадратичная форма на E(Q)/torsion. Нельзя смешивать её с нормировкой, вдвое меньшей.

Предыдущий пакет `rigorous_pairing_20260925` доказывает

```
|<Q1,Q2>| < 28,
|hhat(P)−h(x(P))| ≤ log(B)/3,
B=8072568470703647597429129000569820928.
```

Оценка ошибки высоты получена из точных тождеств Безу для рациональной функции удвоения и телескопической суммы. Она глобальна, включая кручение и O. Первое спаривание сводится к конике w²=1−325²z²; её рациональная параметризация и оба отображения заново проверены в `verify.py`.

## 3. Новая строгая оценка для эллиптической вспомогательной кривой

Для пары (Q1,Q0) вспомогательная кривая y_D²=(1−577z)(1−451z)(1+126z) имеет минимальную модель

```
E3: y²=x³+x²−35269395204x+2528948885330016.
```

Формулы сложения дают x(Q1±Q0)=A(x)±B1(x)y, где A,B1 записаны точно в `sections.json`. Выбор знаков квадратных корней лишь меняет эти две функции местами. Обнаружено точное представление

```
u+(x,y)=n(x,y)/h(x,y), u−(x,y)=n(x,−y)/h(x,−y),
h=7450563073503948−59584372201x−115151400y−63504x²,
n=10280496501245973576+19537338496438x−135418046400y−1524665548x².
```

Рассмотрим вложение E3→P³, R↦[1:x:y:x²], O↦[0:0:0:1]. Его образ задан

```
Q=v0v3−v1²=0,
Q'=v2²−v1v3−v1²+35269395204v0v1−2528948885330016v0²=0.
```

Для каждого знака F=n, G=h — линейные формы по vi. В сертификате записаны целочисленные однородные тождества, по одному для каждого i=0,1,2,3:

```
Ai F+Bi G+Ci Q+Di Q'=R vi³, R≠0,
deg Ai=deg Bi=2, deg Ci=deg Di=1.
```

Следовательно, F,G не имеют общей точки на этой проективной кривой. Пусть H=max|vi| для примитивных целых координат и g=gcd(F(v),G(v)). Тождества дают g|R. Взяв координату максимального модуля, получаем

```
|R|H³ ≤ K H² max(|F(v)|,|G(v)|),
H/K ≤ max(|F(v)|,|G(v)|)/g ≤ LH,
```

где L=max(||F||1,||G||1), K=max_i(||Ai||1+||Bi||1). Поэтому

```
h_emb(R)−log K ≤ h(u±(R)) ≤ h_emb(R)+log L.
```

Для обоих знаков получены одинаковые

```
L=10280516175527181962,
K=182902682210414514753161210609970117800775531563607411012510673703370447223800.
```

При A0=LK имеем |h(u+)−h(u−)|≤log A0. Совместно с ошибкой канонической высоты:

```
|<Q1,Q0>| ≤ (3log A0+2log B)/12 <70.
```

Последнее строгое неравенство проверено рационально:
Σ_{j=0}^{769} 840^j/j! > A0³B². Сумма меньше exp(840).
Все восемь однородных тождеств и обе формулы отображений независимо проверены `verify.py`. Это заменяет численную константу 20.169... глобальной доказанной константой 70. Локальные высоты и гипотезы о распределении точек для этого шага не нужны.

## 4. Полнота группы и сравнение высот

Sage/mwrank с `proof=True, use_database=False` подтвердил ранг 2. Дополнительный вызов `E.saturation(gens,max_prime=-1)` проверил насыщенность по всем простым и вернул индекс 1. Получен полный базис свободной части

```
G1=(−2883,250305), G2=(−1610,234024).
E(Q)_tors={O,(−5138,0),(1176,0),(3962,0)}.
```

Для матрицы Грама вычислены гарантированные интервалы через x(64P), точную рациональную арифметику и ошибку log(B)/(3·4⁶); логарифмы вычисляются интервально, с направленным округлением. Из det(Gram)/trace(Gram)>3/2 следует

```
hhat(nG1+mG2+T) ≥ (3/2)(n²+m²).
```

Используются именно границы интервалов, не численные значения `P.height()` и не приблизительное собственное число. Полный регулятор вычислять не нужно.

Для примитивной целой невырожденной матрицы M положим L_M=2max|Mij|. Применяя M и присоединённую матрицу, получаем |h(Mz)−h(z)|≤log L_M. Поэтому для трёх образов

```
|hhat(Q0)−h(z)|≤44,
|hhat(Q1)−h(z)|≤45,
|hhat(Q2)−h(z)|≤43.
```

## 5. Элементарная лемма ранга 2: ограничение бесконечного поиска

Положим H=h(z). В двумерном вещественном пространстве Морделла—Вейля возьмём единичный вектор e=Q1/||Q1|| и разложим Qj=aj e+bj e_perp, j=0,2.
Для H>45 оценки спариваний дают

```
|a0|≤70/sqrt(H−45), |a2|≤28/sqrt(H−45),
A(H)=4900/(H−45).
```

Выбираем ε=±1 так, чтобы b2 и εb0 имели одинаковый знак. Тогда

```
(a2−εa0)² ≤ 98²/(H−45),
|b2²−b0²| ≤ 44+43+A(H),
(|b2|+|b0|)² ≥ 4(H−44−A(H)).
```

Отсюда при положительном знаменателе

```
hhat(Q2−εQ0) ≤ K(H)
=9604/(H−45)+(87+A(H))²/[4(H−44−A(H))].
```

K убывает: A убывает, числители убывают, положительные знаменатели возрастают. Точно над Q:

```
K(7728)=907035504731/604716350368 <3/2.
```

Если H≥7728, нижняя граница на неторсионные точки заставляет Q2−εQ0 быть точкой кручения. Если H<7728, то hhat(Q1)<7773 и

```
n²+m² <5182 при Q1=nG1+mG2+T.
```

Следовательно, полный квадрат |n|,|m|≤72 с четырьмя T заведомо покрывает все точки этого случая. Это 84 100 комбинаций. Никакой верхний предел на знаменатель z здесь не постулируется: конечность выведена из высот.

## 6. Конечная проверка двух случаев

### Случай A: H<7728

Сначала все 84 100 комбинаций проверены в E(Fp) при хороших простых p, где det(M1)≠0. Если восстановленный z конечен по модулю p, все восемь 1+cz должны быть квадратами в Fp, включая нуль. Если знаменатель z равен нулю, комбинация сохраняется. Это необходимое условие; истинная рациональная точка не отбрасывается.

После p=17,19,23,29,31,37,43 остаётся соответственно

```
21025, 3937, 571, 197, 49, 15, 3.
```

Простые 47,53,59 результат не меняют. Три оставшиеся комбинации проверены над Q:

```
−G2+(−5138,0) ↦ z=0,
(1176,0)     ↦ z=∞,
 G2+(−5138,0) ↦ z=0.
```

Ненулевых конечных параметров нет. Это детерминированное конечное решето; никаких вероятностей «99.9%» и предположений о независимости вычетов нет.

### Случай B: Q2−εQ0=T, T∈E(Q)_tors

Поскольку 2T=O, необходимо x(Q0)=x(Q2−T), независимо от ε.
Пусть x=x(Q2). Для T=(t,0) имеем

```
x(Q2−T)=t+(3t²−21739732)/(x−t).
```

Равенство двух восстановленных параметров z даёт, после удаления ненулевых постоянных множителей, четыре многочлена в порядке O,(−5138,0),(1176,0),(3962,0):

```
(x−7490)²,
325x²−4407578x+1927094736,
199x²−2059176x−10219956460,
−96427x+838390574.
```

У двух средних нет рациональных корней. Последний рациональный корень не является x-координатой рациональной точки E. Первый даёт только z=0. Отдельно добавлены исключения Q2=O и Q2=±T, где аффинные формулы имеют полюсы. Список с запасом:

```
z∈{0,∞,1/126,−1/126,−1/325}.
```

Последние три не делают все клетки положительными; 0 вырожден, ∞ не является конечным параметром нормализованного квадрата. Оба случая исчерпаны.

## 7. Статусы и границы результата

- **Математически выведено:** две леммы сравнения высот, ограничение спариваний из сертификатов, лемма ранга 2, разбиение на случаи A/B и полнота конечного набора.
- **Проверено точной CAS-арифметикой:** отображения, восемь однородных тождеств, предыдущие тождества Безу, рациональные экспоненциальные сертификаты, конечное решето и исключительные многочлены.
- **Опирается на сертифицирующие алгоритмы библиотек:** ранг и полная насыщенность Sage/eclib; интервальные логарифмы MPFI. Это обычное компьютерно-поддержанное доказательство, не формальная верификация ядром Lean/Coq и не независимая реализация всей теории спуска.
- **Ещё требует независимого аудита:** воспроизведение полного пакета Claude/Fable и проверка изложенной леммы ранга 2, нормировки и особых точек. Внутренний повтор тождеств независимым скриптом этого внешнего аудита не заменяет.
- **Не доказано:** одновременное закрытие остальных наклонов или глобальное отсутствие магического квадрата.

## 8. Следующий маршрут

1. Claude/Fable: воспроизвести этот пакет целиком. Не переносить статус на старые численные записи автоматически.
2. Для 73/362 и 265/298 выбрать три отображения на общую кривую ранга 2; вывести функции u± и аналогичные однородные сертификаты. При отсутствии линейного представления в L(4O) сначала проверить класс полярного дивизора; успех здесь не означает универсальности этого представления.
3. Автоматизировать тот же аудит для остальных конечных записей: точные константы, полная группа, нижняя оценка формы, конечное решето, отдельные торсионные исключения.
4. Для глобальной задачи исследовать, покрывают ли семейства таких троек все рациональные наклоны и можно ли контролировать ранги/высоты равномерно. Конечный список экспериментальных наклонов не покрывает бесконечность.

Подход относится к методу Демьяненко—Манина: несколько отображений в эллиптическую кривую и ограничения на высоты. Обзор метода: [M. Stoll, Rational points on curves](https://www.mathe2.uni-bayreuth.de/stoll/papers/JA2009-paper.pdf). Общий контекст явных оценок: [E. Viada, An Explicit Manin–Dem’janenko theorem in elliptic curves](https://arxiv.org/abs/1609.04615). Числа и специальные леммы выше выведены непосредственно в этом пакете, а не заимствованы из этих статей.

## 9. Воспроизведение

В архиве сохранены две соседние папки, поскольку новая проверка использует старый сертификат удвоения и первой пары. Нужны SageMath 10.9 или совместимая версия, eclib и MPFI; Magma, лицензия, GPU и сеть не нужны.

Из `rigorous_pairing_20260925`: `sage -python verify_certificate.py`.
Из `elliptic_pairing_20260925`: `sage -python verify.py`, затем `sage -python close_126.py`.
В данной установке вместо `sage -python` используется `env DOT_SAGE=/tmp/codex-linear-sections-sage python3`.

`models.py`, `verify.py`, `close_126.py` не импортируют файлы Fable и не требуют `/home/kep/magicKube`. Итоговые коэффициенты, проверки и журнал находятся в `certificate.json`, `closure.json`, `verify.log`, `close_126.log`. `certify.py` заново строит восемь свидетельств из `sections.json`. Проверяющий должен убедиться, что оба списка `case_A_solutions` и `case_B_solutions` пусты.
