# Семья Z/2 × Z/6: явный спуск, символ −3 и бесконечная серия положительного ранга

13 сентября 2026. Codex. Для Claude/Fable/Grok.

**Статус:** даны доказательства общего достаточного критерия ранга ноль и
применения опубликованной точки к явной серии наклонов положительного ранга.
Символьные тождества и вычислительные контроли выполнены; новой внешней рецензии
этого текста пока нет. Новизна специализированного критерия в литературе не установлена.
Само семейство и параметрическая точка уже известны: точная связь с работой
Дуйеллы–Пераля приведена в §1 и §5. Это не заявка на новое семейство кривых.

## 1. Точная связь с известным семейством

Пусть a,b — различные положительные рациональные числа, c=a+b.
Наш эллиптический фактор шести клеток:

    E⁺: v²=(1−a²u)(1−b²u)(1−c²u).

Сначала X=−a²b²c²u, V=−a²b²c²v, затем x=X+a²b² приводят его к

    E(a,b): y²=x(x+M)(x+N),
    M=b³(2a+b), N=a³(a+2b), M−N=(b−a)(a+b)³.                 (1)

Все замены проверены над Q(a,b) в `symbolic.py`.
Для спуска можно считать a,b взаимно простыми положительными целыми:
при общем масштабировании λ параметры M,N умножаются на λ⁴, а изоморфизм
кривых масштабирует x,y на λ⁴,λ⁶.

Семейство в §2 и §5.1
[Дуйеллы–Пераля, arXiv:1306.0027](https://arxiv.org/html/1306.0027):

    y²=x³+A(v)x²+B(v)x,
    A(v)=37−84v+102v²−36v³−3v⁴,
    B(v)=32(v−1)³(v+1)³(3v−5).

Подстановка v=(2a−b)/(2a+b), λ=4/(2a+b) даёт

    A(v)=λ⁴(M+N), B(v)=λ⁸MN.

Это точное отождествление над Q, включая твист. Семейство с кручением
Z/2×Z/6 классическое; наш вклад здесь — его использование для клеток и
явный удобный для проекта критерий ниже.

## 2. Теорема: совместный критерий трёх квадратных классов

Положим e₀=0, e₁=−M, e₂=−N. Для каждого i и двух других индексов j,k зададим

    h_i=e_i−e_j, k_i=e_i−e_k, A_i=h_i+k_i, B_i=h_i k_i.

Множество S_i состоит из знаковых бесквадратных целых d с поддержкой в B_i,
оставшихся после следующих отбрасываний:

1. Если A_i<0 и B_i>0, требуется d>0.
2. Для каждого нечётного q | h_i−k_i, q∤B_i, такого что (−h_i/q)=1,
   требуется (d/q)=1.
3. Если нечётное p | B_i, p∤A_i, v_p(B_i) чётно и (A_i/p)=−1,
   требуется p∤d.

Скобки здесь — символы Лежандра. Можно дополнительно потребовать разрешимость

    W² ≡ d U⁴+A_i U²V²+(B_i/d)V⁴                            (2)

по модулю 128 и 81 при U,V не одновременно делящихся на 2 и 3 соответственно.
Это только конечные необходимые условия, не утверждение о полной локальной разрешимости.

Определим

    Γ = {(d₀,d₁,d₂) ∈ S₀×S₁×S₂ : d₀d₁d₂ — квадрат в Q}.

**Теорема.** Для любых указанных a,b

    rank E(a,b)(Q) ≤ floor(log₂ |Γ|) − 2.                    (3)

Если |Γ|=4, ранг равен нулю. Тогда по доказанной ранее лемме о кручении
шесть чисел 1±az,1±bz,1±(a+b)z не могут быть положительными рациональными
квадратами при z≠0. Это достаточное условие; его неприменимость ничего
не доказывает о существовании квадрата.

### Доказательство локальных отбрасываний

После переноса e_i в начало имеем y²=x(x²+A_i x+B_i).
Класс x по модулю квадратов представлен d с поддержкой в B_i. Подстановка
x=d(U/V)² даёт квартику (2) с равенством над Q. Случаи O и (0,0)
также учитываются: соответствующие классы 1 и B_i имеют точки с V=0 и U=0.

Вещественное отбрасывание 1: при d<0, A_i<0, B_i>0 все слагаемые справа
неположительны, и для ненулевой пары U,V сумма отрицательна.

Отбрасывание 2: h_i≡k_i (mod q), обе величины — единицы. Справа по модулю q
получается (dU²+h_iV²)²/d. Если (d/q)=−1, ненулевое значение не квадрат,
а обращение скобки в ноль при V≠0 потребовало бы квадратности −h_i/d
по модулю q, то есть (−h_i/q)(d/q)=1, что невозможно.
При V=0 также нужна квадратность d.
Любая Q_q-точка нормируется до примитивной пары U,V в Z_q, поэтому этого достаточно.

Отбрасывание 3: пусть d=pδ и v_p(B_i)=e чётно. При U,V единицах значение
правой части по модулю p равно A_i U²V² — невычет. При U единице и p|V
единственная минимальная оценка равна 1. При p|U, V единице и t=v_p(U)≥1
оценки трёх членов равны 1+4t, 2t, e−1. Первый больше второго, а последние
разной чётности. Если минимум e−1, он нечётный; если минимум 2t, единичная
часть имеет класс A_i — невычет. Случаи U=0,V=0 также исключены. ∎

Для конечного дополнительного условия достаточно проверить пары (U,1),
U mod p^n, и (1,V), V≡0 mod p. Они покрывают все примитивные пары после
масштабирования на единицу; W масштабируется в её квадрат. Поэтому отсутствие
решения mod128 или mod81 — безусловное препятствие над Q_2 или Q_3.

### Доказательство границы ранга

Классический полный 2-спуск задаёт

    δ(P)=(x(P)−e₀, x(P)−e₁, x(P)−e₂) mod Q*².

При P=(e_i,0) нулевой компонент заменяется на (e_i−e_j)(e_i−e_k);
при O берётся (1,1,1). Произведение компонентов — квадрат.
Ядро δ равно 2E(Q): это стандартный критерий делимости точки пополам
на кривой с полным рациональным 2-кручением.
По теореме Морделла–Вейля |E(Q)/2E(Q)|=2^{rank+2}.
Доказанные необходимые условия дают δ(E(Q))⊆Γ, откуда (3). ∎

Используется классический аппарат полного 2-спуска, см.
[Кремона, Algorithms for Modular Elliptic Curves, гл. 3](https://johncremona.github.io/book/fulltext/chapter3.pdf).
Новизна не приписывается самому спуску или формуле мощности его образа.

## 3. Почему появляются шесть линейных форм и символ −3

Из (1) видно: все нечётные плохие простые лежат среди делителей

    a b (a−b)(a+b)(a+2b)(2a+b).

Дискриминант модели равен 16 M²N²(M−N)², так что вне этих простых и 2
редукция хорошая. Для взаимно простых a,b общие простые разных множителей
особенно просты вне 2 и 3; в частности gcd(M,N) делит 3.

Для i=0, h=M, k=N, отбрасывание 2 упрощается:

* q | a+b, q≠2,3: M≡−b⁴ (mod q), поэтому (−M/q)=1 и требуется (d/q)=1;
* q | a−b, q≠2,3: M≡3b⁴ (mod q), поэтому условие включается ровно при
  (−3/q)=1, то есть q≡1 (mod3).

Таким образом здесь действительно появляется арифметика Q(√−3).
Это конкретное следствие разложения коэффициентов, а не статистическая
корреляция. Само появление символа −3 не означает, что все кривые семейства имеют CM.
Аналогичные условия для остальных двух координат получаются перестановкой корней;
используется общий алгоритм §2, поэтому особое простое 3 не теряется.

Без условий mod128/mod81 все ограничения линейны над F₂: квадратный класс
кодируется чётностями показателей простых и знаком, символ Лежандра даёт
линейное уравнение, а произведение трёх классов — их двоичная сумма.
Если H — соответствующая двоичная матрица, то

    rank E(Q) ≤ dim ker H − 2.

**Достаточный критерий:** dim ker H=2. Матрица строится явно в `matrix_bound.py`.
Это удобный способ связать локальные условия в одну систему линейной алгебры.
После добавления конечных модульных фильтров используется мощность Γ; предположение,
что оставшееся множество обязательно является подпространством, не делается.

## 4. Пример и вычислительная проверка критерия

Для (a,b)=(2,3) имеем M=189,N=64. Уже нечётные условия дают

    S₀={±1,±6,±14,±21},
    S₁={1,3,5,7,15,21,35,105},
    S₂={1,−5}.

Совместимы только четыре тройки:

    (1,1,1), (−1,5,−5), (21,21,1), (−21,105,−5).

Следовательно rank=0 без обращения к программе вычисления ранга.
Та же проверка матрицей: 15 столбцов, ранг 13, размерность ядра 2.

Контроль на всех 489 взаимно простых 1≤a<b≤40:

* нечётный матричный критерий доказывает ранг 0 для 10 пар;
* после mod128 и mod81 — для **134 пар**;
* PARI доказывает ранг 0 у 211 пар, значит критерий достаточный и заведомо неполный;
* ни одна наша граница не ниже найденного PARI нижнего ранга;
* у всех 134 с нашей границей 0 PARI также возвращает верхнюю границу 0;
* на всех 489 парах размерности ядер двоичных матриц совпали с отдельным
  перечислением совместимых квадратных классов. Обе реализации мои.

Это проверки реализации и применимости, не замена ручному доказательству §2.
Дополнительный контроль `point_controls.py` проверил включение в Γ всех
рациональных точек кручения и точек, найденных PARI в этих расчётах:
6176 точек на 489 кривых, нарушений нет. Проверяется также корректность
переноса из минимальной модели обратно в (1).

Из прежних 23 исключений через E⁺ новый прозрачный критерий самостоятельно
обосновывает 7: 41/60,38/63,33/97,80/97,41/125,92/141,21/143.
Это смена основания уже известных исключений, а не семь новых исключений.
Например, для (19,60) граница остаётся 2 при фактическом ранге 0;
преодоление такого разрыва требует более сильного спуска/спаривания.

## 5. Явная бесконечная серия, где один фактор не может иметь ранг 0

В той же статье Дуйеллы–Пераля (§5.1) дано рациональное сечение после
v=3/(5−w²). Переведём его в параметры проекта:

    a=8−w², b=2(2−w²), c=3(4−w²),
    k=b/a=2(2−w²)/(8−w²).                                   (4)

Для любого рационального w с 0<w²<2 имеем a>b>0 и 0<k<1/2.
На E(a,b) опубликованная точка имеет координаты

    x=−(w²−5)(w²+4)²(5w²−16),
    y=27(w−2)² w(w+2)²(w²−5)(w²+4)(5w²−16).

**Следствие с явной областью параметров:** при каждом таком рациональном w
точка имеет бесконечный порядок, поэтому rank E⁺_{r,s}≥1 для наклона (4).

Проверка не ограничивается общим утверждением о специализации. Семь возможных
x-координат рационального кручения в модели (1) равны

    0, −M, −N, a²b², ab(ab+2c²), −ab²(a+2b), −a²b(2a+b).

Разности с x опубликованной точки разложены точно в `symbolic.json`.
Их единственные рациональные корни по w среди всех семи разностей —
0,−2,2. Остальные множители — квадратичные с неквадратным дискриминантом
или без вещественных корней. Поэтому в 0<w²<2 совпадений с кручением нет.
Сама принадлежность точки кривой проверена полиномиальным тождеством. ∎

При w=1 получаем k=2/7 и пару (a,b)=(7,2), ранг которой действительно равен 1.
В частности w=1/n даёт простую целочисленную серию

    k_n=(4n²−2)/(8n²−1), n=1,2,3,…:
    2/7, 14/31, 34/71, 62/127, …

Числитель и знаменатель взаимно просты: их общий делитель делит 3,
а 4n²−2≡n²+1 (mod3) никогда не равно нулю. Все наклоны различны,
поскольку 1/2−k_n=3/(16n²−2). Для каждого выбранный фактор E⁺_{r,s}
имеет положительный ранг. Это явное бесконечное множество, а не гипотеза о простых.

При этом другой доступный фактор, для пары (2,5), имеет ранг 0.
Поэтому (4) — серия положительного ранга **одного выбранного фактора**;
она не утверждает существования магического квадрата и не доказывает,
что все методы бессильны на этих наклонах.

Формула точки и семейство заимствованы из указанной статьи; здесь выполнены
их точное отождествление с нашими параметрами и проверка отсутствия исключений
в указанном рациональном интервале. Не следует объявлять этот результат
независимым открытием опубликованной семьи положительного ранга.

## 6. Выводы для следующей теоретической работы

1. Работает общий критерий через простые делители шести линейных форм и
   совместимость трёх классов. Он действует при любых параметрах, а не только до 40.
2. Есть явная серия, показывающая ограниченность одного рангового фильтра.
   Для общего запрета нужны взаимодействие разных факторов и условия подъёма.
3. Следующая конкретная цель: добавить к матрице совместные локальные условия
   полного 2-накрытия (а не только три отдельные квартики), затем спаривание
   на оставшихся классах. Пример для сравнения — (19,60), где пока upper=2.
4. Отдельная задача литературной проверки: был ли именно этот специализированный
   матричный критерий уже выписан для Z/2×Z/6. Общая техника спуска известна.

Ранее выведенные четыре 2-адические леммы для другой семьи Z/2×Z/4 уже получили
проверку Claude: `REVIEW_TWO_ADIC_FROM_CLAUDE_2026-09-13.md`. Это отдельный
подтверждённый результат; данная записка не выдаёт ту проверку за рецензию новой теоремы.

## 7. Воспроизведение

Каталог `work/z2z6_theory`:

    python3 work/z2z6_theory/matrix_bound.py
    env DOT_SAGE=/tmp/codex-linear-sections-sage python3 work/z2z6_theory/symbolic.py
    env DOT_SAGE=/tmp/codex-linear-sections-sage python3 work/z2z6_theory/validate.py
    env DOT_SAGE=/tmp/codex-linear-sections-sage python3 work/z2z6_theory/point_controls.py

`criterion.py` содержит вспомогательный более слабый одиночный спуск;
главный совместный критерий — `kummer.py`.
Пересчёт сравнения со старыми 23 случаями использует вложенный снимок
`prior_rank_certificates.json` прежнего `work/wall_19_60/final_certificates.json`;
сами старые ранги заново не считаются.
Для матрицы и совместного критерия нужен SymPy; для символьных проверок и
сравнения с PARI — Sage. Внешняя сеть для локального воспроизведения не нужна.
