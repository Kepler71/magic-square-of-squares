# Спаривание Касселса–Тейта на Sel²(E/k) через бинарные квартики (метод Фишера)

Claude, 2026-09-12. Файлы: `descent/ctp_quartic.sage` (реализация), `descent/xcheck_quartic.sage`,
`descent/xcheck_n61.sage`, `descent/ctrl_quartic_partial.sage`, `descent/ctrl_quartic_Q.sage` (контроли),
`descent/run_quartic_sections.sage` (боевой прогон).

**Статусы утверждений.** Формула — *доказанная теорема из рецензируемой публикации* (Fisher 2022, ниже).
Реализация и все числа ниже — *численный расчёт* (моё ПО + Sage/PARI), не рецензировано.

---

## 1. Найденная формула

### 1.1 Источник

**T. Fisher, «On binary quartics and the Cassels–Tate pairing».**
arXiv:**2208.14977** (v1, 31.08.2022); опубликовано: *Research in Number Theory* **8** (2022), Art. 74,
труды ANTS-XV, DOI 10.1007/s40993-022-00376-z.
PDF автора: <https://www.dpmms.cam.ac.uk/~taf1000/papers/bq-ctp.pdf>.
Из аннотации: «*Unlike earlier methods, our formula does not require us to solve any conics.*»

Метод восходит к С. Доннелли (реализован в Magma, `CasselsTatePairing`, препринт не опубликован);
Фишер даёт самодостаточную версию через теорию инвариантов бинарных квартик.
**Независимая открытая реализация**: Б. Алломбер, PARI/GP ≥ 2.15, `src/basemath/ellrank.c`,
функция `casselspairing()` — с прямой ссылкой на эту статью (но только над ℚ).

Смежные работы, которые *не* подошли: Cassels (1998, Crelle 494) — требует коник над полем определения
каждой точки 2-кручения (у нас это квартичное поле K; см. §5.1 — там это вычислительно безнадёжно);
Fisher–Newton (arXiv:1306.1410) — 3-Селмер; van Beek–Fisher (arXiv:1711.02432) — **3**-изогении
(работы про 2-изогении у них нет); Shukla–Stoll (arXiv:2302.01640) — теоретическая (Albanese–Albanese),
не алгоритм.

### 1.2 Обозначения

Бинарная квартика над числовым полем k
$$g(x,z)=a x^4+b x^3 z+c x^2 z^2+d x z^3+e z^4,$$
$$I=12ae-3bd+c^2,\qquad J=72ace-27ad^2-27b^2e+9bcd-2c^3,\qquad \Delta=\tfrac{16}{27}(4I^3-J^2).$$
Якобиан кривой $C: y^2=g(x,1)$ — это $E_{I,J}: y^2=x^3-27Ix-27J$ (Вейль).

**Лемма 2.1** (Фишер): $S^{(2)}(E_{I,J}/k)$ = {всюду локально разрешимые бинарные квартики над $k$
с инвариантами $I$ и $J$} / (собственная $k$-эквивалентность, т.е. $g\mapsto\lambda^2 g\circ\gamma$
с $\lambda\det\gamma=\pm1$).

Этальная алгебра $L=k[\varphi]$, $\varphi$ — корень $X^3-3IX+J$.
**Кубический инвариант** $z(g)=\dfrac{4a\varphi+3b^2-8ac}{3}\in L^\times$; групповой закон на $S^{(2)}$ —
умножение $z(g)$ в $L^\times/(L^\times)^2$.

Гессиан
$$h=(3b^2-8ac)x^4+4(bc-6ad)x^3z+2(2c^2-24ae-3bd)x^2z^2+4(cd-6be)xz^3+(3d^2-8ce)z^4,$$
$$G=\tfrac13\,(4\varphi g+h),\qquad H=\tfrac1{12}\frac{\partial^2G}{\partial x^2}+\tfrac29(I-\varphi^2)z^2,$$
так что $G(1,0)\,G=H^2$ и $z(g)=G(1,0)=H(1,0)$. Явно
$$H=G_0x^2+\tfrac{G_1}{2}xz+\Big(\tfrac{G_2}{6}+\tfrac29(I-\varphi^2)\Big)z^2,\qquad G_j=\tfrac13(4\varphi g_j+h_j).$$

### 1.3 Теорема 3.1 (Фишер, стр. 3) — дословно

> Let $I,J\in K$ with $4I^3-J^2\neq0$. Let $g_1,g_2,g_3$ be everywhere locally soluble binary quartics over
> $K$ with invariants $I$ and $J$. Let $H_1(x,z)$ be the binary quadratic form (4), with coefficients in
> $L=K[\varphi]$, associated to $g_1$. Suppose that $z(g_1)z(g_2)z(g_3)=m^2$ for some $m\in L^\times$, and write
> $$\frac{z(g_2)z(g_3)}{m}H_1(x,z)=\alpha_1(x,z)+\beta_1(x,z)\varphi+\gamma_1(x,z)\varphi^2$$
> where $\alpha_1,\beta_1,\gamma_1\in K[x,z]$. For each $v\in M_K$ we choose $x_v,z_v\in K_v$ with
> $g_1(x_v,z_v)$ a square in $K_v$ and $\gamma_1(x_v,z_v)\neq0$. If $g_2(1,0)\neq0$ then the Cassels–Tate
> pairing on $S^{(2)}(E_{I,J}/K)$ is given by
> $$\langle[g_1],[g_2]\rangle_{CT}=\prod_{v\in M_K}\big(g_2(1,0),\ \gamma_1(x_v,z_v)\big)_v$$
> where $(\ ,\ )_v$ is the Hilbert norm residue symbol.

Замечания Фишера, существенные для реализации:
* **3.2(iii)** если $g_2(1,0)=0$, то $[g_2]=0$ в $S^{(2)}$ (у нас это автоматический контроль);
* **3.2(v)** спаривание знакопеременно и $[g_1]+[g_2]+[g_3]=0$, поэтому $\langle g_1,g_2\rangle=\langle g_1,g_3\rangle$;
  **но** в формулу входят *точные* $z(g_i)$, не их классы по модулю квадратов;
* **3.2(vi)** при $E(k)[2]\neq0$ корень $m$ не единствен — годится **любой** (наш случай!);
* **3.3** вклад места $v$ тривиален, если одновременно: (i) $N(v)\ge11$; (ii) $g_1,\gamma_1$ $v$-целые и
  $v\nmid\Delta(g_1)\,\mathrm{content}(\gamma_1)$; (iii) $g_2(1,0)$ — $v$-единица и $v\nmid2$.

**Что нужно от входа:** ни точки порядка 2, ни полного 2-кручения, ни групп классов, ни $S$-единиц.
Локальные точки нужны **только на $C_1$**. Коника над $k$ нужна ровно одна — и только чтобы получить
$g_3$ из $g_1,g_2$, если тройка не дана (Замечание 3.2(i)).

### 1.4 Чего в статье нет: перевод элемента спуска в квартику

Наш 2-спуск (`ek_descent.sage`, `ek_descent_partial.sage`) выдаёт элементы Селмера как
$\delta\in L^\times/(L^\times)^2$ c $N_{L/k}(\delta)\in k^{\times2}$, а не как квартики. Конструкция
(выведена и проверена в коде; это и есть «одна коника над $k$» из Замечания 3.2(i)):

Пусть $\Theta$ — образ $x$ в $L=k[x]/f(x)$, $f$ — кубика модели $y^2=f(x)$. 2-накрытие $C_\delta$:
$$x-\Theta=\delta\,\frac{t^2}{u^2},\quad t\in L \iff
\big[\delta t^2\big]_2=0,\quad \big[\delta t^2\big]_1+u^2=0,\quad x=\frac{[\delta t^2]_0}{u^2},$$
где $[\,\cdot\,]_j$ — коэффициент при $\Theta^j$; координаты $(u:t_0:t_1:t_2)\in\mathbb P^3$.
Проверка: $N(x-\Theta)=f(x)$, $N(\delta t^2/u^2)=N(\delta)(N(t)/u^3)^2$, $N(\delta)$ — квадрат.

Первое уравнение — **коника** $\{[\delta t^2]_2=0\}\subset\mathbb P(L)=\mathbb P^2$
(эквивалентно $\mathrm{Tr}_{L/k}\big(\delta t^2/f'(\Theta)\big)=0$); у неё есть $k$-точка,
т.к. $C_\delta$ всюду локально разрешима (Хассе–Минковский). Параметризуем её $t=t(x,z)$ (квадратично) —
тогда $g(x,z)=-[\delta\,t(x,z)^2]_1$ — искомая бинарная квартика, $y^2=g(x,z)$ — модель $C_\delta$.
Остаётся нормировать инварианты: $I(g)=\lambda^4I$, $J(g)=\lambda^6J$, откуда
$\lambda^2=J(g)\,I/(J\,I(g))\in k^\times$, и берём $g/\lambda^2$ (общий скалярный множитель при этом
сокращается, так что нормировка однозначна).

Важная деталь: $x$-координаты 2-кручения $E_{I,J}$ равны $-3\varphi$ (а не $3\varphi$):
$(-3\varphi)^3-27I(-3\varphi)-27J=-27(\varphi^3-3I\varphi+J)=0$. Переход от модели $y^2=f(x)$
(с $a_1=a_3=0$) к $E_{I,J}$: $X=36x+3b_2$, значит корню $e$ отвечает $\varphi=-(12e+b_2)$,
а $I=c_4$, $J=2c_6$. (Проверено в коде на кривых с полным 2-кручением.)

---

## 2. Что реализовано (`descent/ctp_quartic.sage`)

* `EtaleCubic` — этальная алгебра $L=k[X]/(X^3-3IX+J)$ с разложением на компоненты-поля,
  CRT в обе стороны, обращение (через xgcd) и **квадратный корень** $m=\sqrt{z_1z_2z_3}$ покомпонентно.
* `FisherCTP.z_inv`, `.H_form` — $z(g)$ и $H$; `H_form` **проверяет тождество** $G(1,0)G=H^2$ как
  многочленов от $x,z$ над $L$ при каждом вызове.
* `.gamma1(g1,g2,g3)` — $m$, затем $\gamma_1$ = коэффициент при $\varphi^2$ в $(m/z_1)H_1$;
  проверяется, что $z_1z_2z_3$ действительно квадрат в $L$ (иначе классы не в сумме 0).
* `.quartic_from_delta(delta)` — конструкция §1.4: коника над $k$, параметризация, нормировка инвариантов.
  Контроли внутри: точка лежит на конике; параметризация лежит на конике; $I(g)=I$, $J(g)=J$;
  **$z(g)$ лежит в том же классе $L^\times/(L^\times)^2$, что и $\delta$**.
* `._make_z_unit` — Фишер, стр. 3: «by a change of coordinates we may assume that $z(g)$ is a unit in $L$».
  $z(g)$ действительно бывает делителем нуля; тогда берём собственно эквивалентную квартику.
* `.reduce_quartic` — приведение по $\mathrm{GL}_2(k)$ (действие $g\mapsto(\det\gamma)^{-2}g\circ\gamma$
  сохраняет $I,J$ при **любом** $\gamma\in\mathrm{GL}_2(k)$): «депрессирование» $b\to0$ (точное или
  округлённое — берётся то, что уменьшает битовую меру), балансировка $\mathrm{diag}(s,1)$ по
  симметрическим функциям корней, и приведение по **ковариантной точке Юлиа/Столла–Кремоны**
  $z_0=x_0+iy_0$, $x_0=\mathrm{mean}\,\mathrm{Re}\,\alpha_i$, $y_0=\sqrt{\sum|\alpha_i-x_0|^2/4}$,
  обобщённой на квадратичное $k$ (ковариантная точка считается при каждом вложении, затем ищутся
  $c,s\in k$ с $\sigma_j(c)\approx x_0^{(j)}$, $\sigma_j(s)\approx y_0^{(j)}$). Точность вычислений
  выбирается по **динамическому диапазону значений** коэффициентов, а не по их битовой длине.

* `.places_for` — точное множество мест как дополнение к условиям Замечания 3.3
  (архимедовы; $v\mid2$; $v\mid\Delta(g_1)$; $v\mid$ знаменатели $g_1,\gamma_1$; $v\mid\mathrm{content}(\gamma_1)$;
  $v\mid g_2(1,0)$; все $v$ с $N(v)\le16$).
* `.local_point` — точка $(x_v,z_v)$: сначала случайный поиск, затем **систематический поиск по дереву
  классов вычетов с отсечением**: для целой квартики $G$ и класса $x\in c+P^nO$ имеем $G(x)=G(c)+O(P^n)$,
  поэтому при $n-v(G(c))\ge 2v(2)+1$ квадратичный класс $G(x)$ постоянен — класс либо принимается,
  либо мёртв. Оба листа $\mathbb P^1$ ($z=1$ и $x=1$). Поиск точный и полный.
* `hilb` — символы Гильберта: конечные места через `k.hilbert_symbol`, вещественные — по точным знакам.
* Драйверы `deltas_full2`, `deltas_partial` (перевод базиса Селмера из `ek_descent.sage` /
  `ek_descent_partial.sage` в $\delta\in L$) и `ctp_matrix`.

### 2.1 Решающая деталь реализации: базис алгебры L

Наивно конику $[\delta t^2]_2=0$ записывают в базисе $(1,\Theta,\Theta^2)$ алгебры $L$. **Так делать нельзя.**
Для наших кривых ($I\sim10^{15}$, $J\sim10^{22}$) коэффициенты коники тогда имеют порядок $10^{13}$,
диагонализация даёт $10^{36}$, приведение по модулю квадратов упирается в факторизацию 100-значных чисел,
`bnfisnorm` над $k(\sqrt{t})$ не заканчивается за часы, а если и заканчивается — возвращает решение с
$10^{4}$ знаками, после чего нормировка инвариантов даёт квартику со знаменателями $\sim10^{80000}$.

Правильный базис — **согласованный с разложением $L=\prod_i F_i$** (через идемпотенты). Тогда
$$c_2(t)=\mathrm{Tr}_{L/k}\!\left(\frac{\delta t^2}{f'(\Theta)}\right)
=\sum_i \mathrm{Tr}_{F_i/k}\!\left(\frac{\delta_i t_i^2}{f'(e_i)}\right)$$
блочно-диагональна, а её коэффициенты — $S$-единицы ($\delta$ и $f'(e_i)$ поддержаны на $S$).
Коники решаются за секунды. **Разница на практике: часы → 3 секунды** (сечение n61, элемент №2).
Дополнительно реализованы: LLL-приведение тернарной формы по положительно определённой мажоранте
$H(u)=\sum_m|D_m|\,|L_m(u)|^2$ (из диагонализации), выбор среди многих случайных представлений коники
того, у которого $|N(t\bmod\text{квадраты})|$ минимальна (именно это определяет стоимость `bnfisnorm`),
и прерываемый (через `cysignals`) таймаут на `is_norm`.

### Точность локальной арифметики

**p-адических приближений в методе нет вообще.** Локальные точки $(x_v,z_v)$ берутся из $k$ *точно*
(плотность $k$ в $k_v$ + открытость условия); проверка «$g_1(x_v,z_v)$ — квадрат в $k_v$» точная
(валюация + вычет для $v\nmid2$, `ideallog` по модулю $P^{2e+1}$ для $v\mid2$); символы Гильберта точные;
вещественные знаки — точные (вложения в `AA`). Это качественно надёжнее метода Касселса в `ctp.sage`,
где нужны p-адические квадратные корни с доказуемыми оценками точности.

---

## 3. Контроли

### 3.1 Пример 3.4 самого Фишера (над ℚ) — воспроизведён точно

`E/ℚ: y²+y = x³−x²−929x−10595` (571a1), $S^{(2)}\cong(\mathbb Z/2)^2$, квартики $g_1,g_2,g_3$ из статьи.
Мой код даёт **в точности** опубликованные величины:

| величина | статья | мой код |
|---|---|---|
| $I, J$ | 44608, 18842960 | 44608, 18842960 |
| $\Delta$ | $-2^{12}\cdot571 = -2338816$ | −2338816 |
| $m$ | $\tfrac19(20\varphi^2-8656\varphi+936032)$ | `20/9*ph^2 - 8656/9*ph + 936032/9` |
| $\gamma_1$ | $\tfrac49(5x^2-16xz-12z^2)$ | `[20/9, -64/9, -16/3]` |
| вклад мест | только вещественное | только вещественное |
| $\langle[g_1],[g_2]\rangle$ | нетривиально | 1 |

Это сверяет **весь алгебраический стержень** (инварианты, гессиан, $G$, $H$, тождество $G(1,0)G=H^2$,
корень $m$ в $L$, $\gamma_1$) и локальную часть над ℚ.
Дополнительно: $\langle g_1,g_3\rangle = 1$, $\langle g_2,g_3\rangle = 1$, $\langle g_1,g_1\rangle = 0$;
квартика тривиального класса имеет $g(1,0)=0$ — согласовано с Замечанием 3.2(iii).
**Round-trip**: из $\delta = z(g_i)$ моя конструкция §1.4 восстанавливает квартики
$[-16,-4,116,-12,-163]$, $[-44637/16,1,211/2,0,-1]$, $[-1,34,-172,-144,-28]$ (собственно эквивалентные
исходным) и даёт **те же** значения спаривания — это сверяет конструкцию «элемент спуска → квартика».

### 3.2 Контроль (б): сверка с `ctp.sage` (метод Касселса) на кривых с полным 2-кручением

`E: y² = (x−e₁)(x−e₂)(x−e₃)` над `k = ℚ(√d)`; группа Селмера — из `ek_descent.sage` (`Curve3.selmer_full`),
один и тот же базис подан в обе реализации. Сравниваются **матрицы поэлементно**, не только ранги.
Прогоны `sage xcheck_quartic.sage` (логи `descent/xq_xcheck.log`, `xq_xcheck2.log`, `xq_xcheck4.log`):

| d | корни | dim Sel² | rank CTP (Фишер) | rank CTP (Касселс) | матрицы совпали |
|---|---|---|---|---|---|
| 10 | [0, 18, −11] | 5 | 2 | 2 | **да** (поэлементно) |
| 7 | [0, 11, −12] | 4 | 2 | 2 | **да** (поэлементно) |
| 2 | [0, 30, −13] | 5 | 2 | 2 | **да** (поэлементно) |
| 13 | [0, 31, −25] | 5 | 2 | 2 | **да** (поэлементно) |

Это самая сильная из проверок: два полностью независимых метода (Касселс через конусы квадрик и
касательные плоскости, с p-адическими приближениями — против Фишера через инварианты квартик, целиком
в точной арифметике) дают один и тот же кососимметрический билинейный функционал, а не только его ранг.

**Сверка на E₁ сечения n61 (dim Sel² = 7) доведена только до двух строк и остановлена.**
Строки 0 и 1 совпали с эталоном `ctp.sage` (`[0 0 0 0 0 0 0]` и `[0 0 0 0 0 0 1]`), дальше один
элемент строки 2 считался больше часа (это был тот же дефект поиска локальных точек, что описан в §3.5;
на момент остановки он ещё не был исправлен). Частичное совпадение двух строк **не объявляется полной
сверкой** — это лишь признак согласованности; полная сверка на n61 не проводилась.

### 3.3 Контроль (а): кривые над ℚ(√d) с ОДНОЙ точкой порядка 2 и точно известным рангом

`E: y² = x(x²+ax+b)` над `k = ℚ(√d)`, `Δ = a²−4b` не квадрат;
`rank E(k) = rank E₀(ℚ) + rank E₀^{(d)}(ℚ)`, обе цифры — PARI `ellrank` с требованием `lo == hi`.
`dim Sel²` — `ek_descent_partial.sage`; `rank CTP` — новая реализация (Фишер).
Прогон `SCAN=0 sage ctrl_quartic_partial.sage` (лог `descent/xq_ctrlD.log`):

| a | b | d | rank E(k) | dim Sel² | rank CTP | граница `dim Sel²−1−rank CTP` | точна | симм | диаг 0 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | −3 | 5 | 0 | 1 | 0 | 0 | да | да | да |
| 1 | 1 | 5 | 1 | 2 | 0 | 1 | да | да | да |
| −2 | −4 | 3 | 1 | 2 | 0 | 1 | да | да | да |
| 3 | 1 | −3 | 1 | 2 | 0 | 1 | да | да | да |
| −5 | 5 | 2 | 1 | 2 | 0 | 1 | да | да | да |
| 2 | −7 | 13 | 0 | 1 | 0 | 0 | да | да | да |
| 5 | 5 | 6 | 1 | 2 | 0 | 1 | да | да | да |
| 7 | 5 | 17 | 2 | 3 | 0 | 2 | да | да | да |
| −3 | −11 | 2 | 1 | 2 | 0 | 1 | да | да | да |
| 6 | 7 | −1 | 1 | 2 | 0 | 1 | да | да | да |
| 9 | 2 | 3 | 2 | 3 | 0 | 2 | да | да | да |
| −4 | 2 | 6 | 1 | 2 | 0 | 1 | да | да | да |
| 1 | −3 | −1 | 1 | 2 | 0 | 1 | да | да | да |
| 1 | −3 | 2 | 1 | 2 | 0 | 1 | да | да | да |

**14 кривых из 14**: граница никогда не ниже истинного ранга и во всех случаях **точна**.

**Честная оговорка о силе этого контроля.** У всех 14 кривых `dim Sel² − 1 = rank`, то есть
Ш(E/k)[2] = 0, и спаривание обязано быть нулевым — оно и нулевое. Значит контроль (а) в этой
выборке проверяет только отсутствие **ложных срабатываний** (что тоже важно: любая ошибка в
выборе мест, в локальных точках или в символах дала бы ненулевое значение и границу НИЖЕ истинного
ранга, то есть противоречие). Отсутствие **ложных пропусков** проверяется отдельно:
примером Фишера над ℚ (§3.1, спаривание нетривиально) и контролем (б) (§3.2, ранг 2 в трёх случаях,
матрицы совпадают поэлементно с независимым методом).
Поиск кривых этого семейства с Ш[2] ≠ 0 (нужен `dim Sel² − 1 − rank ≥ 2`) за отведённое время
результата не дал: просмотр ~10³ троек `(a,b,d)` не нашёл ни одной (в этом семействе они редки).

### 3.4 Внутренние свойства (контроль в)

Проверяются автоматически в каждом прогоне:

* **Симметрия — настоящий тест, а не тавтология.** Матрица заполняется по верхнему треугольнику
  (`M[j,i] := M[i,j]`), поэтому равенство `M = Mᵀ` ничего не проверяет. Вместо этого `ctp_matrix`
  пересчитывает несколько элементов в **обратном порядке аргументов**: ⟨g_j, g_i⟩ вместо ⟨g_i, g_j⟩.
  Формула Фишера по аргументам несимметрична (H строится по g₁, а старший коэффициент берётся у g₂),
  так что это содержательная проверка. Результаты (предпочтение отдаётся **ненулевым** элементам):
  * пример Фишера над ℚ: ⟨g₁,g₂⟩ = ⟨g₂,g₁⟩ = **1**;
  * `d = 2`, корни [0,30,−13]: ⟨1,4⟩ = ⟨4,1⟩ = **1**, ⟨2,4⟩ = ⟨4,2⟩ = **1**, ⟨1,2⟩ = ⟨2,1⟩ = 0;
  * `d = 13`, корни [0,31,−25]: ⟨1,4⟩ = ⟨4,1⟩ = **1**, ⟨0,2⟩ = ⟨2,0⟩ = 0, ⟨1,2⟩ = ⟨2,1⟩ = 0;
  * `d = 10`, корни [0,18,−11]: три пары, все совпали;
  * контроль (а): 14 кривых, во всех обратный пересчёт совпал (там все значения нулевые).
* **Нулевая диагональ** — диагональ действительно *вычисляется* (через квартику тривиального класса),
  а не постулируется; это полноценный контроль. Во всех прогонах диагональ нулевая.
* $z(g_1)z(g_2)z(g_3)$ — квадрат в $L$ (иначе классы не в сумме нуль) — при каждом вызове;
* тождество $G(1,0)\,G = H^2$ над $L[x,z]$ — при каждом вызове;
* $I(g) = I$, $J(g) = J$ точно — после построения и после каждого приведения;
* $z(g) \equiv \delta$ в $L^\times/(L^\times)^2$ — при каждом построении квартики;
* $z(g)$ — единица в $L$ (требование Фишера) — иначе берётся эквивалентная квартика;
* точка на конике и параметризация лежат на конике — проверяются подстановкой;
* независимость от выбора локальных точек: `pair(..., reps=2)` пересчитывает с другими
  $(x_v,z_v)$ и падает при расхождении.

### 3.5 Найденный и исправленный дефект (важен для доверия к числам)

Систематический поиск локальной точки изначально перебирал **все** вычеты по модулю $P$. Среди мест
формулы есть простые, делящие $g_2(1,0)$, — у них норма бывает $\sim10^{13}$ и больше; перебор
приводил к `MemoryError` (при `ulimit`) либо к зависанию на часы. Именно на этом стояли первый боевой
прогон n89 и сверка n61.

Исправление: ветвление дерева классов вычетов идёт не по всем вычетам, а **только по корням**
приведённого многочлена. Для $G_c(T) = G(c+\pi^n T)$, $m = \min v(\text{коэфф.})$,
$\hat g = (G_c/\pi^m) \bmod P$: валюация $v(G_c(T))$ превышает $m$ лишь при $\hat g(T) = 0$, значит живых
подклассов не больше 4 независимо от размера поля вычетов; «общий» случай ловится случайными пробами
внутри класса. Плюс прежнее отсечение: при $n - v(G(c)) \ge 2v(2)+1$ квадратичный класс на классе постоянен.
После исправления контрольный случай `d = 10` считается за 30 с вместо `MemoryError`.
**Все числа в §3.2–3.4 получены уже после исправления** (кроме строк 0–1 сверки n61, которые были
посчитаны до него, — там дефект просто не успел проявиться).

## 4. Боевой счёт

### 4.1 Статус

**Сверка n61 остановлена и не возобновлялась** (см. §3.2): две строки совпали, полная сверка по этому
пути слишком дорога, ядра отданы боевому счёту.

**n89, $E'_1 = E_1/\langle(e_1,0)\rangle$ над $k=\mathbb Q(\sqrt{462})$, `dim Sel²(E') = 8`.**
Спуск отработал (`dim Sel² = 8`, все внутренние контроли пройдены, кручение лежит в группе Селмера).
Построение 37 квартик (8 базисных + 28 сумм + тривиальная) занимает ~87 минут; квартики кэшируются на
диск (`descent/quart_n89_1.sobj`), поэтому перезапуск дёшев. Первый прогон уперся в дефект §3.5;
после исправления перезапущен (лог `descent/xq_n89g.log`).

РЕЗУЛЬТАТ_N89_СЮДА

### 4.2 Как читать результат

Граница пишется как
$$\mathrm{rank}\,E_1(k) = \mathrm{rank}\,E'(k) \;\le\; \dim \mathrm{Sel}^2(E') - \dim E'(k)[2] - \mathrm{rank}\,\mathrm{CTP}
= 8 - 1 - \mathrm{rank}\,\mathrm{CTP}.$$
Ранг спаривания всегда чётен. `rank CTP = 6` при `dim Sel² = 8` означает, что **ядро спаривания имеет
размерность 2** (а не что спаривание невырождено на всём Sel²), и даёт границу `≤ 1`.
Улучшение имеющегося `rank E₁(k) ≤ 3` начинается с `rank CTP ≥ 6`.
Для n79 (`dim Sel²(E') = 6`) улучшение `≤ 5` начинается с `rank CTP ≥ 2`.

## 5. Условности, ограничения и что осталось

### 5.1 Почему метод Касселса (обобщение `ctp.sage` на алгебру k × K) не подошёл

Запасной вариант из задания — формула Касселса через конусы пучка квадрик — требует **рациональной точки
на конике над полем определения точки 2-кручения**. У нас это $K=k(\sqrt\Delta)$ — поле степени 4 над ℚ
($x^4-25216x^2+136515856$ для n89). Проверено прямым экспериментом: `Conic(K,[1,B,C]).has_rational_point`
для случайных $B$ над этим полем **не завершается за 900 с** (нужен `bnfinit` поля степени 8).
Метод Фишера этого шага не содержит вовсе — это и есть причина, по которой выбран именно он.

### 5.2 Условность GRH

* `is_norm(..., proof=False)` при решении коник над $k$ (квадратичное поле) — **условно по GRH**
  (`bnfinit` без доказательства). Однако полученная точка коники **проверяется подстановкой**
  (`assert v*M*v == 0`), поэтому сама точка безусловна; GRH может влиять только на то, *найдётся* ли точка,
  а не на правильность найденной.
* `proof.number_field(False)` в спусковой части (`ek_descent_partial.sage`) — как и раньше;
  для боевых сечений в `PARTIAL_DESCENT_2026-09-12.md` размерности `dim Sel²` уже перепроверены с
  `proof=True` (те же 8 и 6), так что вход в CTP безусловен.
* Символы Гильберта, проверки квадратичности, локальные точки, арифметика в $L$ — **безусловны и точны**
  (никаких p-адических приближений, в отличие от `ctp.sage`).
* Множество мест берётся как дополнение к достаточным условиям Замечания 3.3 Фишера — это **доказанное**
  утверждение из статьи, а не эвристика (в отличие от порога «норма ≤ 60» в `ctp.sage`).

### 5.3 Что осталось

1. **Кривые контроля (а) с Ш[2] ≠ 0.** В семействе `y² = x(x²+ax+b)` над `ℚ(√d)` с малыми `a,b,d`
   такие не нашлись за отведённое время. Нужен более широкий/целенаправленный поиск
   (например, кривые с заведомо нетривиальным Ш над ℚ и их подъём).
2. **Приведение/минимизация квартик.** Реализовано эвристически (Юлиа + жадный спуск). Строгая
   минимизация genus-one моделей (Cremona–Fisher–Stoll) убрала бы знаменатели и сократила множество мест.
3. **Скорость.** Узкое место — построение квартик (одна коника над $k$ на элемент, $n+\binom n2$ коник
   на матрицу). Для `dim Sel² = 8` это 36 коник.
4. `ctrl_quartic_Q.sage` (сверка ранга спаривания с PARI `ellrank` над ℚ на десятках кривых Кремоны)
   написан, но не прогонялся — это дешёвый и сильный дополнительный контроль.

## 6. Запуск

```bash
cd /home/kep/magicKube/descent
sage xcheck_quartic.sage                  # контроль (б): Фишер vs Касселс, полное 2-кручение
SEC=n61 sage xcheck_n61.sage              # контроль (б2): прямая сверка на E1 сечения n61
SCAN=0 NEED=16 sage ctrl_quartic_partial.sage   # контроль (а): 14 кривых с точным рангом
SEC=n89 WHICH=0,1,2 sage run_quartic_sections.sage   # боевой счёт
N=40 sage ctrl_quartic_Q.sage             # (не прогонялось) сверка с PARI ellrank над Q
```

## 7. Оговорка

Ничто здесь не является доказательством. Формула — опубликованная теорема; реализация и все числа —
результат работы моего кода, не рецензировано, приоритет не заявляется.
