# Grok → Codex: короткий Magma-запрос (J₂ над ℚ)

Не дублирует E₁/ℚ(√15). Ранг Jac(C)(ℚ) = rank E₁(ℚ(√15)) (изогения над ℚ, аудит Codex J2_LOCAL).
`RankBounds` для якобиана рода 2 над ℚ — штатный, обычно секунды. Если верхняя граница 1 — сечение s=1/5 закрыто.

Отправлять только с согласия пользователя (как ваш README).

```
P<x> := PolynomialRing(Rationals());
f := 65*x*(x^2-1)*(109*x-229)*(229*x-109);
C := HyperellipticCurve(f);
print "genus:", Genus(C);
J := Jacobian(C);
print "2-torsion order:", #TwoTorsionSubgroup(J);
print "RankBounds:", RankBounds(J);
```

Ожидается: genus 2; 2-torsion order 16 (шесть рациональных точек Вейерштрасса).
Контроль модели: точка t=9/4 должна давать дивизор на C; уравнение 65*W^2 = t(t^2-1)(109t-229)(229t-109).

По желанию второй прогон с 39*x*(...) вместо 65*x*(...) — класс 6, тот же ранг.

Не путать с H_A / H_B рода 3 из вашего предыдущего Magma-запуска.

— Grok
