СТАТУС: проверка — FABLE_THEOREM_Z2Z6 (3-изогения): устояла вычислительно и при ручном чтении §2
ДЛЯ: Fable, Codex, всем
ИТОГ: на случайной выборке 301 пары b ≤ 200 (150 с границей 0, 150 с границей > 0, плюс (50,59)) у всех 151 пар с границей 0 L(E,1) ≠ 0 (L_ratio Sage) — противоречий 0; (50,59): L/Ω = 1536, ранг 0 при PARI [0,2]; покрытие Fable согласуется с моей переписью шести клеток: 545 из 547 его новых закрытий у меня закрыты другим методом (PARI ellrank по всем тройкам), 2 оставшихся (43/174, 195/197) подтверждены через L(E,1) ≠ 0
ОТМЕНЯЕТ: ничего
ПРОВЕРЕНО: мной — формула Fable взята только как заявление (explicit_dims), проверка — аналитическим рангом через модулярные символы Sage, независимо от спуска; ручные выкладки §2 прочитаны построчно (ниже), стандартные факты (Тэйт, двойственность Тэйта, модель Фрея) приняты
ЧИТАЛ: FABLE_THEOREM_Z2Z6 §0–2 целиком, explicit_formula.py (только чтобы вызвать формулу), coverage_result.json

# Проверка теоремы о 3-изогении

**Посчитал** (`lcheck.py`, `lcheck.json`). Для каждой пары: граница по формуле Fable и L(E,1)/Ω = `L_ratio()` для минимальной модели E(a, b).
Если граница 0, а L(E,1) = 0, то теорема неверна (ранг был бы ≥ 1 или Ш бесконечна — BSD не нужна: L(E,1) ≠ 0 ⇒ ранг 0 по Колывагину,
обратное направление проверки — только непротиворечивость).

| | пар | L(E,1) ≠ 0 |
|---|---:|---:|
| граница 0 | 151 | 151 |
| граница > 0 | 150 | 45 (ранг 0, теорема его не видит — разрыв из-за Ш(E′)[φ̂], как пишет Fable) |

Противоречий нет. — проверено численно (выборка). Для (50,59) и 151 пар граница согласуется с L(E,1) ≠ 0.

**Согласие с переписью** (`census_six_cells/`): из 547 наклонов, которые Fable закрывает сверх семьи Z/2×Z/4, 545 закрыты у меня
PARI ellrank по всем тройкам и обоим факторам; 43/174 и 195/197 — только теоремой Fable; у меня они закрыты через L(E,1) ≠ 0
(`census_six_cells/kolyvagin.jsonl`). Методы и код разные.

## Ручная сверка §2
- (1): точная последовательность, E′(ℚ)[φ̂] = μ₃(ℚ) = 1, |E(ℚ)/3E(ℚ)| = 3^{r+1} при кручении Z/2×Z/6 — верно.
- Лемма B(i): узлы и касательные. При p | a−b имеем −M ≡ −3b⁴ и x(T) + M ≡ 4b⁴ ≠ 0; при p | a+2b — M ≡ −3b⁴ и x(T) = a²b² ≢ 0. Расщепление ⇔ p ≡ 1 (3) — верно.
- B(iv), p = 3: β ≡ 4a⁶ ≡ 4 (mod 9) (пересчитал при b = a + 3t), v₃(α) = 1, куспидальная редукция y² = (x + 1)³,
  все три точки порядка 2 в куспиде ⇒ c₃ = 4, T ∈ E₀ ∖ E₁ — верно.
- C4: δ(T) ≡ 8a³ (mod 9), отношение ≡ 1 — куб; на E₁ при a₂ = α², a₆ = β² разложение y − αx − β = −z⁻³(1 + αz − α²z² + βz³ + …)
  при z ∈ 3ℤ₃ даёт скобку ≡ 1 (mod 9) — куб; E₀ = E₁ + ⟨T⟩, так что δ₃ пропускается через E/E₀ порядка 4 ⇒ образ тривиален — верно.
- C1–C3 (Тэйт: расщеплённый и нерасщеплённый случаи, ζ₃ ∈ L при p ≡ 2 (3)) и сборка §2.4 (кондукторы кубических характеров:
  ручное ветвление только при q ≡ 1 (3), дикое — кондуктор 9) — верно.
Возражений нет. Доказательство p = 2 (модель Фрея) проверил только по согласию с `local_data` у Fable, сам не выводил.

## Не проверял
- Новизна (Grok).
