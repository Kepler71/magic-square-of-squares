# Записка Codex: G-семейства — полная редукция задачи «семь квадратов» (Claude, 2026-09-12)

Контекст: `BREMNER16_2026-09-12.md`, код в `/home/kep/magicKube/bremner16/`.
Статья Бремнера (Acta Arith. 99 (2001)) скачана: `bremner16/bremner2001.pdf`.

## Главное, что стоит проверить независимо

**Лемма [доказано].** Всякий магический 3×3 над ℚ с ≥ 7 квадратными клетками содержит не менее
двух полных противоположных пар (пар клеток, симметричных относительно центра, обе квадратны).
Доказательство: неквадратных клеток ≤ 2, четыре пары попарно не пересекаются, две клетки портят
не более двух пар.

**Следствие — полная редукция.** В параметризации Бремнера (3)
```
   c+a     c-a-b    c+b
  c-a+b     c      c+a-b
   c-b     c+a+b   c-a
```
клетки нумеруются 0..8 построчно, противоположные пары — (0,8), (1,7), (2,6), (3,5), сумма в
каждой равна 2c. Пусть cell_i = P², cell_i' = Q², cell_k = R², cell_k' = S². Совместность —
одно условие P²+Q² = R²+S² = 2c. Эта квадрика в P³ расщепима над ℚ, её полная параметризация:

    P = m p + n q,  Q = m q - n p,  R = m p - n q,  S = m q + n p.

При q = 1 параметр p пробегает ℚ, и **все девять клеток — квадратичные многочлены от p**,
причём четыре из них квадратны тождественно. С точностью до D₈ таких семейств ровно три:
G1 = пары {1,7},{3,5}; G2 = {0,8},{2,6}; G3 = {1,7},{0,8}.

Обратное восстановление (нужно для проверки полноты): n = (P-R)/2, m = (Q+S)/2, p = (P+R)/(Q+S);
особый случай (Q+S, P-R) = (0,0) даёт P = R, то есть тривиальный квадрат.

Итого: **поиск семи квадратов = для каждого из трёх типов, каждого (m:n) ∈ P¹(ℚ) и каждого p ∈ ℚ
проверить, квадратны ли три из пяти оставшихся клеток (четыре клетки + центр).** Два условия из
трёх — кривая рода 1 (пересечение двух коник) с рациональной точкой в вырожденном слое; третье
условие — накрытие рода **5** (посчитано функциональными полями Sage над ℱ_p: роды 0 → 1 → 5).

Это отличается от схемы Бремнера: у него шесть квадратов живут на **рациональной** кривой на K3
(для конфигурации III он показывает, что все рациональные кривые чётной степени и что до степени 14
их буквально несколько), а здесь — на **эллиптической** кривой, и таких кривых однопараметрическое
семейство. Прежняя наша схема `corners/bremner_type.sage` (одна полная пара + третья клетка) даёт
только 5 гарантированных квадратов при неквадратном центре.

## Что уже посчитано (кратко)

- Перечисление 16 конфигураций перебором + Бёрнсайд; привязка к Fig. 1 Бремнера арифметическая
  (все 2⁹ прочтений токенов, выбор магических с ровно шестью квадратами) — биекция I..XVI.
- Известный пример (1) реализует ровно семь конфигураций: VIII, X, VII, XV, IV, XIII, XI.
  Для остальных девяти (I, II, III, V, VI, IX, XII, XIV, XVI) семиклеточного примера НЕ ИЗВЕСТНО.
- Контроли: пример (1) находится автоматически и эллиптическим движком (F1, F2, F3, F4, G3),
  и полностью независимым прямым перебором `boxscan.py`.
- Прогоны: новых примеров с ≥ 7 квадратами нет.

## Просьбы (если будет время в Magma)

1. **2-спуск на «трудных» квартиках G1.** У G1 вырожденные точки дают квадраты только парами
   {c2,c6} и {c0,c8}; для остальных восьми базовых пар рациональная точка на квартике находится
   примерно в трети случаев, и повышение границы поиска с 600 до 6000 картину не меняет.
   Вещественного препятствия нет (проверено для всех 508 пар (m,n) с m,|n| ≤ 20).
   Вопрос: это p-адические препятствия (тогда «пропуск» безвреден и его можно превратить в теорему
   вида «в G1 при данном (m,n) нет магических квадратов с квадратными c0 и c2») или элементы Ш?
2. **Ранг эллиптической поверхности** над ℚ(m/n) для базовых пар G-семейств: если у какой-то пары
   ранг над функциональным полем ≥ 1, получаем бесконечное семейство шестиклеточных примеров в
   явном виде, и тогда седьмая клетка сводится к одной кривой рода 5 над ℚ(λ) — это точный аналог
   того, что Бремнер делал для II и VII, но в нашей (более симметричной) параметризации.
3. **Chabauty/спуск на кривой рода 5** для конкретных (m,n) с большим рангом базовой кривой.

Файлы: `bremner16/engine2.sage` (движок), `scan2.sage` (прогон G), `boxscan.py` (прямой перебор),
`analysis.py`/`coverage.py` (какая конфигурация каким семейством обслуживается).
