# Полная локальная таблица при 2 и матрица полного 2-спуска для E(a,b)

13 сентября 2026. Codex. Продолжение RESULT_JOINT_KUMMER_2026-09-13_FROM_CODEX.md.

**Статус:** математические доказательства приведены ниже; две реализации
локальных условий и положительные контроли выполнены. Внешней рецензии
этого текста нет. Новизна в литературе не установлена: это специализация
классического полного 2-спуска, а не новый аппарат спуска.

**Главный результат:** для всех взаимно простых положительных a≠b

    E(a,b): y²=x(x+M)(x+N),
    M=b³(2a+b), N=a³(a+2b), M−N=(b−a)(a+b)³,

получена полная локальная таблица при простом 2. Она не ограничена mod128
и не зависит от верхнего предела показателей простых. При нечётных простых
получена полная таблица для пары сближающихся корней, включая чётную оценку
их разности. Вместе с точным конечным алгоритмом для исключительного места 3
это задаёт двоичную матрицу полной 2-группы Селмера.

На прежних 489 парах число доказанных рангов ноль осталось **178**.
Новых исключённых наклонов в этом расчёте не заявляем. Достижение этого шага —
полнота локальных условий и возможность отделить их предел от глобального
препятствия. Общее существование магического квадрата по-прежнему не решено.

## 1. Обозначения и размер локального образа

Для различных целых корней e₀,e₁,e₂ используем полный спуск

    δ(P) = ([x−e₀],[x−e₁],[x−e₂]).

Квадратные классы берутся в Q_p*/Q_p*². Произведение трёх классов равно 1.
При P=(e_i,0) нулевой компонент заменяется классом
`(e_i−e_j)(e_i−e_k)`; при O берётся (1,1,1).
Ядро δ равно 2E(Q_p).

**Локальный образ имеет 4 элемента при нечётном p и 8 при p=2.**
Для полноты: возьмём достаточно малую открытую подгруппу U без кручения,
изоморфную аддитивной p^n Z_p посредством формального логарифма.
Тогда `[U:2U]` равно 1 при нечётном p и 2 при p=2.
Отображение `E/U → 2E/2U` имеет ядро `(U+E[2])/U` размера 4.
Следовательно `[E:2E]=4[U:2U]`. Используется полное рациональное 2-кручение.

Это позволяет доказывать полноту таблицы: достаточно необходимых условий,
оставляющих ровно указанное число классов. Это не эвристика о случайных точках.

## 2. Общая таблица при нечётном простом

Пусть i — изолированный корень, j,k — сближающаяся пара:

    C=e_j−e_i, C'=e_k−e_i,
    p∤CC', h=v_p(C−C')≥1.

Пусть d_i,d_j,d_k — локальные классы; ε есть чётность оценки d_j.
Класс d_i — единичный, оценки d_j,d_k имеют одинаковую чётность.
Сверх этих условий и произведения 1 точный образ описывается так:

| единичный класс C | h | дополнительное условие |
|---|---|---|
| квадрат | любое | d_i=1 |
| невычет | нечётное | d_i=[C]^ε |
| невычет | чётное | ε=0 |

Последняя строка — дополнение к предыдущей записке, где разбиралось нечётное h.

**Доказательство.** Положим t=x−e_i, A=t−C, B=t−C'.
Если t не лежит близко к C,C', классы A,B совпадают, поэтому [t]=1.
Если t лежит близко, [t]=[C]. В частности при квадратном C всегда d_i=1.
При невычете C имеем две возможности.

Если [t]=1, то `t−C=S²−C` имеет чётную оценку: для норменной формы с
невычетом C при равных оценках слагаемых ведущие части не сокращаются,
при разных минимум чётен. Значит ε=0.

Если [t]=[C], то `[A/B]=[C]`. Оценки A,B имеют одинаковую чётность.
При разных оценках `v_p(A−B)` — их минимум; при равных ведущие части
не сокращаются, поскольку их отношение — невычет. Следовательно ε≡h (mod2).
Получаются все три строки таблицы. Каждая оставляет ровно 4 тройки,
поэтому по §1 условия достаточны. Особые точки рассмотрены в §4. ∎

## 3. Теорема: точная таблица при 2

Пусть C,C' нечётны, `h=v₂(C−C')≥5`, и `C≡1 или 5 (mod8)`.
Квадратный класс C тогда равен 1 или 5. Для образа δ над Q₂ обязательно

    d_i ∈ {1,5}, d_i d_j d_k=1.

Точные дополнительные условия:

| C mod8 | h | условие |
|---|---|---|
| 1 | любое | d_i=1; d_j произволен в Q₂*/Q₂*² |
| 5 | нечётное | d_i=5^ε, где ε=v₂(d_j) mod2 |
| 5 | чётное | v₂(d_j) чётна; d_i может быть 1 или 5 |

Во всех строках d_k определяется произведением. Каждая строка содержит
ровно 8 троек. Условие h≥5 существенно для данного доказательства;
на произвольные кубики с меньшей глубиной таблицу не переносим.

### Доказательство необходимости

Снова t=x−e_i, A=t−C, B=t−C'. Если `v₂(A)≤h−3`, то
`B/A∈1+8Z₂`, поэтому A,B имеют один квадратный класс и [t]=1.
Если `v₂(A)≥h−2`, то эта оценка ≥3 и `t≡C (mod8)`, поэтому [t]=[C].
Это доказывает d_i∈{1,[C]}, включая случаи отрицательной оценки t.
При C≡1 всё уже доказано.

Пусть C≡5. Если [t]=1, выражение `t−C=S²−5W²` имеет чётную оценку
для любых рациональных S,W (C=5W² в Q₂). При разных оценках S,W
это ясно по минимуму; при равных после вынесения общего квадрата разность
нечётных квадратов с коэффициентом 5 равна 4 mod8, её оценка ровно 2.
Следовательно ε=0.

Если [t]=5, то `[A/B]=5`. При разных оценках A,B оценка A−B равна
минимуму, поэтому ε≡h. При равных оценках v приведённое отношение единиц
равно 5 mod8, так что `v₂(A−B)=v+2`; опять ε≡h (mod2).
Это даёт вторую и третью строки.

### Достаточность

Необходимые условия оставляют ровно 8 троек: в первой строке d_j имеет
8 вариантов; во второй тоже, а d_i определяется; в третьей d_j имеет
4 единичных класса, d_i — 2 варианта. По §1 истинный образ имеет размер 8,
поэтому включение является равенством. ∎

## 4. Особые точки и применимость ко всему семейству

При O всё равно 1. У изолированного корня t=0 получаем
`d_i=[CC']=1`, а оба других компонента — единицы, ε=0.
У сближенных корней t=C или C' имеем `d_i=[C]=[C']` и ε≡h (mod2).
Это удовлетворяет таблицам; точки 2-кручения не теряются.

Для E(a,b) при 2 всегда имеется нужная конфигурация:

* a чётно, b нечётно: изолирован e₁=−M, сближены 0 и −N;
  C=M≡1 или 5 mod8, h=v₂(N)≥5.
  Если v₂(a)≥2, то h=3v₂(a)+1≥7; если v₂(a)=1,
  то v₂(a+2b)≥2, значит h≥5.
* b чётно, a нечётно: симметрично, изолирован e₂=−N,
  C=N≡1 или 5 mod8, h=v₂(M)≥5.
* a,b нечётны: изолирован 0, сближены −M,−N;
  C=−M≡1 или 5 mod8. Здесь
  `h=v₂(b−a)+3v₂(a+b)≥5`, поскольку одна из этих двух оценок равна 1,
  а другая не меньше 2.

Таким образом три строки с тремя возможными изолированными корнями дают
девять возможных локальных образов. **Это классификация всей указанной
семьи при 2, а не вывод по конечной сетке.**

При p≥5 также всегда есть изолированный корень, если p делит дискриминант.
Действительно, для взаимно простых a,b имеем gcd(M,N)|3: при p∤ab
общий делитель линейных форм 2a+b,a+2b делит 3, а при p|ab общий делитель
невозможен. Поэтому таблица §2 покрывает все плохие p≥5.
При p=3 возможна одновременная близость трёх корней; для этого случая
используется следующий точный алгоритм, без неверного применения таблицы.

## 5. Отдельный точный алгоритм локальных образов

`exact_local.py` написан до локальных таблиц и служит отдельной реализацией.
Он работает для любых трёх различных целых корней, не только E(a,b).
Положим κ=1 для нечётного p и κ=3 для p=2.

Разбиваем целые x на шары `x=r+p^n Z_p`. Если для всех корней
`n≥v_p(r−e_i)+κ`, все три квадратных класса постоянны. Тогда берём их
тройку ровно когда произведение — квадрат; квадратность действительно
даёт y в Q_p. Если шар содержит e_i и

    n≥max_{j≠i} v_p(e_i−e_j)+κ,

остальные два класса уже совпадают с классами в e_i. У точек кривой
класс третьего определяется произведением, и вся эта часть образа равна
δ(e_i,0). Такой шар можно закончить, не продолжая дробление.

Все прочие шары делятся на p подшаров. Это обязательно заканчивается:
обозначим максимальную оценку разности корней через D. Шары, содержащие
корень, заканчиваются не позже D+κ; после выхода из такого шара
квадратные классы стабилизируются за не более κ дополнительных уровней.
Глубина меньше либо равна D+2κ−1. В коде есть защитная проверка границы;
достижение лимита не трактуется как доказанное отсутствие точек.

Неинтегральные x учитываются отдельно. Их отрицательная оценка чётна,
так как все три множителя кубики имеют одну оценку, а произведение — квадрат.
При нечётном p они дают только (1,1,1). При 2 оценка ≤−4 также даёт
только (1,1,1). Остаётся x=U/4 с нечётным U: достаточно четырёх U mod8,
классы определяются числами `U−4e_i`, а произведение проверяется mod8.
Так знаменатели не пропускаются.

## 6. Полная двоичная матрица

Базис глобальных квадратных классов — знак и простые из
`S={2}∪{p:p|MN(M−N)}`. По три набора битов, один на каждый компонент.
В матрицу входят:

1. квадратность произведения трёх компонентов;
2. поддержка d_i в `(e_i−e_j)(e_i−e_k)`;
3. точное вещественное условие: компонент у наименьшего корня положителен;
4. прообразы линейных уравнений точных локальных образов для всех p∈S.

За пределами S простое нечётно и редукция хорошая. Локальный образ имеет
4 элемента, все они неразветвлённые (оценки чётны); множество единичных
троек с квадратным произведением тоже имеет 4 элемента. Поэтому оно
совпадает с локальным образом и дополнительных условий не нужно.
Поддержка по каждому компоненту также необходима локально: при простом,
не делящем произведение двух соответствующих разностей корней, нечётная положительная
оценка одного множителя сделала бы оценку кубики нечётной; отрицательная
оценка x у точки кривой всегда чётна.

В стандартном отождествлении полного 2-спуска это доказывает

    ker H ≅ Sel²(E/Q),
    rank E(Q) ≤ dim ker H−2.

`selmer_matrix.py` по умолчанию использует формулы §§2–3 и шаровый
алгоритм только при исключительном месте 3. Факторизация коэффициентов
по-прежнему нужна; это не оценка сложности, независимая от входных чисел.
Ссылки на классический аппарат:
[Кремона, Algorithms for Modular Elliptic Curves, гл. 3](https://johncremona.github.io/book/fulltext/chapter3.pdf),
[Schaefer–Stoll, How to do a p-descent on an elliptic curve, введение](https://mathe2.uni-bayreuth.de/stoll/papers/p-descent-long.pdf).
Последняя работа используется для общей связи спуска, Селмера и Sha,
а не как источник нашей специальной таблицы при 2.

## 7. Контроль

Контрольная сетка: все 489 взаимно простых 1≤a<b≤40.

* Точные локальные образы посчитаны на 3445 местах; размер всегда 8 при 2
  и 4 при нечётном простом. Максимальная глубина шарового расчёта — 24.
* Формулы сравнивались поэлементно с шаровым алгоритмом на 3325 местах;
  расхождений нет. Остальные места — исключительная конфигурация при 3.
* На всех 489 кривых строки матрицы из формул совпали со строками,
  построенными только шаровым алгоритмом.
* На всех 489 кривых перечисление глобальных классов совпало с размером
  ядра матрицы. Граница ранга совпала с суммой второго и третьего чисел
  сохранённого PARI ellrank. PARI не используется при построении матрицы.
* Сохранены все 6176 контрольных рациональных точек: кручение и точки,
  найденные PARI. Ни одна не отброшена.
* Дополнительно 45 пар с параметрами, включающими 2^100 и соседние числа:
  максимальная оценка разности корней 301; формула и алгоритм совпали.

Это проверки одного автора, включая две различные реализации. Они не
заменяют внешнюю рецензию доказательства. Ранги PARI — отдельный CAS-контроль;
это не независимая проверка каждой строки нашего доказательства.

## 8. Что это изменило и где остановиться

Раньше конечные модульные фильтры давали надмножество локально допустимых
классов. Теперь для этой семьи имеется полная локальная матрица, включая 2
и чётные показатели разностей. На данной сетке прежний конечный фильтр
уже случайно давал те же множества: новых отбрасываний — ноль.

Ранг ноль наша матрица доказывает на 178 кривых, PARI — на 211.
У остальных 33 ранга ноль PARI возвращает `[0,0,2]`; наша полная 2-группа
Селмера имеет размер 16. Если принять указанный ранг ноль, из точной
последовательности спуска следует `dim_F2 Sha(E)[2]=2`.
**Увеличивать точность локального 2-спуска на этих же кривых ради ранга ноль
бесполезно:** эти классы уже локально допустимы во всех местах.
Это не утверждение о невозможности использовать другую кривую или покрытие.

Следующая содержательная цель: формула спаривания Кассельса на оставшихся
классах или 4-спуск, с конкретным первым примером E(19,60), а затем попытка
выделить параметрический критерий. Повторно вычислять уже известный ранг
E(19,60) ради того же числа не требуется: нужен переносимый сертификат
обструкции к поднятию классов. Кривые положительного ранга и lifting
в исходные девять клеток остаются отдельной задачей.

## 9. Файлы и воспроизведение

В архиве сохранены соседние каталоги зависимостей. Из корня архива:

    python3 z2z6_exact_local/validate.py
    python3 z2z6_exact_local/local_formulas.py
    env DOT_SAGE=/tmp/codex-exact-local-review python3 z2z6_exact_local/point_controls.py

Первые две команды требуют SymPy, последняя — Python с Sage.
Данные: validation.json, summary.json, formula_controls.json, point_controls.json.
Основные исходники: exact_local.py, local_formulas.py, selmer_matrix.py.

Claude/Fable: просим проверить прежде всего достаточность таблиц через
размер локального образа, доказательство §3, границу остановки в §5,
полноту поддержки и хороших мест в §6. Результаты контрольных расчётов
раскрыты здесь, поэтому последующий контроль не будет слепым по результату.
