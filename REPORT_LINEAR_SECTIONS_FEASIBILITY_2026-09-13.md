СТАТУС: результат — оценка возможностей метода линейных сечений
ДЛЯ: всем; Codex — §3 (стена на знаменателе 97), Fable — §2 (какие кривые вообще могут работать)
ИТОГ: из 126 эллиптических факторов на наклоне k бесконечно много наклонов могут закрывать только 22; у наклонов 80/97, 73/105, 79/110, 43/145 ни один фактор не даёт ранг 0 — метод Codex в нынешнем виде за знаменатель 96 не пройдёт
ОТМЕНЯЕТ: надежду закрыть все наклоны перебором эллиптических факторов; продление переписи за знаменатель 96 тем же методом
ПРОВЕРЕНО: посчитал сам (скрипты в linear_sections_claude/); предсказание сверено с 60 случайными свидетелями Codex; «ноль» в §3 перепроверен с выводом счётчика ошибок и интервалов ранга
ЧИТАЛ: EXTENSION_TO_48_2026-09-13.md и census_through_48.json Codex (только для сверки предсказания в §2; расчёты §1–§3 — собственный код, без кода Codex)
ЧИТАТЬ ДАЛЬШЕ, ЕСЛИ: продолжаете линейные сечения или ищете, где метод может дать бесконечную серию

# Линейные сечения: что метод может, а что нет

13 сентября 2026. Оценка идеи пользователя в воплощении Codex (`linear_sections_codex/EXTENSION_TO_48_2026-09-13.md`).

## 0. Масштаб: это атака на задачу целиком

Любой невырожденный магический квадрат после деления на центр и симметрий имеет вид с 0 < q < p, p + q < 1,
и рациональным наклоном k = q/p ∈ (0,1). Вырожденные k = 0 и k = 1 дают совпадающие клетки.
**Значит, закрыть все рациональные k — значит решить задачу.** Это не подсемейство вроде G1.
Отсюда же сразу видно, что равномерное закрытие всех k так же трудно, как сама задача.

## 1. На наклоне k эллиптических факторов 126, и почти все бесполезны

На прямой q = kp клетки имеют вид 1 + c·p, c ∈ {±1, ±k, ±(1+k), ±(1−k)}. Произведение трёх клеток (56 наборов)
или четырёх (70 наборов) даёт эллиптическую кривую Y² = ∏(1 + c_i p). На **каждой** есть рациональная точка p = 0,
Y = 1 — вырожденный квадрат, все клетки равны 1.

**Проверено:** при шести случайных k (3/7, 5/11, 7/13, 2/9, 11/17, 13/29) точка p = 0 имеет порядок 4 при всех k
ровно у **12 троек и 10 четвёрок**. У остальных **104** наборов она хотя бы при одном k бесконечного порядка
(у всех 104 — встретился +∞).

**Вывод, статус доказано (ПО).** Точка кручения над полем ℚ(k) остаётся точкой кручения при любой подстановке.
Значит, у 104 наборов вырожденный квадрат даёт сечение бесконечного порядка над ℚ(k). j-инвариант ни у одного
семейства не постоянен (при шести k — разные значения), поэтому по **теореме Сильвермана о специализации**
ранг слоя ≥ 1 для всех k, кроме конечного числа. **Каждый из 104 наборов закрывает лишь конечное число наклонов.**
(ПО — порядки точек посчитаны Sage; сама теорема — классика.)

Бесконечно много наклонов могут закрывать только **22 кривые**: 12 троек и 10 четвёрок из `p0_torsion.json`.
У всех 22 группа кручения при проверенных k — **ℤ/2 × ℤ/4**, та же, что у фактора E_B в теореме о семействе G1.

## 2. Предсказание подтверждено на данных Codex

Из §1 следует: кубические закрытия Codex должны приходиться на 12 «кручёных» троек.
Взял **60 случайных** кубических свидетелей из `census_through_48.json`, для каждого нашёл свою тройку по изоморфизму
кривых: **все 60 — из 12 троек, ни одного из остальных 44.** Статус: проверено численно (выборка).

## 3. Стена: четыре наклона, где метод не работает вовсе

Знак функционального уравнения у каждой из 22 кривых при случайных k — примерно пополам (+1 от 15 до 28 из 40).
Если знак −1, то по **теореме о 2-чётности** (Докчицеры) ранг 2^∞-Селмера нечётен, и **никакой спуск ранг 0 не докажет**.

Перебор всех **6857** наклонов со знаменателем ≤ 150 (`rootno_wall.json`):
- знак −1 у **всех 12 троек** — **217** наклонов (3,2 %): тройками их не закрыть;
- знак −1 у **всех 22 кривых** — **4 наклона: 80/97, 73/105, 79/110, 43/145.**

Для этих четырёх проверены **все 126** факторов: у кривых со знаком +1 (50–64 шт.) ранг доказан PARI `ellrank`
и равен **2** (у шести — 4), ошибок ноль; у остальных знак −1.
**Ни один эллиптический фактор ни на одном из четырёх наклонов не даёт ранг 0.** Статус: доказано (ПО)
(знаки и интервалы ранга — PARI; вывод о спуске — теорема о 2-чётности).

*Контроль честности:* первый прогон этой проверки занял 1,2 с и вернул ноль при заглушённых исключениях — ровно
паттерн «пустой перебор = доказательство». Перезапущен с выводом счётчика ошибок и всех интервалов ранга: ошибок 0,
интервалы (2,2) и (4,4). Ноль настоящий.

**Следствие.** Перепись Codex дошла до знаменателя 48 без препятствий потому, что первая стена — на 97.
Продлить её за 96 методом «ранг 0 у эллиптического фактора» нельзя.

## 4. Что реально достижимо

**(а) Бесконечные серии наклонов — достижимо, стандартная техника.** У всех 22 кривых полное рациональное
2-кручение и точка порядка 4. Для таких кривых 2-спуск через изогению сводится к символам Гильберта в простых,
делящих числитель и знаменатель k и их комбинации. Типичный результат такого вида: «если все простые делители
r, s, r±s, … лежат в таких-то классах по модулю 8, то 2-Селмер тривиален, ранг 0, наклон r/s закрыт».
Это бесконечное (но разреженное) множество наклонов. Материал для поиска условий — работа Fable над данными.
Статус: план.

**(б) Стены — нужны кривые рода 2.** Произведение пяти клеток (56 наборов) или шести (28) даёт кривую
**рода 2**; итого 84 фактора рода 2 на наклон. Шаботи на них работает при ранге якобиана < 2. Четыре наклона
80/97, 73/105, 79/110, 43/145 — готовый испытательный стенд: либо род 2 их закрывает, либо стена прочнее.
Статус: план.

**(в) Недостижимо известными средствами.** Закрыть все k сразу: это равносильно решению задачи.
Судя по данным, стены не исчезают: если бы знаки 22 кривых были независимы, стена встречалась бы с частотой
~2⁻²² ≈ 2·10⁻⁷, а наблюдается 10⁻³ — знаки сильно коррелированы (задаются общими простыми).
Правдоподобно, что стен бесконечно много. Статус: гипотеза.

## Не делать

- Не продлевать перепись за знаменатель 96 тем же методом: на 80/97 он заведомо не проходит.
- Не искать закономерности среди 104 наборов с сечением бесконечного порядка: каждый закрывает конечное число k.
- Не надеяться на «ранг 0 над полем ℚ(k)»: по Сильвермену слои не беднее общего слоя; искать надо условия
  на простые делители, а не общий ранг.
- Не называть «знаменатель ≥ 49» закрытием чего-либо: это необходимое условие.
