СТАТУС: результат — рецензия; все проверенные утверждения Codex подтверждены, ошибок, меняющих выводы, не найдено
ДЛЯ: Codex, Claude, пользователь; Grok — только таблица вердиктов
ИТОГ: A, B, C, лемма §2, предложение §6 и теорема о фиксированных шести клетках (род 5, бесконечность неортогональных семилинейных квадратов в слое Саллоуса) верны; род 5 пересчитан прямо (Sage, три модели, включая точные числа Саллоуса), формула рода (2) сверена с суммой Кани–Розена при r ≤ 12; ветвление на бесконечности при чётном r есть (индекс 2); ранги 2,4,4,2,2 воспроизведены; из рецензии Codex на мои записки признаю все восемь пунктов (с одним уточнением про фибрации)
ОТМЕНЯЕТ: мои формулировки «восемь магических линий = восемь прямых сетки», «род 17 у V∩C», «V = W ∩ Wᵀ», «E_{m,n} 2-изогенна конгруэнтной», «теорема A запрещает фибрации» (в части баз рода 0), «единственная лазейка = накрытия, этальные над U» как готовое сведение; не отменяет ничего у Codex
ПРОВЕРЕНО: мной — руками (Риман–Гурвиц, локальные разложения, S-единицы) и Sage/PARI (`fable_review_codex/g1–g5`, ~3 мин счёта); это повторная проверка, не слепая: ожидаемые ответы (род 5, ранги, значения S) были в проверяемых текстах
ЧИТАЛ: PROTOCOL_NOTES; REVIEW_FABLE_SYMMETRY_2026-09-14_FROM_CODEX.md; THEOREM_INFINITY_CONSTRAINTS_2026-09-14_FROM_CODEX.md; THEOREM_FIXED_SIX_FINITE_SQUARE_SUMS_2026-09-14_FROM_CODEX.md; PROOF_FIXED_SIX_COMPLETE_2026-09-14_FROM_CODEX.md; RESULT_CENSUS_200_2026-09-14.md; свои FABLE_SYMMETRY §§0, 2.3–2.4, 3 и FABLE_CLOSING_INFINITY §§2.1, 2.2 (ради уступок); строку 3 и 11–13 unify_claude/NOTE.md. Архивы Codex (.zip: check.py, geometry.py, genus5_factors.py, verify.py) НЕ открывал
ЧИТАТЬ ДАЛЬШЕ, ЕСЛИ: нужны детали проверки, замечания по формулировкам или точный список моих уступок

# Рецензия на записки Codex о конечности (14.09.2026, вечер)

Метки: **доказано** — проверил вывод сам; **численно** — воспроизвёл расчётом; **прочитал** — беру из источника без проверки;
**наблюдение**/**замечание** — не влияет на выводы.

## 1. Вердикты

| утверждение Codex | вердикт | как проверено |
|---|---|---|
| Лемма §2: род (ℤ/2)^r-накрытия P¹ по r линейным формам с различными нулями равен 1 + 2^{r−2}(r−3); индекс 2 в r+1 точках | **доказано, верно** — в том числе индекс 2 на бесконечности при **любом** r, чётном тоже | руками; Кани–Розен при r ≤ 12; прямой род при r = 2, 3, 4, 5 (Sage: 0, 1, 5, 17) |
| A: фиксированный p₀, четыре клетки 1±q, 1+p₀±q, нули −1, 1, −1−p₀, 1+p₀, совпадения только при p₀ ∈ {0,−1,−2}, род 5, конечность | **доказано, верно** | символьно (`g3`); род 5 при p₀ = 1/3 напрямую (`g1`) |
| A, шесть клеток: совпадения при p₀ ∈ {0,±1,±2}, род 49 | **доказано** (род — по формуле, прямо не считал) | символьно (`g3`) |
| B: фиксированный наклон k, λ ∈ {±1,±k,±(1±k)}, вырождение при k ∈ {0,±1,±½,±2}, род 321 | **доказано, верно**; параметризация совпадает с клетками переписи | символьно (`g3`, `g5`) |
| C: 1±p — S-единицы ⇒ U=(1+p)/2, V=(1−p)/2 — S∪{2}-единицы, U+V=1, конечно много p, затем A | **доказано, верно**; область условия сформулирована корректно | руками (§4 ниже) |
| §6: неплотность U(ℚ) ⇒ бесконечность только от кривой рода 0/1 над ℚ | **доказано, верно** (стандартно) | руками |
| Фиксированные шесть: h−b, h, h+b, h+2a — квадраты ⇒ кривая рода 5, конечность | **доказано, верно**; гладкость модели в P⁴ и род 5 — прямо на числах Саллоуса | Sage `Curve.genus()`, `is_smooth()` (`g1`) |
| Следствие: в слое Саллоуса бесконечно много семилинейных квадратов из девяти различных положительных квадратов с неквадратной S | **доказано, верно** | m = 1…8: три квадрата, семь сумм, положительность, различность, S ≠ □ при m ≥ 2 (`g2`) |
| Кручение E: (ℤ/2)², #E(F₁₁)=12, #E(F₁₃)=20, P неторсионна | **численно, верно** | `g2` |
| Ранги пяти факторов 2, 4, 4, 2, 2 (CAS-контроль) | **численно, верно** | PARI `ellrank`, нижняя = верхняя (`g4`) |
| Сверка с переписью (B ↔ ноль решений при s ≤ 200) | противоречий нет; кривые переписи — это в точности гиперэллиптические факторы леммы §2 | §7 ниже |

**Ошибок, меняющих выводы, не найдено.** Замечания по формулировкам — §8.

## 2. Лемма §2 и вопрос о бесконечности при чётном r — **доказано**

Меня просили проверить: не может ли при чётном r бесконечность **не** ветвиться. Ответ: при чётном r не ветвится на бесконечности
только **гиперэллиптический фактор** y² = f₁⋯f_r (степень чётная), но полное (ℤ/2)^r-накрытие ветвится с индексом 2 при любом r.
Причина: поле содержит √f₁, а v_∞(f₁) = −1 нечётна. Локально при s = 1/t: f_i = s⁻¹(1 − r_i s), и √(1 − r_i s) ∈ ℚ[[s]]
(биномиальный ряд; `g1` проверяет до O(s⁸)). Значит все √f_i лежат в ℚ((s))(√s) — расширение степени 2. Индекс 2, над ∞ ровно
2^{r−1} точек. Вклад (r+1)·2^{r−1} в Римана–Гурвица и формула (2) верны.

Независимый контроль формулы: для абелева накрытия с группой (ℤ/2)^r якобиан изогенен произведению якобианов факторов
C/ker χ (Кани–Розен), то есть g = Σ_{∅≠T⊆{1..r}} g(y² = ∏_{i∈T} f_i) = Σ_T ⌊(|T|−1)/2⌋. Сумма совпала с 1 + 2^{r−2}(r−3)
при всех r от 2 до 12: 0, 1, 5, 17, 49, 129, 321, 769, … (`g1`, **численно**). Прямой род Sage (проективная модель
u_i² − u₀² + r_i z² = 0 в P^r): r = 2 → 0, r = 3 → 1, r = 4 → 5 на трёх наборах корней, r = 5 → 17 (корни 0, 1, −1, 2, −3;
гладкая, 162 с). Решающий момент: если бы при r = 4
бесконечность не ветвилась, Риман–Гурвиц дал бы 2g − 2 = −32 + 4·8 = 0, g = 1, а не 5. Прямой счёт даёт 5.
Контроль на вырождение: корни −1, 1, 0, 0 (p₀ = −1) — модель не гладкая, род 1: лемма действительно требует различных нулей.

Оговорка к формулировке: «r различных нулей» надо читать как «f_i попарно непропорциональны» — это одно и то же, но
слово «различных» без «попарно» можно прочесть слабее. Ведущие коэффициенты роли не играют (над ℚ̄ квадраты) — верно.

## 3. Части A и B — **доказано**

**A.** Клетки формы Лукаса: 1±p, 1±q, 1±(p+q), 1±(p−q) — как множество симметричны при p↔q, поэтому вторая половина A
следует из первой. Четыре клетки (3) с нулями по q: −1, 1, −1−p₀, 1+p₀. Попарные совпадения (`g3`, символьно): ровно
p₀ ∈ {0, −1, −2} — совпадает с Codex. При 0 < |p₀| < 1 они исключены, r = 4, род 5, Фальтингс. Подъём рациональных наборов
корней на гладкую модель обоснован в лемме (якобиан полного ранга) — верно.
Шесть клеток: нули ±1, ±1−p₀, ±1+p₀, совпадения ровно при p₀ ∈ {0, ±1, ±2} (`g3`) — совпадает; род 49 по формуле.

Замечание (не ошибка): конечность q для четырёх клеток верна при **всех** p₀ ∉ {0, −1, −2}, ограничение 0 < |p₀| < 1 нужно
только для положительности и сформулировано честно.

**B.** Восемь клеток 1 + λp, λ ∈ {±1, ±k, ±(1+k), ±(1−k)}. Совпадения λ_i = λ_j ровно при k ∈ {0, ±1, ±½, ±2}, нуль λ = 0 при
k ∈ {0, ±1} (`g3`) — объединение совпадает с Codex. Нули по p равны −1/λ, различны при различных λ. Род 321 по формуле
(Кани–Розен подтверждает). При k = r/s и p = sz набор {λp} — ровно клетки переписи {±sz, ±rz, ±(s±r)z} (`g5`, символьно):
B — это в точности утверждение «на каждом наклоне переписи конечно много точек».

## 4. Часть C: область условия об S-единицах — **доказано**

Вопрос из задания: «1±p как S-единицы означает, что U, V — S∪{2}-единицы? а если у 1+p в числителе простые вне S, но они
сокращаются с 2?» Разбор: гипотеза C наложена на **сами** числа 1+p и 1−p (все простые их сокращённых числителей и
знаменателей лежат в S). Любое простое вне S в числителе 1+p нарушает гипотезу — сокращаться ему не с чем, гипотеза уже
о сокращённой дроби. Тогда supp((1+p)/2) ⊆ supp(1+p) ∪ {2} ⊆ S ∪ {2}: U, V — S′-единицы тривиально, U + V = 1, и по
Зигелю–Малеру (эффективно по Бейкеру; ссылка Codex на Эвертсе–Дьёри уместна) решений конечно. Равносильная запись без
двойки: X + Y = 2, X = 1+p, Y = 1−p, S-единицы — тот же результат. U, V ≠ 0, так как p ≠ ±1 (положительность).
Далее для каждого p — конечность q по A (p ≠ 0, иначе клетки совпадают). Всё верно.

Оценка веса: C — слабое ограничение (только на одну противоположную пару), и Codex сам это говорит («не утверждает, что
число простых должно расти»). Эффективность: граница на p эффективна, шаг A — нет; Codex это отметил. Согласен.

## 5. Предложение §6 — **доказано** (стандартно)

Замыкание по Зарискому U(ℚ) в X, если собственное, — конечное объединение кривых и точек. На каждой ℚ-неприводимой кривой
все ℚ-точки, кроме конечного числа особых, поднимаются в нормализацию (она определена над ℚ). Если кривая геометрически
приводима, её ℚ-точки лежат на пересечениях сопряжённых компонент — конечно. Иначе род нормализации ≥ 2 ⇒ Фальтингс.
Остаются кривые рода 0 (≅ P¹ над ℚ) и 1 (положительный ранг). Верно. Условность маршрута (7) сформулирована честно.

Не проверял (**прочитал**, по цитате проекта): «поверхность общего типа» и «квазигиперболичность = конечность кривых рода
0 и 1» из Auel–Singer / Bruin–Thomas–Várilly-Alvarado. Статус этих двух ссылок в моей рецензии не повышается.

## 6. Теорема о фиксированных шести клетках и следствие для Саллоуса — **доказано**

Нули 0, b, −b, −2a попарно различны при b ≠ 0, 2a ∉ {0, ±b}; положительность шести клеток даёт a > |b| + |c| ≥ |b|
(max(|b+c|, |b−c|) = |b| + |c| — тождество, `g3` контроль) — верно. Ветвление на бесконечности: «все четыре радикала имеют
одну общую ветвящуюся часть» — верно, см. §2; сомнение из задания («при чётном числе множителей произведение регулярно на
бесконечности») относится только к фактору y² = h(h−b)(h+b)(h+2a) рода 1, не к накрытию степени 16. Прямой контроль:
модель (11) в P⁴ на **точных** числах Саллоуса (корни 0, −3360, 3360, −8840) — гладкая, род 5 (`g1`, Sage). Присоединение
K = O(1), степень 8 — согласуется. Аргумент со столбцом t в якобиане (шаг 6) верен.

Следствие 3 (`g2`, **численно**): E: y² = x³ − 3360²x, P = (113², 127·113·97) на кривой; кручение (ℤ/2)², #E(F₁₁) = 12,
#E(F₁₃) = 20, P неторсионна. Более того, x(P), x(P) ± 3360 — квадраты (113², 97², 127²), то есть P ∈ 2E(ℚ), поэтому
**все** кратные mP, а не только 2mP, дают три квадрата — это согласует «2P/3P» из рецензии Codex с «x(2mP)» из PROOF.
Для m = 1…8: h, h ± 3360 — квадраты; семь сумм равны h + 8840; главная диагональ 3h ≠ S; девять клеток положительны и
попарно различны; S — не квадрат при m = 2…8 (при m = 1 это сам Саллоус, S = 147² — ортогональный тип, как и должно).
Формулы удвоения (15) проверены как тождества по модулю уравнения кривой. Значения S для 2P и 3P, корни диагонали 3P и
оценка 314505646² < N < 314505647² совпали с текстами Codex (`g2`, `g5`). Ранги пяти факторов: 2, 4, 4, 2, 2 —
воспроизведены PARI (нижняя = верхняя граница). Признание: мой первый прогон дал 1 у пятого фактора из-за моей ошибки
в переводе квартики в кубику (лишний множитель u); после исправления — 2. Вывод Codex «rank > genus, простой Шаботи не
применим» верен.

## 7. Сверка с переписью

B утверждает конечность на каждом наклоне; перепись (RESULT_CENSUS_200) — ноль невырожденных решений на каждом из 12 230
наклонов при s ≤ 200. Ноль ⊂ конечно, противоречий нет. Полезное наблюдение: кривые переписи — это гиперэллиптические
факторы y² = ∏_{i∈T} f_i леммы §2 при |T| = 3, 4 (род 1) и 5, 6 (род 2), то есть в точности члены разложения Кани–Розена
кривой рода 321; полная кривая наклона содержит их все как факторы якобиана. Это объясняет, почему «дешёвые» методы
переписи закрывают наклоны: достаточно ранга 0 у одного из 2⁸ − 1 факторов (или Шаботи у фактора рода 2).

## 8. Замечания по формулировкам (не ошибки)

1. THEOREM_INFINITY §3, вывод «высоты не могут оставаться ограниченными» — следует из A (конечно много p ограниченной
   высоты, для каждого конечно много q); формулировка верна, но стоило сослаться на A, а не на «A и B».
2. THEOREM_FIXED_SIX: «кратные неторсионной точки P» дают три квадрата потому, что P ∈ 2E(ℚ) (критерий деления пополам);
   в PROOF это обойдено через 2mP. Обе версии верны, но короткая записка не называет причину.
3. В лемме §2 стоит явно сказать «попарно непропорциональные f_i» (см. §2).
4. Утверждения о роде 49 и 321 в этой рецензии подтверждены формулой и суммой Кани–Розена, но не прямым CAS-родом
   (прямой счёт сделан при r ≤ 5: 0, 1, 5, 17; r ≥ 6 не считал). Для конечности по Фальтингсу это несущественно:
   любая ошибка в роде при r ≥ 4 оставила бы род ≥ 2 (уже подкривая из четырёх клеток даёт 5).
5. Ссылки на Auel–Singer в §6 — **прочитал**, не проверял.

## 9. Что признаю из рецензии Codex на мои записки

Прочитал REVIEW_FABLE_SYMMETRY целиком. Признаю **все** перечисленные ошибки; спорю только с формулировкой одного пункта.

1. **Восемь прямых сетки ≠ восемь магических линий** — признаю. Девять клеток — сетка {a + ib + jc}, но прямые i = const
   имеют суммы 3a + 3ib: магичны лишь четыре прямые через центр. Восемь магических линий — это восемь троек индексов
   (i,j) с нулевой суммой: четыре прямые через центр и четыре «ломаные» тройки вида {(1,1), (−1,0), (0,−1)}. Моя
   формулировка в §0 и ИТОГ(1) FABLE_SYMMETRY неверна; переформулировка «двумерная АП из квадратов» остаётся верной,
   но описание линий надо заменить на «тройки с нулевой суммой индексов». Пункт 2 рецензии (тройки с центром в
   полумагической форме не АП; пример строки Саллоуса) — признаю.
2. **Род 9, а не 17** — признаю по аргументу о степени: отображение [q] ↦ [q_i²] на P³ имеет степень 8 (общий знак
   поглощён проективностью), проективная кривая V∩C — (ℤ/2)³-накрытие коники с 8 точками ветвления, 2g − 2 = −16 + 32,
   g = 9. Мой `s2` присоединял четыре корня после выбора представителя — это двойное накрытие, род 17. Численно не
   пересчитывал; аргумент степени достаточен. Конечность по Фальтингсу над ℚ(√3) сохраняется (9 ≥ 2).
3. **Wᵀ = W** — признаю, тривиально: транспонирование сохраняет обе диагонали. Нужное отражение — перестановка левого
   и правого столбцов (или поворот), V = W ∩ ρ(W).
4. **«E_{m,n} 2-изогенна конгруэнтной кривой» неверно** — признаю. Проверил: j(y² = x(x+1)(x+4)) = 35152/9 не целое
   (`g5`), CM нет, изогения с кривой j = 1728 невозможна. В unify_claude/NOTE доказана изогения с фактором B семейства
   G1 — я неверно пересказал.
5. **Теорема A не запрещает фибраций над P¹** — признаю с уточнением. q = 0 запрещает непостоянные отображения на кривые
   рода ≥ 1, то есть фибрации над базой рода ≥ 1 — только они дали бы конечность «сразу» (конечно много слоёв). Именно
   их я имел в виду в ОТМЕНЯЕТ, но написал «фибрации на кривые рода ≥ 2» без указания базы. Фибрации над P¹ со слоями
   рода ≥ 2 не запрещены, и одна такая — сама перепись по наклонам (слой рода 321); они дают конечность лишь послойно
   (часть B Codex). Строку ОТМЕНЯЕТ в FABLE_CLOSING_INFINITY надо читать: «не искать фибраций над кривыми рода ≥ 1».
6. **Из A–D не следует запрет всех бесконечных серий** — признаю; это тот же разрыв, что (6) у Codex: послойная
   конечность не даёт конечности объединения. Слово «вывод» в §2.5 моей записки надо понизить до «рассуждение».
7. **Шевалле–Вейль над открытой частью** — признаю: этальность над U без S-целости не даёт конечного набора скручиваний
   (пример t ↦ t² на G_m верен). «Единственная лазейка» — не готовое сведение, а идея.
8. Мелкое: ромб, а не восьмиугольник; коразмерность 2 не запрещает всех инволюций; инволюция z ↦ 1/(abz) в B;
   баннер «отменяет поиск p-адических условий» сузить; Бремнер говорит об отсутствии известных примеров, не о
   невозможности; Auel–Singer Remark 3.2 не выводит 5/7 из Фальтингса — всё признаю.

Где не согласен: по существу нигде. Единственное уточнение — пункт 5 (какие фибрации теорема A действительно запрещает).

## 10. Не делать

- Не пересчитывать род 5/49/321: подтверждён двумя независимыми способами (§2); при r ≥ 4 любой пересчёт даст ≥ 2.
- Не искать «неветвящуюся бесконечность» при чётном r в полном накрытии — её нет (§2); она есть только у фактора
  y² = ∏ всех f_i, что для конечности несущественно.
- Не ставить под сомнение ранги 2,4,4,2,2 из-за моего первого прогона — расхождение было моей ошибкой конвертации.
- Не считать рецензию слепой: ожидаемые ответы были в текстах; независима только методика (модели в P^r, Кани–Розен).
- Не менять баннеры в записках Codex — оснований нет. Баннеры в мои FABLE_SYMMETRY / FABLE_CLOSING_INFINITY по
  пунктам §9 поставит Claude или я в следующей сессии (в этой я правлю только свои новые файлы).

## 11. Файлы

`fable_review_codex/g1_genus_lemma.py/.log/.json` — Кани–Розен r ≤ 12, прямой род r = 2,3,4 (+ r = 5 с лимитом), ряд
√(1 − rs); `g2_sallows_multiples.*` — E, P, кручение, m = 1…8, тождества (15); `g3_roots.*` — совпадения корней A/B/шести,
тождество max(|b±c|), ромб; `g4_ranks.*` — PARI `ellrank` пяти факторов (исправленная конвертация квартики);
`g5_misc.*` — j(E_{1,2}), корни 3P, isqrt, сверка λ-набора с переписью. Запуск: `python3 <файл>` (Sage в окружении
проекта). Внешних действий не выполнялось.
