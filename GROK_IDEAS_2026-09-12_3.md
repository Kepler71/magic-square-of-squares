# Идеи (Grok, 12.09.2026, пачка 6)

Не доказано. Факты — `GROK_INFO_2026-09-12_2007.md`. Пачки 1–5 не копирую. Sage не менял.

Остаток тот же: (13,8)/(15,8)/(16,5); долг по рангу (11,4) и (19,5); чётное n (Claude уже считает); письма.

---

## 1. Не крутить mwrank second_limit на (11,4)

Класс (1,274,274) **уже в Селмере**. Дальнейший eclib с большим `hlim` не закрывает Sha: second descent уже inconclusive. Это та же дыра, что у (19,16) до CTP-сертификата.

Следующий шаг — **тот же Python-сертификат Фишера Thm 3.1**, что только что сделали для (19,16): квартики `ell2cover`, z(g) = δ, ⟨g1,g2⟩_CT ≠ 0 ⇒ класс не из E(ℚ)/2E(ℚ) ⇒ C(ℚ)=∅ **без** rank=1. PARI ellrank [1,1,2] уже считает этот CTP внутри (Allombert); не хватает выгрузки, которую умеет `verify_certificate.py`.

То же для (19,5), если Claude подтвердит, что (1,193,193) ∈ Sel². Не QC, не поиск H(ℚ).

Контроль: при ненулевом CTP единственный минус может сидеть на ℝ — как у (19,16), это не отсутствие вещественных точек C.

## 2. (15,1) не трогать рангом

Там eclib certain=True **и** Bober `analytic_rank_upper_bound=1` при известной точке бесконечного порядка. Догонять `sha().an()` на 5 ГБ незачем для исключения C. CTP-сертификат — по желанию, для единообразия с (19,16), не блокер.

## 3. Не делать

- QC на (15,1)/(11,4)/(19,5) — C(Q) заявлено пустым; слабость только в ранге двух из трёх.
- Второй watch_magickube / GPU / H0 / сайт / brute I–XVI.
- Считать «ранг матрицы высот = 2» в `run2.log` вторым генератором.
- Повторять even-n 2-адику: скрипты Claude уже запущены.

Конец.
