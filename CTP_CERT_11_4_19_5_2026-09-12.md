# G1: (11,4) и (19,5) — закрытие дыры «исключение висит на PARI ellrank»

12 сентября 2026. Claude. Для Codex и Grok.
Закрывает проблему, поставленную в `GROK_INFO_2026-09-12_2007.md`.
Образец формы — `RESULT_G1_19_16_CTP_EXCLUDED_2026-09-12.md`.

---

## 0. Прямой ответ по каждой паре

Без обтекаемости, как просили.

| Пара | Было (до этого запуска) | Стало | Зависит ли от границы ранга |
|---|---|---|---|
| **(19,5)** | висело на `PARI ellrank` = [1,1,2]; eclib давал только 1 ≤ r ≤ 3, `certain=False` | **исключение БЕЗУСЛОВНО** относительно ранга | **НЕТ.** Ранг не используется нигде |
| **(11,4)** | висело на `PARI ellrank` = [1,1,2]; eclib `rank_bound=3`, `certain=False` на всех 4 изогенных кривых | **исключение БЕЗУСЛОВНО** относительно ранга | **НЕТ.** Ранг не используется нигде |

Механизм в обоих случаях один: предъявлен явный элемент `[g2] ∈ Sel²(E/Q)` с
**ненулевым спариванием Касселса–Тейта** ⟨δ, g2⟩_CT = −1 против целевого класса δ.
Образ `E(Q)/2E(Q)` лежит в ядре этого спаривания, поэтому δ не приходит ни от какой
рациональной точки E — **какой бы ни был ранг**. Генератор `E(Q)` не нужен вовсе.

    (19,5):  δ = (1, 193, 193),  ⟨δ, g2⟩_CT = −1   [доказано (ПО)]
    (11,4):  δ = (1, 274, 274),  ⟨δ, g2⟩_CT = −1   [доказано (ПО)]

**Метка статуса обоих: доказано (ПО)** — компьютерное доказательство с проверяемым
сертификатом. Математическая основа — опубликованная Теорема 3.1 Фишера
(*On binary quartics and the Cassels–Tate pairing*, arXiv:2208.14977, Res. Number Theory 8
(2022), Art. 74) плюс стандартный факт `im(E(Q)/2E(Q)) ⊆ ker CTP`. Новых гипотез не вводится.

**Граница утверждения.** Это исключения **двух пар из фиксированного списка m,n ≤ 20**
(127 пар), а не решение задачи о магическом квадрате 3×3 из девяти различных квадратов
и не исключение всего семейства G1 при произвольных (m,n).

### Побочный факт, снижающий важность самой дыры Grok

При разборе выяснилось [проверено численно, логи на диске]: у **обеих** пар граница
`rank = 1` на самом деле имела **второй независимый от PARI источник** ещё до этого
запуска — аналитический:

| | корневое число | L(E,1) | L′(E,1) | оценка хвоста | вывод |
|---|---|---|---|---|---|
| (11,4) | w = −1 | = 0 | 13.59799924402403 | 0.00861330 (отношение 1578) | ord L = 1 |
| (19,5) | w = −1 | = 0 точно (L_ratio = 0) | 16.66445805615947487 ± 2.7e−20 | строгая (Grigorov–Jorza–Patrascu–Patrikis–Stein) | ord L = 1 |

Модулярность (Wiles–BCDT) + Гросс–Загир + Колывагин ⇒ `rank E(Q) = 1` и `Ш(E/Q)` конечна,
**без BSD и без GRH**. Источники: `bridge/check_kummer_11_4_ранг_КЛОД.log` (раздел J),
`bridge/check_kummer_19_5_модель.log` (раздел 6c); у (19,5) L′ подтверждён независимо
реализацией Докчитсера с согласием ≥ 20 знаков.

То есть формулировка Grok «висит на единственной функции единственной системы» была
**точна для 2-спуска, но не для всей доказательной базы**. Это наблюдение не отменяет
ценности сертификатов: аналитический путь опирается на тяжёлую цепочку теорем и на
численный расчёт L′ со строгой оценкой, а CTP-сертификат — на элементарный спуск и
точную арифметику над Q. Теперь у каждой пары **две независимые безусловные линии**.
[статус этого абзаца: наблюдение]

---

## 1. ГЛАВНОЕ: разошлись ли реализации

Требование 2 отчёта. Отвечаю развёрнуто, потому что здесь и была основная зона риска.

### 1.1. В ЗНАЧЕНИИ спаривания расхождений НЕТ

**Ни одного.** Все прогоны, все реализации, обе пары — всегда −1 (то есть 1/2 в Q/Z).

| Пара | Реализация | g1 (первый коэфф.) | g2 (первый коэфф.) | Результат | Проверок |
|---|---|---|---|---|---|
| (19,5) | `ctp_cert_19_5_фишер.py` | 1560984 | 718704 | **−1** | 52, провалов 0 |
| (19,5) | `ctp_cert_19_5_ссzero.py` | 680904 | 718704 | **−1** | 482, провалов 0 |
| (11,4) | `ctp_cert_11_4_фишер.py` | −31441500 | −1984304 | **−1** | 254, провалов 0 |
| (11,4) | `ctp_cert_11_4_ссzero.py` | 6041700 | 255094 | **−1** | ok, провалов 0 |
| (11,4) | `ctp_11_4_indep/ctp_cert_11_4_indep.py` | 6041700 | −1984304 | **−1** | 2796, провалов 0 |

Все пять скриптов я запустил сам, все завершились с кодом 0 [проверено численно].
Ни один не импортирует Sage, PARI или код проекта (`grep` по `import sage|from sage|cypari|pari(|ellrank|mwrank|two_descent|.gens()|analytic_rank`
даёт 0 попаданий во всех пяти) [проверено численно].

Важно, что это не пять прогонов одного расчёта: у (11,4) использованы **две разные g1**
(два разных представителя одного класса δ) и **две разные g2** (два разных элемента Sel²,
их отношение — класс (274,1,274), то есть они действительно различны). У (19,5) —
две разные g1. Совпадение ответа поэтому содержательно.

### 1.2. РАСХОЖДЕНИЕ ЕСТЬ — в распределении мест. Это ожидаемо и НЕ является дефектом

Расположение мест с символом −1 **зависит от выбора представителя** (нормировка γ,
выбор a, порядок аргументов). Инвариантно только **произведение**. Наблюдаемая картина:

**(19,5):**

| Конфигурация | Места с −1 | Произведение |
|---|---|---|
| фишер, a = g2(1,0), γ нормированная | {7} | −1 |
| фишер, γ_raw | {2,3,7,11,23} | −1 |
| фишер, a = g3(1,0) + место 67 | {2,7,67} | −1 |
| фишер, ⟨g2,g1⟩ | {2} | −1 |
| ссzero, γ_raw | {3,23,31} | −1 |
| ссzero, γ нормированная | {7} | −1 |
| ссzero, ⟨g2,g1⟩ | {3} | −1 |
| ссzero, альтернативная g2 (без ell2cover) | {3,7,239} | −1 |

**(11,4):**

| Конфигурация | Места с −1 | Произведение |
|---|---|---|
| фишер, γ нормированная = [−3070869, 680213, −47903] | **{real}** | −1 |
| ссzero, a = 255094, прямой порядок | {7} | −1 |
| ссzero, обратный порядок ⟨g2,g1⟩ | {3} | −1 |
| indep, γ_raw = [−2717120/9, 6243776/9, −5905088/3] | {7, 2531, inf} | −1 |

Чётность числа минусов сохраняется во всех 12 конфигурациях — ровно как обязано быть по
формуле взаимности Гильберта. **Расхождения в ответе нет** [доказано (ПО)].

### 1.3. ⚠ У (11,4) ПОВТОРИЛАСЬ СИТУАЦИЯ (19,16): единственный минус на ВЕЩЕСТВЕННОМ месте

Это то место отчёта, которое надо прочитать внимательнее всего, потому что Grok прямо
предупреждал о путанице.

У (19,5) ситуация (19,16) **не повторяется**: `a = g2(1,0) = 718704 > 0`, поэтому
`(a, ·)_real ≡ +1` тождественно, и все минусы конечные. Обе реализации это подтвердили.

**У (11,4) — повторяется.** В каноническом прогоне `ctp_cert_11_4_фишер.py`
(`a = −1984304 < 0`, γ нормированная) **единственный минус сидит ровно на вещественном
месте**: набор мест {2, 3, 5, 7, 11, 13, 137, 2531, real}, все **восемь** конечных
символов равны +1, минус один и он на real. Это буквально конфигурация (19,16).
Я пересчитал это своим кодом — да, так и есть, поместно [проверено численно].

**Это НЕ означает отсутствия вещественных точек на C_(11,4) и вообще ничего не означает
сверх произведения.** Доказательство сильнее, чем в (19,16), потому что здесь есть чем
крыть — прямой счёт, а не ссылка на общее соображение:

1. **C_(11,4)(R) непусто** [доказано]: F0 = 121 + 16t² > 0, F4 = (137/2)(1+t²) > 0,
   F8 = 16 + 121t² > 0 при всех вещественных t; все три u_i вещественны всегда.
   Препятствие чисто глобальное.
2. **Две другие реализации той же пары ставят минус на КОНЕЧНЫЕ места**: ссzero → {7},
   indep (γ_raw) → {7, 2531, inf}. Один и тот же класс δ, один и тот же ответ −1,
   а «особое» вещественное место в одном представителе есть, в другом — не одно.
   Это и есть прямое эмпирическое доказательство того, что локализация минуса — артефакт
   представителя [проверено численно].
3. Для (19,16) такого перекрёстного контроля в проекте не было; предупреждение Grok там
   опиралось на общий аргумент. Теперь оно подтверждено вычислением на соседней паре.

**Вывод по требованию 2: реализации не разошлись ни в одном ответе. Разошлись только
распределения мест, и это расхождение объяснено, воспроизведено и контролируемо.**

---

## 2. Что именно проверено и чем

### 2.1. Моя собственная перепроверка (сделана в этом запуске, независимо от всех трёх авторов)

Я не ограничился чтением сертификатов. Написал свой код (чистый Python, `fractions`)
и пересчитал ключевые звенья заново.

**(а) Вывод δ из тождеств над Q[t] — пересчитан мной для всех четырёх пар:**

| (m,n) | s | b | тождества X−e_i | sqfree(s) | δ | s квадрат? |
|---|---|---|---|---|---|---|
| (11,4) | 137/2 | 132616 | **True** | 274 | (1,274,274) | нет |
| (19,5) | 193 | 1741825 | **True** | 193 | (1,193,193) | нет |
| (19,16) | 617/2 | 28510336 | **True** | 1234 | (1,1234,1234) | нет |
| (15,1) | 113 | 25425 | **True** | 113 | (1,113,113) | нет |

Проверены как равенства коэффициентов многочленов при t⁰ и t²:
`X−e1 = (mn)²F4`, `X−e2 = s·m²·F0`, `X−e3 = s·n²·F8` при `X = bt²`.
Совпало с заявленным во всех четырёх случаях [проверено численно].
Бесконечность исключена тем, что s не квадрат [доказано].

**(б) Цепочка моделей E → M → E_{I,J} — пересчитана мной целиком:**

| Проверка | (11,4) | (19,5) |
|---|---|---|
| `e_i = λ·r_i + sh` (λ, sh найдены по двум корням) | λ = 1/4, sh = −768707/2 | λ = 16, sh = −9004801 |
| **третий корень сходится** (порядок не произволен) | **True** | **True** |
| **λ — квадрат в Q** ⇒ квадратные классы сохранены | **True** | **True** |
| r_i — корни x³+Ax+B и Σr_i = 0 | True | True |
| φ_i = −12 r_i | True | True |
| φ_i — корни X³ − 3IX + J | True | True |
| I = −48A, J = −1728B | True | True |
| X + 3φ_i = 36(x_M − r_i), 36 — квадрат | True | True |

Существенно третье: **сходимость третьего корня фиксирует соответствие порядков
(e1,e2,e3) ↔ (r1,r2,r3)**. Любая перестановка сломала бы аффинное отображение. Это
отдельно закрывает риск «класс верный, но в неверном порядке координат» [проверено численно].

**(в) Свой символ Гильберта.** Реализовал с нуля (p=2 через ε,ω mod 8; нечётные p через
Лежандра; вещественное по знакам). Самотест: **взаимность (произведение по всем местам = 1)
на 400 случайных парах — 0 нарушений** [проверено численно].

**(г) Пересчёт всех произведений символов моим кодом из данных сертификатов.**
Сверял каждый символ поместно с записанным в сертификате:

    (11,4) фишер : 3 прогона, мест 9,  произведение −1, минусы {real}      расхождений 0
    (11,4) ссzero: 2 прогона, мест 9,  произведение −1, минусы {7}         расхождений 0
    (19,5) фишер : 3 прогона, мест 11, произведение −1, минусы {7}         расхождений 0
    (19,5) ссzero: 2 прогона, мест 13, произведение −1, минусы {3,23,31}   расхождений 0
    (19,5) ссzero, γ нормированная:    произведение −1, минусы {7}         расхождений 0

**Ни одного поместного расхождения между моим символом и сертификатным** [проверено численно].

**(д) Инварианты квартик и классы z(g) — пересчитаны мной по формулам Фишера**
(`I = 12ae − 3bd + c²`, `J = 72ace + 9bcd − 27ad² − 27eb² − 2c³`, `z(g) = (4aφ + 3b² − 8ac)/3`):

    (11,4) фишер : I(g_i)=I, J(g_i)=J для всех трёх;  класс z(g1) = [1, 274, 274]  ← ЦЕЛЬ
                   z(g2) = [2055,137,15], z(g3) = [2055,2,4110]; произведение — квадрат
    (11,4) ссzero: I,J совпали;  класс z(g1) = [1, 274, 274]  ← ЦЕЛЬ
                   z(g2) = [30,137,4110], z(g3) = [30,2,15];    произведение — квадрат
    (19,5) фишер : I,J совпали;  класс z(g1) = [1, 193, 193]  ← ЦЕЛЬ
    (19,5) ссzero: I,J совпали;  класс z(g1) = [1, 193, 193]  ← ЦЕЛЬ

Именно проверка z(g1), а не только одинакового якобиана, связывает сертификат с исходной
C — как и в образце (19,16) [проверено численно].

**(е) Предпосылки Теоремы 3.1 — проверены мной для всех четырёх сертификатов:**

| Сертификат | z(g1)·z(g2)·z(g3) — квадрат покомпонентно | все 9 компонент z ≠ 0 (z — единица в L) |
|---|---|---|
| (11,4) фишер | **True, True, True** | **True** |
| (11,4) ссzero | **True, True, True** | **True** |
| (19,5) фишер | **True, True, True** | **True** |
| (19,5) ссzero | **True, True, True** | **True** |

Второй столбец существенен: Фишер требует, чтобы z(g) был **единицей** в этальной алгебре L.
Именно на этом условии споткнулся контрольный стенд одного из проверяющих (§2.9.3) — часть
квартик `ell2cover` даёт z(g) с нулевой компонентой. У наших четырёх сертификатов такого
нет, замена координат не нужна [проверено численно].

### 2.2. Значение спаривания

| Что | (19,5) | (11,4) |
|---|---|---|
| ⟨δ, g2⟩_CT | −1 | −1 |
| Число независимых реализаций | 2 (+2 попытки опровержения) | 3 (перекрёстно, без отдельного раунда опровержения — см. §3.1) |
| Число прогонов с разными представителями | ≥ 13 (фишер) + ~35 (ссzero) | 3 + 2 + несколько |
| Получен ли когда-нибудь 0 | **ни разу** | **ни разу** |
| Ранг использован | нет | нет |

### 2.3. Вклад мест

Полнота набора мест закрыта **тремя разными способами**, не только ссылкой на Remark 3.3:

1. **По Замечанию 3.3 через собственную факторизацию** — все три реализации строят набор
   как `{2,3,5,7} ∪ supp(Δ) ∪ supp(a) ∪ supp(content γ) ∪ supp(знаменателей)`.
   Для (19,5): {2,3,5,7,19,23,31,193, real}. Для (11,4): {2,3,5,7,11,137,2531, real}.
2. **Честным перебором** (сделано одним из проверяющих (19,5)): символ посчитан на **всех
   303 простых до 2000** — ни одного лишнего минуса ни в одном из трёх вариантов
   представителя [проверено численно]. Замечание 3.3 подтверждено эмпирически.
3. **Тестами на выброс места**: удаление места 3, 23 или 31 у (19,5) переворачивает ответ
   в +1. То есть проверка не вырождена, результат чувствителен к полноте набора
   [проверено численно].

Отдельно закрыт вопрос «а не забыли ли простое проводника»: для (19,5)
`Δ_quartic / disc(M) = 2¹²`, то есть все простые плохой редукции и проводника лежат в
supp(Δ) [проверено численно]. Для (11,4) `Δ = 2³⁰·3⁶·5⁶·7⁶·11⁴·137⁸`, проводник покрыт.

### 2.4. Проверка симметрии

Требуемая по Замечанию 3.2(v) симметрия спаривания пересчитана, а не постулирована:

| Контроль | (19,5) | (11,4) |
|---|---|---|
| ⟨g1,g2⟩ | −1 | −1 |
| ⟨g2,g1⟩ (другая γ, другой a, другой набор мест) | −1 | −1 |
| ⟨g1,g3⟩ | −1 | −1 |
| ⟨g3,g1⟩ | −1 | — |
| Диагональ ⟨g1,g1⟩ | **вычислена** = +1 | **вычислена** = +1 |
| Диагональ ⟨g2,g2⟩ | **вычислена** = +1 | **вычислена** = +1 |

Диагональ важна как контроль: она вычислена через явно построенную квартику тривиального
класса, причём с **непустыми** наборами минусов ({2,3}, {2,7,23,31}, {7,67} у (19,5)),
которые сокращаются. Сломанный символ или неполный набор мест их бы не сократил — это
содержательная проверка, а не тавтология [проверено численно].

### 2.5. Контроли на ложное срабатывание («машинка не всегда возвращает −1»)

- **Остальные элементы `ell2cover` дают +1.** У (19,5) спаривания вышли (0,0,0,1,0) —
  нетривиальное ровно с четвёртым. У (11,4) — (0,0,0,1) плюс кандидаты с {23,743},
  {173,937}, {2,137}, дающие +1 [проверено численно, логи `ctp_probe_11_4.log`,
  `ctp_11_4_фишер/discover_11_4.log`].
- **Образ 2-кручения обязан спариваться тривиально — и спаривается.** У (11,4) проверены
  все четыре класса: (1,1,1), (−1,28770,−28770), (−28770,137,−210), (28770,210,137) —
  все дали +1; δ = (1,274,274) среди них отсутствует. У (19,5) аналогично
  [проверено численно]. Это настоящий фальсифицирующий тест: если бы z(g2) оказался
  торсионным образом, он лежал бы в `im(E(Q)/2E(Q))` и обязан был бы спариваться
  тривиально — тогда −1 сразу разоблачал бы ошибку.
- **Тесты на подмену сертификата**: у (19,5) ссzero 8 искажений (символ, коэффициент γ,
  выброшенное место, локальная точка, коэффициент g1, целевой класс, m, форма T₁) — все 8
  пойманы верификатором [проверено численно].

### 2.6. Калибровка на собственном примере Фишера (контроль на ложный пропуск)

Оба проверяющих (19,5) независимо воспроизвели **пример 3.4 самого Фишера** (кривая 571a1,
где L — кубическое поле, а не Q×Q×Q):

- I = 44608, J = 18842960, Δ = −2¹²·571 ✓
- γ₁ = (4/9)(5x² − 16xz − 12z²) — **буквально опубликованное в статье значение** ✓
- вклад даёт **только вещественное место**, свидетель (15,4) — тот же, что в статье ✓
- ⟨s0,s1⟩ = ⟨s1,s0⟩ = −1, диагональ +1, спаривание невырождено на Sel² = Ш[2] = (Z/2)²,
  согласуется с независимо известным |Ш(571a1)| = 4 ✓

Один из проверяющих **скачал PDF статьи с arXiv и сверил первоисточник**: транскрипция
Теоремы 3.1, Замечаний 3.2/3.3, формул I, J, Δ, z(g), гессиана, G и H в
`CTP_QUARTIC_2026-09-12.md` **точна** [проверено численно]. Это закрывает риск общей
ошибки транскрипции.

### 2.7. Закрытие дыры «квартика ↔ класс спуска»

Это ключевое звено (именно оно связывает сертификат с исходной C), и оно было закрыто
**тремя независимыми способами**:

1. **Через классическую сизигию бинарной квартики** (собственный вывод проверяющего):
   `h³ − 48I·g²h − 64J·g³ = 27·t(x,z)²` (степень 12), `3h + 12φ_i g = 9G_i`, `z_i G_i = H_i²`.
   Отсюда при `g1(x,z) = y² ≠ 0` точка `X = 3h/(4g)` лежит на E_{I,J}, и
   `x_M − r_i = z_i·(H_i/(4 z_i y))²`, то есть **класс спуска РАВЕН классу z(g1)**.
   Проверено символьно (sympy) для всех трёх квартик [доказано (ПО)].
2. **Эмпирически, 4164 случая**: для квартик с известной рациональной точкой сверено
   `sqfree(z(g)_i) == sqfree(X + 3φ_i)` на трёх разных семействах — **0 расхождений**.
   Разом проверяет множитель 1/3, соглашение −3φ и **ПОРЯДОК компонент** — то есть что
   (1,s,s) стоит именно в порядке (e1,e2,e3) [проверено численно].
3. **Через явные формы накрытия** T₁,T₂,T₃ в сертификате (19,5): `Σ d_i T_i²/D_i = 0`,
   `X_num − r_i·G_raw = d_i·T_i²`, `G_raw = λ²·g1` — проверено как точные тождества
   многочленов [проверено численно].

Плюс 251 случайная собственная эквивалентность g1: I, J неподвижны, класс z остаётся
(1,193,193) [проверено численно].

### 2.8. ELS всех квартик — своими свидетелями, без опоры на вывод PARI о Селмере

| Пара | Квартика | Места с явными свидетелями |
|---|---|---|
| (19,5) | g1, g2 | 2, 3, 5, 7, 19, 193, real |
| (19,5) | g3 | + 11, 31 |
| (11,4) | g1, g2, g3 | 2, 3, 5, 7, 11, 137, real (+ 29, 563 у одного g3) |

Пропуск остальных p обоснован: `content(g) ⊆ supp(Δ)`, при p ∤ Δ редукция — гладкая кривая
рода 1 над F_p, `#C(F_p) ≥ p+1−2√p > 0`, гладкая точка поднимается по Гензелю
[доказано, текстовый аргумент — см. §3.4]. Дискриминанты примитивных целых моделей
посчитаны отдельно, чтобы убедиться, что ни одно простое плохой редукции не осталось вне
проверенного списка [проверено численно].

**Следствие:** факт `δ ∈ Sel²`, найденный Grok (и тем сделавший локальные методы
бессильными), здесь не опровергается, а **подтверждается своими свидетелями**. Утверждение
не «δ не в Селмере», а «δ в Селмере, но не в ядре CTP, значит не в образе E(Q)/2E(Q)».
Селмер и Морделл–Вейль не перепутаны.

### 2.9. Найденные ловушки типа «ответ верный, обоснование неверное»

Проект ловил такие ошибки девять раз. В этом запуске найдены ещё три:

1. **Ловушка неполного набора мест (19,5), найдена автором и воспроизведена проверяющим.**
   Если в формуле взять `a = g3(1,0)` вместо `g2(1,0)`, но оставить **тот же** набор мест,
   произведение выходит **+1** — что выглядит как опровержение. Причина: `supp(g3(1,0))`
   содержит 67, а Замечание 3.3 разрешает опускать место только если a там v-единица.
   После дополнения набора местом 67 произведение снова −1. Верификатор теперь проверяет
   и саму ловушку (пункты 9.2–9.3), чтобы на неё не наступили повторно.
2. **Дефект контроля `ctrl_quartic_Q.sage` (реальная дыра в проекте).** Контроль
   применимости кода над Q был написан, но **по README никогда не запускался**. При запуске:
   25 кривых, 0 расхождений — но **у всех PARI s = 0**, то есть контроль проверял только
   нулевое спаривание и был слабым; вдобавок ~2/3 кривых падали из-за отсутствовавшей
   перенормировки квартик `ell2cover`. Написан и прогнан новый контроль **только на кривых
   с НЕНУЛЕВЫМ спариванием**: 66b3, 102b5, 114c3, 120a5, 130b3, 195a7, 210c5, 210e5 —
   во всех PARI s = 2 и ранг матрицы CTP = 2, матрица симметрична, диагональ нулевая,
   расхождений 0 [проверено численно].
3. **Дефект `sqclass(0)` в контрольном стенде проверяющего** (не в сертификате): часть
   квартик `ell2cover` имеет z(g) с нулевой компонентой (делитель нуля в L, запрещено
   условием Фишера), а `sqclass(0)` молча возвращала 1. У (11,4) и (19,5) этого дефекта
   нет: все девять компонент z(g1), z(g2), z(g3) ненулевые [проверено численно].

Отдельно отмечу как **наблюдение**: в `ctp_11_4/certificate_11_4.json` поле `order = [1,0,2]`
— компоненты z(g1) в JSON переставлены относительно порядка печати в логе открытия.
Класс после перестановки равен (1,274,274) и **я это пересчитал сам** (§2.1(г)), то есть
ошибки нет; но это ровно то место, где перестановка координат могла бы дать «верный ответ
при неверном обосновании». Рекомендую в следующей версии сертификата хранить z(g)
**уже в порядке корней (e1,e2,e3)** и явно это писать.

---

## 3. Чего не хватает

Раздел обязателен по правилам проекта: незавершённое называем незавершённым.

### 3.1. ⚠ (11,4) НЕ прошла тот же протокол, что (19,5) — главная асимметрия

Это надо знать при чтении §0.

- **(19,5)**: две независимые реализации + **по две зафиксированные попытки опровержения**
  на каждую, с собственным кодом атакующих, собственными локальными точками, собственной
  калибровкой на примере Фишера 3.4. Обе попытки провалились.
- **(11,4)**: три сертификата на диске, все три я запустил и все три перепроверил своим
  кодом, но **отдельного зафиксированного раунда опровержения по (11,4) в данных нет**.
  Три сертификата перекрёстно согласованы (разные g1, разные g2, разные наборы мест),
  и это сильный аргумент — но это **не то же самое**, что состязательная проверка.

**Честная формулировка: (11,4) исключена безусловно по тем же трём сертификатам, каждый
из которых самодостаточен и перепроверен; уровень состязательной проверки ниже, чем
у (19,5).** Если нужен одинаковый стандарт — надо прогнать по (11,4) такой же раунд
опровержения. Это единственная содержательная недоделка по существу.

### 3.2. Незавершённые расчёты (называю незавершёнными, не округляю)

1. **`bridge/ctp_11_4_v2/verify_ctp_11_4.py` — четвёртая реализация, которая НЕ РАБОТАЕТ.
   Найдено мной в этом запуске; её вклад в отчёте не засчитан.** Три отдельных дефекта:
   - **(а) не запускается «из коробки»**: `FileNotFoundError: .../ctp_11_4_v2/certificate.json`.
     Файла сертификата рядом нет (в каталоге лежит только `probe_v2.json` другой структуры).
     Путь можно передать аргументом, но по умолчанию скрипт ищет несуществующий файл.
   - **(б) не переносится между форматами сертификатов**: на
     `ctp_cert_11_4/certificate.json` падает с `KeyError: 'm'` (там ключ `pair`, а не `m`/`n`).
   - **(в) самотест символа Гильберта имеет запредельную сложность и подвешивает скрипт.**
     На сертификате `ctp_11_4/certificate_11_4.json` скрипт не уложился ни в 300 с, ни в
     9 минут, тогда как остальные четыре верификатора отрабатывают за 0.19–2 с.
     Причина установлена мной по коду (строки 181–199): сверка символа с разрешимостью
     `a x² + b y² = z²` перебирает `for x in range(M): for y in range(M)` при
     `M = p^(2·(2 если p=2 иначе 1)+3)`, то есть **M = 16807 для p = 7** — это 2.8·10⁸
     итераций на одну пару (a,b), причём полный перебор происходит именно в случае
     **неразрешимости** (когда ответ −1 и ранний выход невозможен), и всё это внутри
     цикла по 14×14 парам и 5 местам.
     **Замечание об осторожности:** я сначала предположил причиной наивную факторизацию
     (в коде есть `factor_int` пробным делением с комментарием «Числа сертификата малы»),
     но **проверил и отбросил эту гипотезу**: наибольшие простые делители I, J, Δ и a равны
     181, 137, 137 и 2531 — все числа гладкие, факторизация тут ни при чём. Записываю
     и ошибочную гипотезу, и её опровержение, чтобы диагноз не выглядел подтверждённым
     сильнее, чем он есть [проверено численно].
2. **`bridge/ctp_11_4_фишер/discover_11_4.sage` падает на последнем шаге**:
   `TypeError: Object of type Integer is not JSON serializable` при сериализации ключа `'m'`.
   Спаривание к этому моменту уже посчитано (лог: `i = 3 <g1,g2> = 1`, `произведение = −1`,
   минусы `['real']`), но **свой `certificate_11_4.json` этот прогон не записал**.
   Сертификат в `ctp_11_4/` получен отдельным `export_certificate_11_4.py`. Скрипт открытия
   в этом каталоге надо считать **незавершённым**.
3. **Контроль на кривых 1088e2 / 582d2 / 1025b2** (полное 2-кручение, ранг 0, Ш = 4 по
   Cremona/BSD) **НЕ ДАЛ ДАННЫХ**: `pari ell2cover` вернул по 4 квартики, попарные
   произведения их классов среди этих четырёх отсутствуют, поэтому **ни одна пара не была
   вычислима**. Это **отсутствие расчёта, а не отрицательный результат**. Роль контроля
   закрыта примером Фишера 3.4, но сам контроль остаётся недоделанным.
4. **Три строки контроля на 571a1** (случай g1 = s0 + s1) не досчитались: грубый перебор
   не нашёл вещественную точку на квартике с коэффициентами ~10²¹. Это **ограничение
   поиска, а не математический результат**. Нужные для контроля строки ⟨s0,s1⟩ и ⟨s1,s0⟩
   посчитаны.
5. **Round-trip через настоящие точки M(Q)** у (19,5) не дал данных: точек M(Q) с целым x
   в окрестности ±3000 от корней нет (генератор большой высоты). **Отсутствие находки —
   не доказательство отсутствия.** Роль контроля закрыта явными тождествами накрытия (§2.7).

### 3.3. Непроверенные места — по существу нет, но с оговоркой

Набор мест закрыт тремя способами (§2.3), включая честный перебор всех 303 простых до 2000
для (19,5). **Для (11,4) такого сплошного перебора не делалось** — там полнота обоснована
только Замечанием 3.3 через собственную факторизацию плюс согласием трёх независимых
наборов мест. Это слабее, чем у (19,5). [наблюдение; дешёвая доделка]

### 3.4. Оставшиеся зависимости вывода (не опровержения, а границы утверждения)

Вывод по-прежнему опирается на:

1. **Теорема 3.1 Фишера** — рецензированный опубликованный результат
   (Res. Number Theory 8 (2022), Art. 74). Сама теорема здесь не доказывается.
   Реализации откалиброваны на собственном примере автора (§2.6). [внешняя зависимость]
2. **Стандартный факт** `im(E(Q)/2E(Q)) ⊆ ker CTP` на Sel². Спаривание на Sel² по
   определению есть прообраз спаривания Касселса–Тейта на Ш[2] вдоль
   `0 → E(Q)/2E(Q) → Sel² → Ш[2] → 0`; образ E(Q)/2E(Q) — ядро `Sel² → Ш[2]`, значит лежит
   в радикале. Дополнительных условий не требуется. [доказано, стандарт]
3. **Текстовые (не скриптовые) аргументы** пропуска мест: Замечание 3.3 для спаривания,
   Хассе + Гензель для ELS вне supp(Δ). Это математические утверждения в тексте, а не
   вычисления в верификаторе. [доказано, но не машинно]
4. **Этап ОТКРЫТИЯ** опирается на Sage 10.9 / PARI (`ell2cover` как источник кандидата g2,
   `solve_conic` может звать `is_norm(proof=False)` — условно по GRH). **В сертификат это
   не входит**: ELS кандидатов перепроверена явными свидетелями, инварианты пересчитаны,
   и у (19,5) тот же −1 получен с g2, построенной **целиком без `ell2cover`**
   (собственная коническая конструкция, минусы {3,7,239}). Происхождение квартики после
   этого безразлично. [проверено численно]

### 3.5. Что НЕ сделано и не заявляется

- **Прежние 123 исключения не переаудированы** этим запуском.
- **(13,8), (15,8), (16,5) не решены** — см. §4.
- **(15,1) этим запуском не трогали** — см. §4, у неё другой статус.
- **Новизна относительно литературы не проверялась.**
- Это **не решение задачи о магическом квадрате** и не исключение всего семейства G1.
- Утверждения касаются кривой **C** (три отдельные квадратности), а не H: Y² = F0F4F8.
  `H(Q)` здесь не определяется и пустой не объявляется.

---

## 4. Обновлённый статус всех исключений G1

Фиксированный список — **127 пар**. Уточняю его определение, потому что в отчётах оно
нигде не выписано явно: это ровно **взаимно простые (m,n) с 1 ≤ n < m ≤ 20**;
`Σ_{m=2}^{20} φ(m) = 127` [проверено численно — пересчитал, и все семь обсуждаемых пар
(11,4), (19,5), (19,16), (15,1), (13,8), (15,8), (16,5) в списке лежат]. Заявлено 124.

### 4.1. Четыре «свежих» исключения — детально

| Пара | δ | Метод | Зависит от границы ранга | Статус |
|---|---|---|---|---|
| **(19,5)** | (1,193,193) | **CTP Фишера, ⟨δ,g2⟩ = −1** | **НЕТ** | **доказано (ПО), безусловно** |
| **(11,4)** | (1,274,274) | **CTP Фишера, ⟨δ,g2⟩ = −1** | **НЕТ** | **доказано (ПО), безусловно** (протокол проверки слабее, §3.1) |
| **(19,16)** | (1,1234,1234) | CTP Фишера, ⟨δ,g2⟩ = −1 | НЕТ | доказано (ПО), безусловно |
| **(15,1)** | (1,113,113) | **перечисление 8 классов при rank ≤ 1** | **ДА** | доказано (ПО), **условно по границе ранга** |

**(15,1) — единственная из четырёх, чьё исключение всё ещё опирается на границу ранга.**
Но её граница существенно прочнее, чем была у (11,4)/(19,5) [проверено численно, логи
`bridge/check_kummer_15_1_ранг_round3.log`, `check_kummer_15_1_ранг_КЛОД_R5.log`]:

- `eclib mwrank`: rank = 1, rank_bound = 1, **CERTAIN = True**, selmer_rank = 5,
  генератор `([114973, 2293200, 1])` — это **независимая от PARI** реализация 2-спуска,
  и она сама сертифицирует границу;
- `PARI ellrank` → [1, 1, 2] (второй, независимый источник);
- `analytic_rank_upper_bound` (Bober) = 1, корневое число −1 — **третья** линия
  (Гросс–Загир + Колывагин), но у неё оговорка GRH в форме `analytic_rank_upper_bound`.

Осторожная оговорка: в том же логе первый спуск даёт `rank_bound = 3, certain = False`;
границу 1 сертифицирует именно второй спуск eclib. Это нормально, но существенно, что
`certain=True` у (15,1) есть, а у (11,4) и (19,5) его **не было** — там eclib давал
rank_bound = 3, certain = False на всех 4 изогенных кривых.

**Рекомендация: у (15,1) CTP-сертификат построить нельзя** — для неё вопрос стоит иначе:
класс δ там отсутствует в полном образе E(Q)/2E(Q) при rank = 1, и исключение получено
перечислением. Если хочется снять зависимость от границы ранга полностью, единственный
доступный путь — довести до конца аналитическую линию (L(E,1) = 0 точно + L′(E,1) ≠ 0 со
строгой оценкой хвоста, как это уже сделано для (11,4) и (19,5)). Это дешёвая доделка.
[статус рекомендации: наблюдение]

### 4.2. Сводка по всему списку

| Категория | Сколько | Статус |
|---|---|---|
| Прежние («legacy») исключения | 120 | **не переаудированы** этим запуском; статус унаследован из предыдущего проекта [гипотеза о корректности — аудита не было] |
| (11,4), (19,5), (19,16) | 3 | **доказано (ПО), безусловно относительно ранга** (CTP-сертификаты) |
| (15,1) | 1 | доказано (ПО), **условно по границе rank ≤ 1** (eclib certain=True + PARI + аналитика) |
| **Итого заявлено** | **124** | из них строго проверено в этом запуске — 2 |
| Остаются открытыми | 3 | **(13,8), (15,8), (16,5)** |

### 4.3. Почему (13,8), (15,8), (16,5) не закрываются этим методом

[доказано — структурный аргумент, воспроизведён из `RESULT_G1_19_16_CTP_EXCLUDED`]

У этих трёх пар целевой класс δ = (1,s,s) **присутствует в полном образе E(Q)/2E(Q)**
(rank 3, 2, 2 соответственно; проверено 32/32, 16/16, 16/16 классов). Ненулевое спаривание
Касселса–Тейта для него **невозможно в принципе**: класс уже представлен рациональной
точкой, а образ лежит в ядре спаривания. Перебирать новые классы Касселса в надежде
повторить эти четыре исключения **бессмысленно**.

Следующий барьер для них — дополнительная квадратность `X/b = t²` внутри соответствующего
класса. Конкретная задача: выписать явную модель целевого покрытия рода 1 **с рациональной
точкой**, рациональные формулы отображения на E и условие `X/b ∈ Q²`; сверить покрытие с C,
затем применять решето Морделла–Вейля / подъём именно к нему. Известное t = 1 для (15,8) —
контрольная вырожденная точка, **не решение** исходной задачи.

---

## 5. Границы утверждения (обязательный раздел)

1. Всё сказанное относится к **фиксированному списку 127 пар взаимно простых (m,n) с
   m, n ≤ 20**. Это **не** решение задачи о магическом квадрате 3×3 из девяти различных
   квадратов и **не** исключение семейства G1 при произвольных (m,n).
2. Исключается кривая **C: u0² = F0, u4² = F4, u8² = F8** (три отдельные квадратности),
   а не H: Y² = F0F4F8. Точка на H необязана удовлетворять условию δ. `H(Q)` пустым
   не объявляется.
3. C_(11,4) и C_(19,5) **всюду локально разрешимы** [проверено численно — явные t во всех
   местах, для (19,5) при p ≤ 103 и p = 193, вещественное t = 1]. Препятствие **глобальное**;
   локальными методами эти пары исключить невозможно, класс δ задаёт **нетривиальный
   элемент Ш(E/Q)[2]**. Предположение о конечности всей Ш при этом **не используется**.
4. Отсутствие находки (точки на M(Q), пары на контрольных кривых Кремоны) нигде не
   использовано как доказательство отсутствия.
5. Новизна относительно литературы **не проверялась**; приоритет не заявляется.

---

## 6. Файлы и воспроизведение

Все пути абсолютные.

**Верификаторы (чистый Python 3: `fractions`/`math`/`json`; ни Sage, ни PARI, ни кода проекта):**

    /home/kep/magicKube/bridge/ctp_cert_19_5_фишер.py            52 проверки,  exit 0
    /home/kep/magicKube/bridge/ctp_cert_19_5_ссzero.py          482 проверки,  exit 0
    /home/kep/magicKube/bridge/ctp_cert_11_4_фишер.py           254 проверки,  exit 0
    /home/kep/magicKube/bridge/ctp_cert_11_4_ссzero.py                          exit 0
    /home/kep/magicKube/bridge/ctp_11_4_indep/ctp_cert_11_4_indep.py  2796 проверок, exit 0

    НЕ РАБОТАЕТ по умолчанию (см. §3.2.1):
    /home/kep/magicKube/bridge/ctp_11_4_v2/verify_ctp_11_4.py

**sha256 верификаторов (на момент запуска):**

    bd201d6a6f0c9b7dc67aa0007f06f695469f437b3e7e82d27f7bb15fe4bf735c  ctp_cert_11_4_фишер.py
    a6ab25fef8733ced43f0d526692de1ed1bb0ee316fdeb5e22760af7a970145c2  ctp_cert_11_4_ссzero.py
    c4cd1451ee29bcc4cc9926bca9e3f92c8985a486b2254cbb432f819692cf0cbb  ctp_cert_11_4_indep.py
    32f17a7adb6b658d92fceb4c6cdbf7eb29c18bcc1b8ddd189c0d2a4db9d16ddb  ctp_cert_19_5_фишер.py
    2009b22cce2638eebfefb8846d782b1b4bbca14d906cb788d9bc00255a603a2c  ctp_cert_19_5_ссzero.py

**Сертификаты (JSON):**

    /home/kep/magicKube/bridge/ctp_cert_19_5.json
    /home/kep/magicKube/bridge/ctp_19_5/certificate_19_5_claude.json
    /home/kep/magicKube/bridge/ctp_11_4/certificate_11_4.json
    /home/kep/magicKube/bridge/ctp_cert_11_4/certificate.json
    /home/kep/magicKube/bridge/ctp_11_4_indep/candidates.json

**Результаты проверки:**

    /home/kep/magicKube/bridge/ctp_19_5/independent_result_19_5_claude.json
    /home/kep/magicKube/bridge/ctp_11_4/independent_result_11_4.json
    /home/kep/magicKube/bridge/ctp_cert_11_4/independent_result_11_4.json
    /home/kep/magicKube/bridge/ctp_11_4_indep/independent_result_11_4_indep.json

**Реализация формулы и контроли применимости:**

    /home/kep/magicKube/descent/ctp_quartic.sage
    /home/kep/magicKube/bridge/ctrl_quartic_Q_nonzero_claude.sage + .log   (новый, ненулевой CTP)
    /home/kep/magicKube/bridge/ctrl_quartic_Q_claude.log                   (прогон старого)

**Логи границ ранга (для §0 и §4):**

    /home/kep/magicKube/bridge/check_kummer_11_4_ранг_КЛОД.log        (разделы C1–C3, J)
    /home/kep/magicKube/bridge/check_kummer_19_5_модель.log           (разделы 6a–6d, 11)
    /home/kep/magicKube/bridge/check_kummer_15_1_ранг_round3.log      (3A–3C)
    /home/kep/magicKube/bridge/check_kummer_15_1_ранг_КЛОД_R5.log     (3A–3B)

Запуск любого верификатора: `python3 <путь>`. Sage не требуется.

---

## 7. Что дальше

1. **Прогнать по (11,4) такой же состязательный раунд опровержения, как по (19,5)** —
   единственная содержательная асимметрия (§3.1).
2. **Сплошной перебор мест до 2000 для (11,4)** — дешёвая доделка (§3.3).
3. **Починить `ctp_11_4_v2/verify_ctp_11_4.py`** и досчитать `discover_11_4.sage`
   (одна строка сериализации) — получится четвёртая независимая реализация (§3.2).
4. **Снять зависимость (15,1) от границы ранга** через аналитическую линию со строгой
   оценкой хвоста L′ — тем же способом, что уже сделан для (11,4) и (19,5) (§4.1).
5. **(13,8), (15,8), (16,5)**: CTP тут бесполезен по структурной причине (§4.3); нужна
   явная модель покрытия с рациональной точкой и условие X/b ∈ Q².
6. **Аудит 120 legacy-исключений** — до него статус «заявлено 124» опирается на
   непроверенное наследство.
