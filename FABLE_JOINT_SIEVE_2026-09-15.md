СТАТУС: результат (посчитал сам) — совместное решето Морделла–Вейля по нескольким эллиптическим множителям кривой наклона при общем z; закрытий наклонов НЕ заявляю
ДЛЯ: пользователь, Claude, Codex; Grok — §0 и §7
ИТОГ: построено и проверено на контролях совместное решето (общий z + согласованные знаки корней восьми клеток); на 204/247, 333/442, 126/451, 329/449 по 16 множителям ранга 1 при N до 480 и ℓ ≤ 1000 выжившие классы = ровно классы вырожденной точки z = 0 (256/256/128/256), невырожденных классов 0; на переписи s ≤ 40 (488 наклонов, все закрыты рангом 0) то же у 478, у 10 остаются «призраки» — классы, отличающиеся от вырожденных на точку, локально делимую на N при всех ℓ, но не глобально (локально-глобальная неделимость); равномерная структура: у всех множителей вне 22 генерических подмножеств точка z = 0 (квадрат из единиц) — генерическое сечение бесконечного порядка и на множителях ранга 1 — образующая (3119 из 3122 случаев в переписи), таблицы решета зависят только от k mod ℓ (лемма доказана и проверена различающим тестом); доказано, что никакое конечное решето не даёт утверждения для всех k сразу (CRT); что осталось — p-адический шаг, для которого на этих наклонах нет ни Шаботи (ранг = размерность), ни готового QC
ОТМЕНЯЕТ: ничего из чужого; из своего — ожидание, что совместность сама по себе закроет 204/247 и 333/442: она сводит задачу к вырожденным классам, но не закрывает
ПРОВЕРЕНО: мной: два контроля (положительный — квадрат Бремнера–Саллоуса на наклоне 247/825 по шести клеткам: класс известной точки не теряется, выжившие = известные ∪ z=0; отрицательный — 22/31 по множителям ранга ≥ 1: только вырожденные классы), перепись s ≤ 40 как массовый отрицательный контроль; положительный контроль поймал две настоящие ошибки (порядок образующих групп E(𝔽_ℓ) и кручения в Sage); ранги — PARI `ellrank` (только множители с нижней = верхней), насыщение — Sage `saturation` (полное), кручение — Sage; внешней рецензии нет
ЧИТАЛ: PROTOCOL_NOTES; FABLE_BOMBIERI_LANG_V (своя); REVIEW_SHARED_CELLS_AND_THEOREM (Codex); RESULT_JOINT_149_QC_FILTER (Codex); шапка и §1.2 FABLE_SYMMETRY (квадрат Саллоуса); код census_genus1/g1census.py, census_300/c300.py; fable_bl/hard_slopes_ranks.json. Bruin–Stoll «The Mordell–Weil sieve: proving non-existence of rational points on curves», LMS J. Comput. Math. 13 (2010) 272–306 — прочитал в источнике аннотацию (arXiv 0906.1934); схема решета — по памяти и по ней. Dvornicich–Zannier (локально-глобальная делимость точек) — по памяти
ЧИТАТЬ ДАЛЬШЕ, ЕСЛИ: нужна точная формулировка того, что решето доказывает (§1, §4), устройство и контроли (§2–3), природа призраков (§5), равномерная структура и её пределы (§6), что осталось (§7)

# Совместное решето Морделла–Вейля при общем z

15 сентября 2026, Fable. Метки: **доказано** / **доказано (ПО)** (зависит от корректности PARI/Sage) / **вычисление** / **наблюдение** / **гипотеза** / **по памяти**.

Обозначения. Наклон k = r/s, клетки 1 + λz, λ ∈ Λ = {±s, ±r, ±(s−r), ±(s+r)}; X_k — кривая наклона (все восемь клеток — квадраты).
Для T ⊂ Λ, |T| ∈ {3, 4}: C_T: y² = ∏_{λ∈T}(1+λz), род 1, точка (0, 1); E_T — модель `weier` из census_genus1 (начало O над z = −1/c₀, c₀ = min T; z: E_T → P¹ степени 2, z = −1/c₀ + L/X). P_T(z) — точка E_T над z с y = ∏_{λ∈T} y_λ, y_λ = √(1+λz) ≥ 0; P_T(0) — точка «квадрата из единиц».

## 0. Итог в десяти строках

1. **Решето (доказано, §1).** Рациональная точка X_k даёт для каждого T точку P_T(z) ∈ E_T(ℚ), и при каждом простом ℓ хорошей редукции набор (P_T(z) mod ℓ)_T лежит в образе X_k(𝔽_ℓ): один z̄ ∈ P¹(𝔽_ℓ) со всеми клетками-квадратами (или 0), и один вектор знаков ε ∈ {±1}⁸ для всех T (y_T = ∏ y_λ). Над z̄ = ∞ (ℓ | знаменатель z) редукция возможна только при ℓ ≡ 1 (mod 4) и всех λ в одном квадратичном классе mod ℓ; в частности **знаменатель z делится лишь на 2 и простые ≡ 1 (mod 4)** — доказано.
2. **Реализация (§2).** `joint_sieve/jsieve.py`: классы в ∏_T E_T(ℚ)/N_T E_T(ℚ) (свои N_T у каждого множителя; подъём N по одному множителю), таблицы допустимых образов при ℓ строятся из X_k(𝔽_ℓ) со знаками, кандидаты фильтруются numpy. Кандидатов никогда не больше 1024; время — дискретные логарифмы в E_T(𝔽_ℓ).
3. **Контроли (§3).** Положительный: квадрат Бремнера–Саллоуса (247/825, z = 168/425², шесть клеток) — класс не теряется, выжившие = 64 класса известной точки ∪ 64 класса z=0, лишних 0. Отрицательный: 22/31 по множителям ранга ≥ 1 — только вырожденные классы. Перепись s ≤ 40: 478 из 488 — только вырожденные.
4. **204/247, 333/442, 126/451, 329/449 (§4):** 16 множителей ранга 1 (доказанного), N_T ∈ {240, 480}, 154–157 простых ℓ ≤ 1000: **выжившие = ровно классы 256 точек над z = 0**, невырожденных 0. — доказано (ПО). Форма: любая невырожденная точка z даёт на каждом из 16 множителей P_T(z) ≡ ε_T P_T(0) (mod N_T E_T(ℚ)) с согласованными знаками; для множителей, где P_T(0) — образующая, это **P_T(z) = (ε_T + N_T m_T)·P_T(0)** — нечётное кратное ≡ ±1 (mod 240) точки «квадрата из единиц».
5. **Это не закрытие.** Решето не различает точку и её класс; класс вырожденной точки исключить решетом нельзя ни при каком N (§7). Закрытие требует p-адического шага.
6. **Призраки (§5).** У 10 наклонов переписи (4/9, 4/13, 2/15, 1/16, 1/17, 13/17, 15/17, 8/25, 2/33, 25/36; все закрыты рангом 0) остаётся вторая знаковая орбита. Разобрано на 1/17: на одном множителе с E(ℚ) ≅ ℤ ⊕ ℤ/4 ⊕ ℤ/2 класс −P₀ + 24G при N = 48; 24G ∈ 48E(𝔽_ℓ) при всех 300 хороших ℓ ≤ 2000, но в E(ℚ) 24G делится на 8 и не на 16. Решето такие классы не убивает никогда; это не точки. — вычисление
7. **Равномерная структура (§6):** P_T(0) — сечение над ℚ(k) каждого семейства; кручение ровно на 22 генерических подмножествах (X₄₄₂₂), бесконечного порядка на 104 остальных; на множителях ранга 1 из этих 104 оно **образующая** (перепись: 3119 из 3122; индекс 2 — три случая). Лемма: в базисе (P_T(0), t_λ) таблицы решета при ℓ зависят только от k mod ℓ — доказано и проверено (2239/2874, 2662/2897 ≡ 204/247 mod 29·31·37: таблицы совпали при каждом ℓ; четыре несравнимых контроля — различаются).
8. **Предел равномерности (доказано):** для любого конечного набора простых и любого невырожденного класса c существуют классы вычетов k mod ∏ℓ, где c не убит; значит утверждение «для всех k» из конечного решета не выводится — только для k в конкретной арифметической прогрессии mod ∏ℓ (с условиями ранга и индекса).
9. **Переформулировка через сечение (§6.3):** невырожденная точка X_k при условиях ранга ⇔ равенство z(n_T·P_T(0)) при всех T для нечётных n_T ≡ ±1 (mod N) — общие значения рациональных функций от k, «маловероятные пересечения» сечений разных эллиптических поверхностей (по памяти: круг идей Массера–Цанье); не доведено.
10. **Что осталось (§7):** p-адический шаг. Шаботи на ∏E_T невозможен (ранг = размерность на множителях ранга 1). Квадратичный Шаботи формально применим к кривой рода 4 Y_T = C_T ×_{P¹} C_{−T} (Jac ~ E_T × E_T × E⁺ × E⁻, ρ ≥ 5, ранг 6 < 8 на 204/247) — не реализован и в этой сессии нереализуем.

## 1. Постройка: что именно доказывает решето

**Лемма 1 (согласованность знаков) — доказано.** Пусть z ∈ ℚ, все 1+λz ≠ 0 — квадраты, y_λ ≥ 0 корни. Для ℓ ∤ 2·∏λ·disc, v_ℓ(z) ≥ 0, положим z̄ = z mod ℓ, ȳ_λ = y_λ mod ℓ. Тогда для каждого T точка P_T(z) mod ℓ — это точка E_T(𝔽_ℓ) над z̄ с координатой Ȳ = L c₀ w̄² ∏_{λ∈T} ȳ_λ (w̄ = 1/(z̄ + 1/c₀)), а точка −P_T(z) отвечает −ȳ_T. Значит набор (P_T(z) mod ℓ)_T = (ε_T Q_T(z̄))_T, где Q_T(z̄) — точка с фиксированным выбором корней, ε_T = ∏_{λ∈T} ε_λ, ε ∈ {±1}⁸ — один для всех T. Если какая-то клетка ≡ 0 mod ℓ, то ȳ_λ = 0, соответствующие точки — 2-кручение (знак не важен). Если z̄ = −1/c₀ — точка O. *Доказательство:* Y = L c₀ w² y_T тождественно по построению модели (`weier`: (y c₀ w²)² = g(w), h(X) = L² g(X/L)), редукция — гомоморфизм. ∎

**Лемма 2 (над ∞) — доказано.** Пусть v_ℓ(z) = t < 0, ℓ нечётное, ℓ ∤ λ ∀λ ∈ Λ. Тогда 1+λz = λz(1 + 1/(λz)), v = t чётно, и ȳ'_λ := (y_λ/ℓ^{t/2}) mod ℓ удовлетворяет ȳ'_λ² = λ z₀, z₀ = (z/ℓ^t) mod ℓ ≠ 0. Отсюда: (а) λz₀ — квадрат при всех λ ∈ Λ, в частности −1 = (−λ z₀)/(λ z₀) — квадрат, т. е. **ℓ ≡ 1 (mod 4)**, и все λ в одном квадратичном классе; (б) точка P_T(z) mod ℓ имеет X̄ = 0, для |T| = 3 это (0,0), для |T| = 4 — Ȳ = L c₀ ∏_{λ∈T} ȳ'_λ / z₀² (зависит лишь от класса z₀), знаки согласованы как в лемме 1. *Следствие (элементарно, без кривых):* если z = a/b — невырожденное решение, то все нечётные простые делители b сравнимы с 1 по модулю 4. ∎

**Решето.** Пусть T₁,…,T_m — множители с E_i(ℚ) = ⊕_j ℤG_ij ⊕ Tors_i (ранг доказан, образующие насыщены хотя бы при простых, делящих N_i; кручение точное), N_i кратны экспоненте Tors_i. Класс точки — (n_ij mod N_i; координаты кручения). При простом ℓ (хорошем для всех E_i и ℓ ∤ 2∏λ) множество **допустимых наборов классов** A_ℓ ⊂ ∏_i E_i(ℚ)/N_i — прообраз множества {(ε_{T_i} Q_{T_i}(z̄))_i : z̄ ∈ Z_ℓ, ε ∈ {±1}⁸} ∪ (над ∞ по лемме 2) при гомоморфизме ∏ E_i(ℚ)/N_i → ∏ E_i(𝔽_ℓ)/N_i. Здесь Z_ℓ = {z̄ ∈ 𝔽_ℓ : все 1+λz̄ — квадраты или 0}. **Теорема (доказано):** класс любой точки z ∈ X_k(ℚ) лежит в ⋂_ℓ A_ℓ. Это стандартная схема Bruin–Stoll с двумя добавками: (i) общий z̄ и общий знаковый вектор для всех множителей одновременно (в этом и состоит «совместность» Codex: восемь условий при одном z), (ii) лемма 2 над ∞.

Почему насыщения при простых, делящих N, достаточно (доказано): если H ⊂ E(ℚ) полного ранга с индексом d, (d, N) = 1, то H/NH → E(ℚ)/NE(ℚ) — изоморфизм. Я использую полное насыщение Sage (`saturation` с границей индекса по высотам), что сильнее.

## 2. Реализация (`joint_sieve/`)

`jsieve.py` — `Factor` (модель, `ellrank`, насыщение, кручение, разложение точки по образующим через матрицу высот, локальные данные при ℓ: инварианты E(𝔽_ℓ), логарифмы образующих и слоёв над Z_ℓ), `local_zset` (Z_ℓ, корни, данные над ∞), `JointSieve` (таблицы A_ℓ как множества кортежей образов; фильтрация кандидатов numpy; добавление множителей по одному; подъём N_i → p·N_i по одному множителю; классы вырожденных точек со знаками `sign_classes`). `run_slope.py r s [nf] [lmax] [maxrank] [--norank0] [--n24|--big]` — все 126 подмножеств, выбор nf множителей доказанного ранга (сначала ранг 1), N₀ = экспонента кручения, подъёмы ×2, ×3 (×2, ×2, ×5 при `--big`), сверка выживших с классами вырожденных точек. `census_sieve.py` — перепись. `test_pos.py`, `uniform_check.py`, `uniform_tables.py`, `ghost.py`, `ghost_local.py`, `diag.py` — контроли и разборы. Логи `log*_r_s.txt`, результаты `res_r_s.json`, `census_3_40.jsonl`.

Что откуда: ранг — PARI `ellrank` (2-спуск + поиск точек; беру множитель, только если нижняя = верхняя = число найденных точек; на 329/449 13 множителей с нижней границей 1 по чётности без найденной точки исключены); насыщение — Sage `E.saturation` (в переписи у 22 множителей индекс 3, у целевых наклонов 8–9 множителей с индексом > 1 — без насыщения решето было бы **неверным**); кручение — Sage; дискретные логарифмы — `abelian_group().discrete_log` (координаты в порядке `gens()`, порядки — у самих образующих; на этом была ошибка, пойманная контролем).

Стоимость: кандидатов ≤ 1024 на всех прогонах (добавление множителя ранга 1 умножает выживших на ≤ N·|Tors| = 32–64, подъём — на p^{r}); время целиком — построение таблиц (~1 с на пару (множитель, ℓ)). 204/247 при 16 множителях, ℓ ≤ 1000, N до 480 — 412 с.

Ограничения кода: только |T| ∈ {3, 4}; исключаются ℓ | disc·L·c₀ любого множителя и ℓ | λ; сравнение таблиц между наклонами — только в равномерном базисе (`uniform_check.py`).

## 3. Контроли

**(а) Положительный — доказано (ПО).** Квадрат Бремнера–Саллоуса 373², 289², 565² / 360721, 425², 23² / 205², 527², 222121: центр 425², p = 138600, q = 41496, наклон q/p = 247/825, z = 168/425²; квадраты — клетки {+s, −s, −r, −(s+r), +(s−r), −(s−r)} (s = 825, r = 247). Решето по восьми множителям из этих шести клеток (ранги 1–2, N = 12…24, ℓ ≤ 400): класс известной точки и все его 2⁶ знаковых варианта на месте на каждом шаге; **выжившие = 64 класса известной точки ∪ 64 класса z=0, лишних 0** (13–23 с). Первый запуск этого контроля выбросил известную точку — ошибка порядка образующих E(𝔽_ℓ) (`invariants()` ≠ порядок `gens()`); исправлено, потом та же ошибка для кручения над ℚ (22 генерических подмножества с ℤ/2×ℤ/4 не строились).

**(б) Отрицательный — доказано (ПО).** 22/31 (закрыт рангом 0), решето только по множителям ранга ≥ 1: 8 множителей, N = 12–24, 87 простых ≤ 500 — выжившие 128 = 128 вырожденных классов, 25 с.

**(в) Массовый отрицательный — вычисление.** Все 488 наклонов с s ≤ 40 (у всех есть множитель доказанного ранга 0, т. е. все закрыты): 8 множителей рангов 1–2 (без ранга 0), N = 12, ℓ ≤ 500; среднее 23 с. **478 — только вырожденные классы; 10 — призраки (§5); ни разу не потерян класс вырожденной точки.**

## 4. Результаты по четырём наклонам — доказано (ПО)

| наклон | множителей (из 126) с доказ. рангом; ранги | взяты | N_T | ℓ ≤ 1000 | выжившие / вырожд. классы | невырожд. | время |
|---|---|---|---|---|---|---|---|
| 204/247 | 126; 36×1, 50×2, 32×3, 8×4 | 16 ранга 1 | 240, 480 | 154 | 256 / 256 | **0** | 412 с |
| 333/442 | 122 (4 недоказ.); 38×1, 52×2, 30×3, 2×4, 2×5 | 16 ранга 1 | 240, 480 | 157 | 256 / 256 | **0** | 292 с |
| 126/451 | 126; 36×1, 68×2, 18×3, 4×4 | 16 ранга 1 | 240, 480 | 155 | 128 / 128 | **0** | 238 с |
| 329/449 | 113 (13 недоказ.); 58×1, 49×2, 8×3 | 16 ранга 1 | 240, 480 | 156 | 256 / 256 | **0** | 282 с |

(Первые прогоны: 8 множителей, N = 12, ℓ ≤ 500 — тот же итог за 20–25 с; N = 24, 16 множителей, ℓ ≤ 1000 — тот же; стабилизация на каждом шаге наступала после ≤ 68 простых.) Множители — в `res_r_s.json` (`factors`, с образующими). Рациональных точек X_k над z = −1/λ нет (проверено), над ∞ их нет ни на каком наклоне (поле вычетов содержит √−1 — доказано). Ни у одного из четырёх наклонов нет множителя доказанного ранга 0 среди 126 (сверка с FABLE_BL, там — 22 + 8).

**Что доказано (ПО) для каждого из четырёх наклонов:** если z — невырожденное решение, то на каждом из 16 множителей P_T(z) ≡ ε_T P_T(0) (mod N_T E_T(ℚ)), N_T ∈ {240, 480}, ε_T = ∏_{λ∈T} ε_λ при общем ε ∈ {±1}⁸. Для множителей с E_T(ℚ) = ℤP_T(0) ⊕ Tors (на первых восьми у каждого наклона — все): **P_T(z) = (ε_T + N_T m_T)·P_T(0)**, m_T ∈ ℤ. Закрытие — нет (§7).

## 5. Призраки: что решето не различает — вычисление

На 1/17 (16 множителей, N до 48, ℓ ≤ 1000) остаются 256 невырожденных классов — одна знаковая орбита. На всех множителях ранга 0 они сидят в z = 0; отличие от вырожденного набора — ровно один множитель T = {−18, −17, −1}, E(ℚ) ≅ ℤG ⊕ ℤ/4 ⊕ ℤ/2, P_T(0) = t₁ + t₂ (кручение): класс призрака −P_T(0) + 24G при N = 48. Он допустим при ℓ через слой над z̄ = 0 ⇔ 24Ḡ ∈ 48E(𝔽_ℓ) или 24Ḡ − 2t̄₁ ∈ 48E(𝔽_ℓ). Проверено: **при всех 300 хороших ℓ ≤ 2000 верно 24Ḡ ∈ 48E(𝔽_ℓ)**; глобально 24G ∈ 8E(ℚ), 24G ∉ 16E(ℚ). То есть 24G — точка, локально делимая на 48 (по крайней мере при ℓ ≤ 2000), но не глобально: локально-глобальная неделимость (Dvornicich–Zannier — по памяти; такие примеры типичны при большом рациональном 2-кручении). Решето Морделла–Вейля таких классов не убивает ни при каком наборе ℓ, если делимость локальна всюду; для 1/17 это не мешает (закрыт рангом 0), но на открытом наклоне такой призрак был бы неотличим от «кандидата». На четырёх целевых наклонах призраков нет — там P_T(0) на взятых множителях не кручение (образующая).

Остальные 9 призрачных наклонов не разбирал; у всех число призраков = числу вырожденных классов (одна орбита), все закрыты рангом 0.

## 6. Равномерная структура

### 6.1. Сечение «квадрат из единиц» — наблюдение на переписи, доказано (ПО) для каждого наклона

P_T(0) = (0, 1) — сечение семейства C_T над ℚ(k) для любого T. На 204/247: кручение ровно на 22 генерических подмножествах (X₄₄₂₂, согласуется с FABLE_BL §1.3), бесконечного порядка на 104 остальных; **на всех 20 множителях ранга 1 из 104 — ±образующая** (то же на 333/442: 24 из 24). Перепись s ≤ 40, использованные множители ранга 1: ±образующая 3119, кручение 782 (генерические), 2·G — 3 (2/7: T = {−9,−5,7}; 1/24: {−25,−24,24}; 24/25: {−49,−25,−1}). По специализации Силвермана: бесконечный порядок в одной точке ⇒ бесконечный порядок над ℚ(k) — так что у 104 «прочих» семейств с положительным генерическим рангом (FABLE_BL) генерическая образующая известна явно: сечение z = 0. — доказано (ПО) для тех T, где проверено; для всех 104 — наблюдение (проверял на двух наклонах).

### 6.2. Лемма равномерности — доказано; тест — вычисление

**Лемма.** Пусть T — образец (тройка/четвёрка меток из {±s, ±r, ±(s−r), ±(s+r)}), и k = r/s таков, что rank E_{T,k}(ℚ) = 1, E_{T,k}(ℚ) = ℤ·P_T(0) ⊕ (ℤ/2)², а ℓ ∤ 2·∏λ·disc. В базисе (P_T(0); t_λ, t_μ) (2-торсионные точки над z = −1/λ) множество допустимых классов A_ℓ ⊂ ∏(ℤ/N ⊕ (ℤ/2)²) зависит только от k mod ℓ. *Доказательство:* при (r,s) ↦ (cr, cs) кривые X_k, C_T и точки P_T(0), t_λ канонически изоморфны (z ↦ z/c), а всё, что входит в A_ℓ — X_k(𝔽_ℓ), E_T mod ℓ, редукции P_T(0), t_λ, слои над Z_ℓ и над ∞ — определяется редукцией (r, s) mod ℓ с точностью до этого изоморфизма. ∎ Следствие: для k ≡ k′ (mod M = ∏ℓ_i) при выполнении условий леммы у обоих решето по {ℓ_i} даёт одно и то же.

**Тест** (`uniform_tables.py`): четыре образца ранга 1 на 204/247, ℓ ∈ {29, 31, 37} (первые хорошие); наклоны 2239/2874 и 2662/2897 ≡ 204/247 (mod 33263) с теми же свойствами образцов: **таблицы A_ℓ совпали при каждом ℓ** (|A| = 1024, 256, 256 из 65536); четыре несравнимых наклона (43/60, 59/60, 52/61, 61/67) — таблицы отличаются при каждом ℓ.

### 6.3. Переформулировка — доказано (при условиях ранга)

Если на образцах T₁…T_m ранг 1 и P_T(0) — образующая (индекс, взаимно простой с N), то невырожденная точка X_k(ℚ) ⇔ существуют нечётные n₁…n_m с **z(n_i·P_{T_i}(0)) = общее z** (и после решета n_i ≡ ±1 mod N). Функции z(n·P_T(0)) ∈ ℚ(k) равномерны по k; вопрос о точках всех X_k сразу — вопрос об общих значениях этих рациональных функций для разных T, т. е. о «маловероятных пересечениях» кратных сечений разных эллиптических поверхностей над одной базой (по памяти: Массер–Цанье о совместном кручении; здесь нужен вариант для совпадения координат кратных). Это точная равномерная форма «работать со всеми сразу»; ничего для неё не выведено.

### 6.4. Предел: конечное решето не даёт «для всех k» — доказано

Для невырожденного класса c и простого ℓ обозначим S_{c,ℓ} ⊂ P¹(𝔽_ℓ) множество вычетов κ, при которых c ∈ A_{ℓ,κ}. Класс c выживает у наклона k при решете по {ℓ_i} ⇔ k mod ℓ_i ∈ S_{c,ℓ_i} ∀i. Если все S_{c,ℓ_i} ≠ ∅, по КТО существуют классы вычетов k mod ∏ℓ_i, где c выживает. S_{c,ℓ} ≠ ∅ для всех ℓ: над z̄ = 0 класс (n_T, t_T) допустим, как только (n_T ∓ 1)P̄_T(0) + t̄_T ∈ N E_T(𝔽_ℓ) для всех T, а это выполняется на положительной доле вычетов κ (например, когда N-часть E_T(𝔽_ℓ) мала). Значит **утверждение для всех k из конечного решета не следует**; выводится только: «для всех k ≡ k₀ (mod ∏_{ℓ∈L} ℓ), у которых на образцах ранг 1 и индекс взаимно прост с N, точки X_k(ℚ) лежат в вырожденных классах mod N» — арифметическая прогрессия с модулем ∏_{ℓ ≤ 1000} ℓ, содержательно пустая. Равномерной является **структура** (таблицы по вычетам, сечение-образующая, переформулировка 6.3), а не заключение.

### 6.5. Локальная равномерная часть — вычисление

Z_ℓ зависит только от k mod ℓ. Для 204/247 среди 166 простых 5 ≤ ℓ < 1000 невырожденные z̄ есть у 79 (первое ℓ = 131; ожидание ℓ/256); ∞ допустима при 9 (73, 137, 257, 401, 661, 761, 809, 881, 953). Для ℓ < 131 любая рациональная точка X_{204/247} имеет z ≡ 0 или −1/λ (mod ℓ) — чисто локальный факт, без Морделла–Вейля.

## 7. Что осталось вывести

1. **p-адический шаг.** Выжившие классы — классы точки z = 0. Классический финал Bruin–Stoll — Шаботи в окрестности известной точки; здесь на ∏E_T ранг = размерность (по одному на множитель ранга 1), Шаботи невозможен. Кривая Y_T = C_T ×_{P¹} C_{−T} (род 4, Jac ~ E_T × E_{−T} × E⁺ × E⁻, E_T ≅ E_{−T} через ι ⇒ ρ ≥ 5): на 204/247 для T = {43, 204, 247}-тройки ранг 1 + 1 + 2 + 2 = 6 < g + ρ − 1 ≥ 8 — квадратичный Шаботи формально применим, но требует p-адических высот на якобиане рода 4; не сделано и в этой сессии не сделать. Это **точная формулировка недостающего** для каждого из четырёх наклонов.
2. **Все k сразу.** По 6.4 — не решетом. Кандидат — 6.3 (общие значения z(n·P_T(0)) для разных T как рациональных функций k): нужна теорема о пересечениях кратных сечений двух эллиптических поверхностей; не искал в литературе в этой сессии.
3. **Призраки** на открытых наклонах: перед тем как считать выживший класс «кандидатом», проверять локально-глобальную делимость разности (§5).

## 8. Не делать

- Не наращивать N и ℓ на 204/247, 333/442, 126/451, 329/449 — выжившие стабильны (только классы z = 0) уже при N = 12, ℓ ≤ 500; закрытия это не даст.
- Не считать призраки §5 кандидатами в точки и не искать по ним z.
- Не брать ранг из нижней границы `ellrank` по чётности без найденной точки (329/449: 13 таких множителей).
- Не сравнивать таблицы решета разных наклонов вне равномерного базиса (P_T(0), t_λ).
- Не запускать решето без насыщения образующих (индексы 3 встречаются в переписи, 8–9 множителей на целевых наклонах).

## 9. Файлы

`joint_sieve/jsieve.py`, `run_slope.py`, `census_sieve.py`, `census_analyze.py`, `test_pos.py`, `uniform_check.py`, `uniform_tables.py`, `ghost.py`, `ghost_local.py`, `diag.py`; результаты `res_204_247.json`, `res_333_442.json`, `res_126_451.json`, `res_329_449.json` (финальные, `--big`), `res_22_31.json`, `res_1_17.json`, `res_4_9.json`; логи `log_*`, `logB_*`, `logC_*`, `logG_*`; перепись `census_3_40.jsonl` (пересчёт с исключением ℓ | λ; результат посчитан дважды — первая версия `census_3_40_v1.jsonl` до этой правки — и совпал полностью: 478/488, те же 10 призраков, 0 ошибок; `census_v2.log`). Внешних действий не выполнялось.
