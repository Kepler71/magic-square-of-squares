СТАТУС: доказанная глобальная оценка для одной пары; точные полиномиальные сертификаты проверены Sage
ДЛЯ: Claude / Fable
ИТОГ: для наклона 126/451 и T1={−325,−126,126}, T2={−126,126,325} доказано |<Q1,Q2>|<28 в нормировке высоты hhat_x=lim 4^-n h(x(2^n P)). Константа не зависит от параметра и не получена из выборки.
ОТМЕНЯЕТ: необходимость использовать численную константу 14.100281900781141 для ЭТОЙ пары; не опровергает её, но заменяет сертифицированным верхним пределом 28.
ПРОВЕРЕНО: точные отображения на E, формулы сложения, шесть тождеств Безу; независимый скрипт проверки; финальное округление рациональной арифметикой.

# 1. Постановка и явные функции

Речь о первом классе наклона 126/451 в batch_four_r2.jsonl. Общая модель

    E: y²=x³−21739732x+23939545056.

Для T1={−325,−126,126} и T2={−126,126,325} вспомогательная кривая:

    w²=(1−325z)(1+325z)=1−325²z².

Параметризация всех её рациональных точек:

    z=2t/[325(1+t²)], w=(1−t²)/(1+t²), t∈P¹(Q).

Для согласованных эллиптических образов Q1,Q2 из системы клеток после перехода к общей модели получаются

    x(Q1+Q2)=u+(t)=(933807098t⁴+1100690500t²+418486250)/(5329t⁴+47450t²+105625),
    x(Q1−Q2)=u−(t)=(418486250t⁴+1100690500t²+933807098)/(105625t⁴+47450t²+5329).

u−(t)=u+(1/t). При w0=t² обе функции имеют степень 2 по w0. Формулы проверены не только вызовом setup_pair: отдельно проверены уравнения E для обоих образов и формула сложения с произведением радикалов. Возможные знаки корней переставляют u+ и u− и не меняют оценку.

# 2. Элементарная глобальная лемма о высоте

Пусть F,G — однородные целочисленные формы одной степени d без общего проективного нуля. Пусть найдены целочисленные формы Ai,Bi степени d−1 и ненулевое целое R такие, что

    A0 F+B0 G=R Z^(2d−1),
    A1 F+B1 G=R X^(2d−1).

Обозначим L=max(||F||1,||G||1), K=max(||A0||1+||B0||1,||A1||1+||B1||1).
Тогда для каждого рационального [m:n]

    d h([m:n])−log K <= h([F(m,n):G(m,n)]) <= d h([m:n])+log L.

Доказательство. Берём взаимно простые m,n, H=max(|m|,|n|), g=gcd(F(m,n),G(m,n)). Из двух тождеств и gcd(m,n)=1 следует g|R. Если максимальна координата m, используем второе тождество, иначе первое:

    |R|H^(2d−1) <= K H^(d−1) max(|F(m,n)|,|G(m,n)|).

После деления на g и использования |R|/g>=1 получаем нижнюю границу H^d/K. Верхняя H^d L следует из суммы модулей коэффициентов. Логарифмирование завершает доказательство; точки 0 и бесконечность включены. Это глобальный аргумент, без ограничений на простые делители координат.

# 3. Применение к u+ и u−

Обе пары числитель/знаменатель рассматриваются как формы степени 2 по [w0:1]. Сертификат содержит их точные коэффициенты и тождества из §2. Для обеих:

    L=2452983848,
    K=116277735437500.

Следовательно для каждого t∈P¹(Q), независимо от высоты t,

    |h(u+(t))−h(u−(t))| <= log(LK) < 54.008.

Десятичное число только иллюстрация; в доказательстве используется точное log(LK).

# 4. Каноническая высота без численного запаса

Определим hhat_x(P)=lim_n 4^-n h(x([2^n]P)), h(x(O))=0. Это нормировка, сравнимая с h(x), и используемое спаривание

    <P,Q>=(hhat_x(P+Q)−hhat_x(P−Q))/4.

Для E с инвариантами b2,b4,b6,b8 формула удвоения:

    x(2P)=(x⁴−b4 x²−2b6 x−b8)/(4x³+b2 x²+2b4 x+b6).

Применяем ту же лемму к форме степени 4. В сертификате:

    L2=472807507271737,
    K2=8072568470703647597429129000569820928,
    B=max(L2,K2)=K2.

Телескопическая сумма даёт для ВСЕХ P∈E(Q)

    |hhat_x(P)−h(x(P))| <= (log B)/3 < 28.328.

Следовательно при A=LK

    |<Q1,Q2>| <= log(A)/4+log(B)/6
              = (3log A+2log B)/12 < 28.

При нормировке канонической высоты, вдвое меньшей hhat_x, спаривание тоже вдвое меньше. Для взаимодействия с чужим кодом нормировку проверять явно. Сравнение с Sage P.height проведено как численный контроль; доказательство основано на указанном пределе, а не на численном вызове библиотеки.

Финальное строгое неравенство проверено без вычисления логарифмов: независимый скрипт устанавливает над Q

    sum_{j=0}^{298} 336^j/j! > A³B².

Так как эта сумма меньше exp(336), имеем 3log A+2log B<336, а значит оценку <28.

# 5. Что это меняет и чего пока не доказывает

Для данной пары разрешено заменить численное c≈14.1003 на строгое c=28. Полиномиальный сертификат не зависит от гипотез BSD, полноты поиска точек, ранга E или оценок по выборке.

Для закрытия всего класса наклона 126/451 требуется также строгая оценка для второго ребра хаба:

    T1={−325,−126,126}, T0={−577,−451,−325,−126},
    T1 Δ T0={−577,−451,126}.

Его вспомогательная кривая эллиптическая; данный рациональный параметр на неё не переносится. Затем требуется пересчитать H0 с новыми константами и сертифицировать высотные/решёточные границы и полноту группы. Поэтому наклон данным результатом НЕ закрыт.

# 6. Артефакты

- derive.py: вывод u±, проверка отображений и сложения, построение тождеств Безу; использует локальные Fac/setup_pair Fable.
- certificate.json: точные целые/рациональные коэффициенты всех тождеств и констант.
- verify_certificate.py: отдельная проверка тождеств, формулы удвоения и строгой границы 28; не импортирует код Fable.
- derive.log, verify.log: результаты.

Запуск: env DOT_SAGE=/tmp/codex-linear-sections-sage python3 derive.py; затем аналогично verify_certificate.py.
Исходные файлы Fable не изменены. Статусы других пар/наклонов не повышены.
