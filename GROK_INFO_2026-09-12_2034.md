# Слежка (Grok), 12.09.2026 ~20:34

Kaliman / план диагоналей — `GROK_INFO_2026-09-12_KALIMAN.md`. Новое после 20:31.

## (11,4): сертификат выгружен, независимый Python ещё не бежал

`bridge/ctp_11_4/certificate_11_4.json` (Sage-экспорт, 20:27). `export_11_4.log`: **CERTIFICATE EXPORTED**.

- assert z(g1) в классе (1,274,274).
- Три прогона Hilbert: все конечные места +1, **real = −1** ⇒ ⟨g1,g2⟩_CT = −1. Набор мест {2,3,5,7,11,13,137,2531,∞}.
- reverse ⟨g2,g1⟩=1, ⟨g1,g3⟩=1, ⟨g1,g1⟩=0.
- Контроль: четыре класса из E(ℚ) (генератор + 2-кручение) спариваются с g2 в 0. Это проверка кода, не часть вывода.
- ELS трёх квартик записан свидетелями.

Независимый проверяющий: `bridge/ctp_cert_11_4_фишер.py` (стандартный Python, без Sage/PARI; пишет `independent_result_11_4.json`). **Файла результата нет, процесс не запущен.** Пока это не тот статус, что у (19,16). Не объявлять новое исключение и не менять 124/127.

(19,5): есть `export_certificate_19_5.py`, нет `certificate_*.json`.

## Контроль реализации CTP

`bridge/ctrl_quartic_Q_claude.log`: на 25 кривых (Cremona), где квартики построились, **расхождений 0** с PARI ellrank (symm True, диаг 0). Много ERR: вырожденная коника или I(g) vs 16·I — те случаи не в счётчике. Это доверие к коду Фишера, не сертификат (11,4).

## n79b

Второй `run_quartic` жив (~13 мин). В `xq_n79b.log` снова **AlarmInterrupt** на конике после Sel²=6 (rank ≤5). ИТОГа нет. Первый процесс (~83 мин) всё ещё в `xq_n89_i23.log` без новых строк. Это тот же затор, что в `xq_n79.log`, не новая граница.

## Процессы
Наблюдатель жив. Sage в проекте не менял.
