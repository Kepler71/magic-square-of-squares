# Ранг E1(Q(sqrt(15))) равен 1: алгебраическая цепочка и расчёт Magma

11 сентября 2026 года. **Ранговое условие получено: rank E1(Q(sqrt(15)))=1.**
Это доказательство с использованием точного CAS-расчёта верхней границы;
оно не использует гипотезу чётности, BSD или предположение о конечности Ш.

## 1. Решающая модель над Q

Обозначим

```
C0: Y² = 65 t(t²−1)(109t−229)(229t−109),
C1: W² = 65 (t²−1)(t²+1)(32161t²−49922t+32161).
```

Онлайн-Magma V2.29-10 вернула

```
RankBounds(Jacobian(C1)) = 0, 1.
```

Первое выполнение завершило эту строку в групповом задании с seed
1655415028; затем задание достигло лимита уже на другой модели.
Отдельное повторное выполнение полностью завершилось:
seed **2639546859**, **3.279 s**, **110.56 MB**. Результат снова **0,1**.
Условных предупреждений не было; GRH-ускорение не запрашивалось.
Это два запуска одного метода, не два независимых алгоритма.

Короткий воспроизводимый вход:

```
Q:=Rationals(); R<t>:=PolynomialRing(Q);
f:=65*(t^2-1)*(t^2+1)*(32161*t^2-49922*t+32161);
lo,hi:=RankBounds(Jacobian(HyperellipticCurve(f)));
print lo,hi;
```

## 2. Изогения между якобианами C0 и C1 определена над Q

Факторизация f0=F1 F2 F3:

```
F1=1622465 t,
F2=t²−1,
F3=t²−(64322/24961)t+1.
```

Определитель коэффициентов (столбцы 1,t,t²) равен δ=3244930 ≠ 0.
Для циклических (i,j,k) положим Gi=(Fj'Fk−FjFk')/δ. Точно получено:

```
G1=−(32161/40498348865)t²+(2/1622465)t−32161/40498348865,
G2=(t²−1)/2,
G3=−(t²+1)/2.
```

Точные тождества:

- F1F2F3=f0;
- δ² G1G2G3=f1;
- Σ Fi(x)Gi(u)=−(x−u)².

Все Fi рациональны. Конструкция Richelot даёт (2,2)-изогению
Jac(C0)→Jac(C1) над Q; δ² — квадратное масштабирование, сохраняющее твист.
Следовательно, rank Jac(C0)(Q)=rank Jac(C1)(Q)≤1.
Формула и случай рациональных факторов описаны в §4 Bruin–Doerksen:
https://arxiv.org/html/0902.3480 .
Точная локальная проверка: local_audit/richelot_models.py, ядро 1.
Контроль числа точек над конечными полями служит только проверкой
реализации, а не заменяет теорему об изогении.

## 3. Перенос на E1 и нижняя граница

Ранее точно проверена цепочка

```
Jac(C0) ~_Q Res_{Q(sqrt(15))/Q}(E1),
E1: y²=x³−100326681x+98571779592r, r²=15.
```

В local_audit/quotient_and_isogenies.py проверены явные два эллиптических
отображения, независимость pullback-дифференциалов и изоморфизм модели
плюс-фактора с этой E1 над Q(r). Сопряжение меняет местами факторы;
индуцированная изогения в ограничение скаляров определена над Q.
Подробности: REPLY_J2_LOCAL_AUDIT.md.

Точка G=(−676r−8619,30420r+182520) на E1 имеет бесконечный порядок:
это подтверждено прежним Magma Order(G)=0 (claude_e1_first_result.txt).
Значит, rank E1(Q(r))≥1. В совокупности:

```
1 ≤ rank E1(Q(r)) = rank Jac(C0)(Q) = rank Jac(C1)(Q) ≤ 1.
```

Таким образом, **все три ранга равны 1**.
По проверенным ранее изогениям то же относится к якобиану твиста D=39
и новому эллиптическому фактору над Q(sqrt(-15)).

## 4. Границы вывода и состояние всей задачи

Ранг установлен точными алгебраическими тождествами и результатом CAS.
Для превращения этого в полностью проверяемое вручную доказательство
потребуется развернуть внутренний сертификат спуска Magma.

В цепочке Claude VERIFY_CODEX_CHAIN_s15.md это снимает условие пункта 5.
Пункты 0–3 им независимо проверены; пункт 4 он заявил доказанным при
ранге 1, включая насыщение. Сам по себе данный сертификат НЕ заменяет
аудит его реализации эллиптического Чабо и оценки индекса. В этой работе
мы их заново не проверяли. Вывод для всего сечения следует при принятии
этих проверок; окончательную сборку цепочки передаём Claude.
Общая задача магического квадрата из девяти различных квадратов
этим не решена: рассматривается конкретное сечение s=1/5.

## 5. Что произошло с серией из 14 моделей

- Новый эллиптический фактор над Q(sqrt(-15)): проверены j и бесконечный
  порядок точки; RankBounds достиг лимита 60 s, без результата.
- Ядро 1: граница [0,1] получена дважды, решающая цель достигнута.
- Ядро 2: групповое задание достигло лимита во время его расчёта.
- Остальные 11 моделей не вычислялись: для доказательства ранга 1
  дальнейший поиск более сильной верхней границы не требуется.
- Дополнительная попытка Order(P) на Jac(C1) вместе с повтором RankBounds
  не вернула нового вывода и была отменена. Причина задержки неизвестна;
  эта попытка не используется в доказательстве. Точность локально
  построенной точки проверена отдельно по b²≡f1 mod a.

Входы и журналы:
richelot1_rank_only.m; online_richelot1_rank_only_result.txt;
online_richelot_batch_1_result.txt; online_negative15_result.txt;
online_richelot1_point_check_status.txt.
