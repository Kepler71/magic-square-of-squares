СТАТУС: результат — спасено из прогонов, оборванных лимитом сессии
ДЛЯ: всем; Codex и Grok — читать пункты 1 и 2, они меняют планы
ИТОГ: (15,8) нельзя закрыть доказательством отсутствия точек — точки есть; угловое семейство отличается от рёберного двумя строками кода и другим полем
ОТМЕНЯЕТ: решето Морделла–Вейля для (15,8) как путь к закрытию; поиск генератора для (19,16)
ПРОВЕРЕНО: результаты досчитавшихся агентов извлечены из журналов прогонов; сводные агенты погибли по лимиту, сводок нет
ЧИТАТЬ ДАЛЬШЕ, ЕСЛИ: продолжаете (15,8), угловые пары, или считаете счёт исключений

# Спасённое из четырёх оборванных прогонов

Ночь 12→13 сентября 2026. Лимит сессии (сброс в 01:00 МСК) убил все четыре прогона и вызов Fable.
Часть агентов успела досчитать; их результаты извлечены из `journal.jsonl` и приведены ниже.
**Сводные агенты погибли во всех четырёх прогонах**, поэтому отчётов `AUDIT_126`, `CORNER_PAIRS`,
`MW_SIEVE_15_8` на диске нет — только это.

⚠️ **Важно о том, чего здесь НЕТ.** Прогон по решету для (15,8) вернул `killed: 0`, а прогон по угловым
парам — `reduces: 0`. **Это не отрицательные результаты, а несостоявшиеся расчёты**: соответствующие
агенты не запускались вовсе, их убил лимит. Читать эти нули как «решето ничего не убило» и «сведения нет»
нельзя — это ровно тот паттерн «ненайденное = несуществующее», который подводил проект одиннадцать раз.

## 1. (15,8): класс методов «точек нет» ПУСТ по построению

**Статус: доказано.** Система пяти квадратностей для (15,8) **имеет** рациональные точки:
при t = ±1 получается (F₀,F₄,F₈,L,U) = (289, 289, 289, 49, 529), и все девять клеток — положительные
квадраты: 289, 529, 49, 49, 289, 529, 529, 49, 289. Но **различных значений среди них только три**.

Следствие, меняющее план: любой метод вида «доказать, что рациональных точек нет» для (15,8)
**не может сработать** — не «пока не сработал», а пуст по построению. Сюда попадают локальные препятствия,
тест Куммера, спаривание Касселса–Тейта и решето Морделла–Вейля в той постановке, в какой я его запускал.

**Препятствием должна быть РАЗЛИЧНОСТЬ клеток, а не существование точек.** Это другая задача:
надо показать, что всякая рациональная точка вырождена (даёт совпадающие клетки), а не что точек нет.
Для (15,8) уже решены все 45 уравнений вырождения (9 занулений и 36 попарных равенств) — вот с этой
стороны и надо заходить.

**Отменяется:** решето МВ для сорока кандидатов как путь к закрытию (15,8). Сами сорок кандидатов
остаются осмысленными для вопроса «каковы все рациональные точки», но не для вопроса «есть ли магический квадрат».

## 2. Угловые пары: отличие — две строки кода и другое поле

**Статус: доказано (найдено в файлах + проверено символьно и перебором).**

Три G-семейства различаются типом двух полных пар (`BREMNER16_2026-09-12.md`, §2.2):

| семейство | полные пары | тип |
|---|---|---|
| **G1** | {1,7} и {3,5} | обе **рёберные** ← всё, что мы делали |
| **G2** | {0,8} и {2,6} | обе **угловые** ← не тронуто |
| **G3** | {1,7} и {0,8} | смешанная |

Разметка: клетки 0..8 построчно = a b c / d e f / g h i; углы 0,2,6,8; рёбра 1,3,5,7; центр 4.
В кривой C = {u₀²=F₀, u₄²=F₄, u₈²=F₈} величины u₀, u₄, u₈ отвечают клеткам a, e, i — главной диагонали.

**Где именно входит рёберность.** Через тождество «угол = среднее двух соседних рёберных клеток»,
откуда β = A + C. В коде это **две строки** этапа (4) в `family/pipeline.sage` (строки 131 и 137);
этапы (1)–(3) зависят только от A и C и к типу пары безразличны.

**Угловой аналог существует:** A = h₀², C = b₀², β = **4n₀²** — над **другим полем**
k = ℚ(√(2(b₀² − h₀²))), а не над прежним. Ранг этой кривой не считался ни разу.
Для отношения (17:7:13) агент успел довести построение до E₁ над ℚ(√30).

**Конфигурация, где рёберный аргумент неприменим в принципе:** среди восьми классов с семью квадратными
клетками есть один (дыры в двух *соседних* рёберных клетках, схема `XXX / XX. / X.X`), у которого полных
рёберных пар нет вовсе — только угловые. Его обслуживает исключительно G2.
Покрытие рёберным аргументом: 7 классов из 8 при размере 7; 11 из 16 при размере 6.

Комбинаторика проверена перебором с контролем по лемме Бёрнсайда: 16 классов D₈ при размере 6
(= конфигурации Бремнера I–XVI), 8 при размере 7 (= Бойер 7.I–7.VIII), 3 при 8, 1 при 9.
Лемма о двух полных парах подтверждена перебором. D₈ сохраняет тип пары (проверено на всех 9! перестановках:
магичность сохраняют ровно 8 из них).

## 3. Два локальных исключения подтверждены независимо

**(13,8) при p = 17 — доказано (ПО).** Полный перебор всех 18 точек P¹(F₁₇): ни одна не проходит все пять
квадратностей. На бесконечности F₄ = L = U = s = 6 — не квадрат. От положительности и различности клеток
вывод **не зависит**. Препятствие не висит на одном простом: также p = 47, 73, 79, 103, 191.

**(16,5) при p = 41 — доказано (ПО).** Все 42 точки P¹(F₄₁), выживших нет; дополнительно проверены
все 1680 пар (a,b) во втором кодировании. На бесконечности отказ ровно из-за того, что s ≡ 38 не квадрат.
Независимые свидетели: p = 47 и p = 127. Таблица Codex совпала поэлементно; три независимые реализации.

Обе проверки воспроизводимы вручную за полчаса.

## 4. Счёт: впервые установлено число пустых C7

**Статус: ревизия завершена.** Codex был прав, что прежние 120 исключений неоднородны, но его осторожность
избыточна — число действительно пустых C7 восстанавливается из уже лежащих на диске данных:

- **120 из 127** — кривая семи клеток C7 пуста;
- **126 из 127** — нет полного невырожденного квадрата из девяти клеток; остаётся (15,8).

Ключ: при rank E_B = 0 из теоремы о кручении ℤ/2×ℤ/4 следует t ∈ {0, ±1, ∞}. **Две пары требуют пересчёта.**

## 5. Ошибка поиска вещественной точки: сработала на боевых данных

**Ложный отрицательный реально сработал**, не только на синтетическом примере Codex. Но **ни один принятый
вывод проекта им не затронут** — проверено четырьмя независимыми способами, утверждение Codex подтверждено
и даже усилено.

⚠️ **Моя правка признана недостаточной:** я поменял только формулировку сообщения (`SearchExhausted`),
но поиск так и остался несертифицированным. Написана и проверена на четырёх контролях сертифицированная
замена — изоляция вещественных корней квартики, рациональная точка внутри интервала с точностью
по его ширине, точная проверка знака. **Её нужно внедрить.**

## Не делать

- Не запускать решето МВ для (15,8) как путь к её закрытию: класс методов пуст (см. §1).
- Не искать генератор для (19,16): пара давно закрыта спариванием, генератор не нужен.
- Не читать `killed: 0` и `reduces: 0` из оборванных прогонов как отрицательные результаты.
- Не считать «120» и «126» одним числом: это ответы на разные вопросы.
- Не оставлять правку `SearchExhausted` как окончательную — нужна сертифицированная замена.

## Что перезапустить после сброса лимита (01:00 МСК)

Прогоны возобновляемы по `resumeFromRunId` — досчитанные агенты вернутся из кэша мгновенно:
- `wf_c46199b9-eac` (аудит счёта) — нужны только сводка;
- `wf_1de2ecb2-c07` (угловые пары) — нужны «сведение?» и «дешёвые победы», самое ценное;
- `wf_dda9880d-687` (решето) — **переформулировать** под §1, а не возобновлять как есть;
- `wf_148c8111-97c` (проверка Куммера) — сводка.
