# Дополнительные модели n61: K7 и K13

2026-09-11. Пользователь разрешил продолжить исследование, сохранив минимум 40% недельного лимита. На входе инструмент показывал 48%, после расчёта — 47%; значения округлены сервисом. Лимит не нужно расходовать до нижней границы.

Проверены оставшиеся две модели сечения (71,49,61), попадающие в уже проверенную область нового алгоритма: три неприводимых вещественных квадратичных множителя. Обе имеют поля Q(√8762), Q(√8762), Q(√3061), с возможной перестановкой.

| Модель | dim fake Sel₂ | dim Sel₂ | Базовая граница ранга | После Pic¹ |
|---|---:|---:|---:|---:|
| K7 | 8 | 9 | 7 | 5 |
| K13 | 8 | 9 | 7 | 5 |

K7 воспроизводит прежнюю границу Magma. Для K13 прежний серверный запрос не вернул ответа; теперь получена локальная вычислительная граница 5. Обе слабее уже известной границы 3 на K8/K10, поэтому общий результат **1 ≤ r ≤ 3 не изменился**.

Каждая модель запущена с двумя начальными значениями генератора выборок. Локальные пространства и глобальные матрицы совпали. Все четыре сертификата воспроизведены проверяющим кодом без случайного поиска, включая независимый способ проверки квадратности локальных свидетельств и сертификаты несовместности системы Pic¹. Статус — проверенный результат CAS; ограничения общего стека Sage/PARI и общих координатных функций сохраняются, как в исходном пакете EVEN_DESCENT_2026-09-11.zip.

Алгоритм не расширялся: остальные модели имеют линейные либо невещественные квадратичные множители и не входят в его текущую область. Не делается вывода о невозможности их анализа другими методами.

Дальнейшая содержательная цель остаётся прежней: дополнительное ограничение через спуск или спаривание Касселса—Тейта на лучших K8/K10. Простое получение границ на этих двух оставшихся моделях улучшения не дало. Расчёты завершены; фоновых заданий этого эксперимента нет.

Файлы: `run.py` — воспроизведение четырёх запусков и проверок; `even_descent.py`, `verify_certificates.py` — неизменённые снимки предыдущего пакета; `*.m` — исходные модели; `*seed*.json` — сертификаты; `summary.json`, `run.log` — итог и журнал; `manifest.json` — контрольные суммы.

Повторение из распакованной папки:

```sh
DOT_SAGE=/tmp/magic-square-sage /usr/bin/python run.py
```

Этот запуск перезаписывает результаты. Для проверки отдельного сохранённого JSON импортировать `verify` из `verify_certificates`; основной блок того файла относится к контрольному набору исходного пакета и здесь отдельно не запускается.
