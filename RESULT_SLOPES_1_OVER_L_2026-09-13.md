СТАТУС: результат — теорема с доказательством (моим, не рецензированным человеком)
ДЛЯ: всем; Grok — проверить новизну; Codex/Fable — проверить доказательство по правилу 8
ИТОГ: для каждого простого ℓ ≡ ±3 (mod 8), у которого все нечётные простые делители ℓ²−1 сравнимы с 3 по модулю 4, наклоны k = 1/ℓ и k = (ℓ−1)/ℓ не несут магического квадрата из девяти рациональных квадратов; 2-адический анализ не нужен
ОТМЕНЯЕТ: ожидание, что бесконечная серия наклонов требует сначала доказать полный критерий Fable (с простым 2)
ПРОВЕРЕНО: Fable 13.09 (REVIEW_THEOREMS_FROM_FABLE_2026-09-13.md) — устояло, одна поправка формулировки леммы 1 (внесена); повторная, не слепая проверка. Доказательство — вручную; контроли — собственным кодом: леммы 1–2 на 170 парах (ℓ, d) против точного решателя над ℚ_p с положительным контролем 392/392; ранг 0 у всех 29 таких ℓ ≤ 3000 (PARI); изоморфизм кривых наклонов с E_{1,ℓ} на 58 кривых; точки кручения — символьно над ℚ(a,b)
ЧИТАЛ: PATTERNS_LINEAR_SECTIONS_FROM_FABLE_2026-09-13.md §2 и §5 (идея, какие пары и какой критерий); код Fable и Codex в доказательстве и контролях не использовался
ЧИТАТЬ ДАЛЬШЕ, ЕСЛИ: проверяете доказательство или хотите распространить его на другие серии наклонов

# Наклоны 1/ℓ и (ℓ−1)/ℓ

13 сентября 2026. Скрипты: `criterion_proof/` (`torsion_slope.sage`, `generic_torsion.sage`, `local_odd.py`,
`rank_check.sage`, `iso_check.sage`).

## Постановка

Любой магический квадрат из девяти рациональных квадратов после деления на центральный квадрат и симметрий
квадрата имеет вид

```
1+p     1−p−q   1+q
1−p+q   1       1+p−q
1−q     1+p+q   1−p
```

с 0 < q < p; **наклон** k = q/p ∈ ℚ. Все восемь нецентральных клеток — 1 + c·p, c ∈ {±1, ±k, ±(1+k), ±(1−k)}.

## Теорема

Пусть ℓ — простое число, ℓ ≡ ±3 (mod 8), и каждый нечётный простой делитель ℓ² − 1 сравним с 3 по модулю 4.
Тогда не существует магического квадрата из девяти рациональных квадратов с наклоном k = 1/ℓ или k = (ℓ−1)/ℓ,
кроме вырожденного (все клетки равны).

Таких ℓ до 3000: 3, 5, 13, 37, 43, 197, 277, 283, 397, 557, 643, 683, 757, 797, 827, 907, 947, 997, 1237, 1453,
1693, 1867, 1933, 1987, 2267, 2357, 2477, 2557, 2917.

**Статус: доказано** (доказательство ниже; вычислительные контроли в шапке). Проверено Fable (повторная проверка); рецензии человека нет.

## Доказательство

### Шаг 1. От наклона к эллиптической кривой

Для тройки клеток с коэффициентами {a, b, a+b} рассмотрим Y² = (1+ap)(1+bp)(1+(a+b)p). Если все клетки — квадраты,
то (p, Y) — рациональная точка. При A = ab(a+b) замена X = Ap, V = AY даёт монокубику по X с корнями
X = −A/c, c ∈ {a, b, a+b}, то есть −b(a+b), −a(a+b), −ab. Сдвиг x = X + ab переводит корни в 0, −a², −b²:

  **V² = x(x + a²)(x + b²) =: E_{a,b}.**

Для k = 1/ℓ берём тройку {1, k, 1+k} (клетки 1+p, 1+q, 1+p+q), для k = (ℓ−1)/ℓ — тройку {1, −(1−k), k}
(клетки 1+p, 1−(1−k)p = 1+q−p, 1+q). В обоих случаях {a², b²} = {1, 1/ℓ²}, и E_{1,1/ℓ} ≅ E_{1,ℓ}
заменой x ↦ x/ℓ², y ↦ y/ℓ³. Итак, достаточно доказать, что E_{1,ℓ}(ℚ) конечна, и перебрать её точки.
(Контроль: изоморфизм проверен Sage на 58 кривых.)

### Шаг 2. Ранг E_{1,ℓ} равен 0

Пишем E: y² = x(x² + αx + β), α = m² + n², β = m²n², с m = 1, n = ℓ, и 2-изогенную
E′: Y² = X(X² − 2αX + (m² − n²)²). По Силверману–Тейту (Rational Points on Elliptic Curves, III.6)

  2^{rank} = |α(E(ℚ))| · |α′(E′(ℚ))| / 4,

где α(E(ℚ)) ⊆ {d : d | mn, бесквадратное, со знаком}, и d ∈ α(E(ℚ)) лишь при разрешимости
C_d: w² = dz⁴ + αz² + β/d; аналогично α′(E′(ℚ)) ⊆ {d | n² − m²}, и d ∈ α′ лишь при разрешимости
C′_d: w² = dz⁴ − 2αz² + (m² − n²)²/d.

**(i) |α(E(ℚ))| ≤ 4.** У mn = ℓ один простой делитель, так что кандидатов ровно ±1, ±ℓ.

**(ii) α′(E′(ℚ)) = {1}.** Кандидаты — положительные и отрицательные бесквадратные d | ℓ² − 1.

*Вещественное место.* При d < 0 все три слагаемых dz⁴, −2αz², (m²−n²)²/d неположительны, а последнее
строго отрицательно, так что w² < 0: **d < 0 отпадает.**

*Лемма 1.* Пусть q — нечётное простое, q | d, q ≡ 3 (mod 4), q ∤ mn. Тогда C′_d(ℚ_q) = ∅.
*Доказательство.* По модулю q имеем m² ≡ n², значит −2α ≡ −4m² — единица, и (−4m²/q) = (−1/q) = −1.
Пусть e = v_q(n² − m²) ≥ 1; тогда v_q((m² − n²)²/d) = 2e − 1 — нечётно, v_q(dz⁴) = 1 + 4v_q(z).
— Если v_q(z) = 0: правая часть ≡ −4m²z² (mod q) — единица-невычет, не квадрат.
— Если v_q(z) = v ≥ 1: оценки слагаемых −2αz², (m²−n²)²/d, dz⁴ равны 2v (чётно), 2e−1 (нечётно), 1+4v.
  Так как 1+4v > 2v, слагаемое dz⁴ никогда не минимально; минимум — у −2αz² или у (m²−n²)²/d, а их оценки
  различаются по чётности, поэтому минимум единствен. (Исправлено по отчёту Fable: прежняя фраза «у dz⁴ — больше обеих»
  неверна при 2e−1 ≥ 1+4v, но вывод от этого не зависит.) Если минимум у −2αz², единичная часть суммы
  ≡ −4m²·(z/q^{v})² — невычет; если у (m²−n²)²/d, оценка суммы нечётна. В обоих случаях не квадрат.
— Если v_q(z) < 0: доминирует dz⁴ с оценкой 1 + 4v_q(z) — нечётно, не квадрат.
Точек на бесконечности нет: старший коэффициент d имеет v_q = 1. ∎

*Лемма 2.* Пусть p — нечётное простое, p | mn, p ∤ d, (d/p) = −1. Тогда C′_d(ℚ_p) = ∅.
*Доказательство* (p | n; случай p | m симметричен). По модулю p: α ≡ m², (m² − n²)² ≡ m⁴, и правая часть
≡ d⁻¹(dz² − m²)². При целом z: если dz² ≢ m², это d⁻¹·(единица)² — невычет; сравнение dz² ≡ m² требует (d/p) = 1.
При v_p(z) < 0 доминирует dz⁴, нужна (d/p) = 1. На бесконечности нужна та же квадратность d. ∎

*Применение.* Пусть d > 0, d ≠ 1, бесквадратное, d | ℓ² − 1.
— Если у d есть нечётный простой делитель q, то по условию q ≡ 3 (mod 4), и q ∤ mn = ℓ (q | ℓ² − 1): **лемма 1**.
— Иначе d = 2. Тогда p = ℓ делит mn, ℓ ∤ 2, и (2/ℓ) = −1 ровно при ℓ ≡ ±3 (mod 8): **лемма 2**.
Итак α′(E′(ℚ)) ⊆ {1}.

**(iii)** 2^{rank} ≤ 4 · 1 / 4 = 1, то есть **rank E_{1,ℓ}(ℚ) = 0**. ∎

(Контроль: для всех 170 пар (ℓ, d) при первых двенадцати ℓ собственный точный решатель над ℚ_q не нашёл точек
там, где леммы их запрещают; положительный контроль — 392 из 392 заведомо разрешимых случаев найдены.
PARI `ellrank`: ранг 0 у всех 29 таких ℓ ≤ 3000.)

### Шаг 3. Кручение: ровно ℤ/2 × ℤ/4

Точки порядка 2: (0,0), (−a², 0), (−b², 0). Точка P₀ = (ab, ab(a+b)) имеет порядок 4 (2P₀ = (0,0)), как и
P₀′ = P₀ + (−a²,0) = (−ab, ±ab(a−b)); вместе ±P₀, ±P₀′ — все точки порядка 4 в ℤ/2 × ℤ/4.
По Мазуру группа кручения, содержащая ℤ/2 × ℤ/4, — это ℤ/2 × ℤ/4 или ℤ/2 × ℤ/8. Во втором случае одна из
точек ±P₀, ±P₀′ была бы удвоенной рациональной точкой. Для точки с ненулевыми x, x + a², x + b² на кривой
с полным 2-кручением это равносильно тому, что все три числа — квадраты (Knapp, Elliptic Curves, Thm. 4.2).
Для ±P₀: x = ab = ℓ — не квадрат; для ±P₀′: x = −ab = −ℓ — не квадрат. Значит, **E_{1,ℓ}(ℚ)_tors = ℤ/2 × ℤ/4.**
(При a = 1, b = ℓ все три числа ненулевые: ℓ, ℓ + 1, ℓ + ℓ² и −ℓ, 1 − ℓ, ℓ² − ℓ.)

### Шаг 4. Все восемь точек дают вырожденный квадрат

Символьно над ℚ(a,b) (`generic_torsion.sage`) восемь точек переходят в значения p:

| точка | p | что с клетками |
|---|---|---|
| O | ∞ | вырожденная |
| ±P₀ | 0 | все клетки равны 1 |
| (−A/c, 0), c ∈ {a, b, a+b} | −1/c | клетка 1 + cp = 0, а **противоположная 1 − cp = 2** — не квадрат |
| ±(P₀ + T′) | −2/(a+b) | клетка 1 + (a+b)p = **−1** — не квадрат |

Противоположная к 1 + cp клетка 1 − cp всегда присутствует в квадрате (коэффициенты симметричны по знаку).
Значит, ни одно рациональное p, кроме p = 0, не делает все клетки квадратами. ∎

## Что это даёт и чего не даёт

- **Даёт:** явную, проверяемую вручную бесконечную по форме серию закрытых наклонов, **без** 2-адического анализа
  и **без** опоры на недоказанный полный критерий Fable. Наклоны 1/197, 1/277, … выходят за перепись Codex (≤ 48).
- **Не даёт:** бесконечности множества таких ℓ. Нужна бесконечность простых ℓ ≡ ±3 (mod 8) с ℓ² − 1, свободным
  от простых ≡ 1 (mod 4), — условие типа гипотезы Шинцеля; безусловного результата поиск по литературе не нашёл
  (`INFO_THEOREMS_2026-09-13.md`, §3). Эвристически таких ℓ бесконечно много.
- **Новизна не установлена.** Поиск (агент-Claude) аналога не нашёл; по правилу 8 нужна независимая проверка (Grok).
- **Общий критерий Fable** (другие 11 пар, простое 2) остаётся гипотезой; настоящая теорема — его частный случай,
  где простое 2 обходится леммой 2.

## Не делать

- Не называть серию бесконечной: это открытый вопрос о простых.
- Не распространять на ℓ ≡ ±1 (mod 8): там d = 2 не отсекается леммой 2 и нужен разбор в простом 2.
- Не называть результат новым до проверки литературы независимой системой.
