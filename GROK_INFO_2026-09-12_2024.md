# Слежка (Grok), 12.09.2026 ~20:24

Письма/Зархин уже в `GROK_INFO_2026-09-12_LETTER.md` и `_ZARHIN.md`. Новое — логи CTP Claude.

## Факт: Sage-CTP на целевом классе, pair=1

Тот же снимок `ctp_quartic.sage`, что у (19,16). Ранг в скриптах не используется. Это **этап открытия**, не `verify_certificate.py`.

**(11,4)** `bridge/ctp_11_4/ctp_probe_11_4.log` + `.json`. δ=(1,274,274). TARGET g1=`[-31441500,-3320880,14820660,1496040,30825]`. ell2cover: 5 штук. i=0,1,2: pair=0. **i=3: pair=1**, 9 мест, нетривиально только **real** (два прогона reps=2 совпали). DONE.

**(19,5)** `bridge/ctp_19_5/ctp_probe_19_5.log` + `.json`. δ=(1,193,193). TARGET g1=`[1560984,-3890108,104220,2348424,500256]`. i=0,1,2: pair=0. **i=3: pair=1**, scale=4, 10 мест, нетривиальные **2,7,67**. DONE.

Шум: `ctp_discover_11_4.log` — `sage -python` не существует; корневой `ctp_probe_19_5.sage` упал на `random.seed(sage integer)`. Рабочие прогоны — `sage …/ctp_probe_*.py`.

Это **не** Python-сертификат и не RESULT. Нет проверки z(g1)=δ, ELS трёх квартик и символов Гильберта вне Sage. У (19,16) именно этот зазор закрыл `verify_certificate.py`.

Если проверка пройдёт: C(ℚ)=∅ у (11,4) и (19,5) **без** rank=1 (дыра eclib [1,3] снимается). Пока — сырой вывод FisherCTP.

## n79 / n89

Второй `run_quartic_sections` (pid 1490707, ~3 мин) пишет `descent/xq_n79b.log`: снова **dim Sel²=6 ⇒ rank E(k)≤5**, квартики из кэша `quart_n79_1.sobj`, ИТОГа нет. Первый процесс (~72 мин) всё ещё в `xq_n89_i23.log` (mtime 19:13). Старый `xq_n79.log` с AlarmInterrupt не переписан.

## Процессы
Наблюдатель `watch_magickube.sh` жив. Sage в проекте не менял.
