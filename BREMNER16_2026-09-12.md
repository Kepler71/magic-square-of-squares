# Все шестнадцать конфигураций Бремнера и поиск седьмой квадратной клетки

Claude, 2026-09-12. Каталог работы: `/home/kep/magicKube/bremner16/`.
Цель — приз Бойера €1000: магический 3×3 с семью (или более) различными квадратными клетками,
отличный от единственного известного примера Бремнера–Саллоуса, его симметрий и кратных k².

Статусы утверждений размечены: **[доказано]**, **[численно]**, **[наблюдение]**, **[гипотеза]**.

---

## 0. Первоисточник получен

`A. Bremner, On squares of squares II, Acta Arith. 99 (2001) 289–308` скачана
(https://www.impan.pl/shop/publication/transaction/download/product/82367?download.pdf) и лежит в
`bremner16/bremner2001.pdf`, текстовый слой — `bremner2001.txt`. Всё, что раньше было известно только
в пересказе (`GROK_INFO_7_8_SQUARES.md`), теперь сверено с оригиналом.

Что в статье есть (сверено):
- с. 289: единственный известный нетривиальный семиклеточный пример — квадрат (1) (Бремнер/Саллоус);
  восьмиклеточных над ℚ нет, но над ℚ(√3) есть бесконечное семейство (пример (2));
- с. 290, формула (3): любой магический 3×3 над ℚ имеет вид, приведённый ниже; тривиален (есть
  повторяющиеся клетки) ровно при `ab(a²−b²)(a²−4b²)(4a²−b²) = 0`;
- с. 290: шестнадцать способов выбрать шесть клеток с точностью до симметрии; Fig. 1 — по примеру
  на каждую конфигурацию (с. 292–293);
- с. 290: «Demanding that six given entries be square … equivalent to the intersection of three
  quadrics in P⁵»; если поверхность неособа, она K3;
- с. 291: результат Бьюэлла — семиклеточных «песочных часов» с центром < 25·10²⁴ нет;
- с. 291–295: подробный разбор конфигурации II (эллиптическая кривая над ℚ(λ), ранг 1; специализация
  с рангом 2; попытка седьмой клетки → кривая рода 3 и эллиптическая проводника 48 ранга 0);
- с. 296: соответствия II ↔ XIII и VII ↔ XIV, парность внутри VII и XIII;
- с. 296: **«We find in this manner parametrizations for the squares in Categories I–XVI of respective
  degrees 12, 8, 12, 20, 12, 12, 12, 20, 20, 12, 20, 20, 8, 12, 20, 12»** — сами параметризации в
  статье не напечатаны («available from the author»); то есть воспроизвести их по тексту нельзя,
  нужен свой вывод;
- с. 297–298: разбор конфигурации VII; поиск седьмой клетки в области `p+q+num(λ)+den(λ) ≤ 1000`
  дал ровно одно решение — λ = 13, (p,q) = (9,2), то есть квадрат (1);
- с. 298–303: геометрия конфигурации III (K3, 24 коники, 32 прямые, NS ранга 20, Eλ ранга 2 над ℂ(λ));
- с. 301–305: для III — **все рациональные кривые на S имеют чётную степень**; степени 2 дают только
  тривиальные квадраты, степени 4, 8, 12 неприводимых рациональных кривых нет; есть одна степени 6
  (с точностью до 96 симметрий), одна степени 10 и шесть степени 14. Для кривых степеней 6 и 10
  параметризации выписаны; поиск λ, при котором ещё одна клетка — квадрат, дал только λ = ±1
  (тривиально). Дальше степени 14 расчёт формы (25) становится непрактичным;
- с. 305–308: над ℚ(√3) прямая (14) на S даёт квадрат (28) с шестью квадратными клетками, и два из
  трёх оставшихся элементов средней строки становятся квадратами по точкам кривой Y² = X(X²+8X+4)
  ранга 1 над ℚ(√3) — отсюда бесконечное семейство **восьмиклеточных** примеров над ℚ(√3).
  Над ℚ это ничего не даёт (и для приза Бойера не годится).

---

## 1. Шестнадцать конфигураций: независимое перечисление и привязка к Fig. 1

Перечислены **перебором** все C(9,6) = 84 шестиэлементных подмножества клеток 3×3 и
профакторизованы по группе D₈ (`bremner16/configs.py`). Получилось **ровно 16 классов** — совпадает
с Бремнером. **[доказано]** Контроль по лемме Бёрнсайда: неподвижных подмножеств
84 + 0 + 4 + 0 + 10 + 10 + 10 + 10 = 128, 128/8 = 16.

Привязка к нумерации Бремнера сделана не «на глаз», а арифметически. `pdftotext` теряет верхние
индексы, и токен вида `492` может означать и 492, и 49². Перебраны все 2⁹ прочтений каждого примера
Fig. 1; оставлены те, что дают магический квадрат ровно с шестью квадратными клетками. Для пятнадцати
примеров прочтение единственно; для конфигурации I одна клетка в текстовом слое искажена
(`−10999` вместо `−5999`) и восстановлена из магического условия. Результат — **биекция**:
каждая из шестнадцати меток I–XVI отвечает своему классу, все шестнадцать различны. **[доказано]**

Клетки нумеруются 0…8 построчно:

```
 0 1 2
 3 4 5
 6 7 8
```

Противоположные (через центр) пары: (0,8), (1,7), (2,6), (3,5); в каждой сумма равна 2c.

| класс | Бремнер | схема | клетки | дополнение | центр в конфиг. | полных пар | размер орбиты | есть известный 7-пример |
|---|---|---|---|---|---|---|---|---|
| 1 | V | `XXX / XXX / ...` | 0,1,2,3,4,5 | 6,7,8 | да | 1 | 4 | нет |
| 2 | I | `XXX / XX. / X..` | 0,1,2,3,4,6 | 5,7,8 | да | 1 | 4 | нет |
| 3 | X | `XXX / XX. / .X.` | 0,1,2,3,4,7 | 5,6,8 | да | 1 | 8 | да |
| 4 | VIII | `XXX / XX. / ..X` | 0,1,2,3,4,8 | 5,6,7 | да | 1 | 8 | да |
| 5 | IX | `XXX / X.X / X..` | 0,1,2,3,5,6 | 4,7,8 | нет | 2 | 8 | нет |
| 6 | XII | `XXX / X.X / .X.` | 0,1,2,3,5,7 | 4,6,8 | нет | 2 | 4 | нет |
| 7 | XVI | `XXX / X.. / X.X` | 0,1,2,3,6,8 | 4,5,7 | нет | 2 | 4 | нет |
| 8 | XV | `XXX / X.. / .XX` | 0,1,2,3,7,8 | 4,5,6 | нет | 2 | 8 | да |
| 9 | VII | `XXX / .X. / XX.` | 0,1,2,4,6,7 | 3,5,8 | да | 2 | 8 | да |
| 10 | XIV | `XXX / .X. / X.X` | 0,1,2,4,6,8 | 3,5,7 | да | 2 | 4 | нет |
| 11 | III | `XXX / ... / XXX` | 0,1,2,6,7,8 | 3,4,5 | нет | 3 | 2 | нет |
| 12 | VI | `XX. / XXX / .X.` | 0,1,3,4,5,7 | 2,6,8 | да | 2 | 4 | нет |
| 13 | XI | `XX. / XXX / ..X` | 0,1,3,4,5,8 | 2,6,7 | да | 2 | 8 | да |
| 14 | II | `XX. / X.X / .XX` | 0,1,3,5,7,8 | 2,4,6 | нет | 3 | 2 | нет |
| 15 | IV | `XX. / .XX / XX.` | 0,1,4,5,6,7 | 2,3,8 | да | 1 | 4 | да |
| 16 | XIII | `XX. / .XX / X.X` | 0,1,4,5,6,8 | 2,3,7 | да | 1 | 4 | да |

Пояснения к столбцам:
- **VI (класс 12)** — «полный крест плюс один угол», как и говорилось в `GROK_INFO_7_8_SQUARES.md`. Сверено.
- **II (класс 14)** — дополнение есть главная диагональ; это в точности `±b+c = □, ±a±b+c = □` из
  статьи (с. 291). **III (класс 11)** — верхняя и нижняя строки; в статье `±a+c = □, ±b+c = □,
  ±(a+b)+c = □` (с. 299). **VII (класс 9)** — в статье `±b+c=□, ±(a−b)+c=□, −a+c=□, c=□` (с. 297).
  Все три совпали с нашей расшифровкой Fig. 1. **[доказано]** Это независимый контроль привязки.
- «известный 7-пример» = конфигурация реализуется как шестиэлементное подмножество квадратных клеток
  примера (1). Их ровно семь: **VIII, X, VII, XV, IV, XIII, XI**. Для остальных **девяти**
  конфигураций (**I, II, III, V, VI, IX, XII, XIV, XVI**) семиклеточного примера не известно вообще
  никакого, поэтому любое попадание там автоматически «отличный от Бремнера–Саллоуса». **[доказано]**
- «полных пар» = сколько противоположных пар целиком лежит в конфигурации. Ключевой для нас столбец
  (см. §3).

Конфигурация нашей прежней реализации `corners/bremner_type.sage`: там фиксировалась полная пара
(1,7) = (b², h²) и клетка 5 = f², а из оставшихся квадратными делались две — то есть в зависимости от
выбора базовой пары покрывались классы 1, 3, 5, 6, 8, 12, 13, 14, 15 (см. §3). Конфигурация VI
(класс 12) там достижима базовой парой {c0, c3} при квадратном центре.

---

## 2. Параметризация

Используется параметризация Бремнера (3) (эквивалентна упомянутой в задании параметризации
Michaud-Rodgers с точностью до линейной замены):

```
   c+a     c−a−b    c+b
  c−a+b     c      c+a−b
   c−b     c+a+b   c−a
```

Все девять клеток — линейные формы от (a, b, c). Отсюда:

**[доказано]** Требование «шесть данных клеток — квадраты» после исключения a, b, c есть пересечение
трёх квадрик в P⁵ (как и пишет Бремнер): три из шести клеток с независимыми линейными формами задают
(a,b,c) через z₁², z₂², z₃², и оставшиеся три клетки становятся тернарными квадратичными формами
Q₄, Q₅, Q₆ от (z₁,z₂,z₃), а условия — v_j² = Q_j. Это поверхность, поэтому шесть квадратов не
«кривая», а двумерное семейство; седьмая клетка задаёт на нём кривую.

### 2.1 F-семейства (одна полная пара + третья клетка)

Берём одну полную противоположную пару: cell_i = P², cell_{i'} = Q². Тогда автоматически
c = (P²+Q²)/2. Добавляем третью клетку cell_j = t² (j не в паре и не центр). Тогда a и b линейны
по T = t², и **все девять клеток имеют вид α_k + β_k·t²**. **[доказано]**

С точностью до D₈ таких семейств ровно четыре (стабилизатор пары в D₈ имеет порядок 4, и он
транзитивен на углах и на рёбрах):

| тип | полная пара | третья клетка | свободные клетки |
|---|---|---|---|
| F1 | рёберная {1,7} | ребро 5 | 0,2,3,6,8 |
| F2 | рёберная {1,7} | угол 0 | 2,3,5,6,8 |
| F3 | угловая {0,8} | угол 2 | 1,3,5,6,7 |
| F4 | угловая {0,8} | ребро 1 | 2,3,5,6,7 |

(«свободные клетки» — пять клеток, не считая центра; центр во всех случаях равен c и от параметра
не зависит.)

F1 — это в точности семейство `corners/bremner_type.sage` (проверено совпадением формул
TL = (h²+f²)/2 и т. д., см. контроль §5). **F2, F3, F4 — новые, раньше не считались.**

Гарантированные квадраты в F-семействе: 3 (пара + третья клетка) плюс центр, если C = (P²+Q²)/2
квадрат. Ещё два даёт базовая кривая (см. §3). Итого 5 или 6.

Существенное наблюдение, которого не было в прежней реализации: при **квадратном центре** в
вырожденной точке t = Q все пять свободных клеток одновременно квадраты (три из них равны P², Q²,
а две равны C). Поэтому доступны **все 10** базовых пар, а не 6, как в `bremner_type.sage`.
**[доказано]** Именно две из четырёх «новых» пар (c0/c2 и c0/c6 при (P,Q) = (17,31)) независимо
находят пример Бремнера–Саллоуса — см. §5.

### 2.2 G-семейства (две полные пары) — основная новая конструкция

**[доказано] Лемма.** Всякий магический 3×3 с ≥ 7 квадратными клетками содержит не менее двух
полных противоположных пар. (Неквадратных клеток не больше двух, пары попарно не пересекаются,
две клетки портят не более двух пар.)

Поэтому для цели «семь квадратов» достаточно семейств с двумя полными парами. Пусть
cell_i = P², cell_{i'} = Q², cell_k = R², cell_{k'} = S². Условие совместности одно:
P² + Q² = R² + S² = 2c. Эта квадрика в P³ расщепима над ℚ, и полная (Сегре) параметризация есть

```
P = m p + n q,   Q = m q − n p,   R = m p − n q,   S = m q + n p.
```

**[доказано]** Она даёт все рациональные точки: при q = 1 обратно n = (P−R)/2, m = (Q+S)/2,
p = (P+R)/(Q+S); исключение (Q+S, P−R) = (0,0) отвечает P = R, то есть тривиальному квадрату
с повторяющимися клетками.

При q = 1 параметр p пробегает ℚ, и **все девять клеток становятся квадратичными многочленами от p**
с коэффициентами, зависящими от (m,n). Четыре клетки квадратны автоматически при любых (m,n) —
никакого условия «квадратный центр» не нужно.

С точностью до D₈ G-семейств ровно три (D₈ действует на четырёх парах, переставляя рёберные между
собой и угловые между собой):

| тип | пары | свободные клетки (вместе с центром) |
|---|---|---|
| G1 | {1,7} и {3,5} (обе рёберные) | 0, 2, 6, 8, центр 4 |
| G2 | {0,8} и {2,6} (обе угловые) | 1, 3, 5, 7, центр 4 |
| G3 | {1,7} и {0,8} (рёберная + угловая) | 2, 3, 5, 6, центр 4 |

**Итог постановки [доказано]:** задача «семь квадратов» полностью сводится к следующему.
Для каждого из трёх типов G, каждого (m:n) ∈ P¹(ℚ) и каждого p ∈ ℚ имеем магический квадрат с
четырьмя гарантированными квадратами; нужно, чтобы ещё три из пяти оставшихся клеток
(четыре клетки + центр) были квадратами. Два из трёх условий — пересечение двух коник, то есть
кривая рода 1 с рациональной точкой в вырожденном слое; её точки перечисляются по решётке
Морделла–Вейля. Третье условие на этой кривой — накрытие рода ≥ 2 («f(t) = □» Бремнера).

**Род кривой седьмой клетки [численно].** В G-семействе цепочка накрытий имеет роды
`0 → 1 → 5`: одно условие — коника (род 0), два — базовая эллиптическая кривая (род 1), три — кривая
**рода 5**. Посчитано функциональными полями Sage (башня квадратичных расширений ℱ_p(p),
p = 10007 — простое хорошей редукции) для G2 при (m,n) = (5,2), G3 при (3,2) и G1 при (3,2);
во всех трёх случаях `[0, 1, 5]`. Это согласуется с формулой Римана–Гурвица (восемь простых нулей
квадратичной формы на кривой степени 4 над P¹ ⇒ 2g−2 = 2·0 + 8) и с фразой Бремнера про
«гиперэллиптические кривые в общем случае высокого рода».

### 2.3 Сравнение с подходом Бремнера

Это наш собственный вывод; параметризаций Бремнера (степеней 8–20) в статье нет, сверить не с чем.

| | Бремнер (2001) | здесь |
|---|---|---|
| носитель шести квадратов | **рациональная** кривая рода 0 на поверхности S (степени 8–20) | **эллиптическая** кривая: 4 автоматических квадрата (две полные пары) + базовая пара |
| параметр | λ ∈ ℚ вдоль одной кривой | точка P ∈ E(ℚ) плюс (m:n) ∈ P¹(ℚ) — двумерное семейство кривых |
| условие 7-й клетки | f(λ) = □, гиперэллиптическая кривая высокого рода | накрытие E рода **5** |
| как ищутся точки | подстановка λ из области перебора | LLL + перебор по канонической высоте на MW-решётке + модульное сито |
| покрытие | по одной (или нескольким) кривым на конфигурацию | все конфигурации разом; для 7 квадратов покрытие **полное по постановке** (лемма §2.2) |

Принципиальная разница: у Бремнера рациональные кривые на K3-поверхности приходится находить
геометрически (и их мало: для III все рациональные кривые чётной степени, до степени 14 их
буквально несколько), а здесь шесть квадратов достигаются на эллиптической кривой, точек на
которой бесконечно много, и семейство таких кривых само параметризовано (m:n) ∈ P¹(ℚ).
Плата — что «кривая 7-й клетки» у нас рода 5 над эллиптической базой, а не над рациональной.

---

## 3. Какая конфигурация каким семейством обслуживается

Для конфигурации S (6 клеток) «обслуживающий вариант» — это выбор семейства и базовой пары, при
котором все шесть клеток S квадратны гарантированно (или гарантированно кроме одной).
Полный перебор (`bremner16/analysis.py`) даёт:

- **Конфигурации, содержащие центр (10 штук: V, I, X, VIII, VII, XIV, VI, XI, IV, XIII).**
  F-семейство с квадратным центром: S = {пара} ∪ {третья} ∪ {центр} ∪ {базовая пара} — ровно 6 клеток,
  **6 гарантированных квадратов**. Требуется (P,Q) с (P²+Q²)/2 = квадрат (таких пар мало, но они
  рационально параметризуются: P = m²−n²+2mn, Q = |m²−n²−2mn|, c₀ = m²+n²).
- **Конфигурации без центра (6 штук: IX, XII, XVI, XV, III, II).** У всех них ≥ 2 полных пары
  (см. таблицу §1), то есть каждая имеет вид «две полные пары + две клетки»:
  IX = пары (2,6),(3,5) + {0,1}; XII = (1,7),(3,5) + {0,2}; XVI = (0,8),(2,6) + {1,3};
  XV = (0,8),(1,7) + {2,3}; III = три пары (0,8),(1,7),(2,6); II = три пары (0,8),(1,7),(3,5).
  В G-семействе это 4 автоматических + базовая пара = **6 гарантированных квадратов, без всяких
  условий на (m,n)**. **[доказано]**
- Конфигурации с центром и ≥ 2 парами (VII, XIV, VI, XI) тоже обслуживаются G-семейством: там
  центр входит в базовую пару.
- Конфигурации с одной полной парой (V, I, X, VIII, IV, XIII) в G-семейство не укладываются;
  для них работает только F-схема (6 гарантированных при квадратном центре, 5 + «везение» иначе).
  Это не мешает цели: семиклеточный пример всё равно имеет ≥ 2 полные пары, значит виден в G.

Полная таблица схем (по три первых варианта; всего вариантов больше):

| класс | Бремнер | F-схема (нужен квадратный центр) | G-схема (без условий на параметры) |
|---|---|---|---|
| 1 | V | F1 {c2,c8}, F2 {c3,c6} | — |
| 2 | I | F3 {c1,c5}, F4 {c2,c5} | — |
| 3 | X | F1 {c0,c2}, F1 {c6,c8}, F2 {c2,c3} … | — |
| 4 | VIII | F3 {c1,c3}, F3 {c5,c7}, F4 {c2,c3} … | — |
| 5 | IX | — | G3 {c2,c5}, G3 {c3,c6} |
| 6 | XII | — | G1 {c0,c2}, G1 {c0,c6}, G1 {c2,c8} … |
| 7 | XVI | — | G2 {c1,c3}, G2 {c1,c5}, G2 {c3,c7} … |
| 8 | XV | — | G3 {c2,c3}, G3 {c5,c6} |
| 9 | VII | F2 {c2,c6}, F2 {c2,c8}, F2 {c6,c8} … | G3 {c2,c4}, G3 {c4,c6} |
| 10 | XIV | F3 {c1,c6}, F3 {c3,c6}, F3 {c5,c6} … | G2 {c1,c4}, G2 {c3,c4}, G2 {c4,c5} … |
| 11 | III | — | G2 {c1,c7}, G2 {c3,c5}, G3 {c2,c6} |
| 12 | VI | F1 {c0,c3}, F1 {c2,c3}, F1 {c3,c6} … | G1 {c0,c4}, G1 {c2,c4}, G1 {c4,c6} … |
| 13 | XI | F1 {c0,c8}, F1 {c2,c6}, F2 {c3,c8} … | G3 {c3,c4}, G3 {c4,c5} |
| 14 | II | — | G1 {c0,c8}, G1 {c2,c6}, G3 {c3,c5} |
| 15 | IV | F1 {c0,c6}, F2 {c5,c6} | — |
| 16 | XIII | F3 {c3,c7}, F4 {c5,c6} | — |

Насколько эти схемы реально работают (построилась ли базовая кривая), видно из прогонов:

- **G2 и G3 покрыты полностью**: у них есть вырожденное значение p, при котором все пять свободных
  клеток одновременно квадраты, поэтому рациональная точка есть у всех 10 базовых пар. Проверено на
  всех (m,n) прогона: 27 из 27 построено для каждой из 20 пар. **[численно]**
- **G1 покрыт частично**: вырожденные точки дают квадраты лишь парами {c2,c6}, {c0,c8} и отдельно
  центр c4. Для этих пар кривая строится всегда; для остальных нужен поиск точки на квартике, и он
  удаётся примерно в трети случаев (граница поиска 600 и 6000 дают почти одинаковый результат —
  значит дело не в высоте, а в препятствии). Проверено, что **вещественного** препятствия нет
  ни у одной базовой пары G-семейств при m,|n| ≤ 20 (`realobstruct.py`: 0 из 508 для каждой из 30
  пар) — значит препятствия p-адические (Селмер), а не знаковые. **[численно]**
  Практическое следствие: слабее всего в G-схеме покрыта конфигурация **XII (класс 6)**, у которой
  все G-варианты — «трудные» пары G1.

**Вывод [доказано]: у каждой из 16 конфигураций есть однопараметрическая схема с шестью
автоматическими квадратами, и седьмая клетка на ней даёт кривую (рода 5).** Точнее:

- классы 1, 2, 3, 4, 15, 16 (V, I, X, VIII, IV, XIII) — только F-схема, нужна пара (P,Q) с
  квадратным центром; таких пар бесконечно много и они рационально параметризованы
  (P = m²−n²+2mn, Q = |m²−n²−2mn|, c₀ = m²+n²);
- классы 5, 6, 7, 8, 11, 14 (IX, XII, XVI, XV, III, II) — только G-схема, но без всяких условий на
  параметры;
- классы 9, 10, 12, 13 (VII, XIV, VI, XI) — обе схемы.

Это ровно то, что требовалось в п. 2 задания. Для классов 5–8, 11, 14 F-схема тоже применима, но
даёт лишь 5 гарантированных квадратов (центр в конфигурацию не входит, а базовая пара закрывает
только две из трёх оставшихся клеток).

---

## 4. Реализация

Каталог `bremner16/`:

- `configs.py` — перечисление 84 подмножеств, факторизация по D₈, расшифровка Fig. 1;
- `analysis.py` — структурный разбор 16 классов и привязка к (семейство, базовая пара);
- `engine.sage` — движок F-семейств (клетки вида α + βt²);
- `engine2.sage` — общий движок: клетки как произвольные квадратичные многочлены от параметра;
  покрывает и F-, и G-семейства;
- `scan.sage` — прогон F-семейств (режим `sq` — квадратный центр, режим `gen` — остальные);
- `scan2.sage` — прогон G-семейств;
- `control.sage`, `control2.sage` — контроли;
- `boxscan.py` — независимый прямой перебор по (m,n) и p = u/v (без эллиптических кривых);
- `realobstruct.py` — проверка вещественного препятствия для базовых пар;
- `coverage.py` — покрытие по конфигурациям (сколько базовых кривых построено на каждый класс);
- `summarize.py` — сводка логов и нормализация попаданий (к целым, деление на наибольший общий
  квадратный множитель, канонизация по D₈) для сравнения с примером (1);
- `bremner2001.pdf`, `bremner2001.txt` — первоисточник.

Схема поиска в каждом семействе (наследует `corners/lll_sieve.sage`, идея сита и перебора по высоте —
Codex; реализация переписана):

1. Базовая пара условий (F₁(p) = □, F₂(p) = □), обе степени ≤ 2. Рациональная точка p₀ берётся из
   **вырожденных значений параметра**: квадрат тривиален ровно при `ab(a²−b²)(a²−4b²)(4a²−b²) = 0`
   (Бремнер, с. 290), a и b — многочлены от p, отсюда конечный список кандидатов p₀.
2. Коника Y² = F₁(p) с точкой p₀ параметризуется рационально: p = num(m)/den(m),
   num = p₀m² − 2Y₀m + (A p₀ + B), den = m² − A. Второе условие превращается в квартику от m.
3. Точка на квартике ищется `pari.hyperellratpoints` (граница 600); дальше переход к модели
   Вейерштрасса по Коннеллу.
4. Генераторы Морделла–Вейля: PARI `ellrank` (даёт и нижнюю, и верхнюю оценку ранга), при недоборе —
   `mwrank` с ограничением по времени, затем насыщение и LLL-редукция.
5. Перебор точек: все векторы n с nᵀHn ≤ hcap (PARI `qfminim`), не более K штук, вместе с −n и
   **нулевым вектором**, умноженные на все точки кручения. Потолок по высоте обязателен: без него
   при ранге 1 координаты точек доходят до миллионов знаков.
6. Модульное сито по ~50 простым: на E(F_p) строится таблица «может ли точка дать ≥ 7 квадратов»
   (квадрат в ℚ остаётся квадратом или нулём в F_p, поэтому сито не отбрасывает настоящих попаданий).
7. Точная проверка выживших над ℚ по всем девяти клеткам: число квадратов, различность всех девяти
   клеток, положительность (последняя фиксируется отдельным флагом, а не требуется — у Бремнера
   в Fig. 1 примеры с отрицательными неквадратными клетками).
8. Исключительные точки отображения (O и точки порядка 2, где формула для p не определена)
   обрабатываются отдельно по предельным значениям.

---

## 5. Контроли

1. **Параметризация (3)** — все восемь линий магические; пример (1) магический, квадратны клетки
   0,1,2,4,5,6,7. **[доказано]**
2. **F1 ≡ `corners/bremner_type.sage`** — формулы клеток совпали символьно
   (TL = (h²+f²)/2, TR = (b²+2h²−f²)/2, BL = (b²+f²)/2, BR = (2b²+h²−f²)/2, D = b²+h²−f²). **[доказано]**
3. **Пример Бремнера–Саллоуса как точка семейства.** F1 при (P,Q) = (17,31), t = 23/17 даёт ровно
   квадрат (1), делённый на 289. G3 при (m,n) = (61,−23), p = 7/6 даёт его же, делённый на 36.
   **[доказано]**
4. **Движок находит пример автоматически.** В F1 при (P,Q) = (17,31) он найден тремя базовыми парами
   (c0/c2, c0/c6, c2/c6), из которых **две (c0/c2 и c0/c6) недоступны в старой реализации** —
   это ровно те пары, что появляются благодаря квадратному центру. В прогоне `sq` он найден также в
   F2 при (P,Q) = (31,17) (t = 205/17) и в F3, F4 при (P,Q) = (113,41). В G3 при (m,n) = (61,−23)
   он найден шестью базовыми парами из десяти. **[численно, воспроизводимо]**
5. **Сито.** Режим `validate` перепроверяет точно все отброшенные состояния. На девяти базовых кривых
   G-семейств ((61,−23) в G3, (5,2) в G2, (3,2) в G1) перепроверено 20 065 отброшенных состояний,
   **ложных отбрасываний 0**. Во всех прогонах поле `bad=0`: у каждой выжившей точки оба условия
   базовой пары действительно дают квадраты (проверка соответствия «точка кривой ↔ значение
   параметра»). Контроль унаследован из `corners/lll_sieve.sage` (96 тыс. состояний на (31,17)).
   **[численно]**
6. **Все 40 базовых кривых** (4 семейства × 10 пар) для пар с квадратным центром строятся без ошибок.
7. **Прямой перебор (`boxscan.py`)** — метод, не использующий ни эллиптических кривых, ни решёток
   Морделла–Вейля, — независимо находит пример (1) (восемь раз, в эквивалентных представлениях
   (m,n) = (6,±7), (7,±6) и p = ±23/61, ±61/23). **[численно]**
8. **Соответствия Бремнера (с. 296) как независимая проверка расшифровки Fig. 1.** Бремнер даёт явные
   линейные соответствия II ↔ XIII и VII ↔ XIV. Подставляя наш расшифрованный пример VII
   (889, 697, 17², 5², 25², 35², 31², 553, 19²) в формулы
   `n12 = −D+F+I, n21 = C−E+F, n32 = C−G+I`, получаем
   (5², 1561, 17², 889, 25², 19², 31², −311, 35²) — **побуквенно наш расшифрованный пример XIV**.
   Аналогично из II (1945, 1², 37², 23², 1105, 41², 29², 47², 265) по формулам
   `n11 = B−D+G, n23 = B+F−H, n32 = F+G−C` получается
   (313, 23², 41², 47², 29², −527, 1², 1153, 37²) — **побуквенно наш расшифрованный пример XIII**.
   И позиции квадратных клеток в обеих парах совпадают с нашими классами 14↔16 и 9↔10. **[доказано]**
9. **Сквозной контроль G-прогона:** в основном прогоне `scan2.sage` пример (1) найден автоматически
   при (m,n) = (6,7) и (6,−7), (7,±6) — то есть весь конвейер (параметризация → вырожденная точка →
   коника → квартика → кривая Вейерштрасса → MW-решётка → сито → точная проверка) работает от начала
   до конца. **[численно]**

---

## 6. Результаты прогонов

Все прогоны — под `ulimit -v 8000000`, не более четырёх процессов одновременно, с логами.

### 6.1 F-семейства, квадратный центр (`scan.sage sq`, лог `sq_*.log`)
Пары (P,Q) с (P²+Q²)/2 = квадрат, по возрастанию P; 4 семейства × 10 базовых пар на пару.
**6 гарантированных квадратов** на каждую базовую кривую.

### 6.2 F-семейства, неквадратный центр (`scan.sage gen`, логи `gen_*.log`)
Все взаимно простые P > Q ≥ 1 (обеих чётностей — прежняя работа брала только нечётные),
центр не квадрат; 5 гарантированных квадратов + 2 «везения» до семи.

### 6.3 G-семейства (`scan2.sage`, логи `G_*.log`, `G1deep_*.log`, `G23_*.log`)
(m,n) взаимно простые, m,|n| ≤ 60, по возрастанию max(m,|n|); 3 семейства × 10 базовых пар.
**6 гарантированных квадратов**; седьмая клетка — одна из трёх оставшихся.

- `G1deep_0_700.log` — **завершён**: 700 значений (m,n) (max(m,|n|) до ≈ 24), только семейство G1,
  граница поиска точки на квартике поднята до 6000. Построено 2668 базовых кривых, не построено 4332,
  ошибок 0, таймаутов 0, **попаданий с ≥ 7 квадратами 0**.
- `G_0_900.log` — все три семейства, идёт.
- `G23_400_900.log` — G2 и G3 на «хвосте» диапазона, идёт.

### 6.4 Прямой перебор без эллиптических кривых (`boxscan.py`, логи `box_*.log`)
Полностью независимая проверка: для (m,n) и p = u/v все девять клеток суть целочисленные
квадратичные формы от (u,v); перебор по прямоугольнику. Это ближайший аналог области поиска
самого Бремнера. Контроль: пример (1) находится (при (m,n) = (6,7), p = −23/61 и его эквивалентах).

- `box_M60_U400.log`: (m,n) взаимно простые, m,|n| ≤ 60; p = u/v, |u| ≤ 400, 1 ≤ v ≤ 400, gcd(u,v)=1;
  три семейства G1, G2, G3. **Проверено 2 572 521 732 точки (u,v)** (суммарно по семействам и (m,n)).
  Попаданий с ≥ 7 квадратами: 8, все — известный пример (1) в эквивалентных представлениях.
  **[численно]** Порядок величины клеток здесь ≈ (m·u)² ≲ 6·10⁸, то есть эта часть поиска слабее
  границ Чаффина (10¹⁴ при неквадратном центре); её ценность — в независимости метода и в том, что
  она покрывает сразу все три G-семейства, то есть все конфигурации с ≥ 2 полными парами.

### 6.5 Покрытие по конфигурациям (сколько базовых кривых реально построено)

`coverage.py` считает для каждого класса, сколько (семейство, базовая пара) с полным набором из
шести гарантированных квадратов действительно дали эллиптическую кривую, и сколько пропущено.
Две схемы дополняют друг друга точно так, как предсказывает §3:

| класс | Бремнер | F-прогон (квадратный центр): построено / пропущено | G-прогон: построено / пропущено |
|---|---|---|---|
| 1 | V | 172 / 0 | — |
| 2 | I | 170 / 0 | — |
| 3 | X | 344 / 0 | — |
| 4 | VIII | 340 / 0 | — |
| 5 | IX | — | 318 / 0 |
| 6 | XII | — | 1036 / 2338 |
| 7 | XVI | — | 640 / 0 |
| 8 | XV | — | 318 / 0 |
| 9 | VII | 597 / 0 | 318 / 0 |
| 10 | XIV | 425 / 0 | 638 / 0 |
| 11 | III | — | 479 / 0 |
| 12 | VI | 430 / 0 | 544 / 2828 |
| 13 | XI | 597 / 0 | 318 / 0 |
| 14 | II | — | 1845 / 0 |
| 15 | IV | 171 / 0 | — |
| 16 | XIII | 170 / 0 | — |

(числа на момент снятия среза; прогоны продолжались). **Ни одна из 16 конфигураций не осталась
без построенных кривых.** Пропуски есть только у XII и VI и только в «трудных» базовых парах G1.

### 6.6 Ранги базовых кривых

Срез по логам (число найденных генераторов):

| прогон | ранг 0 | 1 | 2 | 3 | 4 | 5 | `INC` (найдено меньше верхней оценки 2-спуска) |
|---|---|---|---|---|---|---|---|
| G (все три семейства) | 198 | 583 | 1917 | 1293 | 212 | 8 | 16 из 5221 (0.3%) |
| F, квадратный центр | 0 | 603 | 1514 | 1153 | 211 | 2 | 18 из 3483 (0.5%) |

**[наблюдение]** При квадратном центре базовые кривые F-семейств никогда не имеют ранга 0: вырожденные
слои дают как минимум две независимые точки. Доля кривых, где перебор шёл по собственной подгруппе
(`INC`), здесь на два порядка меньше, чем в прежнем прогоне `corners/lll_sieve.sage` (там 397 баз из
~3800) — из-за использования PARI `ellrank` с верхней оценкой и явного насыщения.

### 6.7 Сводка (итоговые числа)

(заполняется по завершении прогонов)

---

## 7. Честные оговорки о покрытии

- Поиск конечный: ограничены и область параметров ((P,Q) или (m:n)), и решётка Морделла–Вейля
  (потолок по канонической высоте, не более K векторов). Отрицательный результат — **не
  доказательство отсутствия**. **[наблюдение]**
- «Ранг» в логах — это число фактически найденных генераторов; в строках с пометкой `INC` оно меньше
  верхней оценки 2-спуска PARI, то есть перебор шёл по подгруппе конечного индекса.
- Базовые кривые, для которых не нашлось рациональной точки на квартике (`кривая не построена`),
  пропущены. Проверено, что **вещественного** препятствия у них нет (`realobstruct.py`), и что
  увеличение границы поиска с 600 до 6000 почти ничего не меняет; значит это либо p-адические
  препятствия (тогда пропуск безвреден: таких магических квадратов в этом семействе действительно
  нет), либо нетривиальные элементы Ш. Разделить эти два случая (полный 2-спуск на каждой квартике)
  в данной работе не делалось. **[наблюдение]** Наиболее задета конфигурация XII.
- Отрицательные клетки. Мы НЕ требуем положительности всех девяти клеток (флаг `pos` пишется
  отдельно): в Fig. 1 у Бремнера неквадратные клетки бывают отрицательными. Прежняя реализация
  `corners/bremner_type.sage` требовала всех клеток > 0 — это была дополнительная (не обоснованная
  условиями приза) потеря покрытия, здесь она устранена.
- Пары (P,Q) разной чётности. Прежняя работа брала только нечётные взаимно простые (b,h); при
  разной чётности центр полуцелый, но квадрат от этого не перестаёт быть магическим над ℚ. Здесь
  такие пары включены.
- В G-семействах перебирались (m,n) с gcd = 1 и m, |n| ≤ 60, упорядоченные по max(m,|n|), то есть
  точки (m:n) ∈ P¹(ℚ) ограниченной наивной высоты.
- В F-семействах режима `sq` пары (P,Q) с квадратным центром перебирались по возрастанию P;
  прежняя работа (`corners/lll_sieve.sage`) покрыла 633 такие пары до P ≤ 5000, но только
  семейство F1 и только 6 базовых пар из 10.
