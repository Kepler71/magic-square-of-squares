СТАТУС: результат (посчитал сам; с явно перечисленными допущениями §3.4) — наклоны 265/298 и 73/362 закрыты квадратичным Шаботи + точным локальным решетом «девяти клеток»; 126/451 не закрыт. Версия 2 после рецензии Codex (§6)
ДЛЯ: Claude (идея пользователя о p-адических высотах на произведении якобианов — реализована как QC Balakrishnan–Dogra/Bianchi); Codex — повторная рецензия §3.1–3.4 и `omega_exact.py`
ИТОГ: код Bianchi–Padurariu запущен в Sage 10.9 (две несовместимости и одна ошибка Sage обойдены); воспроизведены примеры BD18/BP22; контроль на (9,22,31): QC при p = 7, 17 нашёл ровно все 12 точек C(ℚ), включая нетривиальную (±31/2, ±42315/8). После рецензии Codex перечисление локальных значений ω_q переписано **точно** (все корни рациональны: ±a,±b,±c,±d,0,∞ — `omega_exact.py`), ошибка x=0 устранена (для 265/298 при q=149 остаётся только −4/3, как доказал Codex), хранятся все наборы тегов, для каждой пары (диск, ω) выдаётся сертификат полноты корней (Гензель + очистка классов). Итог: 265/298 — на кривой (33,298,563) при p = 13 **вообще нет** p-адических кандидатов, при p = 17, 19, 29 — нет кандидатов с восемью клетками-квадратами в ℚ_p (независимо: на (33,265,298) при p = 13, 17, 19); 73/362 — на (73,289,362) нет кандидатов при p = 13, 37, 41, 43, 59 (каждое p в отдельности). 126/451: нет точки бесконечного порядка на E⁺ — QC не запускался
ОТМЕНЯЕТ: черновую версию этой записки (утро 15.09: Ω_direct с ошибкой x=0, теги только tags[l][0], без сертификата корней); для 265/298 и 73/362 — очередь Magma (rb_retry) как единственный путь; для 126/451 ничего не отменяет
ПРОВЕРЕНО: Codex 15.09 (RESULT_JOINT_149_QC_FILTER) — первая версия: найдена ошибка x=0, доказано ω_149/log 149 = −4/3, повторная версия не проверена; свои контроли — §2 и §3.3
ЧИТАТЬ ДАЛЬШЕ, ЕСЛИ: нужен алгоритм, нормировки, точное перечисление ℚ_q-классов, сертификат корней, допущения, как повторить
ЧИТАЛ: PROTOCOL_NOTES; RESULT_CENSUS_200; qc_bielliptic/ranks_open.{py,json} (ранги E± Claude); census_six_cells/census.jsonl (ранги троек — выбор контроля); census_300/c300.py (модель e_pm); статьи BD18 (arXiv 1601.00388, §1, §8) и Bia20 (arXiv 1904.04622, §1, §6) — PDF; код bianchifrancesca/QC_bielliptic (README + qc_g2_bielliptic.sage); рецензии Codex RESULT_JOINT_149_QC_FILTER и REVIEW_SHARED_CELLS_AND_THEOREM (§3–5) — после первого расчёта, до второго. Ожидаемого ответа для открытых наклонов нет ни у кого.

# Квадратичный Шаботи для биэллиптических кривых шести клеток

Каталог: `qc_bielliptic/fable/`. Код Bianchi–Padurariu скачан как текст (`upstream/qc_g2_bielliptic.sage`), исполняется через
`python3` + `sage.all` + препарсер Sage (`qc_load.py`); мои правки — `qc_g2_bielliptic_patched.sage` (помечены `# Fable`);
моё: `models.py`, `omega_direct.py` (первая версия, оставлена для сверки), **`omega_exact.py`** (точное перечисление),
`qc_run3.py` (текущий драйвер), `qc_run.py`/`qc_run2.py` (старые), `gens_*.py`, `test_*.py`, `diag_control.py`;
журналы `qc3_*.log`, итоги `qc3_*.json`, сводка `sieve_summary_v3.json`.

## 1. Алгоритм и источники (прочитал в источнике)

**Постановка (BD18 §1, (2); Bia20 §6).** Биэллиптическая кривая рода 2
    X: y² = x⁶ + a₄x⁴ + a₂x² + a₀,   a_i ∈ ℤ
с двумя факторами якобиана
    E₁: y² = x³ + a₄x² + a₂x + a₀,  φ₁(x,y) = (x², y);
    E₂: y² = x³ + a₂x² + a₄a₀x + a₀²,  φ₂(x,y) = (a₀x⁻², a₀y x⁻³).
Условие: rk E₁(ℚ) = rk E₂(ℚ) = 1 (тогда rk J = 2 = g, ρ(J) ≥ 2 — Jac(C) изогенен, не изоморфен E₁×E₂, уточнение Codex — и лемма 3.2 BD18
даёт конечность X(ℚ_p)₂). Условие r < g + ρ − 1 не обещает отсутствия рациональных точек — оно даёт конечное надмножество.
Наша кривая шести клеток C: y² = (1−a²z²)(1−b²z²)(1−c²z²) после x = 1/z, w = y/z³ становится **монической**
    C: w² = (x²−a²)(x²−b²)(x²−c²) = x⁶ + a₄x⁴ + a₂x² + a₀,  a₄ = −(a²+b²+c²), a₂ = a²b²+a²c²+b²c², a₀ = −a²b²c²,
E₁ = E⁻ (та же модель), E₂ ≅ E⁺ (`models.py`: `is_isomorphic` True). Известные точки: ∞± (z = 0), (±a,0),(±b,0),(±c,0).
x = 0 (z = ∞) не рационально (a₀ < 0).

**Теорема (BD18 Cor. 8.1 = Bia20 Thm 6.1 с поправкой Rem. 6.2).** p — простое хорошей ординарной редукции, h_p — p-адическая высота
Мазура–Тейта (Sage `padic_height`), λ_q — локальные слагаемые, P_i ∈ E_i(ℚ) бесконечного порядка, c_i = h_p(P_i)/Log(P_i)².
Для i = 1, 2 множество W^i = { Σ_{q≠p} (λ_q(φ_i z_q + Q_i) + λ_q(φ_i z_q − Q_i) − 2λ_q(φ_{3−i} z_q)) : (z_q) ∈ Π_q C(ℚ_q) } конечно,
и C(ℚ) ∩ C^{(i)}(ℚ_p) ⊂ { z : ρ_i(z) ∈ W^i },
    ρ_i(z) = 2λ_p(φ_{3−i} z) − λ_p(φ_i z + Q_i) − λ_p(φ_i z − Q_i) − 2c_{3−i}Log(φ_{3−i} z)² + 2c_i Log(φ_i z)² + 2h_p(Q_i).
Механизм: h_p квадратична, на кривой ранга 1 h_p = c·Log², λ_p выражается через p-адическую σ-функцию (Bia20 §6; Sage `padic_sigma`, `padic_E2`).

**Реализация BP22 (`qc_g2_bielliptic.sage`).** Упрощённая функция без сдвига на Q_i: на диске около Q с параметром t
    ρ(t) = h_p^{E₁,min}(φ₁) − h_p^{E₂,min}(φ₂) − 2 log_p x − α₁Log₁(φ₁)² + α₂Log₂(φ₂)²,   α_i = h_p(P_i)/Log(P_i)².
Для z ∈ C(ℚ): ρ(z) = Σ_{q≠p} ω_q(z),  **ω_q(z) = −λ_q^{E₁,min}(φ₁ z) + λ_q^{E₂,min}(φ₂ z) − 2 v_q(x(z)) log_p q**
(моя выкладка из `Omega_set`; λ_q — в нормировке кода: r·log q, r по алгоритму Сильвермана из `non_archimedean_local_height`,
вдвое больше классической). Код строит Ω как декартово произведение надмножеств Prop. 6.5 Bia20; далее на каждом диске вычетов
(mod автоморфизмы (x,y) ↦ (±x,±y)) — корни ρ(t) − ω (PARI `polrootspadic`), распознавание рациональных x через `algdep`.

**Опубликованные примеры для контроля.** BD18 §8.3: y² = x⁶ − 2x⁴ − x² + 1, X(ℚ) = {(0,±1), (±3/2, ±1/8), ∞±} (Thm 8.6).
BP22 (докстринг): y² = x⁶ + 22x⁴ − 19x² + 4 (LMFDB 99856.b), p = 5: 7 точек + 8 лишних.

## 2. Реализация в Sage 10.9 и контроли

### 2.1. Правки кода (посчитал сам; `qc_g2_bielliptic_patched.sage`)
1. Sage 10.9: гиперэллиптические кривые во взвешенном проективном пространстве (1,3,1): бесконечность — (1:±1:0) при чётной
   степени, (1:0:0) при нечётной; `H(0,1,0)` больше не точка. Заменено в 5 местах.
2. **Ошибка Sage 10.9:** `Hp(2,0,1) == Hp(1,0,1)` даёт `True` (точки с y = 0; `diag_control.log`). Дедупликация дисков в коде
   выбрасывала все вейерштрассовы диски, кроме первого, — QC «терял» (±9,0), (±31,0) на контроле. Все сравнения точек заменены
   сравнением кортежей координат. **Предупреждение всем, кто пользуется Sage 10.9 с гиперэллиптическими кривыми.**
3. `gp.polrootspadic` (pexpect) → библиотечный `pari(...).polrootspadic`.
4. Параметр `gens`: точки бесконечного порядка ищутся снаружи (mwrank на всех 8 кривых изогенного класса + перенос по изогениям,
   `gens_e2b.py`). QC нужна любая точка бесконечного порядка (α_i = h/Log² не зависит от кратности), не образующая.
5. Множество Ω: код даёт декартово произведение (|Ω| = 1 213 758 уже у контрольной тройки). Заменено на точное перечисление, §3.1.
6. **Сертификат полноты корней** (`fable_root_certificate`, §3.2) для каждой пары (диск, ω).

### 2.2. Контроль на опубликованных примерах (посчитал сам)
BP22 99856.b, p = 5, n = 20: 8 рациональных точек ((0:1:0) в Sage 10.9 — две точки (1:±1:0)) и 8 лишних — как в докстринге (`test_bp22.log`).
BD18 §8.3 при p = 5 (p = 3 требует внешний репозиторий AWS): 8 точек = X(ℚ) теоремы 8.6; лишних 28 (`test_bd18.log`).

### 2.3. Контроль на кривой переписи с известным ответом
Наклон 22/31 закрыт в переписи через (9,22,53)⁻ ранга 0; тройка (9,22,31): rk E⁺ = rk E⁻ = 1. C: w² = (x²−81)(x²−484)(x²−961).
Перебор x = m/n, |m|,|n| ≤ 1500 (`search_pts.py`): x ∈ {±9, ±22, ±31, **±31/2**}; точка (31/2, ±42315/8) настоящая, но не «девять
квадратов» (клетки 1 ± 18/31 и т.д. не квадраты). QC при p = 7 (20 с) и p = 17 (24 с) (полное Ω, 135 элементов): рациональных
точек ровно эти 12, лишних p-адических 948 / 2572 (X(ℚ_p)₂ ⊋ X(ℚ), как у BD18/BP22; для C(ℚ) целиком нужно решето Морделла–Вейля —
не делалось и для нашей задачи не нужно). Решето девяти клеток (§3.1) на этой же кривой: кандидатов 0 при p = 7 и p = 17 —
согласуется с закрытием переписью.

## 3. Открытые наклоны 265/298, 73/362, 126/451

### 3.1. Точное перечисление локальных значений и решето «девяти клеток» (`omega_exact.py`, `qc_run3.py`)
Нужны не все точки C(ℚ), а z, при которых **все девять** клеток 1, 1 ± az, 1 ± bz, 1 ± cz, 1 ± dz — квадраты (d — четвёртое значение
из {r, s−r, s, s+r}; замечание Codex: проверка факторов по одному теряет совместимость, здесь общий x = 1/z для всех восьми клеток).

*Перечисление.* Ветвление λ_q (алгоритм Сильвермана), квадратность f(x) и клеток (x ± k)/x зависят только от валюаций и вычетов
(mod q, при q = 2 — mod 8) конечного набора многочленов от x, **все корни которых рациональны**: f — ±a,±b,±c; (2Y+a₁X+a₃)² на
E_{i,min} в φ_i(x) — те же ±a,±b,±c (полное 2-кручение обеих E_i); x⁶·cub₂(a₀/x²) — ещё x = 0; клетки — ∓k. Поэтому:
- класс x = q^v(u₀ + q^m ℤ_q), не содержащий корней и отделённый от корней уровня v с запасом μ (μ = 1, при q = 2 μ = 3),
  имеет постоянные валюации и вычеты всех величин ⇒ ω_q и «девятиклеточность» постоянны; берётся представитель;
- класс с корнем e дробится, пока корень не станет единственным (глубина > попарных расстояний + μ); затем перечисляются
  оболочки v(x−e) = v+mm, mm < M₀ (каждая — q−1 (или 4 при q=2) классов с постоянными валюациями), а хвост v(x−e) ≥ v+M₀
  **насыщен**: n = min(B, N/2) = N/2 при мультипликативной редукции, C < 3B при аддитивной (B = v(cub)/2 → ∞, C = v(ψ₃(e)) конечна) —
  порог M₀ вычисляется из κ = v(cub_i) − v(x−e) (постоянство κ проверяется на двух глубинах, assert) и N_i, C; значение в хвосте —
  в самой точке e (y = 0), клетка, обращающаяся в 0 при x = e, в хвосте считается возможным квадратом (консервативно);
- хвосты по v: x → ∞ — ω = λ₂((0, a₀√a₆)) + v(a₆) − 2v_q(u₁) (u₁ — масштаб изоморфизма к минимальной модели; в первой версии
  это слагаемое было пропущено — на результаты не повлияло, но при q = 2 давало лишнее значение 0), клетки 1 ± k/x ≡ 1 ⇒ «nine»;
  x → 0 — ω = −λ₁((0, a₆√a₀)) − v(a₀) + 2v_q(u₂), «nine» **только если** −1 ∈ ℚ_q² и все k/k′ ∈ ℚ_q² (иначе ±k/x не могут быть
  квадратами одновременно) — это и есть исправление ошибки, найденной Codex; пороги Vt печатаются, значения на уровнях Vt+1, Vt+2
  по всем единицам сверяются с предельными (`ok=True` во всех прогонах, `sieve_summary_v3.json`).
V_q^nine ⊂ V_q — значения, достижимые точками с восемью клетками-квадратами в ℚ_q; Ω^nine = Σ_q V_q^nine — 1–2 элемента.
Для (33,298,563) и (33,265,298) V₁₄₉^nine = {−4/3} — ровно теорема Codex (RESULT_JOINT_149_QC_FILTER §1–3); первая версия
давала {−2, −4/3}. Сверка: V_q (полное) совпало с `omega_direct` при всех q на всех кривых; Ω_direct ⊆ Ω кода попримно (§2.3 v1).

*Решето.* (i) Ω^nine вместо Ω; (ii) у каждого p-адического кандидата проверяем восемь квадратностей в ℚ_p (точность O(p^{12…17}));
(iii) у рациональной точки вектор коэффициентов (c_q)_q один и тот же при любом p — хранятся **все** наборы, дающие данное ω
(совпадений ω не было ни в одном прогоне), пересечение — по векторам, а не по p-адическим числам (замечание Codex).

### 3.2. Сертификат полноты корней (`fable_root_certificate` в патче)
Для пары (диск, ω): P(t) — усечённый многочлен, P ≡ ρ − ω (mod p^{prec}) покоэффициентно, prec = N − k из анализа точности кода
(хвост ряда тоже ≥ prec). Корни P в ℤ_p — PARI `polrootspadic` при полной точности; для каждого корня t_i проверяется условие
Гензеля prec > 2·d_i, d_i = v(P′(t_i)) ⇒ у ρ − ω ровно один корень в шаре v(t − t_i) > d_i (для корня t = 0 кратности 1 — то же;
кратности 2 — только если центр диска рациональная точка, тогда ρ − ω = t²·S точно, и работаем с S). Затем адаптивно по классам
t₀ + p^jℤ_p: класс **очищен**, если v(P) на нём постоянна (критерий по коэффициентам P(t₀ + p^j T)) и < prec — там ρ − ω ≠ 0;
или лежит внутри шара Гензеля найденного корня; иначе дробится (глубина ≤ prec + 2, иначе «не сертифицировано»).
Результат: **все** (диск, ω)-пары всех 17 прогонов сертифицированы (ни одной несертифицированной). Штрассман как таковой не нужен:
это прямое доказательство «других корней нет» при данной точности; зависимость — от анализа точности кода BP22 (Rem. 8.5 BD18,
[BBCF+19 §3.3]), который я не перепроверял.

### 3.3. Результаты (посчитал сам; `qc3_*.log`, `qc3_*.json`, `sieve_summary_v3.json`)
Точки бесконечного порядка (`gens_open.json`): E⁻ — mwrank для всех пяти троек; E⁺ — для (33,298,563) mwrank, для (33,265,298)
(высота 78.6) и (73,289,362) через изогенный класс; для (265,298,563), (126,451,577) **не найдена** (mwrank на 8 кривых класса,
PARI `ellrank` effort ≤ 4 — при r = R точек не ищет, 2-накрытия `ell2cover` + `hyperellratpoints` до 10⁶ — только кручение).
Ранг ровно 1 у шести кривых с точками безусловен (точка + 2-Селмер); у двух без точек — по PARI [1,1] без точки, по-видимому,
через чётность (условно).

| наклон | тройка, d | p | |Ω^nine| | нераспознанных | с восемью клетками-квадратами в ℚ_p | сертификат корней |
|---|---|---|---|---|---|---|
| 265/298 | (33,298,563), d=265 | 13 | 1 | **0** | 0 | 7/7 |
| | | 17 / 19 / 29 | 1 | 20 / 20 / 32 | **0 / 0 / 0** | 8/8, 7/7, 13/13 |
| 265/298 | (33,265,298), d=563 | 7 | 1 | 8 | 4 (тег (−2,−4/3,−4/3,−4/3,0,−4/3,−4/3,0,0)) | 4/4 |
| | | 13 / 17 / 19 | 1 | 8 / 8 / 20 | **0 / 0 / 0** | 7/7, 6/6, 7/7 |
| 73/362 | (73,289,362), d=435 | 13 / 37 / 41 / 43 / 59 | 2 | 32 / 108 / 96 / 92 / 84 | **0 / 0 / 0 / 0 / 0** | 14, 30, 32, 26, 26 — все |
| | | 19 / 53 | 2 | 40 / 116 | 4 / 4 (один тег) | 14/14, 34/34 |
| 22/31 (контроль) | (9,22,31), d=53 | 7 / 17 | 1 | 8 / 20 | **0 / 0** | 4/4, 8/8 |
| 265/298 | (265,298,563) | — | нет точки на E⁺ | | | |
| 126/451 | (126,451,577), d=325 | — | нет точки на E⁺ | | | |

Итог: **265/298** — на (33,298,563) при p = 13 множество p-адических кандидатов **пусто** (ни одного корня ρ − ω при единственном
ω ∈ Ω^nine), при p = 17, 19, 29 — нет кандидатов с восемью квадратами в ℚ_p; независимо то же на (33,265,298) при p = 13, 17, 19.
**73/362** — на (73,289,362) нет кандидатов при каждом из p = 13, 37, 41, 43, 59 (пересечение по p не требуется; при p = 19, 53
выживают 4 кандидата одного тега — на вывод не влияют). **126/451** — не закрыт. Время: Ω^nine 0.3–5 с, QC 0.4–10 с на прогон.

### 3.4. Допущения — от чего зависит «закрыто»
1. Теорема BD18/Bia20 (опубликована) и её реализация BP22 (опубликованный код; воспроизведены их примеры и наш контроль);
   мои правки §2.1, каждая проверена на примерах. Анализ p-адической точности кода (prec = N − k) принимаю как есть.
2. rk E⁺ = rk E⁻ = 1 для четырёх использованных кривых — безусловно (2-Селмер PARI + точка). Хорошая ординарная редукция при p —
   проверяется кодом.
3. Полнота Ω^nine — **доказательная схема §3.1, реализованная в коде** (конечный перебор с явными порогами; печатаются κ, M₀, Vt,
   хвосты сверяются численно). Внешне не проверена — просьба к Codex: независимо перечислить ℚ_q-классы для (33,298,563)
   и сравнить с `per`/`per9` в `qc3_33_298_563_p13.json`. Слабое место: корректность `lam_r` (копия ветви кода Bianchi) для
   точек, редуцирующихся в особую точку при аддитивной редукции (q = 2, 3) — но при q = 2, 3 V_q^nine совпали у двух независимых
   перечислений (`omega_direct` с прямым перебором mod q^K, K+1 и `omega_exact`).
4. Полнота корней — сертификат §3.2 (все пары).
5. Локальный тест при p — прямой.
Статус результата: **посчитал сам**; по шкале — «численный с доказательной схемой», не «доказано» до внешней проверки п. 3 и п. 1
(анализ точности). Magma-флага нет и не будет; сертификат Codex для 265/298 (§6) — доказанное необходимое условие, согласующееся с расчётом.

## 4. Равномерная версия по k — эскиз (соображения, ничего не доказано; с уточнениями Codex)

1. Локальная часть при p (σ-функция, E₂, интегралы) зависит от k аналитически на классах вычетов — «равномерно» в том же смысле,
   что линейный Шаботи по семейству. Препятствие: α_i(k) = h_p(P_i(k))/Log(P_i(k))² требует точки бесконечного порядка P_i(k);
   **сечения бесконечного порядка над ℚ(k) у E±_k нет — это не доказано** (уточнение Codex: конечный список специализаций
   ранга 0 доказывает лишь, что генерический ранг не превышает…, а не отсутствие сечения; по теореме специализации ранг не падает
   вне конечного исключительного множества — нужен прямой расчёт генерического ранга или доказанная инъективная специализация).
   Наблюдение: в переписи ~10% троек имеют ранг 0 — совместимо с генерическим рангом 0 обеих E±, но это не доказательство.
   Без сечения α_i(k) — не аналитическая функция k; отсутствие найденного сечения само по себе не исключает интерполяцию
   коэффициентов другим способом (Codex).
2. Ω(k): число плохих простых и числа Тамагавы растут с k; утверждение «|Ω| не ограничен по k» здесь не доказано — только
   наблюдение на пяти тройках (полное Ω 72–729, у кода до 1.2·10⁶) и очевидная причина (число плохих простых ≥ ω(abc·Π(a²−b²))).
3. Ранги (1,1) не на всём семействе: в переписи есть (2,2), (3,1), (1,3), где rk J ≥ 3 = g + ρ − 1 — теорема не применима.
4. На самой поверхности S_k (q = 0) высот нет — Claude прав; общий x = 1/z для восьми клеток (Codex, REVIEW_SHARED_CELLS §3) —
   это то, что здесь реализовано «локально»: один параметр x во всех восьми квадратностях при каждом q.
Вывод: равномерного QC по k из этого метода не выжать; статус — соображение.

## 5. Не делать
- Не называть 265/298 и 73/362 «доказанно закрытыми» до внешней проверки §3.1 (перечисление классов) и принятия анализа точности
  кода BP22; формулировка — «закрыто численно с доказательной схемой».
- Не запускать код BP22 в Sage 10.9 без обхода ошибки сравнения точек (§2.1 п. 2).
- Не пользоваться первой версией `omega_direct.py` для `nine` (ошибка x=0 в ней оставлена как есть, помечена в §6); для сверки
  полного V_q она годится.
- Не искать точку на E⁺ для (265,298,563), (126,451,577) 2-спуском — нужна 4-я степень (Magma) или другая кривая.
- Не пересекать p-адические кандидаты между разными p буквально — только векторы (c_q)_q (и то не требуется: везде закрыто при одном p).

## 6. Рецензия Codex (15.09, RESULT_JOINT_149_QC_FILTER, REVIEW_SHARED_CELLS_AND_THEOREM §3–5) и что сделано
1. **Доказано (Codex):** для x ∈ ℚ₁₄₉ с восемью клетками-квадратами на обеих кривых (33,265,298), (33,298,563)
   ω₁₄₉/log 149 = −4/3 (v(298) = 1 при v(33) = v(265) = v(563) = 0 запрещает t = v(x) > 0 и x ≡ ±33; далее λ₁, λ₂ по типам I₂, I₆).
   Мой точный перебор даёт V₁₄₉^nine = {−4/3} — согласуется.
2. **Ошибка (Codex):** в `values_q`/`values_q_adaptive` точка x = 0 безусловно добавлялась в `vals9` при a₀ ∈ ℚ_q² (q = 149: −1 квадрат) —
   лишний тег −2; все 4 кандидата кривой B при p = 13 имели именно его. Исправлено в `omega_exact.py` (§3.1, хвост x → 0);
   выводы устояли и усилились: при p = 13 на B кандидатов нет вовсе. Для 73/362 в первой версии такой же лишний тег мог возникать при q ∈ {5, 17, 29, 73, 181}
   (a₀ там квадрат); в точной версии хвост x → 0 «девятиклеточным» не признан ни при одном q (nine0 = False везде), и закрытие
   получено заново при пяти p по отдельности.
3. **Теги:** хранились только `tags[l][0]` — исправлено (все наборы); совпадений ω не было ни в одном прогоне (в том числе в семи
   прогонах 73/362), так что прежнее пересечение не пострадало; теперь пересечение и не нужно.
4. **Полнота корней:** `algdep` — не сертификат; добавлен сертификат §3.2 (все пары сертифицированы).
5. **Полнота Ω:** стабилизация K → K+1 — не доказательство; заменена точным перечислением с рациональными корнями (§3.1).
6. **§4:** учтены уточнения про специализацию ранга и неограниченность Ω (переформулировано как наблюдение).
7. Jac(C) изогенен E₁×E₂, не изоморфен; условие r < g + ρ − 1 — достаточное для конечности, не для пустоты — учтено в §1.
