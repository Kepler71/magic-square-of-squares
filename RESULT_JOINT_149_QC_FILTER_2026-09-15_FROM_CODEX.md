СТАТУС: результат — точное локальное ограничение и найденная ошибка фильтра; закрытие наклона пока условно
ДЛЯ: Claude, Fable, пользователь
ИТОГ: для наклона 265/298 у двух рабочих кривых коэффициент ω_149/log_p(149) обязан быть −4/3; код ошибочно добавляет −2 из x=0; все четыре записанных кандидата второй кривой имеют запрещённый тег
ОТМЕНЯЕТ: допустимость тега −2 при 149 для полного квадрата на этих двух кривых; не отменяет Ω для самой вспомогательной кривой
ПРОВЕРЕНО: Codex — доказательство для всех Q_149-точек с полным подъёмом, Sage-контроль моделей и образцов; полнота QC-списков не доказана
ЧИТАЛ: сессия Claude a51edd4e… (релевантные текстовые сообщения), FABLE_QC_BIELLIPTIC, omega_direct.py, qc_run2.py, два qc2-журнала и JSON; проверка не слепая
ЧИТАТЬ ДАЛЬШЕ, ЕСЛИ: нужно исправить фильтр и довести сертификат наклона 265/298

# Совместимость клеток и локальная высота при 149

15 сентября 2026. Файлы, к которым относится вывод:

- `qc_bielliptic/fable/qc2_33_265_298_p7.json` — кривая A;
- `qc_bielliptic/fable/qc2_33_298_563_p13.json` — кривая B.

Обе кривые происходят из одного наклона 265/298. Параметр у них один:
$x=1/z$, восемь нецентральных клеток равны

$$1\pm33/x,\quad1\pm265/x,\quad1\pm298/x,\quad1\pm563/x.\tag{1}$$

В нормировке кода коэффициент локальной поправки для
$C_{a,b,c}:w^2=(x^2-a^2)(x^2-b^2)(x^2-c^2)$:

$$\omega_\ell(x)/\log_p\ell=-\lambda_{1,\ell}/\log_p\ell
 +\lambda_{2,\ell}/\log_p\ell-2v_\ell(x).\tag{2}$$

Здесь p — простое QC, отличное от $\ell$, а $\lambda$ имеют именно
нормировку кода Bianchi/Fable (вдвое классической локальной высоты,
если последняя нормирована как $\tfrac12\log|x|$ возле бесконечности).
Ниже обозначаем рациональный коэффициент $\lambda/\log_p\ell$ через L.

## 1. Утверждение

**Теорема.** Пусть $x\in\mathbb Q_{149}^{\times}$ и все восемь клеток
(1) — ненулевые квадраты в $\mathbb Q_{149}$. Тогда для обеих троек
$(33,265,298)$ и $(33,298,563)$

$$\boxed{\omega_{149}(x)/\log_p149=-4/3.}\tag{3}$$

Это необходимое условие полного подъёма, а не условие для всех точек
каждой вспомогательной кривой.

## 2. Доказательство: допустимые валюации x

Положим $t=v_{149}(x)$. Числа 33, 265, 563 — единицы при 149,
а $v_{149}(298)=1$.

Если $t>0$, то обе клетки $1\pm33/x$ имеют валюацию $-t$,
поэтому квадратность требует чётного t. Значит $t\ge2$.
Но тогда $1\pm298/x$ имеют валюацию $1-t$, нечётную.
Получаем противоречие. Следовательно

$$t\le0.\tag{4}$$

При $t=0$ нельзя иметь $x\equiv\pm33\pmod{149}$.
Например, при $x\equiv33$ величины $x-33$ и $x+265$ обе делятся на 149,
а их разность равна 298 и имеет валюацию ровно 1. Хотя бы одна из
этих величин имеет валюацию 1. После деления на единицу x получается
неквадратная клетка. Случай $x\equiv-33$ аналогичен.
Это рассуждение включает точные нули: они тоже запрещены условием (1).

## 3. Доказательство: вычисление локальных высот

Для каждой тройки обозначим два коэффициента-единицы через A,B,
а C=298. В обоих случаях

$$v(C)=1,\quad v(A)=v(B)=0,\quad v(A^2-B^2)=1.$$

Действительно, $265+33=298$, а $563+33=596=4\cdot149$;
соответствующие разности — единицы при 149.

Первый фактор:

$$E_1:Y^2=(X-A^2)(X-B^2)(X-C^2),\quad(X,Y)=(x^2,w).$$

Второй фактор, с $a_0=-A^2B^2C^2$:

$$E_2:Y^2=(X+A^2B^2)(X+A^2C^2)(X+B^2C^2),
\quad(X,Y)=(a_0/x^2,a_0w/x^3).$$

У этих целых моделей $v(c_4)=0$, значит они минимальны при 149.
Порядки дискриминантов равны 2 и 6 соответственно: типы $I_2,I_6$.
Первая редукция имеет узел $X\equiv A^2$, вторая — $X\equiv0$.
Эти утверждения также проверены непосредственно в Sage.

Используем стандартную формулу мультипликативной локальной высоты
в нормировке (2): возле O $L=\max(0,-v(X))$; в узле модели $I_N$
значение равно $-j(N-j)/N$, где $j=\min(v(2Y),N/2)$ для этих моделей.
При гладкой целой редукции $L=0$.
Эта же ветвь реализована в `lam_r` у Fable; источник общей QC-нормировки —
[Bianchi, 1904.04622](https://arxiv.org/abs/1904.04622).

Если $t<0$, то $v(w)=3t$. На $E_1$ имеем $L_1=-2t$.
На $E_2$ порядки координат равны $2-2t>2$ и 2. Точка в узле,
$j=2$, поэтому $L_2=-2(6-2)/6=-4/3$.
Подстановка в (2) даёт $-(-2t)-4/3-2t=-4/3$.

Если $t=0$, по разделу 2 точка $E_1$ не лежит в узле,
все три множителя уравнения C — единицы, $v(w)=0$ и $L_1=0$.
На $E_2$ обе координаты имеют порядок 2, снова $L_2=-4/3$.
Подстановка в (2) даёт тот же результат. Все случаи (4) рассмотрены. ∎

Переход к глобальным минимальным моделям не меняет этих коэффициентов:
обе исходные модели уже минимальны при 149, локальный множитель
масштабирования — единица. Знак w также ничего не меняет.

## 4. Конкретная ошибка в коде

В `omega_direct.py`, в **обеих** функциях `values_q` и
`values_q_adaptive`, для точки x=0 выполняется безусловное добавление
в `vals9`, если $a_0$ — квадрат в $\mathbb Q_q$.
Для полной девятки это неверно: при x=0 выражения (1) не определены,
а возможность предельного подъёма надо проверять отдельно.

При q=149 число $a_0=-(abc)^2$ — локальный квадрат, так как −1 — квадрат.
Поэтому ветка срабатывает и добавляет коэффициент −2.
Но раздел 2 доказывает, что даже в проколотой окрестности x=0
полных локальных подъёмов нет. Значение −2 допустимо для
**вспомогательной C**, но не для условия `nine`.

Исправление для этого наклона: в `per9[149]` допустимо только −4/3.
Общее исправление: добавлять x=0 в `vals9` только с доказанным подъёмом
на полную кривую, включая проверку всех дополнительных радикалов.
Удалять x=0 из обычного `vals` нельзя: для C это настоящая точка.

Это ошибка в сторону **лишних кандидатов**, сама по себе она не
пропускает решения. Она объясняет все оставшиеся четыре кандидата
записанного расчёта B. Другие вопросы полноты Ω остаются отдельно.

## 5. Что происходит с записанными QC-кандидатами

Точный разбор двух JSON (снимки входят в архив):

| кривая / простое QC | проходят `nine_local` | тег 149 = −4/3 | тег 149 = −2 |
|---|---:|---:|---:|
| (33,265,298), p=7 | 24 | 12 | 12 |
| (33,298,563), p=13 | 4 | 0 | 4 |

Все четыре нераспознанных кандидата B противоречат (3).
Записанные распознанные рациональные точки B имеют x=∞,
то есть z=0 — все клетки равны 1, вырождение.

**Условный вывод:** если QC-список B содержит все рациональные точки
с полным подъёмом, наклон 265/298 исключён. Сейчас эту предпосылку
нельзя повысить до доказанной: Fable сам помечает полноту Ω_direct как
«численный контроль + набросок»; принудительная остановка адаптивного
дробления и конечные диапазоны требуют отдельного доказательства.
Полнота вычисления корней p-адических функций также должна быть
сертифицирована, а не выводиться из успешного `algdep`.

Таким образом, арифметическое препятствие (3) доказано; окончательное
закрытие наклона пока зависит от завершения сертификата QC.

## 6. Дополнительные замечания к фильтру

- Стабилизация при K и K+1 не доказывает отсутствие нового значения
  глубже. Необходимы сертифицированные листья, доказанные хвосты или
  гарантированное надмножество локальных значений.
- `qc_run2.py` сохраняет только `tags[l][0]`. Если несколько наборов
  коэффициентов дают одинаковое ω с текущей точностью, надо хранить
  **все** наборы. Иначе дальнейшее пересечение может ошибочно исключить
  допустимый вариант. В двух проверенных журналах совпадений было 0:
  их нынешний результат эта потенциальная ошибка не затронула.
- Нельзя буквально пересекать числа из $\mathbb Q_7$ и $\mathbb Q_{13}$.
  Сопоставлять надо общие рациональные данные: векторы локальных
  коэффициентов, образы глобальных точек или классы решета Морделла–Вейля.

**Следующий шаг для Claude/Fable:** применить доказанный фильтр (3),
затем сертифицировать покрытие Ω и полноту корней для B при p=13.
Это уже конкретная конечная задача, которая может превратить
нынешний условный вывод в доказанное закрытие наклона.
