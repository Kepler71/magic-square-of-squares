# Полный 2-спуск над этальной алгеброй для кривых с ОДНОЙ рациональной точкой порядка 2

Claude, 2026-09-12. Файлы: `descent/ek_descent_partial.sage` (реализация), `descent/test_partial.sage` (контроль 1),
`descent/cross_check_partial.sage` (контроль 2 + боевые кривые), `descent/run_partial_sections.sage` (боевой прогон).

**Краткий итог: реализация работает и полностью прошла контроли, но улучшения границ ранга для (89,23,65) и (79,47,65) НЕ дала.**
Для (89,23,65) получено `rank ≤ 7` (уже известно `≤ 3` из полного спуска + Касселса–Тейта), для (79,47,65) — `rank ≤ 5`
(ровно то же, что известно). Отрицательный результат.

---

## 1. Что реализовано

`E: y² = f(x) = x(x² + a x + b)` над числовым полем `k`, `Δ = a² − 4b` **не** квадрат в `k`
(т.е. `E(k)[2] = {O, T}`, `T = (0,0)`).

* Этальная алгебра `L = k[x]/f(x) = k × K`, `K = k(√Δ) = k[t]/(t² + at + b)`, `θ` — корень.
  `H¹(k, E[2]) = ker(N_{L/k}: L*/L*² → k*/k*²)`, поскольку `E[2] = ker(N: Res_{L/k} μ₂ → μ₂)`
  и `H⁰(Res μ₂) → H⁰(μ₂)` сюръективно.
* **Отображение Куммера** `μ: E(k)/2E(k) ↪ L*/L*²`, `P = (x,y) ↦ (x, x − θ)`;
  для `T = (0,0)` первая координата заменяется на `f'(0) = b`, то есть **`μ(T) = (b, −θ)`**.
  Условие нормы: `N_{L/k}(μ(P)) = x·N_{K/k}(x − θ) = f(x) = y²`.
* **Глобальная группа** `k(S,2) × K(S_K,2)`, `S` — простые над `2·b·Δ` (все плохие простые и простые над 2),
  `S_K` — все простые `K` над теми же рациональными простыми. Используется `selmer_space` (учитывает классовую группу
  и единицы; возвращает и обратное отображение `to_KSp`, нужное для условия нормы).
  Поле `K` строится как `k(√d)`, где `d` — уменьшенный представитель `Δ` в `k(S,2)` (иначе определяющий многочлен
  огромный); `θ = (−a + s√d)/2`, `s² = Δ/d`.
* **Условие нормы** `α·N_{K/k}(β) ∈ k*²` накладывается явно (оно НЕ следует из локальных условий в `S`:
  именно оно работает в неразветвлённых местах вне `S`).
* **Локальные условия** во всех `v ∈ S` и вещественных: образ `μ_v`. Теоретические размерности
  `dim_F₂ E(k_v)/2E(k_v)`:
  `dim E(k_v)[2]` (конечные `v ∤ 2`), `dim E(k_v)[2] + [k_v:ℚ₂]` (`v | 2`), `dim E(ℝ)[2] − 1` (вещественные).
  Локальные образы набираются `μ(T)` + случайными локальными точками **до точного достижения теоретической
  размерности**; недобор — `RuntimeError` (ошибка выполнения, а не математический ответ), как в `ek_descent.sage`.
  Для мест, где `Δ` — квадрат в `k_v` (тогда `K ⊗ k_v = k_v × k_v`), выборка ведётся не только около `x = 0`,
  но и около приближённых (mod `P⁴⁰`) корней `x² + ax + b`, найденных Ньютоном по `y² = Δ/π^{v(Δ)}` —
  без этого некоторые места не набираются.
* Вне `S` условие автоматично: класс из `L(S,2)` неразветвлён, а при хорошей редукции и `v ∤ 2`
  `Im μ_v = H¹_ur(k_v, E[2])` (совпадение порядков).
* Локальные квадратичные классы: `v_P(·) mod 2` + `ideallog` по модулю `P^(2e+1)` (обобщение `LocalSq` из
  `ek_descent.sage` на произвольные абсолютные поля, здесь степени 4). Вещественные знаки — **точно**, через
  вложения в `AA` (алгебраические вещественные), а не численно.
* Итог: `rank E(k) ≤ dim Sel² − dim E(k)[2] = dim Sel² − 1`.

## 2. Контроль 1: кривые с известным точным рангом над `k = ℚ(√d)`

`E₀: y² = x(x² + ax + b)` над `ℚ`, `k = ℚ(√d)`; `rank E₀(k) = rank E₀(ℚ) + rank E₀^{(d)}(ℚ)`,
обе цифры — PARI `ellrank` с требованием `lo == hi` (твист: `y² = x(x² + adx + bd²)`).
Прогон: `sage test_partial.sage` (сиды 1, 2 — совпадают; с `PROOF=1`, т.е. **без GRH**, — тоже совпадают).

| a | b | d | rank E(k) (PARI, точно) | dim Sel² | моя граница | граница точна |
|---|---|---|---|---|---|---|
| 1 | -3 | 5 | 0 | 1 | 0 | да |
| 1 | -3 | -1 | 1 | 2 | 1 | да |
| 1 | -3 | 2 | 1 | 2 | 1 | да |
| 1 | 1 | 5 | 1 | 2 | 1 | да |
| 1 | 1 | -7 | 0 | 1 | 0 | да |
| -2 | -4 | 3 | 1 | 2 | 1 | да |
| 3 | 1 | -3 | 1 | 2 | 1 | да |
| -5 | 5 | 2 | 1 | 2 | 1 | да |
| 2 | -7 | 13 | 0 | 1 | 0 | да |
| 1 | -5 | -1 | 0 | 1 | 0 | да |
| 5 | 5 | 6 | 1 | 2 | 1 | да |
| 7 | 5 | 17 | 2 | 3 | 2 | да |
| -3 | -11 | 2 | 1 | 2 | 1 | да |
| 6 | 7 | -1 | 1 | 2 | 1 | да |
| 9 | 2 | 3 | 2 | 3 | 2 | да |
| -4 | 2 | 6 | 1 | 2 | 1 | да |

**16 кривых из 16: граница совпала с истинным рангом** (в частности, ни разу не оказалась ниже — это исключает
завышение локальных образов; и ни разу не завысила — это косвенно исключает пропуск условий).
Четыре случая `(3,1,5)`, `(4,-1,5)`, `(1,-6,5)`, `(-1,-6,2)` пропущены: там `Δ` — квадрат в `k` (нужен полный спуск).

## 3. Контроль 2: сверка с изогенным спуском (`ek_descent.sage`) на кривых с полным 2-кручением

Для `E` с полным 2-кручением `Δ` всегда квадрат, поэтому алгебраический спуск применяется не к самой `E`,
а к 2-изогенной `E' = E/⟨(0,0)⟩` (у неё `Δ' = 16b` — не квадрат). `rank E'(k) = rank E(k)`.
Проверяется точная последовательность `Sel^{φ̂}(E') → Sel²(E') → Sel^{φ}(E)` с ядром первой стрелки `ℤ/2`:
`dim Sel²(E') ≤ dim Sel^{φ}(E) + dim Sel^{φ̂}(E') − 1`. Дополнительно Sage проверяет, что
`E.isogeny(E(0,0)).codomain()` изоморфна моей `E'` (все 12 случаев — True).

| сечение | i | Sel^φ(E) | Sel^φ̂(E') | граница изогенного спуска | dim Sel²(E') | моя граница | неравенство |
|---|---|---|---|---|---|---|---|
| s15 (109,229,65) | 1 | 5 | 0 | 3 | 4 | 3 | OK (4 ≤ 4) |
| s15 | 2 | 4 | 3 | 5 | 6 | 5 | OK (6 ≤ 6) |
| s15 | 3 | 4 | 1 | 3 | 4 | 3 | OK (4 ≤ 4) |
| n17 (169,409,34) | 1 | 5 | 2 | 5 | 6 | 5 | OK (6 ≤ 6) |
| n17 | 2 | 3 | 4 | 5 | 6 | 5 | OK (6 ≤ 6) |
| n17 | 3 | 4 | 3 | 5 | 6 | 5 | OK (6 ≤ 6) |

Полный 2-спуск `ek_descent.sage`: s15 `dim Sel² = 5 → rank ≤ 3`, n17 `dim Sel² = 7 → rank ≤ 5`.
Мои границы (3 и 5) **совпали** с ними — противоречий нет.
Истинные ранги известны (s15: `rank = 1`, доказано через Magma/Richelot; n17: `rank = 1`) — границы их не нарушают.

## 4. Контроль 3: правило `μ(T) = (b, −θ)`

Во всех 28 прогонах (16 контрольных + 12 боевых/сверочных) проверено:

* `N_{K/k}(θ) = b` — да;
* образ кручения лежит в вычисленной группе Селмера (`torsion_in_Sel`) — да;
* **независимый контроль правила**: `μ(T) = 1` тогда и только тогда, когда `T ∈ 2E(k)`
  (последнее проверяется Sage: `E(0,0).division_points(2) ≠ ∅`) — совпало во всех случаях.
  Нетривиальный случай: `a = 3, b = 1, k = ℚ(√−3)` — там `T = 2Q` действительно, и `μ(T) = 1`
  (это не ошибка: `μ` инъективно на `E(k)/2E(k)`, а оценка `rank ≤ dim Sel² − dim E(k)[2]` от этого не зависит);
* условие нормы `α·N(β) ∈ k*²` проверено для базиса каждой найденной группы Селмера прямой проверкой `is_square()` в `k` — да;
* локальные образы во всех местах набраны **ровно** до теоретической размерности (иначе прогон падает).

## 5. Боевые вычисления

Для сечения строится `E₁` и корни `e₁,e₂,e₃` функцией `E1_roots` (`descent/e1roots.py`); сдвиг `x → x + e_i` даёт
`y² = x(x² + a x + b)`, `a = 2e_i − e_j − e_k`, `b = (e_i−e_j)(e_i−e_k)`; изогенная кривая
`E'_i: y² = x(x² − 2a x + a² − 4b)`, `Δ'_i = 16 b` (не квадрат ни при каком `i` — проверено).
Сама `E₁` через алгебру не считается: у неё `Δ = (e_j−e_k)²` — квадрат (для неё работает полный спуск из `ek_descent.sage`).

### (89,23,65): A=2377, C=6073, D=910, k = ℚ(√462), корни [5526430, 236600·r, −5526430]

| i | Sel^φ(E₁) | Sel^φ̂(E'_i) | изогенная граница | **dim Sel²(E'_i)** | **граница rank E₁(k)** |
|---|---|---|---|---|---|
| 1 | 7 | 4 | 9 | 8 | **7** |
| 2 | 6 | 5 | 9 | 8 | **7** |
| 3 | 6 | 5 | 9 | 8 | **7** |

Известно ранее: полный 2-спуск `dim Sel²(E₁) = 11 → rank ≤ 9`, спаривание Касселса–Тейта ранга 6 → **`rank ≤ 3`**.
Новый результат `rank ≤ 7` **строго лучше изогенного спуска** (9) и лучше сырого полного 2-спуска (9),
но **хуже** уже имеющегося `≤ 3`. **Цель (граница < 3) не достигнута.**

### (79,47,65): A=3217, C=5233, D=455, k = ℚ(√7), корни [2381015, 709800·r, −2381015]

| i | Sel^φ(E₁) | Sel^φ̂(E'_i) | изогенная граница | **dim Sel²(E'_i)** | **граница rank E₁(k)** |
|---|---|---|---|---|---|
| 1 | 6 | 1 | 5 | 6 | **5** |
| 2 | 4 | 3 | 5 | 6 | **5** |
| 3 | 5 | 2 | 5 | 6 | **5** |

Известно ранее: полный 2-спуск `dim Sel²(E₁) = 7 → rank ≤ 5`, CTP ранга 0 (ничего не срезает).
Новый результат — ровно та же граница `≤ 5`. **Цель (граница < 5) не достигнута.**

### Наблюдение

В s15, n17, n79 везде `dim Sel²(E') = Sel^φ(E) + Sel^φ̂(E') − 1`, то есть связующее отображение
`Sel²(E') → Sel^φ(E)` сюръективно и алгебраический спуск не даёт ничего сверх изогенного.
Единственное исключение — n89: там `8 < 7 + 4 − 1 = 10` (и `< 6 + 5 − 1 = 10`), спуск строго сильнее изогенного,
но всё равно слабее связки «полный спуск + Касселс–Тейт» на самой `E₁`.
Статус этих утверждений: численный расчёт (ПО: мой код + Sage/PARI), не рецензировано.

## 6. Условность GRH

**Её нет.** Все боевые результаты (обе секции, все `i`) перепроверены с `proof.number_field(True)`
(прогон `descent/run_partial_noGRH.sage`, логи `descent/partial_noGRH_n79.log`, `descent/partial_noGRH_n89.log`):
те же `dim Sel²` — 8 и 6.
Контроль 1 также перепроверен с `PROOF=1` — те же 16 строк. По умолчанию в `ek_descent_partial.sage` стоит
`proof.number_field(False)` (для скорости на возможных больших полях `K` степени 4) — при повторении
боевых расчётов это допущение снималось.

Поля `K` здесь оказались небольшими: для n89 `x⁴ − 25216x² + 136515856` и `x⁴ − 922x² + 214369`,
для n79 `x⁴ − 83728x² + 27384289` и `x⁴ − 12x² + 64`; классовые группы считаются за секунды и без GRH.
Это заслуга уменьшения `Δ` до представителя в `k(S,2)` перед построением `K`.

## 7. Что осталось / что можно попробовать дальше

1. **Спаривание Касселса–Тейта на `Sel²(E')`.** Для n89 `dim Sel²(E') = 8`; если CTP на `E'` имеет ранг ≥ 6,
   граница упадёт до ≤ 1. `ctp.sage` написан для кривых с полным 2-кручением (квадратичная форма на `k(S,2)²`),
   для этальной алгебры `k × K` его нужно обобщать (локальные символы над `K_w`, спаривание на `ker N`).
   Это наиболее перспективный следующий шаг именно потому, что на `E₁` CTP уже срезал 11 → 3.
2. **4-спуск** над `k` (Sage не умеет; Magma `FourDescent` — только над ℚ).
3. Для n79 ни 2-спуск, ни CTP не срезают ничего ниже 5 — там, видимо, нужен принципиально иной ход
   (Richelot/Chabauty на кривой рода 2, как для s15).
4. Поиск точек на `E₁` (нижняя граница ранга) — не делался в этой сессии; без него неизвестно, насколько
   границы 3/5 далеки от истины.

## Запуск

```bash
cd /home/kep/magicKube/descent
SEED=1 sage test_partial.sage                    # контроль 1 (PROOF=1 — без GRH)
SEC=s15 sage cross_check_partial.sage            # контроль 2 + боевой прогон (SEC ∈ s15,n17,n61,n79,n89)
SEC=n89 sage run_partial_sections.sage           # только спуск над алгеброй
```
