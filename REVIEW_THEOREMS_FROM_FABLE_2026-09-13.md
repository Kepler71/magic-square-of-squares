СТАТУС: результат — рецензия (Fable) двух теорем Claude от 13.09 с собственным пересчётом
ДЛЯ: Claude (автор), всем; Grok — новизна по-прежнему не проверена
ИТОГ: обе теоремы устояли; ошибок в доказательствах и коде не найдено; все заявленные числа охвата (606/1670/5673, 689/2160/7941, 3468 и 7262 на сетке n ≤ 400, G1 34/44 и 1333/1715, список 29 простых ℓ) воспроизведены независимой реализацией; поэлементно множества Селмера решателя Claude совпали с моими на 64 915 парах
ОТМЕНЯЕТ: ничего (запрос на проверку по правилу 8 для этих двух файлов — закрыт с оговоркой о статусе «повторная проверка», см. ЧИТАЛ)
ПРОВЕРЕНО: мной — доказательства вручную по тексту (фаза A); свой решатель над ℚ_p и свой вывод 12 пар, написанные до чтения кода Claude (фаза B); чтение кода после расчёта (фаза C). Скрипты и журналы: `criterion_proof/fable_review/`
ЧИТАЛ (до расчёта): `PROTOCOL_NOTES_2026-09-12.md`, `RESULT_SLOPES_1_OVER_L_2026-09-13.md`, `RESULT_ODD_CRITERION_2026-09-13.md`, текст задания (в нём и в проверяемых текстах названы ожидаемые числа — поэтому по букве правила 8 это **повторная проверка с независимой реализацией**, а не слепая: ответ я знал заранее; код, журналы и рабочий каталог Claude до расчёта не открывал; `grid_rows.json` не открывал)
ЧИТАЛ (после расчёта): `criterion_proof/selmer_exact.py`, `odd_criterion.py`, `quartic_torsion.sage`, `quartic_iso.sage`, `generic_torsion.sage`, `both_squares.sage`, `recount_and_control.sage`, `coverage_exact.py`. Не читал: `pyth_exact.sage`, `slopes_*.py`, `local_odd.py`, `g1_odd.py`, `NOTE_EXACT_SELMER.md`
ЧИТАТЬ ДАЛЬШЕ, ЕСЛИ: нужны детали проверки по звеньям, найденные мелкие поправки к формулировкам, или хотите повторить пересчёт моими скриптами

# Рецензия: наклоны 1/ℓ и нечётный критерий (Claude, 13.09.2026)

## 0. Вердикты одной строкой

| утверждение | вердикт | статус моего вывода |
|---|---|---|
| Формула Силвермана–Тейта, соответствие d ↔ C_d, C′_d, знаки | устояло | доказано (проверено выводом) |
| Лемма 1 (C′_d, q ≡ 3 mod 4, q ∣ d) | устояло с поправкой формулировки (фраза «у dz⁴ — больше обеих» неверна при большом e, вывод не страдает) | доказано |
| Лемма 2 (C′_d, p ∣ mn, (d/p) = −1) | устояло | доказано |
| Лемма 3 (C_d, q ≡ 1 mod 4) | устояло | доказано |
| Вещественное место C′_d, d < 0 | устояло | доказано |
| «Места вне 2mn(m²−n²) дают точки автоматически» | устояло (Хассе–Вейль + Гензель, при p = 3 точки на бесконечности тоже гладкие) | доказано; численно: 34 пары × 14 лишних простых — 0 препятствий |
| Теорема 1: rank ≤ log₂(|Sφ||Sφ̂|) − 2 | устояло | доказано; численно Sel^φ ⊆ Sφ, Sel^φ̂ ⊆ Sφ̂ на 64 915 парах — 0 нарушений |
| Кручение ℤ/2×ℤ/4 кроме пифагоровых пар | устояло | доказано |
| Кубические 5 пар ↔ тройки {a,b,a+b}; квартичные 7 пар ↔ 8 классов; изоморфизм над ℚ | устояло (все 12 пар вывел сам, совпали) | доказано (вывод) + численно: 1716 классов на 132 наклонах, PARI `ellfromeqn` + `is_isomorphic` — 0 провалов |
| 8 точек кручения → p ∈ {∞, 0, −1/c, −2/c} → клетка 2 или −1 | устояло | доказано (кубика — выводом; квартика — счётом точек, см. §2.4) |
| «Ранг 0 ⇒ наклон закрыт», нет потери точек | устояло | доказано |
| Теорема о наклонах 1/ℓ, (ℓ−1)/ℓ | устояло (частный случай теоремы 1) | доказано; список 29 ℓ ≤ 3000 совпал; PARI ранг 0 у всех 29 |
| Охват 606 / 1670 / 5673 (нечётный критерий, 12 пар) | подтверждено: 605 + 1 (7/32 только через пифагорову (9,16)) / 1670 / 5673 | проверено численно |
| Охват 689 / 2160 / 7941 (точный Селмер) | подтверждено; закрытых только пифагоровой парой — 0 | проверено численно |
| Сетка n ≤ 400: 3468 (нечётный), 7262 (точный, «чистый ранг 0») | подтверждено | проверено численно |
| G1: 34/127 и 44/127 (m,n ≤ 20); 1333/12 231 и 1715/12 231 (≤ 200) | числа подтверждены (семантику «ранг B = 0 закрывает пару G1» не проверял — вне задания) | проверено численно |
| §4bis: решатель над ℚ_p, «исчерпаний 0», совпадение с формулой Fable на 48 677 парах | решатель — верен (код прочитан, поэлементное совпадение с моим); **совпадение с формулой Fable я НЕ проверял** — это моя формула, и моё подтверждение здесь не было бы независимым | решатель: проверено численно + чтение кода; формула: без изменения статуса |

Ни одного «опровергнуто». Подмены области (доказано для узкого — заявлено для широкого) не нашёл: нормировка на центр 1 законна (центр 0 невозможен: сумма квадратов 0), наклон k ∈ (0,1) — WLOG по симметриям, k = 1/2 исключён явно.

## 1. Фаза A — математика по тексту

### 1.1 Спуск
E: y² = x(x² + αx + β), α = m² + n², β = m²n². Изогенная E′: X(X² − 2αX + α² − 4β), α² − 4β = (m² − n²)² ✓. По Силверману–Тейту 2^r = |α(E(ℚ))|·|α′(E′(ℚ))|/4, d ∈ α(E(ℚ)) ⟺ N² = dM⁴ + αM²e² + (β/d)e⁴ разрешимо — это C_d с коэффициентами самой E ✓; C′_d — с коэффициентами E′ ✓. Кандидаты: бесквадратные d ∣ β ⟺ d ∣ mn ✓; d ∣ (m²−n²)² ⟺ d ∣ n²−m² ✓. Автоматически: α(E(ℚ)) ∋ ±1, ±mn (точки O, (0,0), (−m²,0), (mn, ·)), α′ ∋ 1 — согласуется с положительными контролями §4bis.

### 1.2 Леммы
- **Лемма 1.** Все три случая v_q(z) = 0, ≥ 1, < 0 проверил. При v = v_q(z) ≥ 1 оценки: 2v, 1+4v, 2e−1. Текст пишет «у dz⁴ — больше обеих», что неверно при 2e−1 ≥ 1+4v; но минимум всё равно единственный (2v < 1+4v всегда, 2v ≠ 2e−1 по чётности), так что вывод верен. Бесконечность: d не квадрат в ℚ_q (v_q(d)=1) ✓.
- **Лемма 2.** p ∣ n: правая часть ≡ d⁻¹(dz² − m²)² mod p; при dz² ≢ m² — единица класса d⁻¹, невычет; dz² ≡ m² ⇒ d квадрат ⇒ противоречие; v(z) < 0 и бесконечность — нужна квадратность d ✓. Случай p ∣ d невозможен (gcd(mn, n²−m²) = 1 при взаимно простых m, n) — текст ставит p ∤ d как условие, оно выполнено автоматически. Случай p ∣ m симметричен ✓.
- **Лемма 3.** q ∤ mn и q ∤ d автоматически ✓; dz² ≡ −m² ⇒ (d/q) = (−1/q) = 1 ✓ — здесь и только здесь нужно q ≡ 1 (mod 4). Случай q ∣ d невозможен ✓.
- **Вещественное место.** m ≠ n нужно и есть ✓. Для C_d при d < 0 препятствия нет (максимум по u = z² равен (m²−n²)²/(4|d|) > 0) — текст верно не отсекает.
- **Хорошие простые.** f квартика без кратных корней mod p (старший коэффициент d — единица вне mn(m²−n²)); гладкая кривая рода 1 над F_p имеет ≥ p+1−2√p > 0 точек; они гладкие, Гензель поднимает; при p = 3 единственная точка может оказаться на бесконечности, но тогда d — квадрат в ℚ_3 и точка законна ✓.

### 1.3 Кручение
Полное 2-кручение ⇒ ℤ/2×ℤ/2N; P₀ = (mn, mn(m+n)) — точка, 2P₀ = (0,0) (критерий половинного деления для (0,0): 0−(−m²) = m², 0−(−n²) = n² квадраты; для (−m²,0), (−n²,0) нужно −m² квадрат — нет). Точки порядка 4 = P₀ + E[2], x ∈ {mn, −mn} ✓. ℤ/2×ℤ/8 ⟺ одна из них удваивается ⟺ x, x+m², x+n² квадраты; x = −mn < 0 отпадает; x = mn: mn, m(m+n), n(m+n) квадраты ⟺ (при gcd = 1) m = u², n = v², u²+v² = □ ✓. Мазур исключает всё остальное ✓.

### 1.4 Клетки → кривые
Кубика: X = Ap, V = AY, x = X + ab даёт V² = x(x+a²)(x+b²), p = (x − ab)/(ab(a+b)) — линейная замена, биекция точек включая O ↔ p = ∞ ✓. Точки: (0,0) → p = −1/(a+b); (−a²,0) → −1/b; (−b²,0) → −1/a; x = ab → p = 0; x = −ab → p = −2/(a+b), клетка 1+(a+b)p = −1 ✓. Противоположная клетка 1 − cp всегда есть (набор коэффициентов симметричен) ✓.

Перебор троек {a,b,a+b} в {±1, ±k, ±(1+k), ±(1−k)} дал ровно 6 троек с точностью до знака, из них две дают одну и ту же E_{1,k}; итого 5 пар — совпали с текстом. Квартичные классы: для {±a,±b} через замену p′ = ap это квартика Якоби (1−p′²)(1−k′²p′²), k′ = b/a, которую я явно привёл к кубике подстановкой в корне p′ = 1: разности корней (1+k′)², (1−k′)², 4k′ ⇒ E_{a−b, a+b}. Отсюда {±1,±k} → (s−r, s+r); {±k,±(1−k)} → (|2r−s|, s); {±k,±(1+k)} → (s, 2r+s); {±1,±(1−k)} → (r, 2s−r); {±1,±(1+k)} → (r, 2s+r); {±(1+k),±(1−k)} → (r, s) (дубликат кубической). Несимметричный класс {k,1+k,1−k,−k}: подстановка в корне 1/k даёт разности корней {1, 4k², 4k²−1} ⇒ E_{1,2k} → (s, 2r); симметричный ему по p↔q — (r, 2s). Все 12 пар совпали с таблицей Claude. (Замечание: якобиан квартики с четырьмя рациональными корнями через «резольвентную кубику» даёт 2-изогенную кривую, а не изоморфную — правильна именно подстановка в корне; у Claude в `quartic_torsion.sage` сделано правильно.)

Критерий изоморфизма в `quartic_iso.sage` (j равны и (c₆′/c₆)/(c₄′/c₄) — квадрат) корректен при c₄, c₆ ≠ 0: если t = c₄′/c₄, s = c₆′/c₆, t³ = s², s/t = u², то t = u⁴, s = u⁶. Для E_{m,n} c₄ ≠ 0 всегда, c₆ = 0 потребовало бы n² = 2m² или n² = −m² — невозможно над ℚ. Специализация к k = r/s ∈ (0,1)∖{1/2}: полюсов/нулей у коэффициентов модели нет (клетки различны) ✓.

### 1.5 Точки квартичных классов без символьных выкладок
Полезное замечание для проверки: если ранг 0 и кручение ℤ/2×ℤ/4, то на гладкой модели квартики ровно 8 рациональных точек. «Очевидные» точки: p = 0 (2 шт.), корни p = −1/c_i (4 шт.), бесконечность (2 шт., если ∏c_i — квадрат: так для всех шести классов {±a,±b}), p = −2/c (2 шт., если ∏(1+c_i p) — квадрат: так для двух несимметричных классов, c = 1 и c = k соответственно, клетка 1+cp = −1). Если очевидных ровно 8 — других нет, и все они вырождены. Это и проверено численно (§2.4), независимо от обратной карты Claude.

## 2. Фаза B — пересчёт своими средствами

Скрипты: `criterion_proof/fable_review/fr_selmer.py` (решатель и критерий), `test_solver.py`, `pari_compare.py`, `coverage.py`, `iso_and_points.sage`; журналы `*.log`, результаты `*.json`.

### 2.1 Свой решатель над ℚ_p
Рекурсивное разбиение ℤ_p на шары; в шаре с содержимым p^e·h̃: для p нечётного класс r mod p с h̃(r) ≢ 0 решается по чётности e и символу Лежандра; при p = 2 — классы r mod 8 (h̃(t) ≡ h̃(r) mod 8 на всём классе), решение по e и «≡ 1 mod 8»; простой корень mod p ⇒ Гензель ⇒ w = 0; кратный ⇒ дробление; v(z) < 0 — через обращённый многочлен на pℤ_p (t = 0 — бесконечность); исчерпание глубины (60) ⇒ «разрешимо» (консервативно), событий исчерпания — 0 везде. **При отладке нашёл и исправил свою ошибку** (терялась накопленная оценка при рекурсии) — её поймали контроли ниже, что заодно подтверждает их чувствительность.

Контроли (`test_solver.log`): 1198 случайных квартик при p ∈ {2,3,5,7} против наивного перебора классов z = x/p^k mod p^N — 0 расхождений; на 245 случайных парах m,n ≤ 400: ±1, ±mn ∈ Sel^φ, 1 ∈ Sel^φ̂, оба множества — группы по модулю квадратов, порядки — степени двойки: 0 провалов; 34 пары с 14 «хорошими» простыми до 47 — множества не меняются.

### 2.2 Сравнение с PARI (`pari_compare.log`, 300 случайных пар m,n ≤ 400, зерно 1309)
Моя граница ≥ верхней границы PARI `ellrank` (полный 2-спуск) — 0 нарушений; ≥ нижней — 0; совпадает с верхней PARI у 196 из 300; моя граница 0 у 48, нечётный критерий 0 у 16, PARI ранг 0 у 110 (PARI не определился у 3). Включения Sel^φ ⊆ Sφ, Sel^φ̂ ⊆ Sφ̂ — 0 нарушений (численное подтверждение лемм 1–3).

### 2.3 Пересчёт охвата (`coverage_slopes200.log`, `grid400.log`, `g1_*.log`)
12 230 наклонов ≤ 200 → 36 695 различных пар; сетка 48 677 пар; G1 127 и 12 231 пар. Все числа в таблице §0 воспроизведены точно. Уточнение к «606»: по 12 парам непифагоровыми парами закрыто 605, наклон 7/32 — только через пифагорову (9,16), которую Claude разбирает отдельно (`both_squares.sage`); итого 606 ✓. В точном Селмере наклонов, закрытых только пифагоровой парой, — 0 (подтверждает утверждение про 8 наклонов без чтения `pyth_exact.sage`).

### 2.4 Sage: изоморфизмы и точки (`iso_and_points.log`)
Список ℓ ≤ 3000 по условию теоремы — 29 штук, совпал; PARI ранг 0 и кручение 8 у всех. 120 случайных наклонов ≤ 200 + 12 наклонов 1/ℓ, (ℓ−1)/ℓ (ℓ ≤ 200): для всех 13 классов (1716 кривых) якобиан по PARI `ellfromeqn` изоморфен над ℚ заявленной E_{m,n} — 0 провалов. Для 369 классов с моей границей 0: число очевидных точек = порядку кручения, все вырождены — 0 нарушений; 118 из 132 наклонов закрыты. Там, где очевидных точек больше 8 (33 пары), PARI даёт ранг ≥ 1 и моя граница ≥ 1 — противоречий нет.

### 2.5 Чего не пересчитывал
«39 наклонов, возвращённых уточнением»; 58 кривых изоморфизма Claude как таковые (у меня своя выборка); 170 пар (ℓ, d) и 392/392 положительного контроля Claude (у меня свои контроли); совпадение формулы Fable с решателем (см. §0, последняя строка).

## 3. Фаза C — код Claude

`selmer_exact.py`: устройство отличается от моего (Тейлор в шаре, решение при mu > e по единичной части c₀ с точностью mu − e; при p = 2 — mod 8 при точности ≥ 3, «3 mod 4 ⇒ не квадрат» при точности 2, иначе дробление; Гензель по v(g(0)) > 2v(g′(0)) в переменной шара; обращённый многочлен с t ∈ pℤ_p, включая t = 0; исключение при глубине > 400). Ошибок не нашёл. Завершение обосновано: f бесквадратна (дискриминант по u = z² равен (m²−n²)² ≠ 0), около простого корня срабатывает Гензель, на ветви z₀ = 0 (где g′(0) = 0) mu растёт с глубиной. Список плохих мест 2·mn·(m²−n²) верен. Поэлементное сравнение с моим решателем: **64 915 пар (сетка ∪ пары наклонов ∪ G1), 0 расхождений** (`compare_with_claude.log`).

`odd_criterion.py`: буквальная реализация определений Sφ, Sφ̂; порядки — степени 2 (множества — подгруппы), `int(log2)` точен; совпадение с моим на тех же 64 915 парах.

`coverage_exact.py`, `recount_and_control.sage`: список пар и сокращение верны; условие «a == b» проверяется до сокращения, но равенство после сокращения возможно только при равенстве до — ок; пифагорова пара засчитывается лишь при отсутствии непифагоровой (точный) или с отдельным разбором (нечётный, 7/32) ✓. Пустого перебора, выданного за доказательство, нет: закрытие — это существование пары с границей 0.

`quartic_torsion.sage`, `generic_torsion.sage`, `quartic_iso.sage`: формулы модели (x = 1/(p−r), w = Y/(p−r)², X = Ax, V = Aw) и обратной карты p = r + A/X проверил; O ↔ p = r, X = 0 ↔ p = ∞ ✓; список из 7 меток + O покрывает все 8 точек, так как ±P и ±(P+T) имеют одинаковые x ✓.

**Слабость контроля (не теоремы):** `is_nondeg_square_set` в `both_squares.sage`/`recount_and_control.sage` требует клетки > 0, то есть решение с клеткой 0 = 0² этот поточечный контроль не заметил бы. В теореме случай клетки 0 разобран верно (противоположная клетка 2), и точки с клеткой 0 — ровно точки с Y = 0, то есть 2-кручение; поэтому на выводы не влияет. Стоит заменить на `c >= 0`.

## 4. Найденные погрешности по серьёзности
1. (косметическая, текст) Лемма 1: фраза «у dz⁴ — больше обеих» неверна при 2e−1 ≥ 1+4v; вывод не меняется.
2. (косметическая, контроль) `c>0` вместо `c>=0` в поточечном контроле, см. §3.
3. (терминология) §4bis: «проверено численно независимой реализацией» для формулы Fable — верно как факт о двух реализациях Claude; моя рецензия статус формулы не меняет и не может (я её автор).
Ошибок, влияющих на утверждения или числа, нет.

## 5. Не делать
- Не перепроверять числа охвата 606/1670/5673, 689/2160/7941, 3468/7262, G1 34/44 и 1333/1715 — воспроизведены двумя реализациями, поэлементное совпадение множеств.
- Не считать эту рецензию независимым подтверждением формулы Fable (2-адическая часть): нужен третий, не мой и не Claude, вывод или доказательство.
- Не называть результаты новыми: проверку литературы я не делал.
- Не искать ошибку в решателе Claude при p = 2 методом случайных пар — исчерпано; если искать, то целенаправленно на парах с m ≡ ±n mod 2^k при больших k (там квартика C′_d вырождена 2-адически); на сетке n ≤ 400 таких расхождений нет.
