# Слежка (Grok), 12.09.2026 ~20:07

Новых `RESULT_*` / `REPORT_*` / `NOTE_*` / `messages.json` нет. Предыдущий `RESULT_G1_19_16_CTP_EXCLUDED` уже в `GROK_INFO_2026-09-12_1916.md`.

Сдвинулись логи Claude в `bridge/` (аудит Kummer, не новые исключения).

## Факт: (11,4) — класс в Селмере, ранг не закрыт eclib

`bridge/selmer_11_4.log` (счёт закончен).

- Требуемый класс подъёма **(1, 274, 274) лежит в Sel²(E/ℚ)**.
- |Sel²| = 32 = 2⁵, совпало с `mwrank selmer_rank=5`.
- Класс локален во всех плохих местах {ℝ, 2, 3, 5, 7, 11, 137}.
- Вывод исключения C(ℚ)=∅ **целиком висит на rank(E)=1**. При rank=3 образ = Селмер и класс достигается: заявление Codex было бы ложным.

`bridge/rank_attack_11_4.log` (ещё пишется): eclib 2-спуск **1 ≤ rank ≤ 3**, second descent inconclusive (ELS-потомки есть, точек нет). Это то же [1,3], что в `RESULTS_G1_RANK_BRIDGE` / `REPORT_G1_SIX_KUMMER`. `check_kummer_11_4.log`: `E.rank(proof=True)` → RuntimeError, lower bound=1 — ожидаемо.

Codex уже писал: верх rank=1 у (11,4) и (19,5) — **CAS PARI ellrank [1,1,2]**, не Python-сертификат. Новое здесь — явная принадлежность δ Селмеру, не сам разрыв eclib.

PARI ellrank = 2-спуск Симона + CTP Фишера (реализация Allombert; [доклад PARI 2022](https://pari.math.u-bordeaux.fr/Events/PARI2022/talks/ellrank.pdf), [Fisher arXiv:2208.14977](https://arxiv.org/abs/2208.14977)). Для этой кривой s=2 как раз режет 5−2−2=1. Это не «другой метод», чем сертификат (19,16); это тот же CTP, только без выгруженных квартик/γ.

## (15,1) — аудит ранга, ещё не RESULT

`scratchpad/run2.log` (скрипт `check_kummer_15_1_ранг.sage` жив).

- Контроль (15,8), t=1 пройден; модель E совпала с Codex.
- eclib: rank=1, **certain=True**, selmer=5; генератор есть.
- PARI ellrank [1,1,2] на original и minimal.
- `analytic_rank_upper_bound` (Bober) = 1, root number = −1. Это **другая** линия, чем 2-спуск: при an.rank≤1 теоремы Gross–Zagier + Kolyvagin дают alg.rank = an.rank.
- Образ δ: 8/8 классов, **(1,113,113) нет**. Совпало с таблицей Codex.
- Печать «ранг матрицы высот = 2» при двух копиях одной точки (h=4.697… на обеих моделях) — ошибка скрипта, не вторая независимая точка.
- `Sage rank_bounds()=(1,3)`, `rank(proof=True)=1`: второе почти наверняка снова PARI, не третий независимый спуск.

`rank2.sage` на той же E (15,1) считает `sha().an()` / Kolyvagin — жив, RSS ~5 ГБ, лог остановился после ellrank.

## (19,5) — то же, что знали

`main_19_5.log`: δ=(1,193,193) нет в 8 классах; eclib certain=False, bound=3; PARI [1,1,2]. Идёт `check_kummer_19_5_ранг3.sage` (L′ и BSD-контроль). QC на эту пару не нужен: C(ℚ) уже заявлено пустым при rank=1.

## Чётное n

Claude гоняет `even_n_2адика.sage` / `even_n_численно.sage` / `even_n_источник.sage` (идея пачки 4). Вывода в лог проекта ещё нет.

## Процессы

- `sage run_quartic_sections.sage` **жив** (~54 мин, timeout 12 ч). Пишет `descent/xq_n89_i23.log` (mtime 19:13, буфер). Итога i=2,3 нет.
- `xq_n79.log` по-прежнему мёртв: `rank E1(k) <= None`, AlarmInterrupt на кониках. Не граница ранга.
- Наблюдатель `watch_magickube.sh` жив (poll md/messages.json). Не плодил второй.

Sage в проекте не менял.
