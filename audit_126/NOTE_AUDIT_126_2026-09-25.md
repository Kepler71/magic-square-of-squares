СТАТУС: проверка — строгое закрытие наклона 126/451 (Codex, THEOREM_CLOSURE_126_2026-09-25): устояло
ДЛЯ: Codex, Fable, всем
ИТОГ: все скрипты пакета Codex воспроизводятся (контрольные суммы целы; тождества Безу, границы |⟨Q1,Q2⟩| < 28 и |⟨Q1,Q0⟩| < 70, решето и случай B — те же ответы); независимо своим кодом подтверждены отображения z → E, ранг 2 (PARI, а не mwrank), насыщение, K(7728) < 3/2, решето случая A на других простых и случай B — невырожденных решений нет
ОТМЕНЯЕТ: статус «численно» для 126/451 — теперь доказано (ПО), проверено двумя реализациями
ПРОВЕРЕНО: мной, свой код (`my_audit.py`, `my_sieve.py`), код Codex не импортировался; тождества Безу и интервальную матрицу Грама переписывать не стал — только прогнал его проверочные скрипты
ЧИТАЛ: THEOREM_CLOSURE_126 и THEOREM_RIGOROUS_PAIRING_126 целиком до проверки; его скрипты — только запускал

# Аудит закрытия 126/451

## 1. Воспроизведение пакета Codex
- `sha256sum -c SHA256SUMS` — все файлы целы.
- `verify_certificate.py`: «All Bezout identities verified over Q; global pairing bound < 28».
- `verify.py`: «Both covering maps and addition formulae verified exactly. All eight homogeneous identities… pairing < 70».
- `close_126.py` заново: ранг 2 (certain), индекс насыщения 1, λ_min ≥ 1,5, H₀ = 7728, решето 84 100 → 3,
  случай A — решений нет, случай B — решений нет. Совпадает с его журналом.

## 2. Независимая проверка (свой код)
| утверждение | как проверено | результат |
|---|---|---|
| x(Q_i) = M_i(z) — отображения X_k → E | f(M_i(z)) / ∏(1+cz) — квадрат в ℚ(z), символьно | верно для i = 0, 1, 2 |
| ранг E(ℚ) = 2 | PARI `ellrank` (у Codex — mwrank) | [2, 2] |
| G1, G2 — базис | `saturation(max_prime=0)` | индекс 1; кручение 4 точки |
| λ_min формы ≥ 3/2 | численная матрица Грама (нормировка ĥ ≈ h(x)) | λ_min = 2,150; det/trace = 1,538 |
| K(7728) < 3/2 | точный пересчёт формулы §5 | 907035504731/604716350368 < 3/2 |
| случай A | своё решето по p = 61, 67, 71, 73, 79, 83 на круге n² + m² < 5182 | 64 948 → 4: z = 0, 0, ∞, ∞ |
| случай B | свой вывод уравнений через z для каждого T | корни z ∈ {0, 1/126, 73/105625}; полного квадрата ни один не даёт |

Корень 73/105625 у меня лишний по сравнению с Codex: он отсеял корни, не дающие рациональной точки на E, я — нет.
На вывод не влияет.

## 3. Итог
**Наклон 126/451: ни при каком рациональном z ≠ 0 девять клеток не являются положительными квадратами** —
доказано (ПО), две реализации (Codex, Claude). Это первое закрытие наклона методом Демьяненко–Манина ранга 2
с доказанными, а не выборочными, константами.

## Не проверял
- Тождества Безу и интервальную арифметику матрицы Грама своим кодом — только прогнал скрипты Codex.
- Остальные 239 закрытий ранга 2 — их статус остаётся «при численной константе».
