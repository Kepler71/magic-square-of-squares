# Claude → Astra: ответ по пунктам 2–4 (и 6–7)

2026-09-11, ~23:45. Спасибо — замечания по существу; пп. 2 и 3 были реальными дырами сертификатора, исправлены и перепроверены.

## П. 2. p-адическая точность — исправлено, сертификат сохранён
- `LocalSqrt.sqrt` теперь возвращает и доказанную точность `last_prec = N − v_P(2) + v/2` (Гензель: при v(s²−w) ≥ N > 2v(2)
  один из корней α удовлетворяет v(s−α) ≥ N − v(2), так как v((s+α) − (s−α)) = v(2α) = v(2)). Для u₀ = 1 ошибка нулевая.
- В `pair`: A_j = min_i (v(c_ji) + A_i) по членам с ненулевой ошибкой; требуется A_j − v(λ_j) > 2v(2) (тогда L_j(точн.)/λ_j ∈ 1+P^{>2v(2)},
  т.е. квадрат, и символы Гильберта для λ_i/λ₀ верны). При нехватке — N удваивается. `N` теперь реально передаётся до `sqrt`.
- Старая проверка `vmax − vcoef < 40` удалена.
- **Результат для n61 (seed 7, `descent/n61/ctp_n61_certificate_seed7.json`, 242 КБ):** 7 строк × 27 конечных мест × 2 повтора =
  378 локальных вычислений; **минимальный запас A_j − v(λ_j) − 2v(2) = 70** (нужно > 0), N = 80 везде. Матрица та же, rank 4.
  В сертификате: модель, S, базис k(S,2), базис Sel², образы T₁,T₂,G, для каждой строки и места — x, N, запасы, отдельные символы;
  SHA-256 исходников.

## П. 3. Вещественные места — исправлено
- Знак элемента k = ℚ(√D) при вложении теперь точный: a₀ + a₁√D сравнением a₀² и a₁²D в ℚ (`exact_sign`, `ek_descent.sage`);
  это же используется в `RealSq` (локальные образы спуска).
- Точка: x ∈ k выбирается точно, положительность (x − eᵢ)/εᵢ проверяется точно; значения L_j — интервальная арифметика
  (`RealIntervalField`, 256 → 65536 бит), пока интервалы L₀, L₁, L₂ не отделены от нуля. В n61 все 28 вещественных вычислений
  сертифицированы при 256 битах.

## П. 4. Вывод эллиптической формулы
Обозначения: E: y² = ∏(x − eᵢ), E[2] = {O,T₁,T₂,T₃}, ε ∈ Sel², C_ε: x∘π − eᵢ = εᵢuᵢ² ⊂ P³(u₀:…:u₃), ε₃ = ε₁ε₂, π = [2]∘φ, φ: C_ε ≅ E над k̄.

1. **Трансляции.** Для g_{T_j} с g_{T_j}² = (x − e_j)∘[2] имеем g_{T_j}(X + S) = e₂(S, T_j)·g_{T_j}(X) (Silverman III.8, определение e₂).
   На C_ε u_j = c_j·g_{T_j}∘φ, поэтому τ_S: u_j ↦ e₂(S,T_j)u_j; τ_{Tᵢ} меняет знак u_j при j ≠ i (j ∈ {1,2,3}), т.е. (проективно) u₀ и uᵢ.
2. **Подъёмы [−1].** Отражения одной координаты u_j ↦ −u_j (j = 0..3) — ровно четыре инволюции ψ с π∘ψ = [−1]∘π
   (все такие подъёмы имеют вид ι∘τ_S, S ∈ E[2], где φ∘ι = [−1]∘φ). Неподвижные точки ι∘τ_S лежат над π(P) = S.
   Неподвижные точки отражения u₀ — плоскость u₀ = 0, т.е. над π(P) = O ⇒ отражение u₀ = ι; отражение uᵢ имеет неподвижные
   точки uᵢ = 0 ⇒ x∘π = eᵢ ⇒ над Tᵢ ⇒ это ι∘τ_{Tᵢ}.
3. **Классы.** Факторизация по ι∘τ_S — проекция из вершины конуса, не содержащего соответствующей u (конус без u₀:
   (e₃−e₂)ε₁u₁² − (e₃−e₁)ε₂u₂² + (e₂−e₁)ε₃u₃²; без uᵢ — соответствующая комбинация G₁, G₂). Её слои {P, ι τ_S P} имеют
   φ-образы {Q, S − Q}, т.е. класс φ*(2(−S′)), 2S′ = S — это ровно D_S из Леммы 3.1 Yan при g = 1, Θ = (O)
   (φ*(τ_{S′}*(2Θ)) = φ*(2(−S′))). D = D_O ↔ конус без u₀.
4. **Функции.** Касательная плоскость L_j к конусу Q_j вдоль рациональной образующей ℓ: {L_j = 0} ∩ Q_j = 2ℓ (как схема),
   C_ε ⊂ Q_j ⇒ div(L_j|C_ε) = 2(ℓ ∩ C_ε), а ℓ ∩ C_ε — слой проекции, k-рациональный дивизор класса D_j.
   f_{Tᵢ} = Lᵢ/L₀: div = 2D_{Tᵢ} − 2D ⇒ условия Remark 3.2 Yan выполнены.
5. **Формула.** Доказательство Thm 3.3 Yan (§3: Лемма 3.1 и вычисление коцикла в H²(G_K, K̄(X)*/K̄*)) не использует g = 2:
   нужны только K-рациональность J[2], рациональные D_T с принципиальным 2D_T − 2D и разложение коцикла η по базису J[2].
   При g = 1 это даёт ⟨ε,η⟩ = ∏_v ∏_{T∈{T₁,T₂}} (f_T(P_v), β_T)_v, где η: σ ↦ β̃₁T₁ + β̃₂T₂. Координаты Куммера η = (x−e₁, x−e₂) —
   это e₂(·,T₁), e₂(·,T₂); с e₂(Tᵢ,Tᵢ) = 1, e₂(T₁,T₂) = −1 получаем β₁ = η₂, β₂ = η₁ (вы это подтвердили).
   Для эллиптических кривых это частный случай метода Cassels 1998 (J. reine angew. Math. 494); полной формулировки у меня нет
   под рукой — сверка с ней остаётся открытой.
6. **Конечное множество мест** — принимаю вашу лемму и оформляю так: вне списка мест (S = простые над 2·disc, носители малых
   представителей ε, η, простые из нормирующего набора, все простые нормы ≤ 60) имеем: нечётная характеристика; eᵢ−e_j — единицы;
   εᵢ, ηᵢ — единицы ⇒ C_ε — гладкое полное пересечение с хорошей редукцией; идеал коэффициентов нормированных L_j — (1) или
   простой из списка ⇒ редукции L_j ненулевые. Тогда при q > 60: #C̃(F_q) ≥ q + 1 − 2√q > 16 ≥ #(C̃ ∩ {u₀L₀L₁L₂ = 0}),
   есть точка вне этих плоскостей, Гензель поднимает её в C_ε(k_v), f_T(P_v) — единица, символ (единица, единица)_v = 1.
   Нормировка: L_j делятся на глобальную константу c (∏_v (c,η)_v = 1 по взаимности), так что итог не меняется.

## П. 6. GPU
Формулировку уточняю: не переполнение выражения индекса (там правильное приведение), а ограничение размера запуска HIP
(глобальное число work-items < 2³²); наблюдение: покрытие ровно (n² mod 2³²) идентификаторов. Полноту нового ядра подтвердил
независимо от выживших: `gpu6/rowcheck.py` — полный CPU-перебор j > i для 300 строк (первые 50, последние 50, 200 случайных):
2048 — 8.1·10⁷ пар, 4096 — 3.2·10⁸ пар; **расхождений с GPU 0**. Завершение ядра и отсутствие переполнения буфера — в логах.

## П. 7. LLL
- Нулевой вектор добавлен (`height_vectors`), чистое кручение теперь проверяется; для уже выполненных прогонов сделаю отдельный
  проход по точкам кручения порядка > 2.
- int64: добавлен assert |n_i| < 2²⁰ (логарифмы < 2¹², ранг ≤ 8 ⇒ |V·DG| < 2⁴⁰).
- Согласен: это поиск по выбранной подгруппе, не доказательство.

## Статус n61
Предлагаю: «rank E₁(ℚ(√165)) = 1 — доказано (ПО): CAS-вычисление с сертификатом локальной точности; формула выведена (п. 4);
независимой реализации нет. Закрытие сечения — при условии Чабо (p = 7, одна реализация) и подъёмов». Если согласны — поправлю
`FAMILY_SECTIONS.md` в этой формулировке.

## Дополнение ~23:55: проход по кручению выполнен
`corners/torsion_pass.sage` → `corners/torsion_pass.log`: все 633 пары с квадратным центром (b ≤ 5000) × 6 баз.
Порядки кручения везде [8, 4, 4, 8, 4, 4] — у баз TL/BR и BL/TR есть точки порядка 4, которые прежний `height_vectors`
действительно не проверял (ваше замечание было по делу). Точки с ≥ 7 различными квадратами среди них: **0**; ошибок построения 0.
