# Идеи (Grok, 11.09.2026, пачка 3)

Не доказано. Факты — в `FAMILY_SECTIONS.md` и `GROK_INFO_QC_AND_CT.md`.
Скрипты Sage не менял. Пачки 1–2 не повторяю.

Остаток, под который это написано:

- w = −1, ранг не доказан: (71,49,61), (89,23,65), (79,47,65);
- w = +1 (эллиптический Чабо над k неприменим): шесть сечений, из них (7,1,5) уже имеет Magma `[0,2]`;
- у (23,7,17) нет второй реализации Чабо;
- H₀ / GPU / порт Bianchi QC на мнимое поле — не трогать.

---

## 1. Вторая реализация Чабо для (23,7,17): Magma `Chabauty` на роде 2

Это не эллиптический Чабо над ℚ(√30). Rank Jac(K10) = 1 и род 2 ⇒ классический Чабо **над ℚ**. В Magma это штатная функция Stoll (Чабо + решето МВ). Handbook: `Chabauty(P : ptC)` для `JacHypPt`, ранг 1, нужен один рациональный точка на кривой и генератор J(ℚ).

Контроль: набор t-координат на K10 должен после обратной Richelot-карты дать те же X, что цепочка Claude (5/3, 4, 3/5, 1/4 и знаки / обратные). Если набор шире — дыра в эллиптическом Чабо; если уже — независимая реализация, уровень как у s = 1/5.

Тот же трюк закрывает аудит s = 1/5: `Chabauty` на C₁ (уже `[0,1]`), без Sage-рядов.

Готовый payload (K10, многочлен из `RESULT_FROM_CODEX_…_RANK_ONE_N17.md`):

```
P<t> := PolynomialRing(Rationals());
f := 5806578242153*t^6 + 27991946991346*t^5 + 11864374812529*t^4
   - 73557190977572*t^3 - 13387366557973*t^2 + 70041521544098*t
   - 25489492726581;
C := HyperellipticCurve(f);
pts := Points(C : Bound := 1000);
print "pts:", pts;
J := Jacobian(C);
print "RankBounds:", RankBounds(J); // ожидается [0,1] или [1,1]
// Генератор: образ известной точки (t = 25/9 на C0, перенесённый на K10)
// или первый некрутильный элемент из Points(J : Bound := …).
// Затем:
// Chabauty(Pgen : ptC := pts[1]);
```

Если `Points(J)` не находит генератор за Bound — не крутить: вернуться к эллиптическому `Chabauty(mwmap, EtoP1)` над ℚ(√30) в той же Magma. Это тоже вторая реализация, уже эллиптическая, но не Sage.

---

## 2. Очередь Richelot-ядер: не 13 моделей подряд

Для (23,7,17) сработало не K0, а конкретное разбиение шести точек ветвления {0, ∞, 1, −1, C/A, A/C}:

- **K10** `{(0, C/A), (∞, −1), (1, A/C)}` → `[0,1]`;
- **K8**  `{(0, −1), (∞, A/C), (1, C/A)}` → `[0,3]`;
- K0 `{(0, ∞), (1, −1), (C/A, A/C)}` → `[0,5]`;
- K1–K3 — коэффициенты ~10¹⁶, бесполезны, пока мелкие не прогнаны.

Для оставшихся w = −1 слать **в этом порядке**, одним коротким пакетом, а не «все 14»:

1. аналог K10: `{(0, C/A), (∞, −1), (1, A/C)}`;
2. аналог K8:  `{(0, −1), (∞, A/C), (1, C/A)}`;
3. C0 (нечётная степень — Magma `RankBounds` это любит; для n = 61 K0 уже падала по памяти, C0 ещё нет как отдельный успешный прогон в том же смысле);
4. остальные, **отсортированные по наивной высоте коэффициентов** (K4/K5/K9 у n = 61 на порядки короче K1–K3).

Индексы K* в `richelot_all.sage` — порядок `matchings`, он стабилен. Не гадать: взять строки с этими скобками из `family/richelot_all_lines.txt` / `richelot_n61_lines.txt`.

Если какая-то модель даёт **hi = 3** (как K8 у n = 17) — это не «ещё не 1», а правильная мишень для Cassels–Tate (п. 7). C0 с hi = 5 для CT дороже.

---

## 3. Сечения с w = +1: не отменять Magma

Отмена запроса по n = 5 была верна для *эллиптического* Чабо над k. Для ранга Jac над ℚ она вредна: (7,1,5) уже имеет `RankBounds(Jac C₁) = [0,2]`. Это лучший объект из шести.

Задача после ранга 2 — не `{P ∈ E₁(k) : x(P) ∈ ℚ}` как QC на E/K (готового солвера нет, см. `GROK_INFO_QC_AND_CT.md` §0). Нужны рациональные точки **самой** C_{J₂} / C₁ рода 2 над ℚ. При rank Jac = 2 = род классический Чабо не режет; это стандартная постановка quadratic Chabauty рода 2 над ℚ, не порт Bianchi с мнимого поля.

Как закрыть ранг ровно 2 у (7,1,5) **без чётности**:

- нижняя 1 уже есть (образ сечения);
- верхняя 2 уже есть (Magma);
- найти **вторую независимую** точку на Jac(C₁) или E₁(ℚ(√3)) поиском большей высоты. Две независимые + hi = 2 ⇒ ранг 2, без Докчитсеров и без Sha.

Контроль независимости: регулятор / ненулевой определитель высотной матрицы 2×2, не «нашлась ещё одна точка на кривой» (она может быть в ⟨G₀, кручение⟩).

Остальные пять w = +1: всё же слать C0/K0 в Magma. Если снова hi = 2 — тот же путь. Если hi ≥ 4 — сначала п. 2 и п. 4, не QC.

---

## 4. Расщепление Jac над ℚ: `richelot_split` и Magma `Degree2Subcovers`

`family/richelot_split.sage` ищет Richelot-двойственную, у которой инволюция P¹ поднимается над ℚ (κ — квадрат), тогда Jac ~ E_a × E_b над ℚ и ранг = сумма mwrank. Прогон был **только** для s = 1/5, расщепления нет.

Имеет смысл прогнать на всех оставшихся сечениях, включая w = +1. Это дешёвый Sage, не Magma 60 с.

В Magma V2.29 у Stoll есть `Degree2Subcovers` / `Degree3Subcovers` (род 2 → эллиптические). Один вызов на C0 и на мелкие K:

```
C := HyperellipticCurve(f);
Degree2Subcovers(C);
```

Если непусто — ранги факторов mwrank, proof. Если оба ранга 1 — Flynn–Wetherell / Bianchi `QC_bielliptic` (github bianchifrancesca/QC_bielliptic) на **этой** модели.

Оговорка: C_{J₂} в исходных координатах, скорее всего, **не** бисэллиптична над ℚ. Инволюция t ↦ 1/t сохраняет ветвление, но κ = −1 не квадрат в ℚ (подъём y требует √(−1)). Поэтому `y^2 = a6 x^6 + a4 x^4 + a2 x^2 + a0` для QC_bielliptic нужно искать на двойственной с рациональным подъёмом, не на C0. Пустой `Degree2Subcovers(C0)` — не конец, проверить K*.

Контроль расщепления: #C(F_p) = p+1−a_p(E_a)−a_p(E_b) на хороших p.

---

## 5. `TwoCoverDescent` на кривой, не на якобиане

`RankBounds(J)` — 2-Selmer **якобиана**. Magma `TwoCoverDescent(C)` — фейковый 2-Selmer **кривой** (Bruin–Stoll). Это другой объект: иногда режет C(ℚ), не трогая ранг J.

На (7,1,5) многочлен короткий:

```
P<t> := PolynomialRing(Rationals());
f := 15*(t^2-1)*(t^2+1)*(769*t^2-962*t+769);
C := HyperellipticCurve(f);
Hk, AtoHk := TwoCoverDescent(C);
print #Hk;
```

Известные точки сечения лежат на C, Selmer не пуст. Цель — список дзета-классов достаточно мелкий, чтобы 2-накрытия перебрать или скормить эллиптическому Чабо над кубическим/квадратичным полем накрытия. Не ждать rank 0 у 330 фактор-кривых из `quotients_family` — это уже закрыто.

---

## 6. p-чётность без фразы «ранг чётный»

Теорема Докчитсеров / Нековара про p-чётность говорит про **коранг p^∞-Selmer**, безусловно, не про rank E(k). Писать «w = +1 ⇒ ранг чётный ⇒ ≥ 2» как теорему нельзя (это уже было в пачке 1; здесь — что вместо этого делать).

Практично: для (7,1,5) верхняя 2 уже есть. p-чётность отличает {0,2} от {1}, но 0 отсекается известной точкой только если коранг Sha[p^∞] = 0. Надёжнее п. 3 (вторая точка), чем ссылка на чётность.

Для оставшихся w = −1 с hi = 5: p-чётность даёт нечётный коранг Selmer ∈ {1,3,5}, ничего не закрывает. Не тратить на это запрос.

---

## 7. Fisher–Yan без локальной Magma — только на мелком hi = 3

Код: github.com/TomAFisher/Cassels-Tate-pairing--Genus-2, `Attach` двух файлов `g2minimisation.m` + `genus2ctp.m`, затем `TwoDescentGenus2` + `CasselsTatePairingGenus2`. Онлайн-калькулятор `Attach` с диска не делает; вставить оба исходника теоретически можно, но demo на **короткой** кривой уже 10 с + 9 с. Наши K1 с коэффициентами 10²⁴ в 60 с не живут.

Имеет смысл только если есть модель с hi = 3 **и** маленькими коэффициентами (образец — K8 у n = 17, ~10¹³). Тогда: либо локальная Magma, либо вставка исходников + эта одна кривая. Не на C0 с hi = 5 и не на n = 61 K1.

Встроенный `CasselsTatePairing(C,D)` — для 2-накрытий **эллиптической** кривой, не замена Fisher–Yan на Jac рода 2.

---

## 8. Что не делать (коротко, чтобы не вернулось)

- Не портировать `quad_chab_ell_im_quad` / Jha QC_ECIm на вещественное k и срез x ∈ ℚ.
- Не ставить quadratic Chabauty на H₀ (род 22) и на фактор рода 11.
- Не ждать GPU-сито как запрет; поиск примера — отдельная ветка Codex.
- Не гонять ещё один Singular genus параллельно с `genus_h0.sage nom1`.
- Не объявлять шесть сечений с w = +1 безнадёжными: у (7,1,5) уже hi = 2.

---

## 9. Мелочь, дешёвая Sage

Семейство b² + h² = 2n²: посчитать, у каких примитивных троек A или C — квадрат. У (23,7,17) A = 13², и сработало ядро K10, в котором участвует C/A. Если дальше появятся такие сечения с w = −1 — их слать раньше, чем (71,49,61).

Контроль: A = (h²+n²)/2 ∈ ℚ^×².

Конец.
